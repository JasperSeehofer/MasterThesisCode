---
context: default
phase: fisher-frame-mismatch-fix
task: 3
total_tasks: 10
status: in_progress
last_updated: 2026-04-27T16:05:34Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through analysis this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Bug scope is limited to migrated pre-Phase-43 data — new simulation runs are already correct. Do NOT rotate Fisher covariances for new data. The migration script must only act on old migrated CSVs (has `_coord_frame`, no `_cov_frame`).
- [ ] CONSTRAINT: Physics change is already APPROVED — do NOT re-run the physics-change protocol. Go straight to implementation.
- [ ] CONSTRAINT: For the partial-migration case (`_coord_frame` present, `_cov_frame` absent), load `.bak_equatorial` backup to get original equatorial positions for computing `J_rot`. Do NOT use the already-ecliptic `qS`/`phiS` from the migrated CSV.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| phi wrapping in finite differences | `phi_ecl ± δ` can wrap around 0/2π giving wrong Jacobian J[0,*] | advisory | Use `_angular_diff(a, b) = (a-b) - 2π·round((a-b)/(2π))` when computing central differences for `phi_ecl` |

<current_state>
Physics-change protocol complete and APPROVED. About to implement the covariance rotation in `scripts/migrate_crb_to_ecliptic.py`. All test file locations have been read and the full scope of test fixture changes is known.

Implementation is starting from scratch — no code has been written yet.
</current_state>

<completed_work>

- **Debug investigation complete** — `gsd-debugger` produced Root Cause Report with 7 bugs identified
- **Physics-change protocol** — all 5 items presented and approved by user:
  - Old formula: `migrate_crb_to_ecliptic.py:98-100` only writes `phiS`, `qS`, `_coord_frame`
  - New formula: `Σ_ecl = J_rot · Σ_eq · J_rot^T` for 2×2 angular block; `v_ecl = J_rot · v_eq` for cross-terms
  - Reference: Taylor *An Introduction to Error Analysis* Eq. (3.47); Cutler & Flanagan (1994) arXiv:gr-qc/9402014
  - Dimensional analysis: J_rot dimensionless, Σ in rad², result in rad² ✓
  - Limiting cases: zero obliquity → identity; ecliptic equator → orthogonal J, area preserved ✓
</completed_work>

<remaining_work>

- **Task 3** (NEXT): Implement `scripts/migrate_crb_to_ecliptic.py`:
  - Add `_COV_FRAME = "ecliptic_BarycentricTrue_J2000"` and `_JACOBIAN_DELTA = 1e-7`
  - Add `_angular_diff(a, b)` helper (wrapping-safe angle subtraction)
  - Refactor `_icrs_to_ecliptic(ra_rad, dec_rad) -> (phi_ecl, q_ecl)` (extract from migrate_csv)
  - Add `_compute_ecliptic_jacobian(ra_rad, dec_rad) -> (J00, J01, J10, J11)` — central finite differences
  - Add `_rotate_covariance_block(df, J00, J01, J10, J11) -> None` — modifies df in place
  - Update `migrate_csv()` for 3 states (fully migrated / pos-only / unmigrated)
  - Update `main()` summary stats to count `cov_migrated` separately

- **Task 4**: Update `master_thesis_code_test/test_migrate_crb_to_ecliptic.py`:
  - Add Fisher columns to `_make_synthetic_csv` (delta_phiS_delta_phiS, delta_qS_delta_qS, delta_phiS_delta_qS, delta_phiS_delta_M, delta_qS_delta_M, delta_phiS_delta_luminosity_distance, delta_qS_delta_luminosity_distance)
  - Add `test_cov_frame_column_added`
  - Add `test_covariance_block_rotated` — compare against manual `J_rot Σ J_rot^T`
  - Add `test_partial_migration` — simulate old migrated file (has `_coord_frame`, no `_cov_frame`, backup exists), verify covariance-only rotation works
  - Update `test_other_columns_unchanged` to exclude the rotated Fisher columns

- **Task 5**: Add frame guard to `master_thesis_code/datamodels/detection.py:__init__`:
  ```python
  _ECLIPTIC_FRAME = "ecliptic_BarycentricTrue_J2000"
  if parameters.get("_coord_frame") != _ECLIPTIC_FRAME or parameters.get("_cov_frame") != _ECLIPTIC_FRAME:
      raise ValueError(
          f"CRB row not in ecliptic frame "
          f"(_coord_frame={parameters.get('_coord_frame')!r}, _cov_frame={parameters.get('_cov_frame')!r}). "
          "Run scripts/migrate_crb_to_ecliptic.py first."
      )
  ```

- **Task 6**: Add `"_coord_frame": "ecliptic_BarycentricTrue_J2000"` and `"_cov_frame": "ecliptic_BarycentricTrue_J2000"` to:
  - `test_detection.py:_make_series()`
  - `cosmological_model_test.py:_make_detection_series()`

- **Task 7**: Update `master_thesis_code_test/fixtures/evaluation/generate_fixtures.py`:
  - Add `_COORD_FRAME = "ecliptic_BarycentricTrue_J2000"` constant
  - Add `"_coord_frame": _COORD_FRAME` and `"_cov_frame": _COORD_FRAME` to `_make_detected_row()` row dict
  - Add both keys to `DETECTED_COLUMNS` list
  - Re-run `uv run python master_thesis_code_test/fixtures/evaluation/generate_fixtures.py` to regenerate CSV fixtures

- **Task 8**: Add comment to `master_thesis_code/galaxy_catalogue/handler.py:get_possible_hosts_from_ball_tree` (documentation only):
  - Note above `sigma_matrix = ...` line that phi_sigma/theta_sigma/cov_theta_phi must be in same ecliptic frame as theta/phi

- **Task 9**: Run extended migration on production data:
  - `uv run python scripts/migrate_crb_to_ecliptic.py simulations/` (after task 3 is done)

- **Task 10**: Run full test suite and commit:
  - `uv run pytest -m "not gpu and not slow"`
  - Commit with `[PHYSICS]` prefix
</remaining_work>

<decisions_made>

- **Covariance rotation formula**: `Σ_ecl = J_rot · Σ_eq · J_rot^T` where `J_rot = ∂(φ_ecl, q_ecl)/∂(φ_eq, q_eq)` computed via central finite differences (δ=1e-7 rad) per source using astropy ICRS→BarycentricTrueEcliptic
- **Cross-terms**: `v_ecl = J_rot · v_eq` for `(delta_phiS_delta_M, delta_qS_delta_M)` and `(delta_phiS_delta_luminosity_distance, delta_qS_delta_luminosity_distance)`. Non-angular `delta_M_delta_M`, `delta_luminosity_distance_delta_luminosity_distance`, `delta_M_delta_luminosity_distance` unchanged.
- **3-state migration logic**: (1) both `_coord_frame` + `_cov_frame` present → skip; (2) only `_coord_frame` → load `.bak_equatorial` for original equatorial positions, covariance-only rotation; (3) neither → full rotation
- **Detection.__init__ guard**: Hard `ValueError` (not warning) — prevents future silent frame bugs
- **Bug scope**: Only pre-Phase-43 migrated CSVs are affected. Post-Phase-43 new simulation runs produce Fisher in ecliptic frame natively (Fisher is computed at ecliptic qS/phiS values).
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/debug/fisher-frame-mismatch-angles.md` — full Root Cause Report with all 7 bugs and fix plan
2. `scripts/migrate_crb_to_ecliptic.py` — current migration script to be extended
3. `master_thesis_code/datamodels/detection.py:96-115` — Detection.__init__ to add frame guard
4. `master_thesis_code_test/test_migrate_crb_to_ecliptic.py` — tests to update
5. `master_thesis_code_test/datamodels/test_detection.py:9-46` — `_make_series()` to update
6. `master_thesis_code_test/cosmological_model_test.py:12-44` — `_make_detection_series()` to update
7. `master_thesis_code_test/fixtures/evaluation/generate_fixtures.py` — fixture generator to update

## Critical Anti-Patterns (do NOT repeat these)
- [PHI WRAPPING IN FINITE DIFF]: When computing `∂(phi_ecl)/∂(phi_eq)` via central differences, `phi_ecl` near 0 or 2π can wrap, giving `(2π-ε) - ε = 2π-2ε` instead of `-2ε`. → Use `_angular_diff(a, b) = (a-b) - 2π·round((a-b)/(2π))` for the phi numerator only.

## Infrastructure State
- Python env: `uv sync --extra cpu --extra dev` (`.venv/` present)
- Production CSVs: `simulations/` — have `_coord_frame` column but NO `_cov_frame`; ready for covariance-only migration after task 3

<context>
Session was a debug + physics-change session. No GSD phase was planned — this work was triggered directly from the previous session's diagnostic output. Debug investigation confirmed and extended the known bug. Physics change approved. Implementation is cleanly scoped: primary change is the migration script; downstream changes are test fixtures (mechanical) and a validation guard in Detection.

The debugger made a key clarification that post-Phase-43 simulations are already correct — important for not over-engineering the fix.
</context>

<next_action>
Start with: Read `scripts/migrate_crb_to_ecliptic.py` in full, then implement Task 3 — add `_angular_diff`, `_icrs_to_ecliptic` (extracted from migrate_csv), `_compute_ecliptic_jacobian`, `_rotate_covariance_block` helpers, then update `migrate_csv` to handle 3 states. Physics change approved — implement directly.
</next_action>
