---
phase: 05-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
user_setup: []

must_haves:
  truths:
    - "CLAUDE.md has a Cluster Deployment section listing all cluster/ scripts and key CLI flags"
    - "README.md has a Running on HPC section that points to cluster/README.md"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Cluster Deployment section for Claude Code context"
      contains: "Cluster Deployment"
    - path: "README.md"
      provides: "Running on HPC section for project visitors"
      contains: "Running on HPC"
  key_links:
    - from: "CLAUDE.md"
      to: "cluster/README.md"
      via: "pointer in Cluster Deployment section"
      pattern: "cluster/README\\.md"
    - from: "README.md"
      to: "cluster/README.md"
      via: "pointer in Running on HPC section"
      pattern: "cluster/README\\.md"
---

<objective>
Add cluster documentation sections to CLAUDE.md and README.md.

Purpose: CLAUDE.md needs a "Cluster Deployment" section so Claude Code has quick context on cluster scripts and flags (DOCS-02). README.md needs a "Running on HPC" section pointing users to the full guide (DOCS-03).

Output: Updated CLAUDE.md and README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cluster Deployment section to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md
    .planning/phases/05-documentation/05-CONTEXT.md
    cluster/modules.sh
    cluster/setup.sh
    cluster/submit_pipeline.sh
    cluster/simulate.sbatch
    cluster/merge.sbatch
    cluster/evaluate.sbatch
    cluster/resubmit_failed.sh
    cluster/vpn.sh
  </read_first>
  <action>
Insert a new `## Cluster Deployment` section in CLAUDE.md immediately after the `## Running the Code` section (after line 112, before `## Running Tests` on line 114). Per D-09, this section provides quick context for Claude Code -- no command duplication from cluster/README.md.

The section content must be:

```markdown
## Cluster Deployment

The `cluster/` directory contains everything needed to run EMRI simulations on bwUniCluster 3.0 (KIT). See `cluster/README.md` for the full quickstart guide.

### Key CLI Flags

| Flag | Where | Purpose |
|------|-------|---------|
| `--use_gpu` | `arguments.py` | Enable GPU acceleration (always used on cluster) |
| `--num_workers N` | `arguments.py` | Multiprocessing pool size for Bayesian inference; defaults to `os.sched_getaffinity(0) - 2` |
| `--simulation_index I` | `arguments.py` | Maps to `SLURM_ARRAY_TASK_ID`; indexes per-task output files |
| `--seed S` | `arguments.py` | Random seed; on cluster, per-task seed = `BASE_SEED + SLURM_ARRAY_TASK_ID` |

### Script Inventory

| Script | Purpose |
|--------|---------|
| `cluster/modules.sh` | Loads environment modules; exports `$WORKSPACE`, `$PROJECT_ROOT`, `$VENV_PATH` |
| `cluster/setup.sh` | First-time setup: installs uv, allocates workspace, creates venv |
| `cluster/simulate.sbatch` | GPU array job -- one EMRI simulation per task (`gpu_h100`, 1 GPU, 2h) |
| `cluster/merge.sbatch` | CPU job -- merges per-task CSVs via `emri-merge`, prepares detections via `emri-prepare` |
| `cluster/evaluate.sbatch` | CPU job -- Bayesian inference for H0 posterior (16 CPUs, auto-detected workers) |
| `cluster/submit_pipeline.sh` | Pipeline orchestrator -- chains simulate -> merge -> evaluate via `--dependency=afterok` |
| `cluster/resubmit_failed.sh` | Resubmits only failed/timed-out array tasks; cleans up partial output first |
| `cluster/vpn.sh` | University VPN connection via openconnect |

### Quick Reference

```bash
# First-time setup (run once)
bash cluster/setup.sh

# Submit a full campaign
bash cluster/submit_pipeline.sh --tasks 100 --steps 50 --seed 42

# Resubmit failed tasks
bash cluster/resubmit_failed.sh JOB_ID $RUN_DIR BASE_SEED SIM_STEPS
```
```

Per D-10, the script inventory table gives Claude Code quick context on what each script does. Per D-09, no duplication of detailed instructions -- just flags, inventory, and pointer to the full guide.
  </action>
  <verify>
    <automated>grep -q "## Cluster Deployment" CLAUDE.md && grep -q "cluster/README.md" CLAUDE.md && grep -q "\-\-use_gpu" CLAUDE.md && grep -c "cluster/" CLAUDE.md | awk '{exit ($1 >= 8) ? 0 : 1}' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `## Cluster Deployment` heading
    - Section appears after `## Running the Code` and before `## Running Tests`
    - Contains `cluster/README.md` pointer (per D-09)
    - Contains a "Key CLI Flags" table with `--use_gpu`, `--num_workers`, `--simulation_index`, `--seed`
    - Contains a "Script Inventory" table listing all 8 cluster/ scripts with one-line descriptions (per D-10)
    - Contains a "Quick Reference" code block with setup, submit, and resubmit commands
    - Does NOT duplicate detailed instructions from cluster/README.md (per D-09)
  </acceptance_criteria>
  <done>CLAUDE.md has a Cluster Deployment section between Running the Code and Running Tests, with CLI flags table, script inventory table, quick reference commands, and pointer to cluster/README.md.</done>
</task>

<task type="auto">
  <name>Task 2: Add Running on HPC section to README.md</name>
  <files>README.md</files>
  <read_first>
    README.md
    .planning/phases/05-documentation/05-CONTEXT.md
  </read_first>
  <action>
Insert a new `## Running on HPC` section in README.md after the `## Usage` section (after the SNR analysis code block around line 59) and before `## Running Tests` (line 60). Per D-11, this is brief -- just enough to let the reader know cluster support exists.

The section content must be exactly:

```markdown
## Running on HPC

This project runs on bwUniCluster 3.0 (KIT) as SLURM array jobs. The `cluster/` directory
contains all scripts for environment setup, job submission, and failure recovery.

See [`cluster/README.md`](cluster/README.md) for the complete guide covering:
- First-time cluster setup
- Submitting simulation campaigns
- Monitoring and retrieving results
- Troubleshooting common failures
```

Per D-11, this is intentionally brief. The full guide lives in cluster/README.md. Per the CONTEXT.md integration point, this sits after "Usage" and before "Running Tests".
  </action>
  <verify>
    <automated>grep -q "## Running on HPC" README.md && grep -q "cluster/README.md" README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Running on HPC` heading
    - Section appears after `## Usage` and before `## Running Tests`
    - Contains `cluster/README.md` link
    - Section is brief (under 10 lines of content, per D-11)
    - Mentions SLURM, bwUniCluster, and the cluster/ directory
  </acceptance_criteria>
  <done>README.md has a Running on HPC section pointing to cluster/README.md, placed between Usage and Running Tests.</done>
</task>

</tasks>

<verification>
- CLAUDE.md contains `## Cluster Deployment` between Running the Code and Running Tests
- CLAUDE.md script inventory table lists all 8 scripts
- README.md contains `## Running on HPC` between Usage and Running Tests
- Both files point to `cluster/README.md`
</verification>

<success_criteria>
CLAUDE.md gives Claude Code immediate context on cluster scripts and CLI flags. README.md lets project visitors discover that HPC support exists and where to find the guide.
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-02-SUMMARY.md`
</output>
