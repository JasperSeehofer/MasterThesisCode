---
phase: 37-parameter-estimation-correctness
plan: 03
type: verify
wave: 3
routing: GSD
depends_on:
  - "37-01-PLAN.md (chore(37) hygiene commit)"
  - "37-02-PLAN.md ([PHYSICS] PE-01 commit + GPD [PHYSICS] PE-02 commit)"
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/37-parameter-estimation-correctness/37-03-SUMMARY.md  (NEW)

interactive: false

contract:
  scope:
    question: "Are all 7 Phase 37 success criteria satisfied, and is the GSD state correctly advanced to Phase 38?"
  claims:
    - id: claim-all-scs-pass
      statement: "SC-1..SC-7 all pass (verified by running the acceptance commands in this plan)."
      deliverables: [deliv-sc-checklist]
      acceptance_tests: [test-sc1, test-sc2, test-sc3, test-sc4, test-sc5, test-sc6, test-sc7]
    - id: claim-phase36-green
      statement: "All 9 Phase 36 coordinate roundtrip tests remain GREEN after Phase 37 changes."
      deliverables: []
      acceptance_tests: [test-roundtrip-regression]
    - id: claim-state-advanced
      statement: "ROADMAP.md Phase 37 marked complete; STATE.md advanced to Phase 38."
      deliverables: [deliv-roadmap-update, deliv-state-update, deliv-summary]
      acceptance_tests: []
  deliverables:
    - id: deliv-sc-checklist
      kind: documentation
      path: ".planning/phases/37-parameter-estimation-correctness/37-03-SUMMARY.md"
      description: "SC checklist with PASS/FAIL status and evidence for each of SC-1..SC-7."
    - id: deliv-roadmap-update
      kind: documentation
      path: ".planning/ROADMAP.md"
      description: "Phase 37 checked off (- [ ] → - [x]); Plans section updated with 37-01/02/03."
    - id: deliv-state-update
      kind: documentation
      path: ".planning/STATE.md"
      description: "STATE.md status advanced to Phase 38; Phase 37 noted as complete."
  acceptance_tests:
    - id: test-sc1
      kind: regression
      procedure: "uv run pytest master_thesis_code_test/test_parameter_space_h.py::test_set_host_galaxy_parameters_h_ratio -v"
      pass_condition: "PASSED — d_L at h=1.0 is 2× d_L at h=0.5, rtol=1e-10"
    - id: test-sc2
      kind: regression
      procedure: "uv run pytest master_thesis_code_test/test_parameter_space_h.py::test_set_host_galaxy_parameters_requires_h -v"
      pass_condition: "PASSED — TypeError raised when h omitted"
    - id: test-sc3
      kind: regression
      procedure: "uv run pytest -k 'pe02 or epsilon or fisher_det' -v (or whatever test name GPD used)"
      pass_condition: "PASSED — Fisher determinant change < 1% on 4 seeds"
    - id: test-sc4
      kind: smoke
      procedure: "uv run python -c \"from master_thesis_code.cosmological_model import LamCDMScenario; s = LamCDMScenario(); assert s.Omega_m.lower_limit == 0.04 and s.Omega_m.upper_limit == 0.5\""
      pass_condition: "No AssertionError"
    - id: test-sc5
      kind: smoke
      procedure: "grep -rn --include='*.py' -E '\\bsnr_threshold\\s*[:=].*\\b15\\b' master_thesis_code/ (must return empty)"
      pass_condition: "Zero matches"
    - id: test-sc6
      kind: smoke
      procedure: |
        uv run python -c "
        from master_thesis_code.constants import C, SPEED_OF_LIGHT_KM_S
        assert 'C / 1000' in open('master_thesis_code/constants.py').read()
        assert abs(SPEED_OF_LIGHT_KM_S - C/1000) < 1e-9
        print('SC-6 PASS')
        "
      pass_condition: "Prints SC-6 PASS"
    - id: test-sc7
      kind: smoke
      procedure: |
        uv run python -c "
        import pandas as pd
        from master_thesis_code.galaxy_catalogue.handler import GalaxyCatalogueHandler, InternalCatalogColumns
        obj = object.__new__(GalaxyCatalogueHandler)
        obj._angles_mapped_to_ecliptic = False
        obj.reduced_galaxy_catalog = pd.DataFrame({
            InternalCatalogColumns.PHI_S: [0.0], InternalCatalogColumns.THETA_S: [45.0]
        })
        obj._map_angles_to_spherical_coordinates()
        try:
            obj._map_angles_to_spherical_coordinates()
            raise RuntimeError('No guard')
        except AssertionError:
            print('SC-7 PASS')
        "
      pass_condition: "Prints SC-7 PASS"
    - id: test-roundtrip-regression
      kind: regression
      procedure: "uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py -v"
      pass_condition: "9 PASSED, 0 FAILED, 0 ERROR (Phase 36 regression intact)"
  references:
    - id: ref-roadmap-sc-all
      kind: internal
      locator: ".planning/ROADMAP.md §Phase 37 Success Criteria SC-1..SC-7"
  forbidden_proxies:
    - "Marking Phase 37 complete in ROADMAP.md or STATE.md before all 7 SCs pass."
    - "Skipping test-roundtrip-regression — Phase 36 tests must not regress."
    - "Committing state/docs update without running the full test suite first."
---

<objective>
Verify all 7 Phase 37 success criteria, then advance GSD state to Phase 38.

This plan has no code changes. It is a verification + documentation wave that:
1. Runs all SC acceptance tests (SC-1..SC-7) and the Phase 36 regression
2. Records results in 37-03-SUMMARY.md
3. Updates ROADMAP.md (Phase 37 → [x]) and STATE.md (Phase 38 next)
4. Commits the docs update as docs(37)
</objective>

<execution_context>
Wave 3 of Phase 37. Pure GSD execution. No physics changes. No [PHYSICS] prefix.
Commit: docs(37): phase complete, state advanced to Phase 38

All three commits from Plans 37-01 and 37-02 must already be on main:
  - chore(37): software hygiene (PE-03/04/05/COORD-05)
  - [PHYSICS] PE-01: h-threading
  - [PHYSICS] PE-02: per-parameter epsilon (from GPD)

Verify git log shows all three before proceeding.
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-parameter-estimation-correctness/37-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm all three Plan 37-01/02 commits are on main</name>
  <action>
    Run: git log --oneline -5
    Confirm the three Phase 37 commits appear (most recent first):
    1. [PHYSICS] PE-02: per-parameter derivative_epsilon
    2. [PHYSICS] PE-01: thread h_inj into set_host_galaxy_parameters
    3. chore(37): software hygiene — Omega_m limits, SNR unification, C/1000, idempotency guard

    If any commit is missing, stop and resolve before proceeding.
  </action>
  <verify>
    git log --oneline -5 shows all three Phase 37 commits.
  </verify>
  <done>All prerequisite commits confirmed on main.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and Phase 36 regression</name>
  <action>
    uv run pytest -m "not gpu and not slow" -q 2>&1 | tail -5

    Expected: all tests pass (≥519+). If any fail, investigate and fix before continuing.

    Then specifically verify Phase 36 roundtrip tests:
    uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py -v
    Expected: 9 PASSED, 0 FAILED.
  </action>
  <verify>
    Test output shows ≥519 passed, 0 failed. Roundtrip tests 9 PASSED.
  </verify>
  <done>Full suite green; Phase 36 regression intact.</done>
</task>

<task type="auto">
  <name>Task 3: Run all 7 SC acceptance tests</name>
  <action>
    Run each acceptance test from the contract (test-sc1..test-sc7) in order.
    For each, record: PASS or FAIL + any output/error message.

    SC-1: uv run pytest master_thesis_code_test/test_parameter_space_h.py::test_set_host_galaxy_parameters_h_ratio -v
    SC-2: uv run pytest master_thesis_code_test/test_parameter_space_h.py::test_set_host_galaxy_parameters_requires_h -v
    SC-3: uv run pytest -k 'epsilon or pe02 or fisher_det' -v  (adapt to actual test name from GPD)
    SC-4: uv run python -c "from master_thesis_code.cosmological_model import LamCDMScenario; s = LamCDMScenario(); assert s.Omega_m.lower_limit == 0.04 and s.Omega_m.upper_limit == 0.5; print('SC-4 PASS')"
    SC-5: grep -rn --include='*.py' -E '\bsnr_threshold\s*[:=].*\b15\b' master_thesis_code/  (must be empty)
    SC-6: uv run python -c "from master_thesis_code.constants import C, SPEED_OF_LIGHT_KM_S; assert abs(SPEED_OF_LIGHT_KM_S - C/1000) < 1e-9; print('SC-6 PASS')"
    SC-7: (double-call python -c block from contract acceptance_tests above)

    If any SC fails: fix the underlying issue before writing the SUMMARY.md.
  </action>
  <verify>
    All 7 SCs produce PASS output. No SC is FAIL or ERROR.
  </verify>
  <done>All 7 SCs verified PASS. Evidence collected for SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 4: Write 37-03-SUMMARY.md</name>
  <files>.planning/phases/37-parameter-estimation-correctness/37-03-SUMMARY.md  (NEW)</files>
  <action>
    Create the SUMMARY.md with:
    - Phase 37 goal (one line)
    - Commits landed (3 commits with SHAs from git log)
    - SC checklist:
      | SC | Criterion | Status | Evidence |
      |----|-----------|--------|---------|
      | 1  | h-ratio test passes (rtol=1e-10) | PASS | test_parameter_space_h.py::test_set_host_galaxy_parameters_h_ratio |
      | 2  | TypeError without h | PASS | test_parameter_space_h.py::test_set_host_galaxy_parameters_requires_h |
      | 3  | Fisher det < 1% change, stable 4 seeds | PASS | (test name from GPD) |
      | 4  | Omega_m lower < upper, both in [0.04, 0.5] | PASS | Python assertion |
      | 5  | No literal 15 adjacent to snr in production | PASS | grep zero matches |
      | 6  | SPEED_OF_LIGHT_KM_S = C/1000 | PASS | Python assertion |
      | 7  | Idempotency guard raises AssertionError | PASS | Python double-call |
    - Phase 36 regression: 9 roundtrip tests GREEN
    - Physics changes: PE-01 (limiting case: h=0.73 → identical), PE-02 (Vallisneri 2008)
    - Phase 38 unblocked: stable CRB inputs, per-parameter epsilon landed
    - Note for Phase 40 VERIFY-02: Phase 36 regression pickle (36-superset-regression.pkl)
      may need updating for the PE-02 epsilon change (per 36-CONTEXT.md D-24); Phase 40 should
      use post-Phase-37 values as its reference, not the Phase 36 pickle.
  </action>
  <verify>
    File exists at .planning/phases/37-parameter-estimation-correctness/37-03-SUMMARY.md
    Contains SC checklist with all 7 PASS entries and the Phase 40 VERIFY-02 note.
  </verify>
  <done>37-03-SUMMARY.md written with complete SC checklist and Phase 40 note.</done>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md — mark Phase 37 complete</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    In ROADMAP.md §Phase 37:
    1. Change the milestone list entry:
       From: - [ ] **Phase 37: Parameter Estimation Correctness**
       To:   - [x] **Phase 37: Parameter Estimation Correctness** (completed YYYY-MM-DD)
       Use today's date (2026-04-22 or later if this is a later session).

    2. Update the §Phase 37 Plans section (currently "TBD"):
       **Plans**:
         - [x] 37-01-PLAN.md — Software hygiene (PE-03, PE-04, PE-05, COORD-05)
         - [x] 37-02-PLAN.md — Physics tasks (PE-01 h-threading + PE-02 per-parameter epsilon)
         - [x] 37-03-PLAN.md — Verification + state update

    Do not change any other phases or sections.
  </action>
  <verify>
    grep "\[x\].*Phase 37" .planning/ROADMAP.md returns one match.
    grep "37-0[123]-PLAN" .planning/ROADMAP.md returns 3 matches.
  </verify>
  <done>ROADMAP.md Phase 37 marked complete with plans listed.</done>
</task>

<task type="auto">
  <name>Task 6: Update STATE.md — advance to Phase 38</name>
  <files>.planning/STATE.md</files>
  <action>
    Update the STATE.md frontmatter and body:

    Frontmatter changes:
      status: executing  →  status: planning
      stopped_at: "Phase 38 next — Statistical Correctness"
      last_updated: "YYYY-MM-DDT[time]Z"
      last_activity: YYYY-MM-DD
      progress.completed_phases: 2 → 3  (Phase 37 complete)
      progress.completed_plans: 8 → 11  (3 new plans from Phase 37)

    Body changes:
    - Current Position: Phase 38, Plan Not started, Status Ready to plan
    - Add a Phase 37 notes entry (brief: "Phase 37 complete — COORD-05, PE-01..PE-05 all resolved.
      PE-02 per-parameter epsilon committed under Vallisneri 2008 protocol. SC-1..SC-7 all PASS.
      Phase 40 VERIFY-02: use post-37 CRB values as reference, not Phase 36 pickle.")
    - Next command: /gsd:plan-phase 38 (or /gpd:plan-phase 38 if Phase 38 is physics-dominant)
      Note: ROADMAP §Phase 38 shows STAT-03 always routes to GPD; STAT-01 may too.
      Recommend: /gpd:plan-phase 38 if STAT-01 requires a fix; /gsd:plan-phase 38 if equivalence proof.

    Do not overwrite the v2.0/v2.1 history sections.
  </action>
  <verify>
    grep "Phase 38" .planning/STATE.md returns at least 2 matches (current focus + next command).
    grep "Phase 37.*complete" .planning/STATE.md returns 1 match.
  </verify>
  <done>STATE.md advanced to Phase 38; Phase 37 history noted; PE-02/Phase 40 note recorded.</done>
</task>

<task type="auto">
  <name>Task 7: Commit docs update as docs(37)</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/37-parameter-estimation-correctness/37-03-SUMMARY.md
  </files>
  <action>
    Stage the three documentation files.

    Commit message:
    ```
    docs(37): Phase 37 complete — all 7 SCs pass, state advanced to Phase 38

    Phase 37: Parameter Estimation Correctness — all 6 REQ-IDs resolved:
    - COORD-05: idempotency guard on _map_angles_to_spherical_coordinates
    - PE-01: h_inj threaded into set_host_galaxy_parameters (Fisher CRBs now self-consistent)
    - PE-02: per-parameter derivative_epsilon (Vallisneri 2008 protocol)
    - PE-03: Omega_m limits corrected (lower=0.04, upper=0.5)
    - PE-04: SNR threshold unified (PRE_SCREEN_SNR_FACTOR, no stale literal 15)
    - PE-05: SPEED_OF_LIGHT_KM_S derived from C

    SC-1..SC-7 all PASS. Phase 36 roundtrip tests (9) remain GREEN.
    Phase 40 note: PE-02 epsilon change may perturb fisher_sky_2x2 in Phase 36 regression
    pickle — VERIFY-02 should use post-Phase-37 values as its reference.

    STATE.md: Phase 38 (Statistical Correctness) next.
    ```

    Do NOT amend. Do NOT skip hooks.
  </action>
  <verify>
    1. git log -1 --format=%s shows "docs(37): Phase 37 complete...".
    2. git show --stat HEAD lists exactly three documentation files.
    3. uv run pytest -m "not gpu and not slow" -q — all tests still pass (smoke check).
  </verify>
  <done>docs(37) commit landed. Phase 37 is complete. Phase 38 is the active next phase.</done>
</task>

</tasks>

<verification>
- All 7 SCs verified by running the acceptance test commands from the contract.
- Phase 36 regression (9 roundtrip tests) GREEN throughout Phase 37.
- ROADMAP.md shows Phase 37 as [x] with all three plan files listed.
- STATE.md advanced to Phase 38 with Phase 37 completion note and VERIFY-02 reminder.
- 37-03-SUMMARY.md records evidence for each SC.
</verification>

<success_criteria>
- 7/7 SCs PASS (verified by running the acceptance commands).
- 9/9 Phase 36 roundtrip tests GREEN.
- docs(37) commit landed with ROADMAP + STATE + SUMMARY.
- STATE.md current phase is 38; Phase 37 appears in history.
- No test regressions from Phase 37 changes.
</success_criteria>

<output>
Phase 37 complete. Proceed to Phase 38: Statistical Correctness (STAT-01..STAT-04).
Routing recommendation: /gpd:plan-phase 38 (STAT-03 always GPD; STAT-01 may require GPD if L_cat
equivalence proof fails and a fix is needed). GSD+GPD mixed phase per ROADMAP.
</output>
