---
phase: 37-parameter-estimation-correctness
plan: 01
type: execute
wave: 1
routing: GSD
depends_on: ["36-coordinate-frame-fix (36-05-SUMMARY.md committed, 9 roundtrip tests GREEN)"]
files_modified:
  - master_thesis_code/constants.py
  - master_thesis_code/cosmological_model.py
  - master_thesis_code/main.py
  - master_thesis_code/plotting/paper_figures.py
  - master_thesis_code/galaxy_catalogue/handler.py

interactive: false

conventions:
  units: SI (C in m/s → SPEED_OF_LIGHT_KM_S in km/s; division by 1000 exact)
  snr_threshold: "SNR_THRESHOLD = 20.0 is authoritative; no literal 15 or 20 adjacent to snr in production code"
  idempotency: "_map_angles_to_spherical_coordinates must guard against double-call via boolean flag"

contract:
  scope:
    question: "Do the four hygiene fixes (PE-03, PE-04, PE-05, COORD-05) land atomically in a single chore(37) commit with no physics formula changes?"
  claims:
    - id: claim-omega-fixed
      statement: "LamCDMScenario.Omega_m has lower_limit=0.04, upper_limit=0.5 (was swapped)."
      deliverables: [deliv-omega-fix]
      acceptance_tests: [test-omega-limits]
    - id: claim-speed-derived
      statement: "SPEED_OF_LIGHT_KM_S is defined as C / 1000 (not the bare 300000.0 literal)."
      deliverables: [deliv-speed-fix]
      acceptance_tests: [test-speed-precision]
    - id: claim-snr-unified
      statement: "No stale literal 15 adjacent to snr-related tokens in master_thesis_code/; PRE_SCREEN_SNR_FACTOR = 0.3 defined in constants.py."
      deliverables: [deliv-snr-fix]
      acceptance_tests: [test-snr-grep]
    - id: claim-idempotency-guard
      statement: "_map_angles_to_spherical_coordinates raises AssertionError on second call; existing single-call path unaffected."
      deliverables: [deliv-coord05]
      acceptance_tests: [test-double-call]
  deliverables:
    - id: deliv-omega-fix
      kind: code
      path: "master_thesis_code/cosmological_model.py"
      description: "Swap lower_limit=0.04, upper_limit=0.5 on LamCDMScenario.Omega_m (was lower=0.5, upper=0.04)."
    - id: deliv-speed-fix
      kind: code
      path: "master_thesis_code/constants.py"
      description: "SPEED_OF_LIGHT_KM_S: float = C / 1000 (was 300000.0)."
    - id: deliv-snr-fix
      kind: code
      path: "master_thesis_code/constants.py, master_thesis_code/cosmological_model.py, master_thesis_code/main.py, master_thesis_code/plotting/paper_figures.py"
      description: "Add PRE_SCREEN_SNR_FACTOR = 0.3 to constants.py; replace stale literal 15 in cosmological_model.py:171 and paper_figures.py:553; replace 0.3 literal in main.py:421 with PRE_SCREEN_SNR_FACTOR."
    - id: deliv-coord05
      kind: code
      path: "master_thesis_code/galaxy_catalogue/handler.py"
      description: "_angles_mapped_to_ecliptic: bool = False field + AssertionError guard at top of _map_angles_to_spherical_coordinates."
  acceptance_tests:
    - id: test-omega-limits
      subject: claim-omega-fixed
      kind: smoke
      procedure: "python -c 'from master_thesis_code.cosmological_model import LamCDMScenario; s = LamCDMScenario(); assert s.Omega_m.lower_limit < s.Omega_m.upper_limit, f\"{s.Omega_m.lower_limit} >= {s.Omega_m.upper_limit}\"'"
      pass_condition: "No AssertionError; lower_limit=0.04 < upper_limit=0.5."
    - id: test-speed-precision
      subject: claim-speed-derived
      kind: dimensional
      procedure: "python -c 'from master_thesis_code.constants import C, SPEED_OF_LIGHT_KM_S; assert abs(SPEED_OF_LIGHT_KM_S - C/1000) < 1e-6, f\"{SPEED_OF_LIGHT_KM_S} != {C/1000}\"'"
      pass_condition: "Matches C/1000 to 1e-6; no 300000.0 literal in constants.py (grep check)."
    - id: test-snr-grep
      subject: claim-snr-unified
      kind: smoke
      procedure: "grep -rn --include='*.py' -E '\\b(snr|SNR).*\\b15\\b|\\b15\\b.*(snr|SNR)' master_thesis_code/ (must return empty, excluding comments)"
      pass_condition: "Zero matches in production source. PRE_SCREEN_SNR_FACTOR present in constants.py."
    - id: test-double-call
      subject: claim-idempotency-guard
      kind: smoke
      procedure: |
        python -c "
        import numpy as np, pandas as pd
        from master_thesis_code.galaxy_catalogue.handler import GalaxyCatalogueHandler, InternalCatalogColumns
        obj = object.__new__(GalaxyCatalogueHandler)
        obj._angles_mapped_to_ecliptic = False
        obj.reduced_galaxy_catalog = pd.DataFrame({
            InternalCatalogColumns.PHI_S: [0.0], InternalCatalogColumns.THETA_S: [45.0]
        })
        obj._map_angles_to_spherical_coordinates()
        try:
            obj._map_angles_to_spherical_coordinates()
            raise RuntimeError('No AssertionError raised on second call')
        except AssertionError:
            print('PASS: double-call raises AssertionError')
        "
      pass_condition: "Prints PASS; RuntimeError not raised."
  references:
    - id: ref-context-d10
      kind: decision
      locator: ".planning/phases/37-parameter-estimation-correctness/37-CONTEXT.md §decisions D-10 (Omega_m)"
    - id: ref-context-d11
      kind: decision
      locator: ".planning/phases/37-parameter-estimation-correctness/37-CONTEXT.md §decisions D-11..D-14 (SNR unification)"
    - id: ref-context-d15
      kind: decision
      locator: ".planning/phases/37-parameter-estimation-correctness/37-CONTEXT.md §decisions D-15 (SPEED_OF_LIGHT_KM_S)"
    - id: ref-context-d16
      kind: decision
      locator: ".planning/phases/37-parameter-estimation-correctness/37-CONTEXT.md §decisions D-16 (COORD-05 idempotency)"
    - id: ref-roadmap-sc
      kind: internal
      locator: ".planning/ROADMAP.md §Phase 37 SC-4..SC-7"
  forbidden_proxies:
    - "Adding a physics formula change (e.g., modifying dist(), Fisher matrix, or constants used in waveform) to this commit — those require [PHYSICS] prefix and /physics-change review (land in Plan 37-02)."
    - "Skipping the idempotency guard because existing single-call path is unaffected — the guard is required by SC-7 and D-16."
    - "Using from __future__ import annotations anywhere."
    - "Bare np.ndarray without dtype parameter in type annotations."
    - "Committing with --no-verify."
  uncertainty_markers:
    weakest_anchors:
      - "Line numbers for cosmological_model.py:171, paper_figures.py:553, main.py:421 may have shifted since CONTEXT was written. Verify live with grep before editing."
    disconfirming_observations:
      - "If ruff raises a lint error on C / 1000 (unlikely but possible if C is a Quantity not a float), switch to float(C / 1000) and add a comment."
      - "If the idempotency assertion fires in production (i.e., existing code calls _map_angles_to_spherical_coordinates twice), investigate before removing the guard — this is a sign of a worse bug upstream."
---

<objective>
Land four pure-software hygiene fixes as a single atomic commit with no physics formula changes:
- PE-03: Fix swapped Omega_m lower/upper limits in LamCDMScenario
- PE-05: Derive SPEED_OF_LIGHT_KM_S from C (drop the 300000.0 literal)
- PE-04: Unify SNR threshold — add PRE_SCREEN_SNR_FACTOR = 0.3, remove stale literal 15 at 4 sites
- COORD-05: Add idempotency guard to _map_angles_to_spherical_coordinates

All four are one-liner or near-one-liner fixes. No /physics-change review required (none modifies a computed formula or physical constant value; they fix a data entry error, a code duplication, and a missing runtime guard). Physics tasks (PE-01, PE-02) land in Plan 37-02.
</objective>

<execution_context>
This is Wave 1 of Phase 37. Pure GSD execution — no GPD routing needed.

Commit strategy: ONE commit covering all four REQs.
Commit prefix: chore(37) — no [PHYSICS] (none of these changes alters a computed numerical value; PE-05
changes the source of SPEED_OF_LIGHT_KM_S from a hardcoded literal to C/1000, which is a
representation change, not a physics change — the value is identical to floating-point precision).

Pre-commit hooks (ruff + mypy + pytest) run automatically. Do not skip.

After this commit, run `uv run pytest -m "not gpu and not slow"` to confirm the 9 coordinate roundtrip
tests remain GREEN (Phase 36 regression guard).
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/37-parameter-estimation-correctness/37-CONTEXT.md
@master_thesis_code/constants.py
@master_thesis_code/cosmological_model.py
@master_thesis_code/main.py
@master_thesis_code/plotting/paper_figures.py
@master_thesis_code/galaxy_catalogue/handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: PE-03 — Fix swapped Omega_m limits in LamCDMScenario</name>
  <files>master_thesis_code/cosmological_model.py</files>
  <action>
    Find LamCDMScenario (grep for "LamCDMScenario" to get current line numbers) and locate the Omega_m
    Parameter definition. Per D-10, the limits are currently swapped: lower_limit=0.5, upper_limit=0.04.

    Fix to: lower_limit=0.04, upper_limit=0.5

    This is a one-liner edit. The physically plausible range for Omega_m is [0.04, 0.5]:
    - 0.04: matter-only universe without dark energy (lower plausible bound for cold dark matter)
    - 0.5: upper bound beyond which the universe would be matter-dominated and expand too slowly

    No reference comment needed — this is a data-entry error fix, not a formula change.
  </action>
  <verify>
    uv run python -c "from master_thesis_code.cosmological_model import LamCDMScenario; s = LamCDMScenario(); print(s.Omega_m.lower_limit, s.Omega_m.upper_limit); assert s.Omega_m.lower_limit < s.Omega_m.upper_limit"
    Must print 0.04 0.5 without AssertionError.
  </verify>
  <done>Omega_m lower_limit=0.04, upper_limit=0.5 confirmed; lower < upper assertion passes.</done>
</task>

<task type="auto">
  <name>Task 2: PE-05 — SPEED_OF_LIGHT_KM_S = C / 1000</name>
  <files>master_thesis_code/constants.py</files>
  <action>
    Find SPEED_OF_LIGHT_KM_S in constants.py. Per D-15, it currently reads:
      SPEED_OF_LIGHT_KM_S: float = 300000.0

    Change to:
      SPEED_OF_LIGHT_KM_S: float = C / 1000  # km/s, derived from C (m/s)

    C is already defined via astropy a few lines above (check the actual file to confirm; it will be
    something like `C: float = constants.c.value` or `C = ...`). The derivation is exact — no
    floating-point rounding is introduced because 1000 is exactly representable. The resulting value
    is identical to 300000.0 to the precision stored in astropy's constants.c.

    If C is a Quantity rather than a plain float at the point of use, wrap: float(C) / 1000
    and verify the type annotation still holds.
  </action>
  <verify>
    1. grep "300000" master_thesis_code/constants.py must return zero matches.
    2. uv run python -c "from master_thesis_code.constants import C, SPEED_OF_LIGHT_KM_S; print(SPEED_OF_LIGHT_KM_S, C/1000); assert abs(SPEED_OF_LIGHT_KM_S - C/1000) < 1e-6"
       Must pass without AssertionError.
    3. uv run mypy master_thesis_code/constants.py — clean.
  </verify>
  <done>SPEED_OF_LIGHT_KM_S defined as C/1000; no 300000.0 literal remains in constants.py; mypy clean.</done>
</task>

<task type="auto">
  <name>Task 3: PE-04 — SNR threshold unification (4 sites + PRE_SCREEN_SNR_FACTOR)</name>
  <files>
    master_thesis_code/constants.py
    master_thesis_code/cosmological_model.py
    master_thesis_code/main.py
    master_thesis_code/plotting/paper_figures.py
  </files>
  <action>
    Per D-11..D-14, four sites need updating plus one new constant. Execute in order:

    **Step A — constants.py:** Add after SNR_THRESHOLD:
      PRE_SCREEN_SNR_FACTOR: float = 0.3  # pre-screen heuristic (main.py simulation loop)

    **Step B — cosmological_model.py:** Find the `snr_threshold: int = 15` default in the class/dataclass.
    (Grep for "snr_threshold.*=.*15" to get the live line number.) Change to:
      snr_threshold: float = SNR_THRESHOLD
    Add import: `from master_thesis_code.constants import SNR_THRESHOLD` at the top of the file
    (alongside existing constants imports if any; check existing imports first to avoid duplicating).

    **Step C — paper_figures.py:** Find `snr_threshold: float = 15.0` (grep for "snr_threshold.*15").
    Change to:
      snr_threshold: float = SNR_THRESHOLD
    Add import from constants if not already present.

    **Step D — main.py:** Find `cosmological_model.snr_threshold * 0.3` (approximately line 421).
    Change 0.3 to PRE_SCREEN_SNR_FACTOR:
      cosmological_model.snr_threshold * PRE_SCREEN_SNR_FACTOR
    Add import of PRE_SCREEN_SNR_FACTOR from constants.

    **Step E — bayesian_plots.py (discretionary):** Per D-13, bayesian_plots.py:370 has
    `snr_threshold: float = 20.0` which already matches SNR_THRESHOLD. Import the constant for
    single-source-of-truth, or leave as 20.0 — Claude's discretion. Lean toward importing to prevent
    future drift, but not required.
  </action>
  <verify>
    1. grep -rn --include="*.py" -E "\bsnr_threshold\s*[:=].*\b15\b|\bsnr_threshold.*int.*=.*15" master_thesis_code/
       Must return zero matches.
    2. grep -rn --include="*.py" "PRE_SCREEN_SNR_FACTOR" master_thesis_code/ | grep -v test
       Must return at least 2 matches (definition in constants.py + usage in main.py).
    3. uv run python -c "from master_thesis_code.constants import SNR_THRESHOLD, PRE_SCREEN_SNR_FACTOR; print(SNR_THRESHOLD, PRE_SCREEN_SNR_FACTOR)"
       Prints 20.0 0.3 (or 20 0.3).
    4. uv run ruff check master_thesis_code/ — clean.
    5. uv run mypy master_thesis_code/ — clean (or same error count as before this task).
  </verify>
  <done>PRE_SCREEN_SNR_FACTOR defined; stale literal 15 eliminated from cosmological_model.py and paper_figures.py; main.py pre-screen uses the named constant. SNR_THRESHOLD is the sole source of truth.</done>
</task>

<task type="auto">
  <name>Task 4: COORD-05 — Idempotency guard on _map_angles_to_spherical_coordinates</name>
  <files>master_thesis_code/galaxy_catalogue/handler.py</files>
  <action>
    Per D-16, add a boolean flag to GalaxyCatalogueHandler and guard the method.

    Step A — add field to GalaxyCatalogueHandler (in __init__ or as a dataclass field, whichever
    matches how the class is currently structured):
      self._angles_mapped_to_ecliptic: bool = False
    Place this in __init__ near the other instance-state initializations, before the mapping calls.

    Step B — add guard at the TOP of _map_angles_to_spherical_coordinates, as the very first
    statement of the method body (before any existing code):
      assert not self._angles_mapped_to_ecliptic, (
          "_map_angles_to_spherical_coordinates called twice — "
          "angles are already in ecliptic polar frame"
      )
      self._angles_mapped_to_ecliptic = True

    Do NOT change the existing body of _map_angles_to_spherical_coordinates beyond adding these
    two statements at the top.

    Check: does GalaxyCatalogueHandler already have a __init__ that sets instance fields? If so,
    add `self._angles_mapped_to_ecliptic = False` there. If the class uses __post_init__ (dataclass),
    add it there. Read the file before editing.
  </action>
  <verify>
    1. Double-call smoke test (from contract acceptance_tests above — copy-paste the python -c block).
    2. Single-call smoke: uv run python -c "
       import numpy as np, pandas as pd
       from master_thesis_code.galaxy_catalogue.handler import GalaxyCatalogueHandler, InternalCatalogColumns
       obj = object.__new__(GalaxyCatalogueHandler)
       obj._angles_mapped_to_ecliptic = False
       obj.reduced_galaxy_catalog = pd.DataFrame({
           InternalCatalogColumns.PHI_S: [0.0, 90.0], InternalCatalogColumns.THETA_S: [45.0, -30.0]
       })
       obj._map_angles_to_spherical_coordinates()
       print('Single-call PASS; flag:', obj._angles_mapped_to_ecliptic)
       "
       Must complete without AssertionError and print True.
    3. uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py -v — all 9 tests PASS.
    4. uv run ruff check master_thesis_code/galaxy_catalogue/handler.py — clean.
    5. uv run mypy master_thesis_code/galaxy_catalogue/handler.py — clean.
  </verify>
  <done>_angles_mapped_to_ecliptic flag initialized in __init__; guard asserts at top of _map_angles_to_spherical_coordinates; double-call raises AssertionError; single-call succeeds; 9 roundtrip tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 5: Commit all hygiene fixes as chore(37)</name>
  <files>
    master_thesis_code/constants.py
    master_thesis_code/cosmological_model.py
    master_thesis_code/main.py
    master_thesis_code/plotting/paper_figures.py
    master_thesis_code/galaxy_catalogue/handler.py
  </files>
  <action>
    Run full pre-commit check first:
      uv run pre-commit run --all-files
    If ruff or mypy fails, fix before staging.

    Run pytest smoke:
      uv run pytest -m "not gpu and not slow" -x -q
    Must pass (517+ passing, 0 failures). Phase 36's 9 roundtrip tests must remain GREEN.

    Stage only the five files listed. Use git add per-file (not git add -A or git add .) to avoid
    accidentally staging unrelated changes.

    Commit message:
    ```
    chore(37): software hygiene — Omega_m limits, SNR unification, C/1000, idempotency guard

    - PE-03: Fix swapped Omega_m lower/upper limits in LamCDMScenario (lower=0.04, upper=0.5)
    - PE-04: Add PRE_SCREEN_SNR_FACTOR=0.3 to constants.py; replace stale literal 15 in
      cosmological_model.py and paper_figures.py with SNR_THRESHOLD; name the pre-screen
      coefficient in main.py
    - PE-05: SPEED_OF_LIGHT_KM_S = C / 1000 (derived from astropy C; eliminates 300000.0 literal)
    - COORD-05: Idempotency guard on _map_angles_to_spherical_coordinates (AssertionError on
      second call; _angles_mapped_to_ecliptic: bool flag on GalaxyCatalogueHandler)

    No physics formula change — this commit is pure hygiene (data-entry fix, literal→constant
    substitution, runtime guard). Physics tasks (PE-01 h-threading, PE-02 per-parameter epsilon)
    land in Plan 37-02 under the /physics-change protocol.

    SC-4..SC-7 satisfied. Phase 36 roundtrip tests remain GREEN.

    REQ-IDs: COORD-05, PE-03, PE-04, PE-05
    ```

    Do NOT amend existing commits. Do NOT use --no-verify.
  </action>
  <verify>
    1. git log -1 --format=%s shows the chore(37) subject line.
    2. git show --stat HEAD lists exactly the five files above (or four if bayesian_plots.py was
       not updated — that is acceptable per D-13).
    3. uv run pytest -m "not gpu and not slow" -q — all tests pass.
    4. git diff HEAD~1 -- master_thesis_code/ | grep "^\+" | grep -E "\b(dist|inner_product|Fisher|PSD)\b" — must return empty (no physics formula in diff).
  </verify>
  <done>chore(37) commit landed. PE-03/04/05/COORD-05 REQs satisfied. Ready for Plan 37-02 (physics tasks).</done>
</task>

</tasks>

<verification>
- No physics formula changed: SPEED_OF_LIGHT_KM_S value is identical to 300000.0 to float64 precision; Omega_m limits are a data-entry fix; SNR threshold changes are substituting a constant name for its literal value; the idempotency guard adds a runtime check, not a computation.
- Phase 36 regression: 9 roundtrip tests must remain GREEN after every task in this wave.
- SC-4 (Omega_m physical range): lower_limit=0.04 < upper_limit=0.5, both in [0.04, 0.5].
- SC-5 (SNR grep clean): no literal 15 adjacent to snr in production source.
- SC-6 (C/1000): SPEED_OF_LIGHT_KM_S defined as C/1000; no 300000.0 literal in constants.py.
- SC-7 (idempotency): AssertionError on second call to _map_angles_to_spherical_coordinates.
</verification>

<success_criteria>
- chore(37) commit landed with all four REQ-IDs in the message.
- `uv run pytest -m "not gpu and not slow"` passes (≥517 tests, Phase 36 roundtrip tests GREEN).
- All four acceptance tests from the contract pass (omega-limits, speed-precision, snr-grep, double-call).
- Pre-commit hooks (ruff + mypy) clean on all five modified files.
- No [PHYSICS] prefix on this commit (no physics formula change).
- Plan 37-02 is unblocked: software baseline is stable before physics changes land.
</success_criteria>

<output>
After completion, proceed to Plan 37-02 (PE-01 h-threading + PE-02 per-parameter epsilon under physics protocol).
No SUMMARY.md between waves; Plan 37-03 writes the phase-level verification report.
</output>
