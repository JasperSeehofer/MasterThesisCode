---
phase: 06-data-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore, evaluation/mean_bounds.xlsx]
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "evaluation/mean_bounds.xlsx is not tracked by git"
    - "run_metadata.json is not tracked by git"
    - "evaluation/ directory is in .gitignore"
    - "git status shows a clean working tree (no untracked evaluation artifacts)"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rules for evaluation/ and run_metadata.json"
      contains: "evaluation/"
  key_links:
    - from: ".gitignore"
      to: "evaluation/"
      via: "gitignore pattern"
      pattern: "^evaluation/"
---

<objective>
Remove stale simulation outputs from git tracking and ensure .gitignore prevents future tracking.

Purpose: The repository currently tracks `evaluation/mean_bounds.xlsx` (committed before the gitignore rule was added). This file is a generated simulation artifact and should not be in version control. The `.gitignore` already contains `evaluation/` and `run_metadata.json` entries, but the previously-committed file must be explicitly removed from the git index.

Output: A clean repository with no tracked simulation artifacts and gitignore rules preventing future tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale evaluation/mean_bounds.xlsx from git tracking (DATA-01)</name>
  <read_first>.gitignore, evaluation/mean_bounds.xlsx</read_first>
  <files>evaluation/mean_bounds.xlsx</files>
  <action>
Remove `evaluation/mean_bounds.xlsx` from the git index without deleting it from disk:

```bash
git rm --cached evaluation/mean_bounds.xlsx
```

This file was committed in early project history (commits c878c93, 5577ab3, b1f41fe) before the `.gitignore` entry for `evaluation/` was added. The `--cached` flag removes it from tracking only; the local file remains on disk for reference but will be ignored going forward.

Verify that `run_metadata.json` is NOT tracked (it already appears in `.gitignore` and `git ls-files run_metadata.json` returns empty). No action needed for it — just confirm.

After removing from index, commit the change with message:
```
chore(06-data-cleanup): remove stale evaluation/mean_bounds.xlsx from tracking
```
  </action>
  <verify>
    <automated>git ls-files evaluation/mean_bounds.xlsx | wc -l | grep -q '^0$' && echo "PASS: not tracked" || echo "FAIL: still tracked"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files evaluation/mean_bounds.xlsx` returns empty (0 lines)
    - `git ls-files run_metadata.json` returns empty (0 lines)
    - `evaluation/mean_bounds.xlsx` still exists on disk (not deleted)
    - `git status` shows no staged or untracked evaluation files
  </acceptance_criteria>
  <done>evaluation/mean_bounds.xlsx is no longer tracked by git; file still exists on disk; run_metadata.json confirmed not tracked</done>
</task>

<task type="auto">
  <name>Task 2: Verify .gitignore coverage for evaluation/ directory (DATA-02)</name>
  <read_first>.gitignore</read_first>
  <files>.gitignore</files>
  <action>
Verify that `.gitignore` already contains the necessary entries. Based on current state, the file already has:

- `evaluation/` (line 7, with exclusion `!master_thesis_code_test/fixtures/evaluation/` for test fixtures)
- `run_metadata.json` (line 27)

These entries are already correct and sufficient. No modifications needed to `.gitignore`.

Run a final verification that the entire repository is clean:

```bash
git status
```

Expected: clean working tree (the only change should be the removal of `evaluation/mean_bounds.xlsx` from tracking, which was committed in Task 1).

Also verify that a new file created in `evaluation/` would be ignored:

```bash
touch evaluation/test_ignore_check.tmp
git status --porcelain evaluation/test_ignore_check.tmp
rm evaluation/test_ignore_check.tmp
```

The `git status --porcelain` should produce no output (file is ignored).
  </action>
  <verify>
    <automated>grep -q '^evaluation/' .gitignore && grep -q 'run_metadata.json' .gitignore && echo "PASS: gitignore entries present" || echo "FAIL: missing gitignore entries"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the line `evaluation/`
    - `.gitignore` contains the line `run_metadata.json`
    - `.gitignore` contains the exclusion `!master_thesis_code_test/fixtures/evaluation/` (test fixtures still tracked)
    - `git check-ignore evaluation/new_file.csv` returns `evaluation/new_file.csv` (ignored)
    - `git status` shows clean working tree after Task 1 commit
  </acceptance_criteria>
  <done>.gitignore confirmed to have evaluation/ and run_metadata.json entries; new files in evaluation/ are properly ignored; working tree is clean</done>
</task>

</tasks>

<verification>
Run these checks to confirm the phase is complete:

1. `git ls-files evaluation/` — must return empty
2. `git ls-files run_metadata.json` — must return empty
3. `grep 'evaluation/' .gitignore` — must show the entry
4. `grep 'run_metadata.json' .gitignore` — must show the entry
5. `git status --porcelain` — must return empty (clean tree)
6. `ls evaluation/mean_bounds.xlsx` — file still exists on disk (not deleted, just untracked)
</verification>

<success_criteria>
- No simulation outputs tracked by git (evaluation/mean_bounds.xlsx removed from index)
- .gitignore prevents future evaluation/ and run_metadata.json from being tracked
- Working tree is clean after all changes committed
- Local files preserved on disk (only removed from version control)
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-cleanup/06-01-SUMMARY.md`
</output>
