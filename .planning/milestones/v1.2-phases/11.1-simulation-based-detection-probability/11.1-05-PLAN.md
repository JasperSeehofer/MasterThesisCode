---
phase: 11.1-simulation-based-detection-probability
plan: 05
type: execute
wave: 3
depends_on: ["11.1-03", "11.1-04"]
files_modified: []
autonomous: false
requirements: [D-14, D-15]

must_haves:
  truths:
    - "Injection campaign completes on cluster producing ~10,000 events per h value for 7 h values"
    - "SimulationDetectionProbability loads injection data and builds P_det grids without errors"
    - "Pipeline B evaluation runs with simulation-based P_det producing an H0 posterior"
    - "H0 posterior is compared against P_det=1 baseline (h=0.678 peak)"
    - "h=0.73 falls within the 90% credible interval of the posterior (D-15)"
  artifacts: []
  key_links:
    - from: "cluster/submit_injection.sh"
      to: "simulations/injections/"
      via: "SLURM array jobs produce injection CSVs"
      pattern: "injection_h_"
    - from: "master_thesis_code/bayesian_inference/bayesian_statistics.py"
      to: "simulations/injections/"
      via: "SimulationDetectionProbability loads CSVs at evaluation time"
      pattern: "INJECTION_DATA_DIR"
---

<objective>
Run the injection campaign on the cluster, rsync results, run Pipeline B evaluation with the new simulation-based P_det, and validate that the H0 posterior is physically reasonable (h=0.73 in 90% CI).

Purpose: End-to-end validation that the simulation-based detection probability produces an unbiased H0 posterior, confirming the KDE P_det replacement resolves the bias identified in the debug session.
Output: Injection CSVs on local machine, H0 posterior evaluation results, comparison against P_det=1 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-simulation-based-detection-probability/11.1-CONTEXT.md
@.planning/phases/11.1-simulation-based-detection-probability/11.1-01-SUMMARY.md
@.planning/phases/11.1-simulation-based-detection-probability/11.1-02-SUMMARY.md
@.planning/phases/11.1-simulation-based-detection-probability/11.1-03-SUMMARY.md
@.planning/phases/11.1-simulation-based-detection-probability/11.1-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run injection campaign on cluster and rsync results</name>
  <files></files>
  <action>
The user must run the injection campaign on bwUniCluster and transfer results back.

Steps:
1. Push the current branch to the cluster (or git pull on login node)
2. Run: `bash cluster/submit_injection.sh --tasks_per_h 20 --steps 500 --seed 42`
   This submits 140 SLURM tasks (7 h values x 20 tasks), each producing 500 successful injection events = ~10,000 events per h value
3. Monitor with `squeue -u $USER` until all jobs complete
4. Rsync results back: `rsync -av cluster:/path/to/workspace/simulations/injections/ simulations/injections/`
5. Verify CSVs exist: `ls simulations/injections/injection_h_*_task_*.csv | wc -l` should be ~140
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/MasterThesisCode && ls simulations/injections/injection_h_*_task_*.csv 2>/dev/null | wc -l | xargs -I{} bash -c 'if [ {} -ge 100 ]; then echo "PASS: {} injection CSVs found"; else echo "FAIL: only {} CSVs"; fi'</automated>
  </verify>
  <done>~140 injection CSV files exist locally in simulations/injections/ with columns z, M, phiS, qS, SNR, h_inj, luminosity_distance</done>
</task>

<task type="auto">
  <name>Task 2: Run H0 posterior evaluation and validate</name>
  <files></files>
  <read_first>
    - master_thesis_code/bayesian_inference/bayesian_statistics.py (evaluate method)
    - master_thesis_code/main.py (evaluate entry point)
  </read_first>
  <action>
After injection data is available locally:

1. Run the evaluation pipeline with the new simulation-based P_det:
```bash
uv run python -m master_thesis_code . --evaluate --h_value 0.73
```
This should load injection CSVs via SimulationDetectionProbability, build P_det grids, and compute the posterior.

2. If the single h evaluation works, run the full h-value sweep (same pattern as existing evaluate.sbatch):
```bash
for h in 0.60 0.65 0.68 0.70 0.72 0.73 0.74 0.76 0.80 0.85 0.90; do
    uv run python -m master_thesis_code . --evaluate --h_value $h
done
```

3. Check outputs in `simulations/posteriors/` for the posterior JSON files.

4. Compare against the P_det=1 baseline (from previous evaluation runs -- peaks at h=0.678):
   - Does the posterior peak shift closer to h=0.73?
   - Does h=0.73 fall within the 90% credible interval?

5. Log results summary.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/MasterThesisCode && ls simulations/posteriors/ 2>/dev/null | head -5 && echo "Posterior files exist" || echo "No posterior files yet (expected if injection data not available)"</automated>
  </verify>
  <acceptance_criteria>
    - Posterior JSON files exist in `simulations/posteriors/`
    - Evaluation completes without errors for at least h=0.73
    - Results are logged for comparison against P_det=1 baseline
  </acceptance_criteria>
  <done>H0 posterior evaluation runs with simulation-based P_det, results compared against baseline per D-14</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validate H0 posterior results</name>
  <files></files>
  <action>User reviews H0 posterior output from Task 2 and confirms results are physically reasonable.</action>
  <verify>
    <automated>echo "Manual checkpoint -- user validates H0 posterior"</automated>
  </verify>
  <done>User confirms h=0.73 is within 90% credible interval per D-15, or identifies follow-up actions needed</done>
</task>

</tasks>

<verification>
1. Injection CSVs exist in simulations/injections/ for all 7 h values
2. Each h value has ~10,000 events (20 tasks x 500 events)
3. Pipeline B evaluation completes without errors
4. H0 posterior results exist in simulations/posteriors/
5. h=0.73 falls within 90% credible interval of the posterior (D-15)
</verification>

<success_criteria>
- Injection campaign data produced and transferred from cluster
- Pipeline B runs end-to-end with SimulationDetectionProbability (no fallback to old class)
- H0 posterior is physically reasonable: peak near h=0.73, true value in 90% CI
- Phase 11.1 goal achieved: KDE-based P_det replaced with simulation-based P_det
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-simulation-based-detection-probability/11.1-05-SUMMARY.md`
</output>
