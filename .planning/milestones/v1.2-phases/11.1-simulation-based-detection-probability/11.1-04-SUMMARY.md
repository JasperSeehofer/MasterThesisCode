---
phase: 11.1-simulation-based-detection-probability
plan: 04
subsystem: infra
tags: [slurm, sbatch, gpu, injection-campaign, cluster]

requires:
  - phase: 11.1-01
    provides: "--injection_campaign CLI flag and injection event loop in main.py"
provides:
  - "GPU sbatch script (inject.sbatch) for running injection campaign on cluster"
  - "Submission script (submit_injection.sh) for launching multi-h injection array jobs"
affects: [cluster-deployment, detection-probability-grid]

tech-stack:
  added: []
  patterns: ["seed isolation via h_index * 10000 offset between h values"]

key-files:
  created:
    - cluster/inject.sbatch
    - cluster/submit_injection.sh
  modified: []

key-decisions:
  - "Seed isolation: BASE_SEED + h_index * 10000 gives non-overlapping seed ranges per h value"
  - "Followed simulate.sbatch pattern exactly for environment setup, validation, symlink strategy"

patterns-established:
  - "Multi-h submission: loop over h values array, submit separate sbatch per h with exported H_VALUE"

requirements-completed: [D-05, D-08, D-09]

duration: 2min
completed: 2026-03-31
---

# Phase 11.1 Plan 04: Injection Campaign Cluster Scripts Summary

**SLURM cluster scripts for GPU injection campaign: inject.sbatch runs one h value per array job, submit_injection.sh launches all 7 h values with seed isolation**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-31T09:14:29Z
- **Completed:** 2026-03-31T09:16:23Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Created inject.sbatch GPU job script mirroring simulate.sbatch patterns with injection-specific flags
- Created submit_injection.sh that submits array jobs for all 7 h values (0.60-0.90) with configurable tasks-per-h and events-per-task
- Seed isolation between h values prevents seed collisions across the full campaign

## Task Commits

Each task was committed atomically:

1. **Task 1: Create inject.sbatch GPU job script** - `3c0083d` (feat)
2. **Task 2: Create submit_injection.sh submission script** - `cf6a1bc` (feat)

## Files Created/Modified
- `cluster/inject.sbatch` - GPU sbatch script for injection campaign; validates RUN_DIR, BASE_SEED, INJ_STEPS, H_VALUE; runs `python -m master_thesis_code` with `--injection_campaign`
- `cluster/submit_injection.sh` - Submission script; parses --tasks_per_h/--steps/--seed/--h_values; submits array jobs per h value with seed isolation

## Decisions Made
- Used h_index * 10000 offset for seed isolation between h values (supports up to 10,000 tasks per h value before overlap)
- Followed simulate.sbatch exactly for environment setup, PROJECT_ROOT fallback, symlink strategy, and variable validation pattern
- Log file naming uses underscore-separated h label (e.g., inject_h_0_73) matching existing conventions

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Cluster scripts ready for injection campaign submission on bwUniCluster
- Requires Plan 01 (injection campaign CLI) to be merged to the cluster branch
- Compatible with SimulationDetectionProbability CSV loading pattern from Plan 02/03

---
*Phase: 11.1-simulation-based-detection-probability*
*Completed: 2026-03-31*
