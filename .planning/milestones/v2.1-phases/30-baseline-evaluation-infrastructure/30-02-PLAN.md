---
phase: 30-baseline-evaluation-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - .planning/debug/baseline.json
autonomous: true
requirements: [DIAG-03]
gap_closure: true

must_haves:
  truths:
    - "baseline.json in .planning/debug/ contains real production data from run_v12_validation (11 h-values, non-zero bias)"
    - "baseline.json is committed to git (tracked, not untracked)"
  artifacts:
    - path: ".planning/debug/baseline.json"
      provides: "Real production baseline snapshot for cross-phase reference"
      contains: "map_h"
  key_links:
    - from: ".planning/debug/baseline.json"
      to: "evaluation/run_v12_validation/simulations/posteriors/"
      via: "--save_baseline CLI flag reading real h_*.json files"
      pattern: "h_0_"
---

<objective>
Generate the real production baseline.json from run_v12_validation posteriors and commit it.

Purpose: Phase 30 verification found that .planning/debug/baseline.json contains synthetic test data and is untracked. This gap closure runs --save_baseline against real production posteriors (11 h-values) and commits the result so all subsequent phases (31-34) can reference it.

Output: Committed `.planning/debug/baseline.json` with real MAP h, 68% CI, bias %, and event counts from the production evaluation run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-baseline-evaluation-infrastructure/30-VERIFICATION.md
@.planning/phases/30-baseline-evaluation-infrastructure/30-01-SUMMARY.md

@master_thesis_code/main.py
@master_thesis_code/bayesian_inference/evaluation_report.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate real baseline from production posteriors and commit</name>
  <files>.planning/debug/baseline.json</files>
  <action>
1. Run the --save_baseline CLI flag against the real production posteriors directory:

```bash
cd /home/jasper/Repositories/MasterThesisCode
uv run python -m master_thesis_code evaluation/run_v12_validation --save_baseline
```

This calls `_save_baseline("evaluation/run_v12_validation")` which constructs
`posteriors_dir = Path("evaluation/run_v12_validation") / "simulations" / "posteriors"`,
reads all 11 `h_*.json` files, computes MAP h, 68% CI, bias %, event count,
and writes the result to `.planning/debug/baseline.json`.

2. Validate the output baseline.json contains real data:
   - Must have 11 h-values (not 5 synthetic ones)
   - bias_percent must be non-zero (the known ~-9% bias from production)
   - n_events must reflect the actual detection count from the production run
   - map_h should be around 0.66 (the known biased MAP from run_v12_validation)

3. If the existing synthetic baseline.json is overwritten, verify the new file by
   inspecting its contents with `python -c "import json; d=json.load(open('.planning/debug/baseline.json')); print(f'h-values: {len(d[\"h_values\"])}, MAP: {d[\"map_h\"]:.4f}, bias: {d[\"bias_percent\"]:.1f}%, events: {d[\"n_events\"]}')"`.

4. Stage and commit the real baseline.json to git:
```bash
git add .planning/debug/baseline.json
git commit -m "docs(30): commit real production baseline from run_v12_validation"
```
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/MasterThesisCode && python -c "import json; d=json.load(open('.planning/debug/baseline.json')); assert len(d['h_values']) == 11, f'Expected 11 h-values, got {len(d[\"h_values\"])}'; assert d['bias_percent'] != 0.0, 'Bias should be non-zero'; print(f'OK: {len(d[\"h_values\"])} h-values, MAP={d[\"map_h\"]:.4f}, bias={d[\"bias_percent\"]:.1f}%')" && git log --oneline -1 -- .planning/debug/baseline.json | grep -q "baseline" && echo "COMMITTED"</automated>
  </verify>
  <done>
    - .planning/debug/baseline.json contains real production data (11 h-values from run_v12_validation)
    - MAP h, bias %, CI bounds, and event count reflect actual production posterior state
    - File is tracked and committed to git (not untracked)
    - DIAG-03 requirement fully satisfied
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem -> evaluation_report | Reads existing production posteriors; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | I (Info Disclosure) | baseline.json | accept | Contains only simulation metrics (MAP h, CI, bias). No PII or secrets. Intentionally committed per DIAG-03. |
</threat_model>

<verification>
1. `python -c "import json; d=json.load(open('.planning/debug/baseline.json')); print(len(d['h_values']), d['map_h'], d['bias_percent'])"` shows 11 h-values with non-zero bias
2. `git log --oneline -1 -- .planning/debug/baseline.json` shows a commit message referencing the baseline
3. `git status .planning/debug/baseline.json` shows nothing (file is tracked and clean)
</verification>

<success_criteria>
1. baseline.json contains 11 h-values from run_v12_validation production posteriors
2. bias_percent is non-zero (reflecting the known H0 bias under investigation)
3. File is committed to git and tracked
4. DIAG-03 gap from 30-VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/30-baseline-evaluation-infrastructure/30-02-SUMMARY.md`
</output>
