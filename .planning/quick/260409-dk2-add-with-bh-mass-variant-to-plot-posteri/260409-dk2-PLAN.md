---
phase: quick
plan: 260409-dk2
type: execute
wave: 1
depends_on: []
files_modified:
  - master_thesis_code/plotting/paper_figures.py
  - master_thesis_code_test/plotting/test_paper_figures.py
autonomous: true
must_haves:
  truths:
    - "plot_posterior_convergence shows both without-BH-mass and with-BH-mass convergence curves"
    - "Both variants use the same subset-sampling methodology with errorbar bands"
    - "The outdated delta-function docstring comment is removed"
    - "No other paper figure functions are missing a variant (audit confirms)"
  artifacts:
    - path: "master_thesis_code/plotting/paper_figures.py"
      provides: "Updated plot_posterior_convergence with both variants"
      contains: "posteriors_with_bh_mass"
    - path: "master_thesis_code_test/plotting/test_paper_figures.py"
      provides: "Test coverage for the dual-variant convergence plot"
  key_links:
    - from: "master_thesis_code/plotting/paper_figures.py"
      to: "_load_per_event_no_mass, _load_per_event_with_mass_scalars"
      via: "data loading for both variants"
      pattern: "_load_per_event.*posteriors"
---

<objective>
Add the with-BH-mass variant to `plot_posterior_convergence` so both analysis channels
appear on the convergence plot with separate errorbar curves. Remove the outdated docstring
claiming with-BH-mass posteriors are delta functions.

Purpose: The corrected posteriors now have proper distributions for both variants. The
convergence plot must show both to be scientifically complete and consistent with all
other paper figures that already display both variants.

Output: Updated `paper_figures.py` and test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@master_thesis_code/plotting/paper_figures.py
@master_thesis_code/plotting/_helpers.py
@master_thesis_code/plotting/_colors.py
@master_thesis_code_test/plotting/test_paper_figures.py

<interfaces>
From master_thesis_code/plotting/_colors.py:
```python
CYCLE: list[str]  # 7 Okabe-Ito colors; [0]=orange, [3]=yellow, index 3 used for with-mass
TRUTH: str        # bluish green
REFERENCE: str    # sky blue
```

From master_thesis_code/plotting/_helpers.py:
```python
def compute_credible_interval(h_values, posterior, level=0.68) -> tuple[float, float]
def get_figure(nrows=1, ncols=1, figsize=None, preset=None, **kwargs) -> tuple[Figure, Any]
```

From master_thesis_code/plotting/paper_figures.py:
```python
def _load_per_event_no_mass(base: Path) -> tuple[NDArray, dict[str, NDArray]]
def _load_per_event_with_mass_scalars(base: Path) -> tuple[NDArray, dict[str, NDArray]]
```
</interfaces>

Audit result: Only `plot_posterior_convergence` needs changes. The other paper figure
functions already show both variants (`plot_h0_posterior_comparison`, `plot_single_event_likelihoods`,
`plot_h0_posterior_kde`) or are variant-agnostic (`plot_snr_distribution` uses CRB CSV data).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add with-BH-mass variant to plot_posterior_convergence</name>
  <files>master_thesis_code/plotting/paper_figures.py</files>
  <action>
Modify `plot_posterior_convergence` (lines 410-540) to show both analysis variants:

1. **Update the docstring** (lines 417-441):
   - Change title from "without-BH-mass channel" to "both analysis variants"
   - REMOVE the paragraph (lines 420-422) that says "The with-BH-mass channel is intentionally omitted: its per-event posteriors collapse to a delta function on the coarse h-grid, making CI-width vs N scientifically meaningless."
   - Add `posteriors_with_bh_mass/` to the data_dir description
   - Update the module-level docstring (line 10) similarly: remove "(without-BH-mass channel only)"

2. **Load with-BH-mass data** after the existing no-mass loading (after line 447):
   - Add: `h_values_wm, events_wm = _load_per_event_with_mass_scalars(data_dir / "posteriors_with_bh_mass")`
   - Build a parallel `valid_ids_wm`, `event_matrix_wm`, `log_event_matrix_wm` following the same pattern as the no-mass data (lines 451-461)

3. **Refactor the subset-sampling loop** (lines 471-493):
   - Extract the existing loop body into a helper function `_compute_convergence_stats(log_event_matrix, n_events_total, subset_sizes, n_subsets, rng, h_values)` that returns `(used_sizes, medians, lo_pctiles, hi_pctiles)`. Define this as a module-level private function above `plot_posterior_convergence`.
   - Call it twice: once for no-mass, once for with-mass, each with the appropriate log_event_matrix and h_values.
   - Use the SAME rng instance (seeded once) so both variants get reproducible but independent draws.

4. **Plot both variants** (lines 501-530):
   - Keep the existing no-mass errorbar with `color=CYCLE[0]`, label `r"Without $M_z$"`, marker `"o"`
   - Add a second errorbar call for with-mass: `color=CYCLE[3]`, label `r"With $M_z$"`, marker `"s"`, linestyle for caps matching the existing style
   - Keep the single N^{-1/2} reference line anchored to the no-mass largest-N median (unchanged)

5. **Color consistency**: Use `CYCLE[0]` (orange) for without-mass and `CYCLE[3]` (yellow) for with-mass, matching all other paper figures.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/MasterThesisCode && uv run ruff check master_thesis_code/plotting/paper_figures.py && uv run ruff format --check master_thesis_code/plotting/paper_figures.py && uv run mypy master_thesis_code/plotting/paper_figures.py</automated>
  </verify>
  <done>
    - plot_posterior_convergence loads both posteriors/ and posteriors_with_bh_mass/ directories
    - Both variants rendered as separate errorbar curves with consistent colors (CYCLE[0] and CYCLE[3])
    - Outdated delta-function docstring removed
    - Module docstring updated
    - Passes ruff and mypy
  </done>
</task>

<task type="auto">
  <name>Task 2: Add smoke test for dual-variant convergence plot</name>
  <files>master_thesis_code_test/plotting/test_paper_figures.py</files>
  <action>
Add a new test to the `TestPaperFigureGracefulDegradation` class (or a new class `TestPosteriorConvergenceDualVariant`):

1. **test_posterior_convergence_with_synthetic_data**: Create synthetic per-event posterior JSON files in both `tmp_path / "posteriors"` and `tmp_path / "posteriors_with_bh_mass"` directories. For each:
   - Create 15 synthetic h-value JSON files (h_0_60.json through h_0_90.json, step 0.02)
   - Each file contains event IDs "0" through "19" (20 events) with random positive likelihood values seeded for reproducibility
   - Call `plot_posterior_convergence(data_dir=tmp_path, subset_sizes=[5, 10, 15], n_subsets=5)`
   - Assert returns (Figure, Axes)
   - Assert the axes has at least 2 errorbar collections (both variants plotted)
   - Close figure with `plt.close(fig)`

2. **For the with-mass JSONs**: The format must match what `_load_per_event_with_mass_scalars` expects -- each file has event IDs as keys with single-element list values like `{"0": [1.234], "1": [5.678], ...}`. Since the function reads only the tail of the file via regex, a simple JSON with this format works fine (the regex pattern `'"(\d+)": \[(\d+\.\d+(?:e[+-]?\d+)?)\]'` matches it).

3. **For the no-mass JSONs**: Format is `{"0": [1.234], "1": [5.678], ...}` matching what `_load_per_event_no_mass` expects.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/MasterThesisCode && uv run pytest master_thesis_code_test/plotting/test_paper_figures.py -x -v</automated>
  </verify>
  <done>
    - New test creates synthetic data for both variants and calls plot_posterior_convergence
    - Test verifies both variants are plotted (2 errorbar collections on axes)
    - All existing tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed -- this is internal plotting code with no user input or external data validation concerns.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | I (Information Disclosure) | paper_figures.py | accept | Plotting code only; no secrets or PII involved |
</threat_model>

<verification>
1. `uv run ruff check master_thesis_code/plotting/paper_figures.py` passes
2. `uv run mypy master_thesis_code/plotting/paper_figures.py` passes
3. `uv run pytest master_thesis_code_test/plotting/test_paper_figures.py -x -v` all tests pass
4. Module docstring and function docstring no longer mention "without-BH-mass only" or delta functions
</verification>

<success_criteria>
- plot_posterior_convergence produces a figure with two errorbar series (without and with BH mass)
- Colors match other paper figures: CYCLE[0] for without-mass, CYCLE[3] for with-mass
- No outdated delta-function comments remain
- All lint/type/test checks pass
</success_criteria>

<output>
After completion, create `.planning/quick/260409-dk2-add-with-bh-mass-variant-to-plot-posteri/260409-dk2-SUMMARY.md`
</output>
