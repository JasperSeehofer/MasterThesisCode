---
phase: 39-hpc-visualization-safe-wins
plan: 06
type: execute
wave: 4
depends_on: [39-01, 39-02, 39-03, 39-04, 39-05]
files_modified:
  - .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [HPC-01, HPC-02, HPC-03, HPC-04, HPC-05, VIZ-01, VIZ-02]
requirements_addressed: [HPC-01, HPC-02, HPC-03, HPC-04, HPC-05, VIZ-01, VIZ-02]
tags: [verification, phase-close, state-update]

must_haves:
  truths:
    - "All 7 ROADMAP Phase 39 success criteria (SC-1..SC-7) pass with a checkbox tick and evidence"
    - "Phase 36 regression tests (test_coordinate_roundtrip.py — 9 tests) remain GREEN"
    - "Phase 37 regression tests (test_parameter_space_h.py) remain GREEN"
    - "Phase 38 regression tests (test_l_cat_equivalence.py) remain GREEN"
    - "Full CPU suite ≥525 tests (baseline 524 + Plan 02 +1 new + Plan 03 +3 new + Plan 04 +4 new ≥ 532)"
    - "STATE.md position advanced to Phase 40; ROADMAP.md Phase 39 checkbox flipped to [x]"
    - "REQUIREMENTS.md HPC-01..HPC-05, VIZ-01, VIZ-02 checkboxes flipped to [x]"
    - "GitHub issues for HPC/VIZ findings (if any open) are closed with commit references"
  artifacts:
    - path: ".planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md"
      provides: "Phase 39 verification gate with SC-1..SC-7 evidence, test counts, commit hashes"
      contains: "SC-7"
    - path: ".planning/STATE.md"
      provides: "Current Position advanced to Phase 40"
      contains: "Phase: 40"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 39 marked [x] complete; plan checkboxes all ticked"
      contains: "[x] **Phase 39: HPC & Visualization Safe Wins**"
    - path: ".planning/REQUIREMENTS.md"
      provides: "HPC-01..HPC-05, VIZ-01, VIZ-02 status flipped to Done"
      contains: "HPC-01 | Phase 39 | GSD | Done"
  key_links:
    - from: "39-VERIFICATION.md SC-1"
      to: "39-01-SUMMARY.md CPU-import subprocess output"
      via: "cross-reference"
      pattern: "SC-1.*HPC-01"
    - from: "39-VERIFICATION.md SC-5"
      to: "39-05-VERIFICATION.md decision record"
      via: "cross-reference"
      pattern: "SC-5.*HPC-05"
---

<objective>
Run the end-to-end Phase 39 verification harness and drive the three planning ledgers (STATE.md, ROADMAP.md, REQUIREMENTS.md) to their Phase-39-complete state. Every ROADMAP Success Criterion SC-1..SC-7 must pass with grep-checkable evidence. Phase 36/37/38 regressions must remain GREEN. GitHub issues that track HPC/VIZ bugs (if any are open) are closed with commit references per CLAUDE.md §GitHub Integration.

Purpose: Close Phase 39 cleanly so Phase 40 (Verification Gate for the full campaign) starts from a known-good CPU-importable, I/O-batched, LaTeX-ready, HDI-banded codebase. Phase 40 itself will re-evaluate CRB posteriors — it depends on Phase 39 being provably complete.

Output: One verification markdown, three ledger updates, one set of GitHub issue closures (if applicable), one "phase-close" commit.

NOTE: `autonomous: false` because this plan includes a final human checkpoint to confirm SC pass/fail before advancing STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/39-hpc-visualization-safe-wins/39-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-hpc-visualization-safe-wins/39-01-PLAN.md
@.planning/phases/39-hpc-visualization-safe-wins/39-02-PLAN.md
@.planning/phases/39-hpc-visualization-safe-wins/39-03-PLAN.md
@.planning/phases/39-hpc-visualization-safe-wins/39-04-PLAN.md
@.planning/phases/39-hpc-visualization-safe-wins/39-05-PLAN.md

<interfaces>
<!-- ROADMAP Phase 39 Success Criteria (verbatim from ROADMAP.md lines 109-117) -->
<!-- SC-1: from master_thesis_code.parameter_estimation.parameter_estimation import ParameterEstimation succeeds on a machine without cupy installed; pytest -m "not gpu" runs the module's tests without skipping on CPU-only CI -->
<!-- SC-2: _crb_flush_interval = 25 in parameter_estimation.py:99; SIGTERM handler still flushes on termination; a single-task dry run writes the CSV in batches of 25 (verified by a small N=50 simulation test) -->
<!-- SC-3: memory_management.py no longer clears _fft_cache per Fisher iteration; cache clear happens only on explicit free_gpu_memory() call or memory-pressure trigger -->
<!-- SC-4: _crop_frequency_domain no longer appears in parameter_estimation.py; grep returns empty -->
<!-- SC-5: flip_hx=True status is documented in waveform_generator.py with either a kept-for-reason comment citing current fastlisaresponse behavior, or removed via /physics-change with waveform regression test showing pre/post identical result -->
<!-- SC-6: main.py:generate_figures calls apply_style(use_latex=True) when shutil.which("latex") is truthy and falls back to mathtext otherwise; smoke test confirms both branches run without crash -->
<!-- SC-7: Static plot_h0_convergence displays a 16/84 percentile shaded HDI band; visual smoke test confirms the band is present and sits inside the CI rails -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the SC-1..SC-7 verification harness and write 39-VERIFICATION.md</name>
  <files>.planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 39 §Success Criteria, lines 109-117 — verbatim conditions)
    - .planning/phases/39-hpc-visualization-safe-wins/39-01-SUMMARY.md, 39-02-SUMMARY.md, 39-03-SUMMARY.md, 39-04-SUMMARY.md, 39-05-SUMMARY.md (prior plan outputs)
    - .planning/phases/39-hpc-visualization-safe-wins/39-05-VERIFICATION.md (HPC-05 decision record)
    - .planning/phases/36-coordinate-frame-fix/36-VERIFICATION.md, .planning/phases/37-parameter-estimation-correctness/37-VERIFICATION.md, .planning/phases/38-statistical-correctness/38-VERIFICATION.md (for the D-30 regression baseline)
    - CLAUDE.md §GitHub Integration (issue-closure requirements)
  </read_first>
  <action>
    **Step 1 — Run the canonical test gates.** Execute the following in sequence and capture each exit code and output line count:

    ```bash
    # Gate 1: full CPU test suite
    uv run pytest master_thesis_code_test/ -m "not gpu" -q --tb=short 2>&1 | tail -20

    # Gate 2: Phase 36 regressions (must stay GREEN)
    uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py -v

    # Gate 3: Phase 37 regression
    uv run pytest master_thesis_code_test/test_parameter_space_h.py -v

    # Gate 4: Phase 38 regression
    uv run pytest master_thesis_code_test/test_l_cat_equivalence.py -v

    # Gate 5: lint + type
    uv run ruff check master_thesis_code/
    uv run mypy master_thesis_code/
    ```

    **Step 2 — Run the per-SC verification commands.** For each SC, run the corresponding command and record the output. These map 1:1 to the ROADMAP phrasing:

    ```bash
    # SC-1: CPU-importable
    uv run python -c "import sys; sys.modules['cupy'] = None; sys.modules['cupyx'] = None; sys.modules['cupyx.scipy'] = None; sys.modules['cupyx.scipy.fft'] = None; from master_thesis_code.parameter_estimation.parameter_estimation import ParameterEstimation; print('OK')"

    # SC-2: flush interval + SIGTERM drain
    rg "_crb_flush_interval: int = 25" master_thesis_code/parameter_estimation/parameter_estimation.py
    uv run pytest master_thesis_code_test/parameter_estimation/parameter_estimation_test.py::test_sigterm_drain_with_flush_interval_25 -v

    # SC-3: fft cache lifecycle
    rg "_fft_cache\.clear" master_thesis_code/ | wc -l      # expect 1
    rg "free_gpu_memory_if_pressured" master_thesis_code/main.py | wc -l   # expect 2

    # SC-4: dead code removed
    rg "_crop_frequency_domain" master_thesis_code/ master_thesis_code_test/   # expect empty

    # SC-5: flip_hx documented
    rg "fastlisaresponse|arXiv:2204\.06633" master_thesis_code/waveform_generator.py
    cat .planning/phases/39-hpc-visualization-safe-wins/39-05-VERIFICATION.md | rg "\[x\]"

    # SC-6: LaTeX gating
    rg "shutil\.which\(\"latex\"\)" master_thesis_code/main.py | wc -l   # expect 1
    rg "apply_style\(use_latex=True\)" master_thesis_code/main.py | wc -l   # expect 1
    uv run pytest master_thesis_code_test/test_generate_figures.py -k "latex" -v

    # SC-7: HDI band
    rg "bootstrap_bank" master_thesis_code/plotting/convergence_plots.py | wc -l   # expect >= 6
    rg "bootstrap_bank=bootstrap_bank" master_thesis_code/main.py | wc -l         # expect 1
    uv run pytest master_thesis_code_test/plotting/test_convergence_plots.py -k "bootstrap_bank" -v
    ```

    **Step 3 — Write `.planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md`** with this structure:

    ```markdown
    # Phase 39: HPC & Visualization Safe Wins — Verification

    **Date:** <YYYY-MM-DD>
    **Requirements:** HPC-01, HPC-02, HPC-03, HPC-04, HPC-05, VIZ-01, VIZ-02

    ## Summary
    All 7 ROADMAP Phase 39 success criteria PASS. 524-test Phase-38 baseline preserved
    at <actual-count> tests post-Phase-39 additions. Phase 36/37/38 regressions intact.

    ## Success Criteria

    ### SC-1 — HPC-01 CPU-importable parameter_estimation.py
    **Status:** [x] PASS
    **Evidence:**
    - `python -c "..." → OK` (subprocess with cupy shadowed)
    - Commit: <39-01 commit hash>
    - Refactored call sites: <count> self._xp / self._fft references

    ### SC-2 — HPC-02 _crb_flush_interval = 25 + SIGTERM drain
    **Status:** [x] PASS
    **Evidence:**
    - `rg "_crb_flush_interval: int = 25" parameter_estimation.py` → 1 match
    - `test_sigterm_drain_with_flush_interval_25` → PASSED
    - Commit: <39-02 commit hash>

    ### SC-3 — HPC-03 FFT cache lifecycle decoupled
    **Status:** [x] PASS
    **Evidence:**
    - `rg "_fft_cache\.clear" master_thesis_code/` → exactly 1 (inside `clear_fft_cache`)
    - `rg "free_gpu_memory_if_pressured" main.py` → 2 (both call sites migrated)
    - Commit: <39-03 commit hash>

    ### SC-4 — HPC-04 _crop_frequency_domain removed
    **Status:** [x] PASS
    **Evidence:**
    - `rg "_crop_frequency_domain" master_thesis_code/ master_thesis_code_test/` → empty
    - Commit: <39-01 commit hash>

    ### SC-5 — HPC-05 flip_hx documented/removed
    **Status:** [x] PASS (path: <KEEP or REMOVE>)
    **Evidence:**
    - `arXiv:2204.06633` cited in `waveform_generator.py`
    - Decision recorded in `39-05-VERIFICATION.md`
    - Commit: <39-05 commit hash>

    ### SC-6 — VIZ-01 LaTeX auto-detection
    **Status:** [x] PASS
    **Evidence:**
    - `shutil.which("latex")` + `apply_style(use_latex=True)` both present in main.py
    - Both smoke test branches PASSED (TestApplyStyleLatexGating)
    - Commit: <39-04 commit hash>

    ### SC-7 — VIZ-02 HDI band on convergence plot
    **Status:** [x] PASS
    **Evidence:**
    - `bootstrap_bank` kwarg + `fill_between` on right panel
    - `test_plot_h0_convergence_with_bootstrap_bank_adds_fill_between` PASSED
    - Commit: <39-04 commit hash>

    ## Regression check (D-30)
    - test_coordinate_roundtrip.py (Phase 36): 9/9 GREEN
    - test_parameter_space_h.py (Phase 37): N/N GREEN
    - test_l_cat_equivalence.py (Phase 38): 3/3 GREEN
    - Full CPU suite: <actual-count> tests, 0 failures

    ## Lint / type gate
    - ruff: 0 issues
    - mypy: 0 errors

    ## Plan summary pointers
    - 39-01-SUMMARY.md
    - 39-02-SUMMARY.md
    - 39-03-SUMMARY.md
    - 39-04-SUMMARY.md
    - 39-05-SUMMARY.md
    - 39-05-VERIFICATION.md
    ```

    Fill in every `<...>` placeholder with the actual output captured in Steps 1 and 2. If any SC FAILS, mark its checkbox `[ ]`, report the failure verbatim, and STOP — do not advance to Task 2. The human checkpoint in Task 3 decides remediation.

    **Step 4 — GitHub issue closures.** Per CLAUDE.md §GitHub Integration: check for open issues tagged `hpc` or `physics` related to HPC-01..HPC-05 or VIZ-01..VIZ-02:

    ```bash
    gh issue list --label "hpc" --state open
    gh issue list --label "physics" --state open | rg -i "flip_hx|crop_frequency|flush_interval|fft.*cache|cupy.*import"
    ```

    For each matching open issue, close it with a comment referencing the fixing commit(s):

    ```bash
    gh issue close <N> --comment "Fixed in Phase 39 <plan-id> (commit <hash>): <one-line summary>. See .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md SC-<N>."
    ```

    If no open issues match, note "No open GitHub issues matched HPC/VIZ REQ-IDs" in the VERIFICATION.md `## GitHub Integration` section. Add the section even if empty.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md
      rg "SC-1|SC-2|SC-3|SC-4|SC-5|SC-6|SC-7" .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md | wc -l   # expect >= 14
      rg "\[x\]" .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md | wc -l   # expect >= 7 (all SCs ticked)
      rg "\[ \]" .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md | wc -l   # expect 0 (no unticked SCs) — OR stop plan if >0
      # Re-confirm all technical gates still hold:
      uv run pytest master_thesis_code_test/ -m "not gpu" -q
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md` exists and contains all 7 SC sections
    - Every SC section contains a `[x]` checkbox and at least one commit-hash or grep-output evidence line
    - Regression check section lists Phase 36/37/38 test pass counts
    - `rg "\[ \]" .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md` returns 0 (all SCs passed) — if non-zero, stop and report failure to Task 3 checkpoint
    - Full CPU suite `uv run pytest master_thesis_code_test/ -m "not gpu" -q` exits 0 with ≥525 tests (≥524 baseline + Plan 02 +1 new; likely ≥532 after all plans)
    - `## GitHub Integration` section present with either closure references or "no matches" note
  </acceptance_criteria>
  <done>
    Verification markdown complete, all SCs marked PASS, regression baseline preserved, GitHub issues closed or explicitly noted absent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md, ROADMAP.md, REQUIREMENTS.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (entire file; locate frontmatter `progress.*` keys, `Current Position`, `Last activity`, `Phase Notes (v2.2 — active)`)
    - .planning/ROADMAP.md (Phase 39 entry at line 23 + Plans subsection + Progress table at line 165)
    - .planning/REQUIREMENTS.md §HPC Hygiene (lines 34-40), §Visualization (lines 42-45), Traceability table (lines 92-120)
    - .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md (just written by Task 1)
  </read_first>
  <action>
    **Step 0 — Reconcile the STATE.md baseline BEFORE applying any Phase 39 deltas.**
    Read the CURRENT values of the three progress counters:

    ```bash
    rg "^(total_phases|completed_phases|total_plans|completed_plans|percent):" .planning/STATE.md
    ```

    At the time this plan was authored (2026-04-22), STATE.md showed:

    ```yaml
    total_phases: 8
    completed_phases: 3
    total_plans: 14
    completed_plans: 12
    percent: 86
    ```

    Check that the `total_plans` baseline already includes every prior-phase plan (Phases 35, 36, 37, 38) AND every Phase 39 plan. The 2026-04-22 baseline (`total_plans: 14`) does NOT yet include Phase 39's 6 plans. Before applying any Phase 39 deltas, bring the baseline into a self-consistent state by adding Phase 39's 6 plans to `total_plans`. If the baseline at execution time has ALREADY been bumped (some other process may have done this), skip this adjustment — never double-count.

    Executor: compute the correct target values by INCREMENTS, not hardcoded numbers, so a stale baseline auto-corrects:

    - `completed_phases` → increment by **1** (Phase 39 ships as one whole phase)
    - `total_plans` → increment by **6** IF the current baseline does not yet include Phase 39 (the 6 plans: 39-01..39-06); otherwise increment by **0**
    - `completed_plans` → increment by **6** (all 6 Phase 39 plans, INCLUDING this meta closure plan 39-06 — each represents real work delivered and committed)
    - `percent` → recompute as `round(100 * completed_plans / total_plans)` after the increments are applied

    **Worked example assuming the 2026-04-22 baseline is still current at execution time:**

    ```
    total_phases     :   8 (unchanged)
    completed_phases :   3  + 1  =   4
    total_plans      :  14  + 6  =  20
    completed_plans  :  12  + 6  =  18
    percent          :  round(100 * 18 / 20) = 90
    ```

    If the baseline differs at execution time, use the same `+1 / +6 / +6 / recompute` recipe against the current values — never paste the numbers from this worked example verbatim.

    **Step 1 — STATE.md updates** (use Edit tool on existing file):

    - Change frontmatter `stopped_at:` to `Phase 39 complete; Phase 40 ready`
    - Change frontmatter `last_activity:` to `<YYYY-MM-DD> — Phase 39 complete; SC-1..SC-7 PASS; 524+ tests GREEN`
    - Change frontmatter `last_updated:` to current ISO-8601 timestamp
    - Apply the reconciled increments from Step 0 to `progress.completed_phases`, `progress.total_plans`, `progress.completed_plans`, `progress.percent`
    - Under `## Current Position`, update `Phase:` from `39` to `40 — NEXT, READY TO EXECUTE` and `Status:` to `In-progress (Phase 39 COMPLETE <date>; Phase 40 queued)`
    - Under `### Phase Notes (v2.2 — active)`, insert a new sub-section ABOVE the Phase 38 entry:

      ```markdown
      **Phase 39 (HPC & Visualization Safe Wins) — COMPLETE <YYYY-MM-DD>:**
      HPC-01..HPC-05, VIZ-01, VIZ-02 all resolved. SC-1..SC-7 all PASS.
      - HPC-01: parameter_estimation.py CPU-importable via self._xp/self._fft shim (commit <...>)
      - HPC-02: _crb_flush_interval raised 1→25; SIGTERM drain regression test passes (commit <...>)
      - HPC-03: memory_management API split (free_memory_pool + clear_fft_cache + free_gpu_memory_if_pressured); main.py call sites migrated (commit <...>)
      - HPC-04: _crop_frequency_domain deleted; grep gate clean (commit <...>)
      - HPC-05: <KEEP or REMOVE>; see 39-05-VERIFICATION.md (commit <...>)
      - VIZ-01: LaTeX auto-detection in generate_figures; both branches smoke-tested (commit <...>)
      - VIZ-02: Bootstrap HDI band on plot_h0_convergence right panel (commit <...>)
      Full CPU suite: <N> tests GREEN. Phase 36/37/38 regressions intact.
      ```

    - Under `## Session Continuity`, update `Next command:` to `/gsd:execute-phase 40` (or the GPD variant if Phase 40 is routed GPD — the ROADMAP shows `GSD+GPD` for Phase 40).

    **Step 2 — ROADMAP.md updates**:

    - Change line 23 `- [ ] **Phase 39: HPC & Visualization Safe Wins**` to `- [x] **Phase 39: HPC & Visualization Safe Wins**`
    - In the "Phase 39" subsection (around line 102-117), update the `**Plans**: TBD` line (line 117) to:

      ```markdown
      **Plans**: 6 plans
        - [x] 39-01-PLAN.md — HPC-01 self._xp/self._fft shim + HPC-04 dead code removal (Wave 1)
        - [x] 39-02-PLAN.md — HPC-02 _crb_flush_interval=25 + SIGTERM regression test (Wave 2)
        - [x] 39-03-PLAN.md — HPC-03 memory_management API split + main.py call-site migration (Wave 2)
        - [x] 39-04-PLAN.md — VIZ-01 LaTeX auto-detect + VIZ-02 HDI band (Wave 3)
        - [x] 39-05-PLAN.md — HPC-05 flip_hx verify + reference comment (<KEEP or REMOVE>) (Wave 3)
        - [x] 39-06-PLAN.md — Phase 39 verification + state update (Wave 4)
      ```

    - In the Progress table (line 165+), update the Phase 39 row: `| 39. HPC & Visualization Safe Wins | GSD+GPD | 6/6 | Complete | <YYYY-MM-DD> |`

    **Step 3 — REQUIREMENTS.md updates**:

    - Lines 36-40 (HPC-01..HPC-05 checkboxes): change `[ ]` → `[x]` on each
    - Lines 44-45 (VIZ-01, VIZ-02 checkboxes): change `[ ]` → `[x]` on each
    - Traceability table (lines 92-120): for each of HPC-01, HPC-02, HPC-03, HPC-04, HPC-05, VIZ-01, VIZ-02 rows, change `Pending` → `Done`
    - Update the `*Last updated: ...*` trailer at the bottom of the file (line ~132) to reflect `<YYYY-MM-DD> — Phase 39 complete: HPC-01..HPC-05, VIZ-01, VIZ-02 checkboxes flipped; traceability status Pending → Done`

    **Step 4.** Verify the three files are internally consistent — no orphan `[ ]` checkboxes for Phase 39 requirements, no `Pending` entries for REQ-IDs that the VERIFICATION.md marked PASS. Verify `progress.completed_plans <= progress.total_plans` (sanity).
  </action>
  <verify>
    <automated>
      # STATE.md updated
      rg "Phase: 40" .planning/STATE.md | wc -l   # expect >= 1
      rg "Phase 39 \(HPC & Visualization Safe Wins\) — COMPLETE" .planning/STATE.md | wc -l   # expect 1
      # Progress counters are internally consistent (completed <= total, percent is the ratio)
      uv run python -c "
      import re
      txt = open('.planning/STATE.md').read()
      cp = int(re.search(r'completed_plans:\s+(\d+)', txt).group(1))
      tp = int(re.search(r'total_plans:\s+(\d+)', txt).group(1))
      cph = int(re.search(r'completed_phases:\s+(\d+)', txt).group(1))
      pct = int(re.search(r'percent:\s+(\d+)', txt).group(1))
      assert cp <= tp, f'completed_plans {cp} > total_plans {tp}'
      assert cph >= 4, f'completed_phases {cph} should be >= 4 after Phase 39 close'
      expected_pct = round(100 * cp / tp)
      assert abs(pct - expected_pct) <= 1, f'percent {pct} != round(100*{cp}/{tp}) = {expected_pct}'
      print(f'OK: completed_plans={cp}, total_plans={tp}, completed_phases={cph}, percent={pct}')
      "
      # ROADMAP.md updated
      rg "^\- \[x\] \*\*Phase 39: HPC & Visualization Safe Wins\*\*" .planning/ROADMAP.md | wc -l   # expect 1
      rg "39-01-PLAN\.md" .planning/ROADMAP.md | wc -l   # expect 1
      rg "39. HPC & Visualization Safe Wins.*Complete" .planning/ROADMAP.md | wc -l   # expect 1
      # REQUIREMENTS.md updated
      rg "^\- \[x\] \*\*HPC-0" .planning/REQUIREMENTS.md | wc -l   # expect 5
      rg "^\- \[x\] \*\*VIZ-0" .planning/REQUIREMENTS.md | wc -l   # expect 2
      rg "HPC-0[1-5].*Done|VIZ-0[12].*Done" .planning/REQUIREMENTS.md | wc -l   # expect 7
    </automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter `last_activity` references Phase 39 complete
    - STATE.md frontmatter `progress.completed_phases` incremented by exactly 1 (Phase 39)
    - STATE.md frontmatter `progress.completed_plans` incremented by exactly 6 (Phase 39's six plans, including this closure plan)
    - STATE.md frontmatter `progress.total_plans` reflects inclusion of Phase 39's six plans (either by +6 increment if not yet present, or unchanged if baseline already counted them)
    - STATE.md frontmatter `progress.percent` matches `round(100 * completed_plans / total_plans)` within ±1
    - STATE.md `## Current Position` shows `Phase: 40` (or similar "READY TO EXECUTE" for 40)
    - STATE.md has a new "Phase 39 (HPC & Visualization Safe Wins) — COMPLETE" sub-section listing all 7 REQ-IDs and commit hashes
    - ROADMAP.md Phase 39 entry (line ~23) starts with `- [x]`
    - ROADMAP.md Phase 39 `**Plans**:` subsection lists all 6 plans, each with `[x]`
    - ROADMAP.md Progress table Phase 39 row shows `Complete` with date
    - REQUIREMENTS.md HPC-01..HPC-05 checkboxes all `[x]`
    - REQUIREMENTS.md VIZ-01, VIZ-02 checkboxes all `[x]`
    - REQUIREMENTS.md Traceability table: HPC-01..HPC-05, VIZ-01, VIZ-02 all `Done`
    - `rg "HPC-0[1-5].*Pending|VIZ-0[12].*Pending" .planning/REQUIREMENTS.md` returns empty (no orphan Pending entries)
  </acceptance_criteria>
  <done>
    All three ledgers updated consistently; progress counters arithmetically sound (completed ≤ total, percent matches ratio); no orphan checkboxes; Phase 40 marked as the next executable phase.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — confirm Phase 39 complete and ready for Phase 40</name>
  <what-built>
    Phase 39 (HPC & Visualization Safe Wins) has been executed across 5 implementation plans (39-01..39-05) plus this verification plan (39-06). All 7 ROADMAP success criteria (SC-1..SC-7) are marked PASS in `.planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md`. STATE.md, ROADMAP.md, and REQUIREMENTS.md have been updated. Full CPU test suite is GREEN with ≥524 tests (likely ≥532 after all plans' new tests).

    Key deliverables:
    - `parameter_estimation.py` is CPU-importable (verified via cupy-shadowed subprocess)
    - `_crb_flush_interval = 25` with SIGTERM-drain regression test
    - `memory_management.py` split into pool/cache/pressure-gated methods
    - `_crop_frequency_domain` dead code removed (grep clean)
    - `flip_hx` verified and documented (or removed via /physics-change per 39-05 decision)
    - LaTeX auto-detection in `generate_figures`
    - Bootstrap HDI band on `plot_h0_convergence` right panel
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md` and confirm every SC has `[x]` and an evidence line. Specifically check:
       - SC-1: CPU-import subprocess printed "OK"
       - SC-2: `_crb_flush_interval: int = 25` grep returned 1 match
       - SC-3: `_fft_cache.clear` grep returned exactly 1 match
       - SC-4: `_crop_frequency_domain` grep returned empty
       - SC-5: `arXiv:2204.06633` citation present in `waveform_generator.py`
       - SC-6: `shutil.which("latex")` + `apply_style(use_latex=True)` present in `main.py`
       - SC-7: `bootstrap_bank` + `fill_between` present in `convergence_plots.py`
    2. Open `.planning/STATE.md` and confirm `## Current Position` lists Phase 40 as NEXT. Sanity-check progress counters: `completed_plans ≤ total_plans`, `completed_phases ≥ 4`, `percent ≈ 100 * completed_plans / total_plans`.
    3. Open `.planning/ROADMAP.md` line 23 and confirm Phase 39 checkbox is `[x]`.
    4. Run `uv run pytest master_thesis_code_test/ -m "not gpu" -q` locally and confirm the exit code is 0.
    5. (Optional) Check GitHub: `gh issue list --label hpc --state closed` for any issues closed by this phase.

    Respond `approved` to advance STATE.md + commit the phase-close or describe any gap you see.
  </how-to-verify>
  <action>Present the deliverable summary in <what-built> to the user and wait for approval. Re-read 39-VERIFICATION.md + STATE.md + ROADMAP.md to confirm all checkboxes are correctly ticked. If the user responds "approved" proceed to Task 4. If the user reports gaps, stop and address them in a new plan before advancing.</action>
  <verify><automated>rg "\[x\]" .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md | wc -l</automated></verify>
  <acceptance_criteria>
    - User has explicitly responded "approved" OR described a blocking gap
    - All 7 SCs in 39-VERIFICATION.md are ticked (`[x]` count >= 7)
  </acceptance_criteria>
  <done>Human has approved advancing to the phase-close commit.</done>
  <resume-signal>Type "approved" to commit the phase-close or describe issues blocking the close</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Final commit — phase close</name>
  <files>.planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - git status (via gsd-sdk query status or equivalent)
  </read_first>
  <action>
    After Task 3 approval, commit the four updated ledger files together with the phase-close message:

    ```bash
    git add .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md \
            .planning/STATE.md \
            .planning/ROADMAP.md \
            .planning/REQUIREMENTS.md
    git commit -m "chore(39): Phase 39 complete — HPC/VIZ safe wins shipped

    SC-1..SC-7 all PASS. HPC-01..HPC-05, VIZ-01, VIZ-02 resolved.
    Phase 36/37/38 regressions intact. Full CPU suite GREEN.
    Next: Phase 40 Verification Gate.

    See .planning/phases/39-hpc-visualization-safe-wins/39-VERIFICATION.md"
    ```

    Do NOT push. The user runs `git push` themselves after reviewing locally. Do NOT run `/gsd-progress` or start Phase 40 — those are separate user-invoked workflows.
  </action>
  <verify>
    <automated>
      git log -1 --oneline | rg "chore\(39\): Phase 39 complete"
      git diff HEAD~1 --name-only | rg "STATE\.md|ROADMAP\.md|REQUIREMENTS\.md|39-VERIFICATION\.md" | wc -l   # expect 4
    </automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` contains the message `chore(39): Phase 39 complete — HPC/VIZ safe wins shipped`
    - The commit includes all four files (39-VERIFICATION.md, STATE.md, ROADMAP.md, REQUIREMENTS.md)
    - Working tree is clean (`git status` shows no uncommitted changes)
  </acceptance_criteria>
  <done>
    Phase 39 committed atomically. The repository is in a consistent "Phase 39 shipped, Phase 40 queued" state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning ledger ↔ source-of-truth test output | VERIFICATION.md asserts SC pass/fail based on grep and pytest output; false ticks would mislead Phase 40 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06-01 | Tampering | VERIFICATION.md could be written with [x] checkboxes for SCs that actually fail | mitigate | Task 1's `<automated>` gate asserts `rg "\[ \]" 39-VERIFICATION.md` returns 0 BEFORE this plan advances to Task 2; Task 3 human checkpoint re-reads the ledger + runs the suite. Two-layer check. |
| T-39-06-02 | Repudiation | STATE.md advanced to Phase 40 without actual Phase 39 completion | mitigate | Human checkpoint (Task 3) gates Task 4 commit. Only one actor — the user — can approve advance. |
| T-39-06-03 | Information Disclosure | STATE.md progress counters drift (completed_plans > total_plans, or stale total_plans causes wrong percent) | mitigate | Task 2 Step 0 reconciles the baseline before applying deltas; Task 2 `<automated>` gate enforces `completed ≤ total` and `percent ≈ round(100 * completed/total)`. |
</threat_model>

<verification>
1. 39-VERIFICATION.md exists with all 7 SCs ticked and evidenced.
2. STATE.md frontmatter + body reflect Phase 40 NEXT.
3. STATE.md progress counters are internally consistent: `completed_plans ≤ total_plans`, `completed_phases` incremented by 1 (Phase 39), `completed_plans` incremented by 6, `percent` matches the ratio.
4. ROADMAP.md Phase 39 `[x]` + plan list + Progress row updated.
5. REQUIREMENTS.md all 7 REQ-IDs flipped to `[x]` + traceability `Done`.
6. Full CPU suite GREEN at ≥525 tests.
7. Phase 36/37/38 regression tests still GREEN (D-30).
8. Phase-close commit exists and groups the four files atomically.
</verification>

<success_criteria>
- Phase 39 is provably complete: SC-1..SC-7 all PASS with evidence.
- Three planning ledgers are internally consistent and point at Phase 40 as next.
- STATE.md progress counters are arithmetically sound after increments.
- GitHub integration honored: HPC/VIZ issues closed with commit references OR explicitly noted absent.
- D-30 regression invariant holds: Phase 36/37/38 tests remain GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/39-hpc-visualization-safe-wins/39-06-SUMMARY.md` documenting:
- Final CPU test count (expected ≥525)
- Per-SC pass/fail status (one line each)
- List of GitHub issues closed (if any)
- The phase-close commit hash
- Pointer to `39-VERIFICATION.md`
</output>
</output>
