---
phase: 39-hpc-visualization-safe-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - master_thesis_code/parameter_estimation/parameter_estimation.py
autonomous: true
requirements: [HPC-01, HPC-04]
requirements_addressed: [HPC-01, HPC-04]
tags: [hpc, cpu-import, cupy-shim, dead-code]

must_haves:
  truths:
    - "parameter_estimation.py imports successfully on a machine without cupy installed"
    - "Every cupy call inside the ParameterEstimation class is routed through self._xp or self._fft"
    - "The _crop_frequency_domain dead method no longer exists in the file"
    - "A one-time RuntimeWarning fires when use_gpu=True but _CUPY_AVAILABLE=False"
    - "Existing 524-test baseline stays GREEN on CPU (Phase 36/37/38 regressions intact)"
  artifacts:
    - path: "master_thesis_code/parameter_estimation/parameter_estimation.py"
      provides: "self._xp / self._fft attributes resolved in __init__; guarded cp.* diagnostic uses"
      contains: "self._xp.argmax"
    - path: "master_thesis_code/parameter_estimation/parameter_estimation.py"
      provides: "CPU-safe imports; no unguarded cp.*/cufft.* calls inside class body"
      contains: "_get_xp"
  key_links:
    - from: "ParameterEstimation.__init__"
      to: "self._xp, self._fft attributes"
      via: "_get_xp(use_gpu), _get_fft(use_gpu) module-local helpers"
      pattern: "self\\._xp = _get_xp\\(use_gpu\\)"
    - from: "_get_cached_psd, scalar_product_of_functions, five_point_stencil_derivative"
      to: "self._xp.*, self._fft.*"
      via: "attribute dispatch"
      pattern: "self\\._(xp|fft)\\."
---

<objective>
Make `master_thesis_code/parameter_estimation/parameter_estimation.py` CPU-importable by routing every `cp.*` / `cufft.*` call inside the `ParameterEstimation` class through a runtime-resolved `self._xp` / `self._fft` pair (HPC-01), and delete the dead `_crop_frequency_domain` method superseded by `_get_cached_psd` (HPC-04). After this plan lands, `from master_thesis_code.parameter_estimation.parameter_estimation import ParameterEstimation` must succeed in a subprocess where cupy is shadowed to raise `ImportError`.

Purpose: Phase 40 verification needs to run the full test suite on CPU-only CI. The unconditional `cp.argmax` / `cp.stack` / `cufft.rfft` calls currently crash import when cupy is missing. This plan unblocks both Phase 40 (CPU regression) and future dev-machine iteration.

Output: Refactored `parameter_estimation.py` with `_get_xp` + `_get_fft` module-local helpers, `self._xp` / `self._fft` attributes on every `ParameterEstimation` instance, dead `_crop_frequency_domain` method removed, one-time `RuntimeWarning` on GPU-requested-but-unavailable fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-hpc-visualization-safe-wins/39-CONTEXT.md
@.planning/phases/39-hpc-visualization-safe-wins/39-PATTERNS.md
@master_thesis_code/parameter_estimation/parameter_estimation.py
@master_thesis_code/memory_management.py

<interfaces>
<!-- Existing module-level cupy import guard (parameter_estimation.py:19-27) — KEEP VERBATIM -->
```python
try:
    import cupy as cp
    import cupyx.scipy.fft as cufft

    _CUPY_AVAILABLE = True
except ImportError:
    cp = None
    cufft = None
    _CUPY_AVAILABLE = False
```

<!-- Peer pattern in memory_management.py:32-37 — runtime CPU fall-through -->
```python
if _CUPY_AVAILABLE and cp is not None:
    self.memory_pool = cp.get_default_memory_pool()
    self._fft_cache = cp.fft.config.get_plan_cache()
else:
    self.memory_pool = None
    self._fft_cache = None
```

<!-- Self-attribute convention (parameter_estimation.py:82-99) — runtime-resolved GPU/CPU switches -->
```python
self._use_gpu = use_gpu
self._use_five_point_stencil = use_five_point_stencil
# ... lazy imports, lisa_configuration, caches ...
self._psd_cache: dict[int, tuple[Any, Any, int, int]] = {}
self._crb_buffer: list[dict] = []
self._crb_flush_interval: int = 1
```

<!-- Existing test file that MUST keep passing on CPU -->
```python
# master_thesis_code_test/parameter_estimation/parameter_estimation_test.py:11-25
try:
    import master_thesis_code.parameter_estimation.parameter_estimation as pe_module
    from master_thesis_code.parameter_estimation.parameter_estimation import ParameterEstimation
    _PE_AVAILABLE = True
except Exception:
    pe_module = None
    _PE_AVAILABLE = False

pytestmark = pytest.mark.skipif(not _PE_AVAILABLE, reason="parameter_estimation unavailable")
```
Goal: after this plan lands, `_PE_AVAILABLE` becomes `True` on CPU → the entire test module stops being skipped.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _get_xp / _get_fft helpers, wire self._xp / self._fft, delete _crop_frequency_domain</name>
  <files>master_thesis_code/parameter_estimation/parameter_estimation.py</files>
  <read_first>
    - master_thesis_code/parameter_estimation/parameter_estimation.py (entire file; confirm current cp.*/cufft.* call sites match the pattern table below)
    - master_thesis_code/memory_management.py:1-48 (mirror the `_CUPY_AVAILABLE and cp is not None` guard pattern exactly)
    - CLAUDE.md §HPC / GPU Best Practices (the _get_xp pattern is mandatory)
    - CLAUDE.md §Typing Conventions (list[...] not List[...]; npt.NDArray[np.float64] for arrays; never cp.ndarray as a type annotation)
    - .planning/phases/39-hpc-visualization-safe-wins/39-PATTERNS.md §parameter_estimation.py (HPC-01 / HPC-04) — pattern table verbatim
    - .planning/phases/39-hpc-visualization-safe-wins/39-CONTEXT.md §decisions D-01..D-05 (HPC-01) and D-13..D-14 (HPC-04)
  </read_first>
  <behavior>
    - After __init__ runs with use_gpu=False, `pe._xp is numpy` is True and `pe._fft is numpy.fft` is True (or module-equivalent).
    - After __init__ runs with use_gpu=True on a CPU-only machine, `pe._xp is numpy` and a single `RuntimeWarning` is emitted with message containing "cupy is not installed" and "falling back to numpy".
    - After __init__ runs with use_gpu=True AND cupy available, `pe._xp is cp` and `pe._fft is cupyx.scipy.fft`.
    - `from master_thesis_code.parameter_estimation.parameter_estimation import ParameterEstimation` succeeds when cupy is shadowed (verified in subprocess with CUPY_SHADOWED=1).
    - `rg "_crop_frequency_domain" master_thesis_code/ master_thesis_code_test/` returns empty.
    - Existing test `test_save_cramer_rao_bound_creates_csv` (parameter_estimation_test.py:47) still passes with `pe._use_gpu = False`.
  </behavior>
  <action>
    Apply the following edits verbatim. Do NOT invent alternative designs; D-01..D-05 are locked.

    **Step 1.** Add two module-local helpers ABOVE the class definition (immediately after the `_LOGGER = logging.getLogger()` line at parameter_estimation.py:48). Use `types.ModuleType` for the return annotation (import `types` at the top-of-file alongside `logging`):

    ```python
    def _get_xp(use_gpu: bool) -> types.ModuleType:
        """Resolve the array namespace: cupy when use_gpu and cupy available, else numpy."""
        if use_gpu and _CUPY_AVAILABLE and cp is not None:
            return cp  # type: ignore[return-value]
        return np


    def _get_fft(use_gpu: bool) -> types.ModuleType:
        """Resolve the FFT namespace: cupyx.scipy.fft when use_gpu and cupy available, else numpy.fft."""
        if use_gpu and _CUPY_AVAILABLE and cufft is not None:
            return cufft  # type: ignore[return-value]
        return np.fft
    ```

    **Step 2.** In `__init__` (lines 73-100), immediately after `self._use_five_point_stencil = use_five_point_stencil` (line 83), insert:

    ```python
    self._xp = _get_xp(use_gpu)
    self._fft = _get_fft(use_gpu)
    if use_gpu and not _CUPY_AVAILABLE:
        warnings.warn(
            "ParameterEstimation: use_gpu=True but cupy is not installed; "
            "falling back to numpy. Expect ~100x slowdown.",
            RuntimeWarning,
            stacklevel=2,
        )
    ```

    `warnings` is already imported at line 12; no new top-of-file import needed.

    **Step 3.** Rewrite the 13 `cp.*` / `cufft.*` call sites inside the class body per the pattern table in 39-PATTERNS.md §HPC-01. Concrete replacements (current line numbers are approximate; use `rg` to locate each):

    | Current code | Replace with |
    |--------------|--------------|
    | `fs_full = cufft.rfftfreq(n, self.dt)[1:]` (line 110) | `fs_full = self._fft.rfftfreq(n, self.dt)[1:]` |
    | `lower_idx = int(cp.argmax(fs_full >= MINIMAL_FREQUENCY))` (111) | `lower_idx = int(self._xp.argmax(fs_full >= MINIMAL_FREQUENCY))` |
    | `upper_idx = int(cp.argmax(fs_full >= MAXIMAL_FREQUENCY))` (112) | `upper_idx = int(self._xp.argmax(fs_full >= MAXIMAL_FREQUENCY))` |
    | `psd_stack = cp.stack([...])` (117) | `psd_stack = self._xp.stack([...])` |
    | `result = cp.stack(result)` (138) | `result = self._xp.stack(result)` |
    | `return cp.array([...])` (273) | `return self._xp.array([...])` |
    | `a_ffts = cufft.rfft(...)` (317) | `a_ffts = self._fft.rfft(...)` |
    | `b_ffts_cc = cp.conjugate(cufft.rfft(...))` (318) | `b_ffts_cc = self._xp.conjugate(self._fft.rfft(...))` |
    | `float(cp.trapz(...))` (331) | `float(self._xp.trapz(...))` |
    | `snr = cp.sqrt(...)` (432) | `snr = self._xp.sqrt(...)` |
    | `xp = cp if (_CUPY_AVAILABLE and cp is not None) else np` (359) | `xp = self._xp` (remove local resolution) |

    **KEEP guarded `cp.*` diagnostic calls (lines 207, 253, 386):** these are pool-bytes logging and the `cp.asnumpy()` Fisher→CPU transfer. They already sit behind `if _CUPY_AVAILABLE and cp is not None:` branches. Leave them using `cp` directly — they only run on GPU per D-04 rationale.

    **Step 4.** Delete `_crop_frequency_domain` method verbatim (current lines 334-347, the entire `@staticmethod def _crop_frequency_domain` block). Function is superseded by `_get_cached_psd` (lines 102-124) per D-13. After removal, `rg "_crop_frequency_domain" master_thesis_code/ master_thesis_code_test/` must return empty (D-14).

    **Step 5.** Update the `__init__` docstring or add a short note near `self._xp = _get_xp(...)` that downstream methods (`_get_cached_psd`, `scalar_product_of_functions`, `five_point_stencil_derivative`, etc.) use `self._xp` / `self._fft` rather than module-level `cp` / `cufft`.

    **Step 6.** Do NOT touch:
    - Module-level `try/except ImportError` block (lines 19-27) — stays verbatim.
    - Lazy imports of fastlisaresponse/few (handled inside `waveform_generator.py`).
    - `LISA_configuration.py` unguarded `import cupy` — known code-health bug, out of Phase 39 scope per CONTEXT.md.
  </action>
  <verify>
    <automated>
      # Run the following sequence and confirm each exits 0:
      uv run ruff check master_thesis_code/parameter_estimation/parameter_estimation.py
      uv run ruff format --check master_thesis_code/parameter_estimation/parameter_estimation.py
      uv run mypy master_thesis_code/parameter_estimation/parameter_estimation.py
      # CPU-import smoke test (subprocess with cupy shadowed):
      uv run python -c "import sys; sys.modules['cupy'] = None; sys.modules['cupyx'] = None; sys.modules['cupyx.scipy'] = None; sys.modules['cupyx.scipy.fft'] = None; from master_thesis_code.parameter_estimation.parameter_estimation import ParameterEstimation; print('OK')"
      # Existing tests still pass on CPU:
      uv run pytest master_thesis_code_test/parameter_estimation/parameter_estimation_test.py -m "not gpu" -q
      # Grep gates:
      rg "_crop_frequency_domain" master_thesis_code/ master_thesis_code_test/ || echo "GATE_OK: no occurrences"
      rg "self\._xp|self\._fft" master_thesis_code/parameter_estimation/parameter_estimation.py | wc -l   # expect >= 12
      rg "^def _get_xp|^def _get_fft" master_thesis_code/parameter_estimation/parameter_estimation.py | wc -l   # expect 2
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg "self\._xp" master_thesis_code/parameter_estimation/parameter_estimation.py` returns ≥8 matches
    - `rg "self\._fft" master_thesis_code/parameter_estimation/parameter_estimation.py` returns ≥4 matches
    - `rg "^def _get_xp\(use_gpu: bool\)" master_thesis_code/parameter_estimation/parameter_estimation.py` returns exactly 1 match
    - `rg "^def _get_fft\(use_gpu: bool\)" master_thesis_code/parameter_estimation/parameter_estimation.py` returns exactly 1 match
    - `rg "_crop_frequency_domain" master_thesis_code/ master_thesis_code_test/` returns empty (no matches)
    - `rg "cp\.argmax|cp\.stack|cp\.array|cp\.trapz|cp\.sqrt|cp\.conjugate|cufft\.rfft" master_thesis_code/parameter_estimation/parameter_estimation.py` returns only matches inside `_get_xp` / `_get_fft` or inside existing `if _CUPY_AVAILABLE and cp is not None:` guarded diagnostic blocks at the lines that now map to the old 207, 253, 386
    - CPU subprocess import succeeds: `uv run python -c "import sys; sys.modules['cupy'] = None; sys.modules['cupyx'] = None; sys.modules['cupyx.scipy'] = None; sys.modules['cupyx.scipy.fft'] = None; from master_thesis_code.parameter_estimation.parameter_estimation import ParameterEstimation; print('OK')"` prints `OK` and exits 0
    - `uv run pytest master_thesis_code_test/parameter_estimation/parameter_estimation_test.py -m "not gpu" -q` exits 0 with 0 failures and 0 errors
    - `uv run mypy master_thesis_code/parameter_estimation/parameter_estimation.py` exits 0
    - `uv run ruff check master_thesis_code/parameter_estimation/parameter_estimation.py` exits 0
  </acceptance_criteria>
  <done>
    Module is CPU-importable; `self._xp` / `self._fft` routed through all hot-path call sites; dead `_crop_frequency_domain` removed; existing tests pass on CPU; mypy and ruff clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: gpu-audit on refactored parameter_estimation.py</name>
  <files>master_thesis_code/parameter_estimation/parameter_estimation.py</files>
  <read_first>
    - .claude/skills/gpu-audit/SKILL.md
    - master_thesis_code/parameter_estimation/parameter_estimation.py (post Task 1)
  </read_first>
  <action>
    Invoke the `gpu-audit` skill on the refactored `parameter_estimation.py`. Per CLAUDE.md §GSD Workflow Enforcement the skill is triggered after array/GPU computation code changes. Confirm the audit finds:

    1. No unguarded `cp.*` calls inside the class body (only guarded diagnostic blocks at the three retained sites).
    2. No unguarded `cufft.*` calls.
    3. `self._use_gpu` still threads through every constructor call (no regression).
    4. No GPU→CPU transfer inside the hot path (`scalar_product_of_functions`). The only `cp.asnumpy(...)` call remains guarded in `compute_Cramer_Rao_bounds` (outside the inner 105-call Fisher loop).

    If the audit reports any new finding, fix inline in this task and re-run the audit. Commit only when the audit passes.
  </action>
  <verify>
    <automated>
      # gpu-audit skill output is textual; capture and check for absence of FAIL lines.
      # (Executor should interpret the skill output; automated assertion is a grep on the skill log.)
      # Fallback: the same static grep as Task 1 must still hold.
      rg "^[[:space:]]*cp\.[a-z]" master_thesis_code/parameter_estimation/parameter_estimation.py | rg -v "if _CUPY_AVAILABLE and cp is not None|# type: ignore" || echo "GATE_OK: all cp.* guarded or inside _get_xp"
    </automated>
  </verify>
  <acceptance_criteria>
    - gpu-audit skill output contains 0 FAIL findings on `parameter_estimation.py`
    - Every `cp.*` usage outside `_get_xp`/`_get_fft` is either (a) inside an `if _CUPY_AVAILABLE and cp is not None:` branch, or (b) inside a `# type: ignore` return statement of the helper
    - `rg "cp\.ndarray" master_thesis_code/parameter_estimation/parameter_estimation.py` returns empty (no `cp.ndarray` type annotations per CLAUDE.md §Typing Conventions)
  </acceptance_criteria>
  <done>
    gpu-audit passes cleanly on the refactored file; no unguarded cupy calls; no type-annotation violations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CPU host process ↔ numpy/cupy runtime | Code path switch based on `use_gpu` flag and `_CUPY_AVAILABLE`; no external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Tampering | `_get_xp` / `_get_fft` helpers | accept | Module-local helpers; no user input parses into branch selection; `use_gpu` is a hardcoded constructor kwarg controlled by `main.py` CLI args |
| T-39-01-02 | Information Disclosure | `RuntimeWarning` message | accept | Warning text contains no secrets, only an actionable message ("cupy not installed, falling back to numpy") |
| T-39-01-03 | Denial of Service | CPU fallback unintentionally engaged in a GPU production job | mitigate | `RuntimeWarning` is emitted; cluster jobs set `use_gpu=True` and cupy is installed via `uv sync --extra gpu`, so fallback only engages in broken installs where operator should notice immediately |
</threat_model>

<verification>
1. **CPU subprocess import test** — `uv run python -c "import sys; sys.modules['cupy'] = None; ...; from ...parameter_estimation import ParameterEstimation"` exits 0.
2. **Existing regression tests GREEN** — `uv run pytest master_thesis_code_test/parameter_estimation/ -m "not gpu"` exits 0.
3. **Dead code removed** — `rg "_crop_frequency_domain"` returns empty in both source and test trees.
4. **Shim attributes wired** — ≥8 `self._xp` + ≥4 `self._fft` references inside the class body.
5. **Phase 36/37/38 regressions** — `uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py master_thesis_code_test/test_parameter_space_h.py master_thesis_code_test/test_l_cat_equivalence.py -m "not gpu"` exits 0.
6. **mypy/ruff clean** — `uv run mypy master_thesis_code/parameter_estimation/parameter_estimation.py && uv run ruff check master_thesis_code/parameter_estimation/parameter_estimation.py` exits 0.
</verification>

<success_criteria>
- HPC-01 ROADMAP SC-1 satisfied: `from master_thesis_code.parameter_estimation.parameter_estimation import ParameterEstimation` succeeds on a machine without cupy; `pytest -m "not gpu"` runs without import-time skips on parameter_estimation tests.
- HPC-04 ROADMAP SC-4 satisfied: `_crop_frequency_domain` no longer appears in `parameter_estimation.py`.
- No Phase 36/37/38 regression (D-30).
- gpu-audit clean.
</success_criteria>

<output>
After completion, create `.planning/phases/39-hpc-visualization-safe-wins/39-01-SUMMARY.md` documenting:
- The 13 call sites rewritten (file:line references post-edit)
- The subprocess-import verification output (OK line)
- gpu-audit findings (if any) and resolutions
- Regression test result (test count + pass count)
- Commit hash
</output>
