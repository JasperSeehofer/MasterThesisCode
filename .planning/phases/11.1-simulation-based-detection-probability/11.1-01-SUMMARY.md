---
phase: 11.1-simulation-based-detection-probability
plan: 01
subsystem: simulation
tags: [injection-campaign, snr, cli, csv, detection-probability]

# Dependency graph
requires: []
provides:
  - "--injection_campaign CLI mode for SNR-only event loop"
  - "injection_campaign() function in main.py"
  - "INJECTION_CSV_PATH and INJECTION_DATA_DIR constants"
affects: [11.1-02, 11.1-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "h-dependent luminosity distance via dist(z, h=h_value) instead of set_host_galaxy_parameters()"
    - "Per-task CSV naming with {h_label} and $index placeholders for SLURM array compatibility"

key-files:
  created: []
  modified:
    - master_thesis_code/arguments.py
    - master_thesis_code/constants.py
    - master_thesis_code/main.py

key-decisions:
  - "Followed data_simulation() exception handling pattern for consistency"
  - "No rng parameter threading since existing randomize_parameters() uses np.random global state"

patterns-established:
  - "Injection campaign stores ALL events (no SNR threshold) for P_det grid construction"

requirements-completed: [D-01, D-03, D-04, D-05, D-07, D-08, D-09]

# Metrics
duration: 2min
completed: 2026-03-30
---

# Phase 11.1 Plan 01: Injection Campaign CLI & Event Loop Summary

**SNR-only injection campaign mode via --injection_campaign flag, storing all EMRI events (detected + undetected) to per-task CSVs with h-dependent luminosity distances**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-30T21:37:10Z
- **Completed:** 2026-03-30T21:39:28Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added --injection_campaign CLI flag to arguments parser with corresponding property
- Added INJECTION_CSV_PATH and INJECTION_DATA_DIR constants for per-task CSV output
- Implemented injection_campaign() function in main.py: draws EMRI events from Model1CrossCheck, computes SNR only (no Fisher matrix/CRB), stores all events to CSV with columns z, M, phiS, qS, SNR, h_inj, luminosity_distance

## Task Commits

Each task was committed atomically:

1. **Task 1: Add injection campaign CLI flag and constants** - `f553e2d` (feat)
2. **Task 2: Implement injection campaign event loop** - `e0aec73` (feat)

## Files Created/Modified
- `master_thesis_code/arguments.py` - Added --injection_campaign flag and property
- `master_thesis_code/constants.py` - Added INJECTION_CSV_PATH and INJECTION_DATA_DIR constants
- `master_thesis_code/main.py` - Added injection_campaign() function and wired into main()

## Decisions Made
- Followed data_simulation() exception handling pattern for consistency across simulation modes
- Did not add rng parameter since existing randomize_parameters() uses np.random global state (matches current codebase pattern)
- Used dist(z, h=h_value) directly instead of set_host_galaxy_parameters() to avoid hardcoded h=0.73

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Worktree was based on old `main` branch; required merge of `resolving_the_bias` to get current codebase
- Python 3.13 dataclass mutable default error prevented import-level verification (pre-existing issue on this branch, not caused by this plan's changes); verified via grep-based source inspection instead

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- injection_campaign() is ready to be called from CLI and SLURM jobs
- Plan 02 (SimulationDetectionProbability class) will load the CSVs produced by this injection campaign
- Plan 03 will wire the new P_det into the Bayesian likelihood

---
*Phase: 11.1-simulation-based-detection-probability*
*Completed: 2026-03-30*
