---
phase: 11.1-simulation-based-detection-probability
plan: 03
type: execute
wave: 2
depends_on: ["11.1-02"]
files_modified:
  - master_thesis_code/bayesian_inference/bayesian_statistics.py
  - master_thesis_code/bayesian_inference/detection_probability.py
  - master_thesis_code/constants.py
autonomous: true
requirements: [D-10, D-11, D-12, D-13]

must_haves:
  truths:
    - "BayesianStatistics uses SimulationDetectionProbability instead of old DetectionProbability"
    - "All ~15 P_det call sites in bayesian_statistics.py pass h parameter and no longer check _DEBUG_DISABLE_DETECTION_PROBABILITY"
    - "Old KDE-based detection_probability.py is deleted"
    - "No references to UNDETECTED_EVENTS_OUTPUT_PATH remain in bayesian_statistics.py"
    - "child_process_init passes SimulationDetectionProbability to worker processes"
  artifacts:
    - path: "master_thesis_code/bayesian_inference/bayesian_statistics.py"
      provides: "Updated Pipeline B with simulation-based P_det"
      contains: "SimulationDetectionProbability"
    - path: "master_thesis_code/bayesian_inference/detection_probability.py"
      provides: "DELETED"
  key_links:
    - from: "master_thesis_code/bayesian_inference/bayesian_statistics.py"
      to: "master_thesis_code/bayesian_inference/simulation_detection_probability.py"
      via: "import and construction"
      pattern: "from.*simulation_detection_probability import"
    - from: "master_thesis_code/bayesian_inference/bayesian_statistics.py"
      to: "detection_probability.*h="
      via: "h parameter passed at all call sites"
      pattern: "h=.*self\\.h|h=h_value"
---

<objective>
Replace old KDE-based DetectionProbability with SimulationDetectionProbability in Pipeline B, remove the debug disable flag, delete the old class, and clean up undetected events references.

Purpose: Completes the P_det replacement by wiring the new class into all ~15 call sites in bayesian_statistics.py and removing dead code.
Output: Updated bayesian_statistics.py, deleted detection_probability.py, cleaned constants.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-simulation-based-detection-probability/11.1-CONTEXT.md
@.planning/phases/11.1-simulation-based-detection-probability/11.1-RESEARCH.md
@.planning/phases/11.1-simulation-based-detection-probability/11.1-02-SUMMARY.md

@master_thesis_code/bayesian_inference/bayesian_statistics.py
@master_thesis_code/bayesian_inference/detection_probability.py
@master_thesis_code/constants.py

<interfaces>
<!-- From Plan 02 SimulationDetectionProbability -->
```python
class SimulationDetectionProbability:
    def __init__(
        self,
        injection_data_dir: str,
        snr_threshold: float,
        h_grid: list[float] | None = None,
    ) -> None: ...

    def detection_probability_with_bh_mass_interpolated(
        self,
        d_L: float | npt.NDArray[np.float64],
        M_z: float | npt.NDArray[np.float64],
        phi: float | npt.NDArray[np.float64],
        theta: float | npt.NDArray[np.float64],
        *,
        h: float,
    ) -> float | npt.NDArray[np.float64]: ...

    def detection_probability_without_bh_mass_interpolated(
        self,
        d_L: float | npt.NDArray[np.float64],
        phi: float | npt.NDArray[np.float64],
        theta: float | npt.NDArray[np.float64],
        *,
        h: float,
    ) -> float | npt.NDArray[np.float64]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace DetectionProbability with SimulationDetectionProbability in bayesian_statistics.py</name>
  <files>master_thesis_code/bayesian_inference/bayesian_statistics.py</files>
  <read_first>
    - master_thesis_code/bayesian_inference/bayesian_statistics.py (FULL FILE -- all ~1000+ lines, need to find every P_det call site)
    - master_thesis_code/bayesian_inference/simulation_detection_probability.py (new class API from Plan 02)
    - master_thesis_code/constants.py (INJECTION_DATA_DIR, SNR_THRESHOLD, UNDETECTED_EVENTS_OUTPUT_PATH)
  </read_first>
  <action>
**Import changes (top of file):**
1. Replace `from master_thesis_code.bayesian_inference.detection_probability import DetectionProbability` with `from master_thesis_code.bayesian_inference.simulation_detection_probability import SimulationDetectionProbability`
2. Add `INJECTION_DATA_DIR, SNR_THRESHOLD` to the constants import (keep existing CRAMER_RAO_BOUNDS_OUTPUT_PATH, PREPARED_CRAMER_RAO_BOUNDS_PATH, H)
3. Remove `UNDETECTED_EVENTS_OUTPUT_PATH` from constants import (per D-13)

**Remove debug flag (lines ~60-67):**
1. Delete `_DEBUG_DISABLE_DETECTION_PROBABILITY = True` and the associated TODO comment
2. Delete the `if _DEBUG_DISABLE_DETECTION_PROBABILITY:` warning block

**Update BayesianStatistics.__init__():**
1. Remove `self.undetected_events = pd.read_csv(UNDETECTED_EVENTS_OUTPUT_PATH)` (line ~117, per D-13)

**Update BayesianStatistics.evaluate() -- P_det construction (lines ~163-172):**
Replace the old DetectionProbability construction block:
```python
# OLD:
detection_probability = DetectionProbability(
    luminosity_distance_lower_limit=..., ...,
    detected_events=self.cramer_rao_bounds,
    undetected_events=self.undetected_events, ...)
```
With:
```python
detection_probability = SimulationDetectionProbability(
    injection_data_dir=INJECTION_DATA_DIR,
    snr_threshold=SNR_THRESHOLD,
)
```
Remove the variables `luminosity_distance_lower_limit`, `luminosity_distance_upper_limit`, `mass_lower_limit`, `mass_upper_limit` if they are ONLY used for the old DetectionProbability construction (check if they are used elsewhere in evaluate() first -- they may be used for integration limits).

**Update ALL P_det call sites (~15 locations per RESEARCH.md table):**
Each call site currently looks like:
```python
p_det = (
    1.0
    if _DEBUG_DISABLE_DETECTION_PROBABILITY
    else detection_probability.detection_probability_without_bh_mass_interpolated(
        d_L, phi, theta
    )
)
```
Replace with:
```python
p_det = detection_probability.detection_probability_without_bh_mass_interpolated(
    d_L, phi, theta, h=h_value
)
```
Where `h_value` is the current h being evaluated. In the integrand functions inside `single_host_likelihood` and `single_host_likelihood_integration_testing`, the h value is available as `h_value` (parameter of the outer function) or via the module-level global. Check each call site to find the correct h variable name.

Specific call sites (from RESEARCH.md):
- Line ~567: `numerator_integrant_without_bh_mass` in `single_host_likelihood` -- h is in scope as the function parameter `h_value`
- Line ~586: `denominator_integrant_without_bh_mass` in `single_host_likelihood` -- same
- Line ~651: `numerator_integrant_with_bh_mass` in `single_host_likelihood` -- same
- Line ~699: `denominator_integrant_with_bh_mass_vectorized` in `single_host_likelihood` -- same
- Lines ~760, 772, 816, 830, 893, 906, 964: in `single_host_likelihood_integration_testing` -- check h variable name in that function

For `with_bh_mass` variants: add `h=h_value` keyword argument.
For `without_bh_mass` variants: add `h=h_value` keyword argument.

**Update child_process_init() (lines ~1007-1012):**
Change the type annotation of `current_detection_probability` parameter from `DetectionProbability` to `SimulationDetectionProbability`. The actual pickling/passing mechanism stays the same.

**Update module-level global type:**
Change `detection_probability: Any = None` (line ~74) to `detection_probability: SimulationDetectionProbability | None = None` or keep as `Any` if it causes import issues.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/MasterThesisCode && uv run python -c "
from master_thesis_code.bayesian_inference import bayesian_statistics as bs
import inspect
src = inspect.getsource(bs)
assert 'SimulationDetectionProbability' in src, 'Missing new class'
assert '_DEBUG_DISABLE_DETECTION_PROBABILITY' not in src, 'Debug flag still present'
assert 'UNDETECTED_EVENTS_OUTPUT_PATH' not in src, 'Undetected events path still present'
assert 'from master_thesis_code.bayesian_inference.detection_probability import' not in src, 'Old import still present'
print('PASS: all replacements verified')
" 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `bayesian_statistics.py` imports `SimulationDetectionProbability` not `DetectionProbability`
    - `bayesian_statistics.py` does NOT contain `_DEBUG_DISABLE_DETECTION_PROBABILITY`
    - `bayesian_statistics.py` does NOT contain `UNDETECTED_EVENTS_OUTPUT_PATH`
    - `bayesian_statistics.py` does NOT contain `self.undetected_events`
    - `bayesian_statistics.py` constructs `SimulationDetectionProbability(injection_data_dir=INJECTION_DATA_DIR, snr_threshold=SNR_THRESHOLD)`
    - All P_det call sites pass `h=` keyword argument
    - `uv run mypy master_thesis_code/bayesian_inference/bayesian_statistics.py` exits 0
    - `uv run ruff check master_thesis_code/bayesian_inference/bayesian_statistics.py` exits 0
  </acceptance_criteria>
  <done>bayesian_statistics.py uses SimulationDetectionProbability at all call sites, debug flag removed, undetected events removed</done>
</task>

<task type="auto">
  <name>Task 2: Delete old DetectionProbability and clean up constants</name>
  <files>
    master_thesis_code/bayesian_inference/detection_probability.py,
    master_thesis_code/constants.py
  </files>
  <read_first>
    - master_thesis_code/bayesian_inference/detection_probability.py (confirm it is the file to delete)
    - master_thesis_code/constants.py (find UNDETECTED_EVENTS_PATH and UNDETECTED_EVENTS_OUTPUT_PATH)
  </read_first>
  <action>
1. **Delete** `master_thesis_code/bayesian_inference/detection_probability.py` entirely (per D-11). Use `git rm` or `os.remove`.

2. **In `constants.py`**: Remove lines containing `UNDETECTED_EVENTS_PATH` and `UNDETECTED_EVENTS_OUTPUT_PATH` (lines ~64-65, per D-13). These paths are no longer needed since the injection campaign replaces the undetected events mechanism.

3. **Check for any remaining imports** of the old class or constants:
   - `grep -r "detection_probability" master_thesis_code/ --include="*.py"` -- should only find `simulation_detection_probability` references
   - `grep -r "UNDETECTED_EVENTS" master_thesis_code/ --include="*.py"` -- should find nothing
   - `grep -r "DetectionProbability" master_thesis_code/ --include="*.py"` -- should only find `SimulationDetectionProbability`
   - Fix any remaining references (likely in `cosmological_model.py` which has backward-compat re-exports)

4. **Check `cosmological_model.py`** for re-exports of `DetectionProbability` and remove them if present.

5. **Check `bayesian_inference/__init__.py`** (if it exists) for exports of the old class and update.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/MasterThesisCode && test ! -f master_thesis_code/bayesian_inference/detection_probability.py && uv run python -c "from master_thesis_code.constants import INJECTION_DATA_DIR; print('constants OK')" && echo "PASS: old file deleted, constants clean" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `master_thesis_code/bayesian_inference/detection_probability.py` does NOT exist on disk
    - `constants.py` does NOT contain `UNDETECTED_EVENTS_PATH` or `UNDETECTED_EVENTS_OUTPUT_PATH`
    - `grep -r "from.*detection_probability import DetectionProbability" master_thesis_code/` returns no results
    - `grep -r "UNDETECTED_EVENTS" master_thesis_code/ --include="*.py"` returns no results
    - `uv run mypy master_thesis_code/` exits 0 (no broken imports)
    - `uv run ruff check master_thesis_code/` exits 0
  </acceptance_criteria>
  <done>Old KDE DetectionProbability class deleted per D-11, undetected events constants removed per D-13, no broken imports remain</done>
</task>

</tasks>

<verification>
1. `test ! -f master_thesis_code/bayesian_inference/detection_probability.py` exits 0 (file deleted)
2. `grep -rn "_DEBUG_DISABLE_DETECTION_PROBABILITY" master_thesis_code/` returns nothing
3. `grep -rn "UNDETECTED_EVENTS" master_thesis_code/ --include="*.py"` returns nothing
4. `grep -rn "DetectionProbability" master_thesis_code/ --include="*.py" | grep -v Simulation | grep -v test_ | grep -v __pycache__` returns nothing
5. `uv run mypy master_thesis_code/bayesian_inference/bayesian_statistics.py` exits 0
6. `uv run ruff check master_thesis_code/` exits 0
</verification>

<success_criteria>
- Pipeline B (bayesian_statistics.py) exclusively uses SimulationDetectionProbability
- All ~15 P_det call sites pass h parameter (no more debug bypass)
- Old detection_probability.py is deleted
- UNDETECTED_EVENTS constants removed from constants.py
- No broken imports anywhere in the codebase
- All type checks and linting pass
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-simulation-based-detection-probability/11.1-03-SUMMARY.md`
</output>
