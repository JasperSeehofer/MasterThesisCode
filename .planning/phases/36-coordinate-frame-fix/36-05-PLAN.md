---
phase: 36-coordinate-frame-fix
plan: 05
type: execute
wave: 4
depends_on: ["36-01", "36-02", "36-03", "36-04"]
files_modified:
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/36-coordinate-frame-fix/36-VERIFICATION.md  # new
interactive: true  # D-28 stop/rethink gate may require user decision

conventions:
  units: n/a (no physics changes in this plan)

dimensional_check: {}

approximations: []

contract:
  scope:
    question: "Are all four Phase 36 physics commits landed with the expected test state (9/0/0/0 on test_coordinate_roundtrip.py, regression pickle committed), and is the phase ready for Phase 37 handoff?"
  claims:
    - id: claim-tests-green
      statement: "uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py -v reports ≥9 passed, 0 xfailed, 0 XPASS, 0 FAIL (D-27 decisive deliverable)."
      deliverables: [deliv-verification-md, deliv-state-update]
      acceptance_tests: [test-roundtrip-green]
      references: [ref-d27, ref-d28]
    - id: claim-regression-committed
      statement: "The regression pickle at .planning/phases/36-coordinate-frame-fix/36-superset-regression.pkl exists, is git-tracked, schema-complete, and old ⊆ new assertion passes."
      deliverables: [deliv-verification-md]
      acceptance_tests: [test-pickle-schema, test-pickle-superset]
      references: [ref-d24, ref-d27]
    - id: claim-four-commits-landed
      statement: "Git history contains exactly four [PHYSICS] commits tagged with REQ-IDs COORD-02, COORD-02b, COORD-03, COORD-04 (one per REQ; atomic per D-25)."
      deliverables: [deliv-verification-md]
      acceptance_tests: [test-physics-commits]
      references: [ref-d25]
    - id: claim-no-unrelated-regression
      statement: "uv run pytest -m 'not gpu and not slow' passes (no test outside test_coordinate_roundtrip.py regresses)."
      deliverables: [deliv-verification-md]
      acceptance_tests: [test-full-pytest]
      references: [ref-verify01]
    - id: claim-state-updated
      statement: ".planning/STATE.md, .planning/MILESTONES.md, .planning/ROADMAP.md, and .planning/REQUIREMENTS.md all reflect Phase 36 completion. COORD-02/02b/03/04 marked done. Phase 36 row flipped to 'Complete'."
      deliverables: [deliv-state-update, deliv-milestones-update, deliv-roadmap-update, deliv-requirements-update]
      acceptance_tests: [test-state-consistency]
      references: [ref-verify01]
  deliverables:
    - id: deliv-verification-md
      kind: documentation
      path: ".planning/phases/36-coordinate-frame-fix/36-VERIFICATION.md"
      description: "Phase 36 verification report with goal-achievement summary, ROADMAP SC verification, gate checklist, and deferred-items list (Phase 37 PE-01 depends on this)."
      must_contain: ["PASSED", "COORD-02", "COORD-02b", "COORD-03", "COORD-04", "36-superset-regression.pkl", "9 passed"]
    - id: deliv-state-update
      kind: documentation
      path: ".planning/STATE.md"
      description: "STATE.md current-phase advances to Phase 37; Phase 36 listed as complete with date."
      must_contain: ["36", "complete"]
    - id: deliv-milestones-update
      kind: documentation
      path: ".planning/MILESTONES.md"
      description: "v2.2 Pipeline Correctness milestone progress: Phase 36 checkbox flipped to [x]."
      must_contain: ["[x]", "Phase 36"]
    - id: deliv-roadmap-update
      kind: documentation
      path: ".planning/ROADMAP.md"
      description: "Progress table updated: Phase 36 status Complete; Completed date entered; Plans Complete shows 5/5."
      must_contain: ["Phase 36", "Complete"]
    - id: deliv-requirements-update
      kind: documentation
      path: ".planning/REQUIREMENTS.md"
      description: "Checkbox flip: - [x] **COORD-02**, - [x] **COORD-02b**, - [x] **COORD-03**, - [x] **COORD-04**. Traceability table status: 'Pending' → 'Done'."
      must_contain: ["[x] **COORD-02**", "[x] **COORD-02b**", "[x] **COORD-03**", "[x] **COORD-04**"]
  acceptance_tests:
    - id: test-roundtrip-green
      subject: claim-tests-green
      kind: smoke
      procedure: "Run `uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py -v` and capture output."
      pass_condition: "Output reports `9 passed` (or more, if any supplementary tests were added), `0 xfailed`, `0 FAILED`, `0 XPASS` in the summary line."
      evidence_required: [deliv-verification-md]
    - id: test-pickle-schema
      subject: claim-regression-committed
      kind: schema
      procedure: "`python -c \"import pickle; d = pickle.load(open('.planning/phases/36-coordinate-frame-fix/36-superset-regression.pkl','rb')); expected = {'event_id','event_qS_pre_fix','event_phiS_pre_fix','event_qS_post_fix','event_phiS_post_fix','old_candidate_indices','new_candidate_indices','fisher_sky_2x2','git_commit'}; assert expected.issubset(d.keys()), f'missing: {expected - d.keys()}'\"`"
      pass_condition: "All D-24 fields present."
      evidence_required: [deliv-verification-md]
    - id: test-pickle-superset
      subject: claim-regression-committed
      kind: regression
      procedure: "`python -c \"import pickle; d = pickle.load(open('.planning/phases/36-coordinate-frame-fix/36-superset-regression.pkl','rb')); assert d['old_candidate_indices'] <= d['new_candidate_indices']\"`"
      pass_condition: "Assertion passes (new is a superset of old)."
      evidence_required: [deliv-verification-md]
    - id: test-physics-commits
      subject: claim-four-commits-landed
      kind: smoke
      procedure: "`git log --format='%s' --grep='\\[PHYSICS\\]' -n 20 | head -4` returns exactly four [PHYSICS] lines tagged with COORD-02, COORD-02b, COORD-03, COORD-04 (order may vary based on wave execution)."
      pass_condition: "Four commits found, one per REQ-ID; each message format matches CONTEXT.md §specifics locked templates."
      evidence_required: [deliv-verification-md]
    - id: test-full-pytest
      subject: claim-no-unrelated-regression
      kind: integration
      procedure: "Run `uv run pytest -m 'not gpu and not slow'` and capture exit code."
      pass_condition: "Exit code 0; no test regression."
      evidence_required: [deliv-verification-md]
    - id: test-state-consistency
      subject: claim-state-updated
      kind: smoke
      procedure: "Cross-check STATE.md, MILESTONES.md, ROADMAP.md, REQUIREMENTS.md all agree Phase 36 is complete and COORD-02/02b/03/04 are done."
      pass_condition: "No contradictions across the four files; Phase 37 marked as current/next."
      evidence_required: [deliv-state-update, deliv-milestones-update, deliv-roadmap-update, deliv-requirements-update]
  references:
    - id: ref-d24
      kind: decision
      locator: ".planning/phases/36-coordinate-frame-fix/36-CONTEXT.md §decisions D-24 (regression pickle schema)"
    - id: ref-d25
      kind: decision
      locator: ".planning/phases/36-coordinate-frame-fix/36-CONTEXT.md §decisions D-25 (four atomic [PHYSICS] commits)"
    - id: ref-d27
      kind: decision
      locator: ".planning/phases/36-coordinate-frame-fix/36-CONTEXT.md §decisions D-27 (decisive deliverables: pickle + 9/0/0/0 tests)"
    - id: ref-d28
      kind: decision
      locator: ".planning/phases/36-coordinate-frame-fix/36-CONTEXT.md §decisions D-28 (stop/rethink gate if tests do not flip)"
    - id: ref-verify01
      kind: internal
      locator: ".planning/REQUIREMENTS.md §Verification Gate VERIFY-01 (full regression suite passes on CPU; Phase 40 gate)"
  forbidden_proxies:
    - "Marking Phase 36 complete if any of the four [PHYSICS] commits is missing or if any test xfails."
    - "Committing the regression pickle without the old ⊆ new assertion passing — that would poison the Phase 40 VERIFY-02 anchor."
    - "Writing 36-VERIFICATION.md that claims success while the D-28 gate is triggered — must trigger the gate explicitly and halt."
    - "Advancing STATE.md to Phase 37 when D-28 gate is triggered."
    - "Updating ROADMAP.md Progress table to 'Complete' if VERIFICATION.md status is not 'PASSED'."
  uncertainty_markers:
    weakest_anchors:
      - "D-28 gate conditions are binary (tests flip XPASS or they do not; ≥99% or not) but the user wrote the gate specifically for the edge case where 'fixes appear to land but behave oddly' — watch for subtle failures (e.g., 99/100 recovery PASSED but barely)."
    disconfirming_observations:
      - "pytest reports 9 passed but one of them prints a DeprecationWarning about astropy — investigate if warning indicates a silent drift."
      - "Regression pickle old_candidate_indices is EMPTY — could indicate a setup bug (pre-fix radius logic failed to produce candidates on the reference event); investigate before declaring PASSED."
      - "`test-physics-commits` finds a 5th [PHYSICS] commit on the branch — indicates scope creep; investigate and document."
      - "NON-[PHYSICS] commits on the branch: the Phase 36 branch is expected to contain at least one preceding `chore(36): ...` scaffolding commit (Plan 36-01 Task 6: helper + REQUIREMENTS + test-shim). Additional `chore(36)` or `test(36)` commits are allowed if they landed for planned support work. Log non-physics commits in 36-VERIFICATION.md's `## Commit History` section but do NOT flag them as scope creep."
  links:
    - id: link-verification-supports-handoff
      source: deliv-verification-md
      target: deliv-state-update
      relation: supports
      verified_by: [test-roundtrip-green, test-pickle-superset, test-physics-commits]
---

<objective>
Verify Phase 36 deliverables, apply the D-28 stop/rethink gate if necessary, write the 36-VERIFICATION.md report, and update all four GSD state files (STATE.md, MILESTONES.md, ROADMAP.md, REQUIREMENTS.md) to reflect Phase 36 completion. This plan is software-only — no [PHYSICS] commits, no new formulas, no physical changes.

Purpose: Lock in the handoff contract to Phase 37 and Phase 40 VERIFY-02. The regression pickle becomes the Phase 40 anchor; the VERIFICATION.md becomes the source of truth for "Phase 36 is done".
Output: 36-VERIFICATION.md under the phase directory; state updates across four files; a single `docs(36)` commit marking closure.
</objective>

<execution_context>
All four prior plans (36-01, 36-02, 36-03, 36-04) must have landed their atomic [PHYSICS] commits. Plan 36-05 is sequential after all four: it aggregates and verifies.

This plan does NOT use /physics-change; no formula changes are made. It may still trigger the D-28 gate (which is explicitly a stop/rethink, not a fix). If D-28 triggers, STOP and await user guidance before completing Tasks 3–5.

Commit message (NOT [PHYSICS] — this is documentation/state):

    docs(36): complete Phase 36 verification, regression pickle anchor locked, state advanced to Phase 37

Use `gpd commit`.
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/36-coordinate-frame-fix/36-CONTEXT.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-coordinate-bug-characterization/35-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the decisive verification commands</name>
  <files>.planning/phases/36-coordinate-frame-fix/36-VERIFICATION.md</files>
  <action>
    Run the full suite of verification commands and capture results:

    1. **Phase 35 tests green check** (D-27 decisive):
       ```bash
       uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py -v --tb=short 2>&1 | tee /tmp/phase36_roundtrip.log
       ```
       Expected: `9 passed, 0 xfailed, 0 failed, 0 XPASS`. If supplementary tests exist, expected is `≥9 passed`.

    2. **D-28 stop/rethink gate**:
       Parse the pytest summary. If the summary shows ANY of:
       - `xfailed` count > 0 AND we said at Plan 36-02 that all xfails should be gone, OR
       - `FAILED` count > 0, OR
       - `XPASS` count > 0, OR
       - The N=100 random test passed but with recovered_count < 99 per the assertion message

       Then STOP. Do not proceed to Tasks 2–5. Report to user via Task 6 (D-28 gate section below) and await decision.

    3. **Regression pickle schema**:
       ```bash
       uv run python -c "
       import pickle
       with open('.planning/phases/36-coordinate-frame-fix/36-superset-regression.pkl', 'rb') as f:
           d = pickle.load(f)
       expected = {'event_id','event_qS_pre_fix','event_phiS_pre_fix','event_qS_post_fix','event_phiS_post_fix','old_candidate_indices','new_candidate_indices','fisher_sky_2x2','git_commit'}
       missing = expected - set(d.keys())
       assert not missing, f'missing keys: {missing}'
       assert d['old_candidate_indices'].issubset(d['new_candidate_indices']), 'superset property violated'
       print('Pickle schema and superset property: OK')
       print(f'old |{len(d[\"old_candidate_indices\"])}| new |{len(d[\"new_candidate_indices\"])}|')
       "
       ```

    4. **Four atomic [PHYSICS] commits**:
       ```bash
       git log --format="%s" --grep="\[PHYSICS\] COORD-" --oneline -n 10
       ```
       Expected: exactly 4 commits matching `[PHYSICS] COORD-XX: ...` shape, one for each of COORD-02, COORD-02b, COORD-03, COORD-04. Capture the commit hashes.

    5. **Full pytest regression**:
       ```bash
       uv run pytest -m "not gpu and not slow" 2>&1 | tail -30 | tee /tmp/phase36_full_pytest.log
       ```
       Expected: exit code 0, no FAILED. Prior Phase 35 baseline was 508 tests; Phase 36 did not add tests (it removed xfail markers, which converts xfailed → passed).

    6. **Git diff summary** (handoff completeness):
       ```bash
       git log --format="%h %s" $(git merge-base HEAD main)..HEAD
       ```
       Expected: chain of 4 [PHYSICS] commits matching the locked templates from CONTEXT.md §specifics.

    Store the command outputs for embedding in 36-VERIFICATION.md.
  </action>
  <verify>
    1. All six verification commands produce their expected outputs.
    2. If any verification step fails, Task 6 is triggered (D-28 gate). Do NOT proceed to Task 2.
  </verify>
  <done>All six verification commands run; outputs captured; either (a) all green — proceed to Task 2, or (b) D-28 gate triggered — jump to Task 6.</done>
</task>

<task type="auto">
  <name>Task 2: Write 36-VERIFICATION.md report</name>
  <files>.planning/phases/36-coordinate-frame-fix/36-VERIFICATION.md</files>
  <action>
    Mirror the Phase 35 verification format at `.planning/phases/35-coordinate-bug-characterization/35-VERIFICATION.md`. Required sections:

    ```markdown
    ---
    phase: 36-coordinate-frame-fix
    verified: <ISO 8601 timestamp with Z suffix>
    status: passed
    score: 5/5 SC satisfied
    overrides_applied: 0
    re_verification: null
    gaps: []
    deferred:
      - truth: "Posterior re-evaluation at h=0.73 under v2.2 corrections"
        addressed_in: "Phase 40 VERIFY-02"
        evidence: "Phase 40 goal + abort gate (MAP shift >5% from v2.1 baseline)"
      - truth: "h-threading through set_host_galaxy_parameters"
        addressed_in: "Phase 37 PE-01"
        evidence: "Phase 37 SC-1 regression test on h=0.5 vs h=1.0 d_L ratio"
      - truth: "Idempotency guard on _map_angles_to_spherical_coordinates"
        addressed_in: "Phase 37 COORD-05"
        evidence: "Phase 37 SC-7 AssertionError on second call"
      - truth: "Deprecated get_possible_hosts (non-balltree) removal"
        addressed_in: "Phase 39 HPC-04"
        evidence: "Phase 39 SC-4 grep empty"
    human_verification: []
    ---

    # Phase 36: Coordinate Frame Fix — Verification Report

    **Phase Goal:** Fix four coordinate-frame bugs in handler.py (COORD-02, COORD-02b, COORD-03, COORD-04) via the /physics-change protocol, landing four atomic [PHYSICS] commits and committing the Phase 40 VERIFY-02 regression anchor.
    **Verified:** <timestamp>
    **Status:** PASSED
    **Re-verification:** No — initial verification

    ---

    ## Goal Achievement

    <One-paragraph summary: four atomic [PHYSICS] commits landed per D-25; six Phase 35 xfail markers removed per D-26; regression pickle committed per D-24 at the locked path; test_coordinate_roundtrip.py reports 9 passed, 0 xfailed.>

    ### ROADMAP.md Success Criteria — Verbatim Verification

    **SC-1:** (≥99% recovery rate on N=100 random ecliptic-equator). VERIFIED. <Evidence: pytest output line referencing TestNRandomEclipticEquatorRecovery.>

    **SC-2:** (`astropy.coordinates.SkyCoord.transform_to(BarycentricTrueEcliptic())` applied; docstring declares "all stored angles henceforth in ecliptic SSB frame"). VERIFIED. <Evidence: grep `_rotate_equatorial_to_ecliptic` in handler.py:178; docstring review.>

    > **SC-2 reinterpretation note (D-13):** The ROADMAP letter of SC-2 specifies astropy transform INSIDE `_map_angles_to_spherical_coordinates`. D-13 locks a deviation: the rotation is implemented as a separated `_rotate_equatorial_to_ecliptic()` method, called from `__init__` BEFORE `_map_angles_to_spherical_coordinates`. The rotation happens at ingestion, and the storage-convention docstring is declared in `_rotate_equatorial_to_ecliptic` — SC-2 spirit (rotation applied on ingestion, frame documented) is fully satisfied. The letter deviation — rotation is NOT inlined into `_map_angles_to_spherical_coordinates` — was a deliberate single-responsibility choice (D-13 rationale). This reinterpretation is approved as part of the Phase 36 planning and does NOT trigger re-verification.

    **SC-3:** (BallTree Cartesian embedding `(sin θ cos φ, sin θ sin φ, cos θ)` consistent across setup + query). VERIFIED. <Evidence: grep `_polar_to_cartesian` count == 4 (definition + 3 call sites: 3D setup, 3D query, 4D/5D setup, 4D/5D query).>

    **SC-4:** (regression pickle shows new ⊇ old). VERIFIED. <Evidence: pickle load + assertion from Task 1 step 3.>

    **SC-5:** (reference comments + /physics-change post-implementation checks). VERIFIED. <Evidence: grep `# Eq. \(` count in handler.py increased by 4+ reference comments; /physics-change review record from Plan 36-01 Task 1.>

    ### Observable Truths Summary

    | # | Truth | Status | Evidence |
    |---|-------|--------|----------|
    | 1 | 4 atomic [PHYSICS] commits (COORD-02, 02b, 03, 04) | VERIFIED | git log output (embed command + result) |
    | 2 | test_coordinate_roundtrip.py: 9 passed, 0 xfail, 0 XPASS, 0 FAIL | VERIFIED | pytest output (embed summary line) |
    | 3 | 36-superset-regression.pkl exists, schema-complete, new ⊇ old | VERIFIED | pickle inspection (embed script output) |
    | 4 | No unrelated regression (`pytest -m "not gpu and not slow"`) | VERIFIED | full-suite pytest output (embed summary line) |
    | 5 | REQUIREMENTS.md, STATE.md, MILESTONES.md, ROADMAP.md consistent | VERIFIED | cross-check (embed grep output) |

    **Score:** 5/5 truths verified

    ---

    ## REQ-ID Coverage Matrix

    | Requirement | Description | Plans | Files | Commit | Status |
    |-------------|-------------|-------|-------|--------|--------|
    | COORD-02 | Polar-correct BallTree Cartesian embedding | 36-02 | handler.py | <hash> | SATISFIED |
    | COORD-02b | 4D BallTree spherical sky sub-space | 36-04 | handler.py | <hash> | SATISFIED |
    | COORD-03 | Equatorial→ecliptic rotation on GLADE ingestion | 36-01 | handler.py | <hash> | SATISFIED |
    | COORD-04 | Eigenvalue sky search radius | 36-03 | handler.py, bayesian_statistics.py, scripts/generate_phase36_regression.py, .planning/phases/36-coordinate-frame-fix/36-superset-regression.pkl | <hash> | SATISFIED |

    ---

    ## Gate Checklist

    <Mirror Phase 35's gate checklist format. Tables per deliverable type.>

    ### handler.py modifications
    | Check | Result | Evidence |
    |-------|--------|----------|
    | `_rotate_equatorial_to_ecliptic` exists | PASS | grep |
    | `_polar_to_cartesian` module-level helper | PASS | grep |
    | 3D setup site uses helper | PASS | grep |
    | 3D query site uses helper | PASS | grep |
    | 5-D setup site uses helper | PASS | grep |
    | 5-D query site uses helper | PASS | grep |
    | Eigenvalue radius formula at handler.py:~313 | PASS | grep `eigvalsh` |
    | `cov_theta_phi` kwarg in signature | PASS | inspect.signature |
    | Range asserts in `_rotate_equatorial_to_ecliptic` | PASS | grep `assert np.all` |
    | Reference comments above each changed line | PASS | grep `# Eq\.` |

    ### Physics-Change Protocol
    | Check | Result | Evidence |
    |-------|--------|----------|
    | Single /physics-change review for all 4 REQs (D-25) | PASS | Plan 36-01 Task 1 checkpoint record |
    | Four atomic [PHYSICS] commits | PASS | git log grep |
    | Each commit prefixed [PHYSICS] | PASS | git log |
    | Commit templates match CONTEXT.md §specifics | PASS | message inspection |
    | Pre-commit hooks ran clean on each commit | PASS | no --no-verify in history |

    ### xfail Discipline (D-26)
    | Check | Result | Evidence |
    |-------|--------|----------|
    | Markers removed in SAME commit as fix | PASS | `git show` on each commit |
    | No bulk-removal commit | PASS | git log for "remove xfail" |
    | Current xfail count is 0 | PASS | grep count |

    ### Regression Pickle (D-24)
    | Check | Result | Evidence |
    |-------|--------|----------|
    | Pickle committed at locked path | PASS | git ls-files |
    | All D-24 schema fields present | PASS | pickle inspection |
    | old ⊆ new assertion passes | PASS | assertion output |
    | fisher_sky_2x2 is positive-definite 2×2 | PASS | np.linalg.det > 0 |
    | git_commit is 40-char SHA | PASS | len check |

    ---

    ## Required Artifacts

    | Artifact | Expected | Status |
    |----------|----------|--------|
    | handler.py (modified) | 4 physics changes + helpers | VERIFIED |
    | bayesian_statistics.py (modified) | Caller updated with cov_theta_phi | VERIFIED |
    | scripts/generate_phase36_regression.py (new) | Regression CLI | VERIFIED |
    | 36-superset-regression.pkl | D-24 schema | VERIFIED |
    | test_coordinate_roundtrip.py (modified) | 6 xfail markers removed | VERIFIED |
    | REQUIREMENTS.md (modified) | COORD-02b added + 4 checkboxes flipped | VERIFIED |

    ---

    ## Key Link Verification

    <Table of cross-module wiring: handler.py → bayesian_statistics.py caller; handler.py _polar_to_cartesian shared across 4 call sites; pickle → Phase 40 VERIFY-02 anchor.>

    ---

    ## Behavioral Spot-Checks

    | Behavior | Command | Expected | Status |
    |----------|---------|----------|--------|
    | 9 tests pass | `uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py -v` | 9 passed, 0 xfail | PASS |
    | Pickle superset | `python -c "..."` | True | PASS |
    | No regression | `uv run pytest -m "not gpu and not slow"` | exit 0 | PASS |
    | Handler import | `python -c "from master_thesis_code.galaxy_catalogue.handler import ..."` | OK | PASS |

    ---

    ## Deferred Items

    | # | Item | Addressed In | Evidence |
    |---|------|-------------|----------|
    | 1 | Posterior re-evaluation at h=0.73 | Phase 40 VERIFY-02 | ROADMAP §Phase 40 abort gate |
    | 2 | h-threading through set_host_galaxy_parameters | Phase 37 PE-01 | ROADMAP §Phase 37 SC-1 |
    | 3 | Idempotency guard on `_map_angles_to_spherical_coordinates` | Phase 37 COORD-05 | ROADMAP §Phase 37 SC-7 |
    | 4 | Deprecated `get_possible_hosts` removal | Phase 39 HPC-04 | ROADMAP §Phase 39 SC-4 |

    ---

    ## Notable Finding: Eccentric vs ecliptic-equator host density

    <A short observation section. The Phase 35 baseline reported 0/42 events in ±5° band (deficiency). After Phase 36, the bug is structurally fixed. However, the re-run of the simulation to produce new CRBs under the fixed frame is Phase 40 VERIFY-02, not Phase 36. State that the post-fix re-run has NOT yet been performed and cite Phase 40 as the place to measure the recovery.>

    ---

    ## Gaps Summary

    No gaps. All four REQ-IDs satisfied; all five ROADMAP SCs satisfied; D-28 gate NOT triggered.

    ---

    ## Verdict

    **ACCEPT**

    All Phase 36 deliverables exist, pass quality gates, and satisfy REQ-IDs COORD-02, COORD-02b, COORD-03, COORD-04. Regression pickle locked as Phase 40 VERIFY-02 anchor. Ready for Phase 37 handoff.

    ---

    _Verified: <timestamp>_
    _Verifier: Claude (gpd-executor)_
    ```

    Populate all `<...>` placeholders with actual values captured in Task 1.
  </action>
  <verify>
    1. `test_coordinate_roundtrip.py` shows ≥9 passed; 36-VERIFICATION.md reports this.
    2. All 5 SCs marked VERIFIED.
    3. Four commit hashes populated in REQ-ID Coverage Matrix.
    4. No `<placeholder>` strings remain in the file.
  </verify>
  <done>36-VERIFICATION.md written with all sections populated and status: PASSED.</done>
</task>

<task type="auto">
  <name>Task 3: Update .planning/REQUIREMENTS.md (flip 4 checkboxes + traceability status)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    1. Flip four checkboxes in §Coordinate Frame Correctness:
       - `[ ] **COORD-02**` → `[x] **COORD-02**`
       - `[ ] **COORD-02b**` → `[x] **COORD-02b**` (note: COORD-02b line was added in Plan 36-01)
       - `[ ] **COORD-03**` → `[x] **COORD-03**`
       - `[ ] **COORD-04**` → `[x] **COORD-04**`

    2. Update traceability table at bottom: change status `Pending` → `Done` for COORD-02, COORD-02b, COORD-03, COORD-04.

    3. Do NOT touch COORD-05 (Phase 37 scope, not this phase).

    4. Update the "Last updated" footer date to today.
  </action>
  <verify>
    1. `grep -c "^- \[x\] \*\*COORD-0[234]" .planning/REQUIREMENTS.md` returns ≥4 (counts the four checkboxes flipped).
    2. `grep -c "Pending" .planning/REQUIREMENTS.md` decreased by 4 from pre-task value.
    3. `grep "^- \[x\] \*\*COORD-05" .planning/REQUIREMENTS.md` returns 0 matches (COORD-05 is Phase 37).
  </verify>
  <done>REQUIREMENTS.md reflects Phase 36 completion; 4 REQs marked done.</done>
</task>

<task type="auto">
  <name>Task 4: Update .planning/ROADMAP.md, MILESTONES.md, STATE.md (state consistency)</name>
  <files>
    .planning/ROADMAP.md
    .planning/MILESTONES.md
    .planning/STATE.md
  </files>
  <action>
    **ROADMAP.md:**

    1. Under `### Phase 36: Coordinate Frame Fix`, change `**Plans**: TBD` → `**Plans**: 5 plans`.
    2. Add the plan list:
       ```markdown
         - [x] 36-01-PLAN.md — COORD-03 rotation + _polar_to_cartesian helper (Wave 1)
         - [x] 36-02-PLAN.md — COORD-02 3D BallTree embedding (Wave 2)
         - [x] 36-03-PLAN.md — COORD-04 eigenvalue search radius + regression pickle (Wave 3)
         - [x] 36-04-PLAN.md — COORD-02b 4D BallTree spherical embedding (Wave 3)
         - [x] 36-05-PLAN.md — Verification + state update (Wave 4)
       ```
    3. In the `## Progress` table at the bottom, update the Phase 36 row:
       - `Plans Complete`: `0/?` → `5/5`
       - `Status`: `Not started` → `Complete`
       - `Completed`: `-` → today's ISO date

    **MILESTONES.md:**

    1. Under the v2.2 Pipeline Correctness milestone, flip the Phase 36 checkbox from `[ ]` to `[x]`.
    2. If MILESTONES.md tracks progress-to-date, update the aggregate counter.

    **STATE.md:**

    1. Update the "current phase" pointer: Phase 36 → Phase 37 (Parameter Estimation Correctness).
    2. Add a "Recently completed" entry: `Phase 36 (COORD-02/02b/03/04)` with today's date.
    3. If STATE.md tracks active milestone progress, increment the count (e.g., "2/8 phases complete" if tracking Phase 35 + Phase 36 of 8).

    All three files edited in ONE commit with Task 5.
  </action>
  <verify>
    1. `grep "36-01-PLAN.md" .planning/ROADMAP.md` returns 1 match (now listed).
    2. `grep -E "Phase 36.*Complete" .planning/ROADMAP.md` returns 1 match (Progress table row updated).
    3. `grep "Phase 36" .planning/MILESTONES.md` shows `[x]` checkbox.
    4. `grep -iE "phase 37|current phase" .planning/STATE.md` points to Phase 37.
  </verify>
  <done>Four state files updated with consistent Phase 36 completion.</done>
</task>

<task type="auto">
  <name>Task 5: Commit documentation atomically</name>
  <files>
    .planning/phases/36-coordinate-frame-fix/36-VERIFICATION.md
    .planning/STATE.md
    .planning/MILESTONES.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Stage the five files and commit:

    ```
    docs(36): complete Phase 36 verification, regression pickle anchor locked, state advanced to Phase 37

    - 36-VERIFICATION.md: PASSED (5/5 SCs, 4 REQ-IDs, 9/0/0/0 roundtrip test state).
    - REQUIREMENTS.md: COORD-02, COORD-02b, COORD-03, COORD-04 checkboxes flipped; traceability status Pending → Done.
    - ROADMAP.md: Phase 36 row Complete; plan list populated (5 plans; 4 waves).
    - MILESTONES.md: v2.2 Pipeline Correctness Phase 36 checkbox flipped.
    - STATE.md: current phase advanced to Phase 37.

    Phase 36 handoff contract:
    - .planning/phases/36-coordinate-frame-fix/36-superset-regression.pkl is the Phase 40 VERIFY-02 anchor.
    - test_coordinate_roundtrip.py: 9 passed, 0 xfail, 0 FAIL, 0 XPASS.
    - Four [PHYSICS] commits landed: COORD-02, COORD-02b, COORD-03, COORD-04.

    References:
      - .planning/phases/36-coordinate-frame-fix/36-CONTEXT.md D-24, D-25, D-26, D-27.
      - .planning/ROADMAP.md §Phase 36 SC-1 through SC-5.
    ```

    Use `gpd commit`. Pre-commit hooks clean; no [PHYSICS] prefix needed (this is documentation/state).

    Do NOT amend. Do NOT skip hooks.
  </action>
  <verify>
    1. `git log --format="%s" -1` starts with `docs(36):`.
    2. `git show --stat HEAD` lists the five files.
    3. No code files (handler.py, etc.) in this commit.
    4. Pre-commit hooks passed.
  </verify>
  <done>Phase 36 closed. Single documentation commit landed. Ready for /gpd:plan-phase 37.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 6: D-28 stop/rethink gate (only triggered if Task 1 failed)</name>
  <decision>If Task 1 uncovered xfail tests remaining, FAILED tests, XPASS, or <99% recovery on N=100 random, STOP. Do not proceed to Tasks 2–5. Document the failure state, escalate to user with structured diagnostic, and await decision.</decision>
  <context>
    Per CONTEXT.md D-28:
    > If after all four commits the Phase 35 tests do NOT flip XPASS (or do flip but with <99% recovery on the N=100 random test), STOP. Do not remove xfail markers. Do not advance to Phase 37. Ask the user before any further commits.

    Possible failure modes and recommended diagnosis:

    **Mode A — test still xfails after Plan 36-02:**
    - If `TestBallTreeRecoversEclipticEquatorGalaxy` xfails: `_polar_to_cartesian` logic error OR synthetic catalog `already_rotated=True` is producing wrong input. Check: `uv run python -c "from master_thesis_code.galaxy_catalogue.handler import _polar_to_cartesian; import numpy as np; print(_polar_to_cartesian(np.array([np.pi/2]), np.array([np.pi])))"` should print approximately `[[−1, 0, 0]]`.
    - If `TestBallTreeRecoversNorthCelestialPole` xfails: either rotation not applied OR NCP ecliptic coordinates wrong OR BallTree can't find pole point. Check astropy round-trip: `uv run python -c "from master_thesis_code_test.fixtures.coordinate import equatorial_to_ecliptic_astropy; print(equatorial_to_ecliptic_astropy(0, 90))"` should print approximately `(0.0, 1.1614..., 0.4094...)`.

    **Mode B — TestNRandomEclipticEquatorRecovery reports 50/100 (or similar <99):**
    - Check synthetic catalog band width: `sky_band="ecliptic_equator"` builder is supposed to pin galaxies within ±something of θ=π/2. Verify the band is narrow enough that query at θ=π/2 retrieves them all.
    - Alternative: BallTree radius in the test is too small. Per test line 326, radius = 0.05 rad; check if this is reasonable for the synthetic band.
    - Alternative: off-by-one in `_polar_to_cartesian` axis ordering. Matches Mode A diagnosis.

    **Mode C — test FAILS (not xfailed):**
    - A regression was introduced. Check most recent [PHYSICS] commit's diff — likely a sign error or wrong variable.
    - Run `uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py::<class>::<method> -v --tb=long` on the failing test.

    **Mode D — pickle superset assertion fails (new ⊄ old):**
    - `sigma_matrix`, `jacobian`, or `sigma_scaled` has wrong axis ordering.
    - Check: at θ=π/2 (the reference event worst case), |sin θ|=1 so J=I and Σ'=Σ. Eigenvalue = max(σ_φ², σ_θ²) ≥ max(σ_φ, σ_θ)² / 1 — should be equal or larger than old radius squared. Therefore new radius ≥ old radius → new set ⊇ old set. If violated, the eigenvalue routine is buggy.

    **Mode E — `pytest -m "not gpu and not slow"` regresses on some unrelated test:**
    - Likely an integration test that exercised `get_possible_hosts_from_ball_tree` with positional args is now broken. Fix: change that caller to use kwargs (or move `cov_theta_phi` to end of signature).

    Present all failing outputs + recommended mode diagnosis to user. Await explicit user go/revise decision.

    DO NOT commit 36-VERIFICATION.md with status: passed if D-28 is triggered. DO NOT advance STATE.md. Write a failure report at `.planning/phases/36-coordinate-frame-fix/36-STOP-D28.md` instead, documenting the failure mode and recommended fix, and wait for user.
  </context>
  <options>
    <option id="none-triggered">Task 1 all green; D-28 not triggered. Skip this task. Proceed to Task 2.</option>
    <option id="mode-a-b-c">Test failure mode detected. Write diagnostic report; STOP; wait for user.</option>
    <option id="mode-d">Regression pickle assertion failed. Investigate eigenvalue radius formula; STOP; wait for user.</option>
    <option id="mode-e">Unrelated regression. Investigate the specific failing test; STOP; wait for user.</option>
  </options>
  <resume-signal>User approves revised plan (proceed to fix) or accepts scope reduction (e.g., "land 3 of 4 REQs; defer the failing one to Phase 36.1").</resume-signal>
</task>

</tasks>

<verification>
- Phase 35 test file reports 9 passed, 0 xfail, 0 FAIL, 0 XPASS. (D-27 decisive.)
- Regression pickle at locked path has D-24 schema and old ⊆ new. (D-24 + SC-4.)
- Four atomic [PHYSICS] commits present in git log, matching locked templates. (D-25.)
- All xfail markers from Phase 35 are removed (one per fix per D-26). (D-26.)
- No unrelated pytest regression. (VERIFY-01 prep.)
- STATE/MILESTONES/ROADMAP/REQUIREMENTS files consistent across Phase 36 completion status.
- D-28 stop/rethink gate respected: if any verification fails, 36-VERIFICATION.md is NOT written with status passed; STATE.md is NOT advanced.
</verification>

<success_criteria>
- 36-VERIFICATION.md exists with status: passed and score: 5/5 SC satisfied.
- REQUIREMENTS.md has 4 COORD-related checkboxes flipped and traceability rows updated to Done.
- ROADMAP.md Phase 36 row Complete with date populated.
- MILESTONES.md v2.2 Phase 36 checkbox flipped.
- STATE.md advanced to Phase 37.
- Single `docs(36):` commit lands all five files atomically.
- D-28 gate respected: if triggered, no premature completion; escalated to user with diagnostic.
</success_criteria>

<output>
Phase 36 complete. Handoff to:
- /gpd:plan-phase 37 (Parameter Estimation Correctness; depends on Phase 36 fixed frame per ROADMAP.md).
- Phase 40 VERIFY-02 (re-evaluates posterior under fixed frame; uses the regression pickle as the diff anchor).
</output>
