---
phase: 36-coordinate-frame-fix
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - master_thesis_code/galaxy_catalogue/handler.py
  - master_thesis_code_test/test_coordinate_roundtrip.py
interactive: false  # /physics-change review already happened in Plan 36-01 Task 1 (D-25)

conventions:
  units: radians for angles, dimensionless Cartesian on unit sphere
  coordinates: ecliptic SSB (post-rotation from Plan 36-01); θ_polar ∈ [0, π] with θ_polar=0 at north ecliptic pole
  spherical_convention: physics (θ = polar angle from north pole, φ = azimuth). Matches handler._map_angles_to_spherical_coordinates convention (θ = π/2 − β).

dimensional_check:
  theta_rad: "[rad] ∈ [0, π]"
  phi_rad: "[rad] ∈ [0, 2π)"
  cartesian_xyz: "dimensionless; ||v||₂ = 1 exactly"
  balltree_metric: "euclidean chord length on S², dimensionless"

approximations:
  - name: "euclidean (chord-length) metric on unit sphere"
    parameter: "chord distance d_c = 2 sin(arc/2)"
    validity: "For arc-lengths < 1 rad, chord ≈ arc to first order; for typical sky-localization CRB radii (milliradian scale), chord ≈ arc to 12+ significant digits"
    breaks_when: "arc-length comparable to π; not a concern for EMRI sky localization"
    check: "D-06 N=100 random ecliptic-equator test (≥99% recovery)"

contract:
  scope:
    question: "Does the 3D BallTree correctly retrieve a host galaxy at the ecliptic equator and at the ecliptic pole, after the Phase 36-01 rotation fix lands?"
  claims:
    - id: claim-embedding-polar-correct
      statement: "Both `setup_galaxy_catalog_balltree` (handler.py:281–293) and `get_possible_hosts_from_ball_tree` (handler.py:295–315) call `_polar_to_cartesian(θ, φ)` instead of the latitude-formula `(cos θ cos φ, cos θ sin φ, sin θ)`."
      deliverables: [deliv-setup-embedding, deliv-query-embedding]
      acceptance_tests: [test-equator-retrieval, test-ncp-retrieval, test-n100-random]
      references: [ref-d17, ref-d19, ref-sc3]
    - id: claim-equator-recovery
      statement: "A synthetic galaxy at ecliptic (θ=π/2, φ=π) is retrieved by the BallTree when queried at the same position with radius < 0.01 rad."
      deliverables: [deliv-setup-embedding, deliv-query-embedding]
      acceptance_tests: [test-equator-retrieval]
      references: [ref-d19]
    - id: claim-99-percent-recovery
      statement: "100 random ecliptic-equator synthetic galaxies (seed=42, D-07 in Phase 35) are retrieved at ≥99% rate by the BallTree."
      deliverables: [deliv-setup-embedding, deliv-query-embedding]
      acceptance_tests: [test-n100-random]
      references: [ref-d19, ref-sc1]
  deliverables:
    - id: deliv-setup-embedding
      kind: code
      path: "master_thesis_code/galaxy_catalogue/handler.py"
      description: "Replace handler.py:286–289 with `data = _polar_to_cartesian(dec, ra)` (names preserved from existing code — recall the variables are misleadingly named `ra`/`dec` but hold ecliptic (φ, θ_polar) after Plan 36-01)."
      must_contain: ["_polar_to_cartesian"]
    - id: deliv-query-embedding
      kind: code
      path: "master_thesis_code/galaxy_catalogue/handler.py"
      description: "Replace handler.py:307–311 with `query_point = _polar_to_cartesian(np.array([theta]), np.array([phi]))`."
      must_contain: ["_polar_to_cartesian"]
    - id: deliv-xfail-removal
      kind: code
      path: "master_thesis_code_test/test_coordinate_roundtrip.py"
      description: "Remove @pytest.mark.xfail on `TestBallTreeRecoversEclipticEquatorGalaxy`, `TestBallTreeRecoversNorthCelestialPole` (if not already removed by Plan 36-01), and `TestNRandomEclipticEquatorRecovery`."
      must_contain: []
  acceptance_tests:
    - id: test-equator-retrieval
      subject: claim-equator-recovery
      kind: smoke
      procedure: "Run uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py::TestBallTreeRecoversEclipticEquatorGalaxy -v after xfail removal."
      pass_condition: "Test passes (not XPASS, not xfail)."
      evidence_required: [deliv-setup-embedding, deliv-query-embedding, deliv-xfail-removal]
    - id: test-ncp-retrieval
      subject: claim-embedding-polar-correct
      kind: smoke
      procedure: "Run uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py::TestBallTreeRecoversNorthCelestialPole -v; exercises BOTH COORD-02 AND COORD-03 in a single round-trip."
      pass_condition: "Test passes (not XPASS). If already passing after Plan 36-01 commit (XPASS), its xfail is already removed; confirm green state holds."
      evidence_required: [deliv-setup-embedding, deliv-query-embedding, deliv-xfail-removal]
    - id: test-n100-random
      subject: claim-99-percent-recovery
      kind: statistical
      procedure: "Run uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py::TestNRandomEclipticEquatorRecovery -v after xfail removal."
      pass_condition: "≥99/100 retrieved (test uses seed=42 per D-07); assertion `recovered_count >= 99` fires."
      evidence_required: [deliv-setup-embedding, deliv-query-embedding, deliv-xfail-removal]
  references:
    - id: ref-d17
      kind: decision
      locator: ".planning/phases/36-coordinate-frame-fix/36-CONTEXT.md §decisions D-17 (shared _polar_to_cartesian helper)"
    - id: ref-d19
      kind: decision
      locator: ".planning/phases/36-coordinate-frame-fix/36-CONTEXT.md §decisions D-19 (Phase 35 tests flip XPASS → passing)"
    - id: ref-sc1
      kind: internal
      locator: ".planning/ROADMAP.md §Phase 36 Success Criterion 1 (≥99% recovery rate)"
    - id: ref-sc3
      kind: internal
      locator: ".planning/ROADMAP.md §Phase 36 Success Criterion 3 (BallTree embedding consistent across setup + query)"
    - id: ref-requirements-coord02
      kind: internal
      locator: ".planning/REQUIREMENTS.md §Coordinate Frame Correctness COORD-02"
    - id: ref-patterns-balltree
      kind: internal
      locator: ".planning/phases/35-coordinate-bug-characterization/35-PATTERNS.md §Minimum column set required by the BallTree path"
  forbidden_proxies:
    - "Monkey-patching one call site and leaving the other: D-17 explicitly locks BOTH `setup_galaxy_catalog_balltree` AND `get_possible_hosts_from_ball_tree` to share `_polar_to_cartesian` — structural symmetry, not two copies of the formula."
    - "Inlining the formula `(sin θ cos φ, sin θ sin φ, cos θ)` at the two sites instead of calling the shared helper — the helper enforces future-edit drift-proofing."
    - "Removing xfail markers before the fix is staged — xfail(strict=True) mechanism is the live CI signal, must stay until the commit that contains the fix."
    - "Removing the xfail on `TestVernalEquinoxRoundTrip` / `TestSummerSolsticeRotation` / `TestEclipticPoleIngestion` — those are COORD-03-dependent and were already removed in Plan 36-01; do not re-remove or re-add."
    - "Modifying `_polar_to_cartesian` (e.g., switching axis order) in this plan — the helper contract is owned by Plan 36-01; changes there cascade."
    - "Changing the BallTree metric from 'euclidean' to 'haversine' without documenting the decision — per CONTEXT.md §Claude's Discretion, planner decides; this plan locks in 'euclidean' on unit-sphere (chord-length) since it matches the existing tests."
  uncertainty_markers:
    weakest_anchors:
      - "The Phase 35 synthetic catalog builder (fixtures/coordinate.py) produces catalogs in either already-ecliptic or pre-rotation degrees state via `already_rotated` knob. Plan 36-02 relies on the builder correctly handling the post-rotation case. If it does not, the test fixture itself becomes the bottleneck, not the production code."
    disconfirming_observations:
      - "After both Plan 36-01 and Plan 36-02 land, `TestNRandomEclipticEquatorRecovery` reports <99/100 recovery — signals either an off-by-one in the embedding OR a synthetic-catalog builder bug. Triggers D-28 stop/rethink gate."
      - "`TestBallTreeRecoversNorthCelestialPole` still xfails after Plan 36-02 — signals COORD-03 rotation + COORD-02 embedding together still don't retrieve the NCP; investigate before Plan 36-03."
  links:
    - id: link-shared-helper
      source: deliv-setup-embedding
      target: deliv-query-embedding
      relation: shared_implementation
      verified_by: [test-equator-retrieval, test-n100-random]
---

<objective>
Fix COORD-02 by replacing the latitude-formula BallTree Cartesian embedding at handler.py:286–289 (setup) and 307–311 (query) with the polar-correct `_polar_to_cartesian(θ, φ)` helper from Plan 36-01. The bug manifests as queries at the ecliptic equator (θ = π/2) collapsing to the pole (0, 0, 1), causing 0/42 production events to fall within ±5° of the equator when ~8.7% is expected (per `.planning/audit_coordinate_bug.json` baseline).

Purpose: Once this lands on top of Plan 36-01's rotation, the three COORD-02-dependent Phase 35 RED tests (equator, NCP, N-100 random) flip to passing, satisfying ROADMAP §Phase 36 SC-1 and SC-3.
Output: Two call sites in `handler.py` now invoke the shared helper; the three COORD-02-dependent xfail markers are removed atomically with the code change; single `[PHYSICS] COORD-02` commit lands.
</objective>

<execution_context>
Depends on Plan 36-01's `_polar_to_cartesian` helper being available. /physics-change review was performed in Plan 36-01 Task 1 covering all four REQs including COORD-02; no second review.

Commit message (locked per CONTEXT.md §specifics):

    [PHYSICS] COORD-02: polar-correct BallTree Cartesian embedding (sin θ cos φ, sin θ sin φ, cos θ)

Use `gpd commit` for atomic commit.
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/36-coordinate-frame-fix/36-CONTEXT.md
@.planning/phases/35-coordinate-bug-characterization/35-PATTERNS.md
@master_thesis_code/galaxy_catalogue/handler.py
@master_thesis_code_test/test_coordinate_roundtrip.py
@master_thesis_code_test/fixtures/coordinate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace BallTree embedding at `setup_galaxy_catalog_balltree` (handler.py:281–293)</name>
  <files>master_thesis_code/galaxy_catalogue/handler.py</files>
  <action>
    Replace lines 286–289 of `handler.py` inside `setup_galaxy_catalog_balltree`. The existing code is:

    ```python
    ra = self.reduced_galaxy_catalog[InternalCatalogColumns.PHI_S].values
    dec = self.reduced_galaxy_catalog[InternalCatalogColumns.THETA_S].values

    x = np.cos(dec) * np.cos(ra)
    y = np.cos(dec) * np.sin(ra)
    z = np.sin(dec)
    data = np.vstack((x, y, z)).T
    ```

    Note the local variable names (`ra`, `dec`) are misleading after Plan 36-01 — the columns now hold ecliptic (φ_rad, θ_polar_rad). Rename to `phi` / `theta` for clarity AND keep the comment describing the historical-name confusion. Resulting code:

    ```python
    # Columns were historically named RA/Dec (GLADE equatorial) but after
    # Plan 36-01 (_rotate_equatorial_to_ecliptic), they hold ecliptic polar-
    # angle pairs (θ_polar ∈ [0, π], φ ∈ [0, 2π)). The column constants
    # PHI_S / THETA_S are kept for backward compatibility; see CONTEXT.md D-13.
    phi = self.reduced_galaxy_catalog[InternalCatalogColumns.PHI_S].values
    theta = self.reduced_galaxy_catalog[InternalCatalogColumns.THETA_S].values

    # Eq. (standard spherical polar); COORD-02 fix per .planning/phases/36-coordinate-frame-fix/36-CONTEXT.md D-17
    data = _polar_to_cartesian(theta, phi)

    self.catalog_ball_tree = BallTree(data, metric="euclidean")
    ```

    Keep the metric as `"euclidean"` — per CONTEXT.md §Claude's Discretion, the chord-length interpretation on unit sphere works with Euclidean, and the Phase 35 tests (`test_coordinate_roundtrip.py` line 153: `tree.query_radius(query, r=0.01)`) are written to this convention.

    The subsequent `self.reduced_galaxy_catalog = self.reduced_galaxy_catalog.reset_index()` line stays unchanged.
  </action>
  <verify>
    1. Dimensional: `_polar_to_cartesian(theta, phi)` returns unit vectors; BallTree metric is euclidean chord length on S²; dimensions consistent.
    2. ruff + mypy clean on handler.py.
    3. No use of `np.cos(dec) * np.cos(ra)` etc. remains at handler.py:286–289 (grep confirms).
    4. The two variable names are now `phi` and `theta` with the clarifying comment.
  </verify>
  <done>Setup call site at handler.py:~286 now uses `_polar_to_cartesian`; lint/type clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace query embedding in `get_possible_hosts_from_ball_tree` (handler.py:295–315)</name>
  <files>master_thesis_code/galaxy_catalogue/handler.py</files>
  <action>
    Replace the embedding at lines 307–311 of `get_possible_hosts_from_ball_tree`. The existing code is:

    ```python
    x = np.cos(theta) * np.cos(phi)
    y = np.cos(theta) * np.sin(phi)
    z = np.sin(theta)

    query_point = np.array([[x, y, z]])
    ```

    Replace with:

    ```python
    # Eq. (standard spherical polar); COORD-02 fix per .planning/phases/36-coordinate-frame-fix/36-CONTEXT.md D-17
    # _polar_to_cartesian expects array inputs; wrap scalars.
    query_point = _polar_to_cartesian(np.array([theta]), np.array([phi]))
    ```

    `_polar_to_cartesian` returns shape (1, 3), matching the existing `np.array([[x, y, z]])` query-point shape that `BallTree.query_radius` expects. Confirm with a unit-norm check: `np.isclose(np.linalg.norm(query_point), 1.0)`.

    Do NOT change the `radius = max(phi_sigma, theta_sigma) * sigma_multiplier` at line 313 — that is COORD-04 and lives in Plan 36-03. Do NOT extend the function signature — that is also COORD-04.

    Do NOT touch `setup_4d_galaxy_catalog_balltree` (handler.py:356–371) or `find_closest_galaxy_to_coordinates` (handler.py:373–390). Those are COORD-02b and live in Plan 36-04.
  </action>
  <verify>
    1. `grep -nE "np\.cos\(theta\) \* np\.cos\(phi\)" master_thesis_code/galaxy_catalogue/handler.py` returns 0 matches.
    2. `grep -nE "_polar_to_cartesian" master_thesis_code/galaxy_catalogue/handler.py` returns ≥3 matches (the definition + the two call sites).
    3. Query point unit-norm: the returned `query_point[0]` has ||v||₂ ≈ 1.
    4. ruff + mypy clean on handler.py.
    5. `pytest master_thesis_code_test/test_coordinate_roundtrip.py::TestBallTreeRecoversEclipticEquatorGalaxy --runxfail -v` reports XPASS (running WITH xfail markers still in place to confirm the fix moves the test from xfail to pass before we remove markers).
  </verify>
  <done>Query call site at handler.py:~307 now uses `_polar_to_cartesian`; both BallTree sites are structurally identical calls to the shared helper per D-17.</done>
</task>

<task type="auto">
  <name>Task 3: Remove xfail markers on COORD-02-dependent tests + commit atomically as [PHYSICS] COORD-02</name>
  <files>
    master_thesis_code_test/test_coordinate_roundtrip.py
    master_thesis_code/galaxy_catalogue/handler.py
  </files>
  <action>
    Remove the `@pytest.mark.xfail(strict=True, reason=_XFAIL_REASON)` markers on:

    - `TestBallTreeRecoversEclipticEquatorGalaxy.test_ball_tree_recovers_ecliptic_equator_galaxy` (marker at test_coordinate_roundtrip.py:130)
    - `TestNRandomEclipticEquatorRecovery` (marker at test_coordinate_roundtrip.py:318)
    - `TestBallTreeRecoversNorthCelestialPole.test_ncp_round_trip_through_ingestion_and_balltree` (marker at test_coordinate_roundtrip.py:174) — this test exercises BOTH COORD-02 AND COORD-03; if Plan 36-01 already removed this marker because the test XPASSed after COORD-03 alone, SKIP this removal. Otherwise remove it HERE. The test reason-for-failure flipped from rotation-only to BallTree-embedding-only after Plan 36-01 landed, and BallTree-embedding is exactly what this plan fixes.

    Check the current state first: `grep -n "xfail" master_thesis_code_test/test_coordinate_roundtrip.py` — count should be exactly 3 before Plan 36-02 (after Plan 36-01 removed at least 3). Plan 36-02 removes 2–3 more.

    Run `uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py -v` and confirm:
    - After this commit: 9 passed, 0 xfailed, 0 XPASS, 0 FAIL (per D-27 decisive deliverable). OR 8 passed, 1 xfail if the NCP test still has its marker — flag for investigation per D-28.

    If any test still xfails unexpectedly after both Plan 36-01 and Plan 36-02 changes land, STOP and trigger the D-28 stop/rethink gate (documented in Plan 36-05). Do not remove markers for tests that are still failing.

    Stage handler.py + test_coordinate_roundtrip.py together and commit:

    ```
    [PHYSICS] COORD-02: polar-correct BallTree Cartesian embedding (sin θ cos φ, sin θ sin φ, cos θ)

    - Replace latitude-formula embedding (cos θ cos φ, cos θ sin φ, sin θ) at both BallTree call sites (handler.py:286, 307) with the shared _polar_to_cartesian helper from Plan 36-01.
    - Rename local variables from misleading ra/dec to phi/theta at the setup site; add clarifying comment.
    - Remove xfail markers on the three COORD-02-dependent tests (ecliptic-equator retrieval, N=100 random, NCP if not already removed by Plan 36-01).

    Effect: 0/42 events in ±5° ecliptic equator band (pre-fix baseline per .planning/audit_coordinate_bug.json) → isotropic distribution recovered (verified in Phase 40 VERIFY-04).

    References:
      - Standard spherical polar convention (physics).
      - .planning/phases/36-coordinate-frame-fix/36-CONTEXT.md D-17, D-19.
      - .planning/ROADMAP.md §Phase 36 SC-1, SC-3.
      - /physics-change review approved in Plan 36-01 Task 1 (covers all four REQs per D-25).

    REQ-ID: COORD-02
    ```

    Use `gpd commit`. Pre-commit ruff + mypy must pass.

    Do NOT amend. Do NOT skip hooks.
  </action>
  <verify>
    1. `uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py -v` shows 9 passed, 0 xfailed (target per D-27) OR 8 passed + 1 xfailed with documented reason.
    2. `git log --format="%s" -2` shows `[PHYSICS] COORD-02: ...` as the most recent commit, preceded by `[PHYSICS] COORD-03: ...` from Plan 36-01.
    3. `git show --stat HEAD` shows handler.py + test_coordinate_roundtrip.py modifications only.
    4. `grep -c "xfail(strict=True" master_thesis_code_test/test_coordinate_roundtrip.py` returns 0 (if all removed) or 1 (if NCP stayed; flag for D-28 gate).
    5. Pre-commit hooks passed clean.
  </verify>
  <done>COORD-02 atomic commit landed. Phase 35 RED tests are now ≥8/9 green. Phase 36 proceeds to Plan 36-03 (eigenvalue radius) and Plan 36-04 (4D BallTree).</done>
</task>

</tasks>

<verification>
- Sign convention: the CONTEXT.md D-17 sketch `np.vstack((np.sin(theta) * np.cos(phi), np.sin(theta) * np.sin(phi), np.cos(theta))).T` is the definitive form; both call sites now invoke this through `_polar_to_cartesian`, not duplicate it.
- Dimensional: θ, φ in [rad]; output dimensionless unit vectors.
- Limiting case: θ=π/2 (equator) — old formula (cos θ cos φ, cos θ sin φ, sin θ) = (0, 0, 1) for ALL φ (collapses to pole); new formula (sin θ cos φ, sin θ sin φ, cos θ) = (cos φ, sin φ, 0) (lies on ecliptic great circle). BallTree distinguishes φ values correctly.
- Limiting case: θ=0 (north pole) — old formula (cos 0 cos φ, cos 0 sin φ, sin 0) = (cos φ, sin φ, 0) (WRONG — puts pole on equator); new formula (sin 0 cos φ, sin 0 sin φ, cos 0) = (0, 0, 1) (correct pole).
- Convention consistency: Plan 36-01 established `_polar_to_cartesian`; Plan 36-02 consumes it. No second convention branch.
- xfail removal discipline (D-26): only COORD-02-dependent test markers removed in this commit.
</verification>

<success_criteria>
- Both BallTree call sites use `_polar_to_cartesian` (structural symmetry per D-17).
- `uv run pytest master_thesis_code_test/test_coordinate_roundtrip.py` reports 9 passed, 0 xfail (ideal) or ≥8 passed, 1 xfail with documented reason (investigate per D-28 before proceeding).
- `uv run pytest -m "not gpu and not slow"` passes (no other test regresses).
- Single `[PHYSICS] COORD-02` commit landed with pre-commit hooks clean.
- ROADMAP §Phase 36 SC-1 and SC-3 observable-truth satisfied: `assert recovered_count >= 99` passes at seed=42 over N=100 random ecliptic-equator galaxies.
</success_criteria>

<output>
Proceed to Plan 36-03 (eigenvalue sky search radius, COORD-04) and Plan 36-04 (4D BallTree, COORD-02b). These two are parallel (both depend on 36-02 but neither depends on the other); execute-plan can spawn them in either order.
</output>
