---
phase: 23-deploy-validate
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified: []
autonomous: false
requirements: [DEPL-01]

must_haves:
  truths:
    - "The claudes_sidequests branch is merged into main"
    - "Main branch is pushed to origin"
    - "The cluster ~/MasterThesisCode has the latest code with phases 21+22 changes"
  artifacts: []
  key_links:
    - from: "local main branch"
      to: "origin/main"
      via: "git push"
      pattern: "git push"
    - from: "origin/main"
      to: "cluster ~/MasterThesisCode"
      via: "ssh bwunicluster git pull"
      pattern: "ssh bwunicluster"
---

<objective>
Deploy the updated codebase (phases 21+22: log-space accumulation, physics-floor strategy, overflow fix removal, plus the validation report from Plan 01) to bwUniCluster.

Purpose: Ensure the cluster has the numerical stability fixes before any pending evaluate SLURM jobs execute (DEPL-01). Per D-01, check cluster job status first.
Output: Cluster `~/MasterThesisCode` at the latest commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-deploy-validate/23-CONTEXT.md
@cluster/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check cluster job status, merge to main, and deploy</name>
  <files></files>
  <read_first>
    - cluster/README.md (SSH alias and job management commands)
    - .planning/phases/23-deploy-validate/23-CONTEXT.md (D-01: check squeue before deploy, D-02: if evaluate running let it finish, D-03: deploy method)
  </read_first>
  <action>
**Step 1: Check cluster job status** (per D-01)
```bash
ssh bwunicluster 'squeue --me --format="%.18i %.9P %.50j %.8T %.10M %.6D %R" 2>/dev/null || echo "No jobs"'
```
Report what jobs are running/pending. Per D-02, if evaluate jobs are already running, note it but proceed (accept old code for that run; deploy for future runs).

**Step 2: Merge claudes_sidequests into main**
The current branch `claudes_sidequests` has all phases 21-22 commits plus the Plan 01 validation report. Main is far behind (last commit: `92e1adb integral gpu usage removed`).

```bash
git checkout main
git merge claudes_sidequests --no-edit
```

This is a fast-forward or standard merge. All the phase 21-22 work plus validation report will be on main.

**Step 3: Push main to origin**
```bash
git push origin main
```

**Step 4: Pull on cluster via SSH** (per D-03)
```bash
ssh bwunicluster 'cd ~/MasterThesisCode && git pull origin main'
```

**Step 5: Verify cluster deployment**
```bash
ssh bwunicluster 'cd ~/MasterThesisCode && git log --oneline -3'
```
Confirm the latest commits match local main (phases 21-22 changes visible).

**Step 6: Switch back to working branch**
```bash
git checkout claudes_sidequests
```
  </action>
  <verify>
    <automated>ssh bwunicluster 'cd ~/MasterThesisCode && git log --oneline -1' 2>/dev/null | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline main -1` locally matches the latest commit (includes phase 21-22 work)
    - `ssh bwunicluster 'cd ~/MasterThesisCode && git log --oneline -1'` shows the same commit hash as local main
    - If evaluate jobs were running, their status is documented in the SUMMARY
  </acceptance_criteria>
  <done>
    - Main branch has all phases 21-22 changes merged
    - Cluster ~/MasterThesisCode is at the same commit as local main
    - Cluster job status has been checked and documented
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify cluster deployment</name>
  <files></files>
  <action>
Present the deployment results to the user for verification. Show:
1. The cluster job status output from Step 1
2. The git log from the cluster showing the deployed commit
3. Whether evaluate jobs were already running (per D-02)
  </action>
  <verify>User confirms deployment is correct</verify>
  <done>User has approved the cluster deployment</done>
  <what-built>Deployed updated codebase to bwUniCluster with phases 21+22 numerical stability fixes (log-space accumulation, physics-floor strategy, overflow fix removal) plus the v1.4 validation report.</what-built>
  <how-to-verify>
    1. Check the task output above -- confirm the cluster `git log` shows the expected latest commit
    2. If evaluate jobs were listed as running: this is expected per D-02 (let them finish, the fix applies to future runs)
    3. If SSH failed: you may need to establish the ControlMaster session first (`ssh bwunicluster` in a terminal)
  </how-to-verify>
  <resume-signal>Type "approved" if the cluster deployment looks correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Local main branch includes all phases 21-22 commits
- Cluster ~/MasterThesisCode is at the same commit as local main
- Cluster job status was checked before deployment
</verification>

<success_criteria>
- The updated codebase is deployed to the cluster before any new evaluate jobs start
- The deployment is verified by matching git commit hashes (local main == cluster HEAD)
</success_criteria>

<output>
After completion, create `.planning/phases/23-deploy-validate/23-02-SUMMARY.md`
</output>
