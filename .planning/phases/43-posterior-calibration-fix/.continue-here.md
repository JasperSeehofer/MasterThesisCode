---
phase: 43-posterior-calibration-fix
task: 0
total_tasks: "TBD (planning not yet complete)"
status: planning_interrupted
last_updated: 2026-04-24T12:41:42Z
---

<current_state>
Phase 43 infrastructure was set up but planning was interrupted before the researcher agent
was spawned. Next action: spawn gpd-phase-researcher, then gpd-planner, then gpd-plan-checker.
Both root-cause hypotheses (H1 and H2) have been confirmed via code trace in this session.
</current_state>

<derivation_state>
**H1 CONFIRMED — D(h) missing from extract_baseline:**
- `evaluation_report.py:extract_baseline()` (lines 221-289): reads `log_posterior` from h_*.json,
  takes argmax with NO `-N × log D(h)` correction.
- `posterior_combination.py:combine_posteriors()` (lines 476+): calls `combine_log_space` on
  per-event likelihoods with no D(h) normalization.
- `BayesianStatistics.evaluate()` DOES have D(h) via `precompute_completion_denominator`
  (bayesian_statistics.py:350-358). So `--evaluate` should give correct MAP.
- VERIFY-02 MAP=0.735 was NOT a real v2.2 run — it compared v2.1 archive posteriors against
  themselves. True v2.2 --evaluate MAP is UNKNOWN.

**H2 CONFIRMED — BallTree center mismatch:**
- `detection.py:Detection.__init__`: sets `self.phi = parameters["phiS"]` and
  `self.theta = parameters["qS"]` from CRB CSV.
- `bayesian_statistics.py:774`: `galaxy_catalog.get_possible_hosts_from_ball_tree(phi=self.detection.phi, theta=self.detection.theta, ...)`
  uses these as the BallTree search CENTER.
- CRBs in `simulations/prepared_cramer_rao_bounds.csv` were generated BEFORE Phase 36 →
  qS/phiS stored in EQUATORIAL frame. v2.2 BallTree is built from ECLIPTIC catalog → MISMATCH.

**KEY UNKNOWN:** Does `--evaluate` with v2.2 code on the current (equatorial) CRBs give MAP≈0.73?
- If YES: H1 alone explains SC-3 failure; fix is to add D(h) to extract_baseline or just use --evaluate as metric.
- If NO: H2 (wrong BallTree center) is also shifting the actual posterior; need to transform CRBs or re-simulate.
</derivation_state>

<parameter_values>
- SNR_THRESHOLD: 20 (v2.2, post-Phase-37)
- Events in prepared_cramer_rao_bounds.csv after SNR≥20 filter: 60
- h-grid for VERIFY-03 sweep: 38 values (h=0.6 to ~0.86 + h=0.73)
- MAP from extract_baseline (VERIFY-03): 0.860 (wrong, missing D(h))
- Expected MAP: 0.73 ± 0.01
</parameter_values>

<intermediate_results>
- Phase 43 added to .gpd/ROADMAP.md (GPD tracking)
- Phase 43 added to .planning/ROADMAP.md (GSD tracking) — progress table + phase definition
- Phase directory created: .planning/phases/43-posterior-calibration-fix/
- project_contract set in .gpd/state.json with FIX-01/02/03 claims
- GSD STATE.md not yet updated to reflect Phase 43 as active
- RESEARCH.md: NOT YET WRITTEN (researcher not spawned)
- PLAN.md: NOT YET WRITTEN (planner not spawned)
</intermediate_results>

<approximations_active>
None new — same as project baseline (Gaussian Fisher errors, Gray et al. 2020 completeness correction).
</approximations_active>

<completed_work>
- Phase 43 infrastructure: DONE (both roadmaps, phase dir, state.json contract)
- Code investigation H1: DONE
- Code investigation H2: DONE
- Research step: NOT STARTED (interrupted)
- Planning step: NOT STARTED
- Plan verification: NOT STARTED
</completed_work>

<remaining_work>
1. Update GSD STATE.md to set active phase = 43
2. Spawn gpd-phase-researcher with context below
3. Spawn gpd-planner with RESEARCH.md output
4. Spawn gpd-plan-checker
5. Commit plan artifacts
</remaining_work>

<decisions_made>
- Phase 43 placed in .planning/phases/ (following GSD convention for v2.2 phases, same as 36-40)
- Phase numbered 43 (Phase 41 skipped, Phase 42 deferred per user Q2/Q3)
- project_contract set with FIX-01 (D(h) audit), FIX-02 (angle audit), FIX-03 (MAP re-verification)
- Forbidden proxy: extract_baseline MAP alone is NOT sufficient (lacks D(h)); must use --evaluate log
</decisions_made>

<open_questions>
- Does --evaluate on v2.2 code + equatorial CRBs give MAP≈0.73 or ≠0.73? (DIAGNOSTIC FIRST STEP)
- If H2 causes --evaluate MAP≠0.73: which fix — (a) transform CRBs equatorial→ecliptic in CSV,
  (b) re-run simulation with Phase 36 code, or (c) are qS/phiS actually unused by the likelihood itself?
- After fix: does VERIFY-04 anisotropy resolve (was it caused by H2), or does Phase 42 still proceed?
- Convention check flagged gauge_choice/regularization missing — irrelevant for this project (N/A for GW inference), safe to ignore.
</open_questions>

<blockers>
None — both hypotheses confirmed, research and planning not yet started.
</blockers>

<context>
Phase 43 is a diagnostic + fix phase for the SC-3 MAP=0.860 failure from Phase 40. The math
is clear: Gray et al. (2020) Eq. A.19 requires the `-N log D(h)` normalization. Without it,
D(h) grows with h (more detectable events at higher h) so the raw log-likelihood increases
monotonically → MAP biased to h_max.

The H2 issue (BallTree center mismatch) is structurally confirmed but its IMPACT on --evaluate
is unknown. It's possible the BallTree still finds nearby galaxies (within the error ellipse)
even with the equatorial center, so the MAP might still be ≈0.73 from --evaluate. Or the wrong
center could cause no hosts to be found, zeroing out likelihoods for many events.

The fastest diagnostic path: run `uv run python -m master_thesis_code simulations/ --evaluate`
and check the log for "MAP" or check simulations/posteriors/ for the h=0.73 posterior peak.
If peak at 0.73: fix is just adding D(h) to extract_baseline (H1 only).
If peak ≠0.73: H2 is also real and CRB frame transform or re-simulation is needed.
</context>

<next_action>
1. Update .planning/STATE.md: set phase=43, status=planning, stopped_at updated.
2. Then run `/gpd:plan-phase 43` in a fresh context window to spawn the researcher with:
   - Phase goal: diagnose MAP=0.860 (H1+H2), fix, re-verify
   - Key context from DERIVATION-STATE.md (both hypotheses confirmed)
   - Research focus: (a) diagnostic --evaluate run, (b) minimal D(h) fix for extract_baseline,
     (c) CRB frame audit (git log when CRBs were last generated), (d) fix strategy if H2 real
   - First task in plan: run `--evaluate` and read MAP from log (not extract_baseline)
</next_action>
