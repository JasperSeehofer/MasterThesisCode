# Deferred Items — Phase 4 (04-new-figures-data-gated)

Out-of-scope discoveries logged during execution (NOT fixed; pre-existing,
unrelated to fig23/fig24).

- `fig09_detection_efficiency` fails during `--generate_figures results/figures_seed200`
  (pre-existing; data-dependent figure unrelated to the forest/PP-plot work).
  Observed: "Failed to generate fig09_detection_efficiency". Out of scope for
  Phase 4 (no fig09 code touched). Investigate separately.
