---
phase: 05-annotation-decluttering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - master_thesis_code/plotting/convergence_plots.py
  - master_thesis_code/plotting/convergence_analysis.py
  - master_thesis_code/plotting/paper_figures.py
  - master_thesis_code/plotting/evaluation_plots.py
  - master_thesis_code/plotting/simulation_plots.py
  - master_thesis_code/plotting/fisher_plots.py
  - master_thesis_code/plotting/single_event_detail.py
  - master_thesis_code/plotting/_labels.py
  - master_thesis_code/plotting/sky_plots.py
  - master_thesis_code/main.py
  - master_thesis_code/bayesian_inference/bayesian_statistics.py
  - master_thesis_code_test/plotting/test_fisher_plots.py
  - master_thesis_code_test/plotting/test_simulation_plots.py
  - master_thesis_code_test/plotting/test_evaluation_plots.py
  - master_thesis_code_test/plotting/test_labels.py
autonomous: true
requirements:
  - VR-ANNO-01
  - VR-ANNO-02
  - VR-ANNO-03
  - VR-ANNO-04
  - VR-ANNO-05
  - VR-ANNO-06
  - VR-ANNO-07

must_haves:
  truths:
    - "A grep for `fig.tight_layout(` in plotting factory bodies + main.py returns zero hits (the corner-internal handling stays via rc_context, which is not a fig.tight_layout call)."
    - "Per-point markers are gone from the smooth convergence / per-event likelihood curves (no `o-`/`s-`/`s--` format strings on continuous lines)."
    - "fisher_quality_diagnostic returns (fig, ax); its caller in bayesian_statistics.py performs the save_figure; the two 3D scatters are replaced by 2D views."
    - "Every axis/legend text in touched factories routes through LABELS; the Mpc/Gpc inconsistency is reconciled with explicit LABELS entries; the corner plot renders filled/gradient HORIZON contours."
    - "Headline/paper figures carry active declarative titles, named reference rules, and a labeled N^-1/2 scaling law; affected figures regenerate against the archive/seed200 dirs with no traceback."
    - "The check gate (ruff + mypy + pytest 'not gpu and not slow') is green and regenerated paper PDFs stay vector + >=7pt."
  artifacts:
    - path: "master_thesis_code/plotting/_labels.py"
      provides: "Single LABELS provider with reconciled d_L (Mpc) + d_L_gpc (Gpc) entries"
      contains: "d_L_gpc"
    - path: "master_thesis_code/plotting/fisher_plots.py"
      provides: "fisher_quality_diagnostic returning (fig, ax); modernized filled-contour corner plot"
      contains: "fill_contours"
    - path: "master_thesis_code/plotting/evaluation_plots.py"
      provides: "2D replacement for plot_sky_localization_3d (Mollweide / 2D marginal)"
    - path: "master_thesis_code/plotting/simulation_plots.py"
      provides: "2D replacement for plot_cramer_rao_coverage (2D marginals)"
  key_links:
    - from: "master_thesis_code/bayesian_inference/bayesian_statistics.py"
      to: "plot_fisher_diagnostics"
      via: "caller saves the returned (fig, ax)"
      pattern: "plot_fisher_diagnostics"
    - from: "master_thesis_code/main.py"
      to: "plot_cramer_rao_coverage / plot_sky_localization_*"
      via: "manifest entry consumes the new 2D return"
      pattern: "cramer_rao_coverage|sky_localization"
---

<objective>
Sweep the HORIZON + Dark Siren Dispatch annotation discipline across the full static
figure set and fix every layout/convention/labeling defect in the plotting package
(VR-ANNO-01..07). The broad, taste-sensitive polish phase: strip the `tight_layout()`
calls that fight `constrained_layout`, drop per-point markers on smooth curves, restore
convention-violating factories to the `(fig, ax)` + `get_figure`-preset contract, replace
the two 3D-scatter anti-patterns with 2D views, unify all axis/legend text through the
single `LABELS` provider (reconciling the Mpc/Gpc inconsistency), modernize the corner
plot to filled/gradient HORIZON contours, and apply the active-title / named-reference-rule
/ scaling-law annotation layer to the headline and paper figures.

Purpose: makes the figure set "look like a communication, not a plot" (proposal §1 #5,
§3.2 HORIZON, §3.3 Dispatch) and removes the package-wide layout/convention debt every
later edit otherwise re-inherits. No physics — no formula/constant/PSD/waveform/
frequency-limit change, purely presentation, so this NEVER routes to GPD `/physics-change`.

Output: 5 atomic commits (one per task), green check gate, regenerated affected PDFs.
TASTE: every active-title wording and every corner color/style choice is a best-effort
HORIZON/Dispatch default — document each in the SUMMARY for the user to confirm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/VIZ_REDESIGN_PROPOSAL.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly; no exploration. -->

From master_thesis_code/plotting/_helpers.py:
```python
def get_figure(nrows=1, ncols=1, figsize=None,
               preset: Literal["single","double"] | None = None, **kwargs) -> tuple[Figure, Any]
def save_figure(fig: Figure, path: str, *, formats=("pdf",), dpi=300, close=True) -> None
def make_heatmap_norm(data, *, mode: Literal["robust","log"]="robust", low=2.0, high=98.0) -> Normalize
def make_colorbar(mappable, fig, ax, label=None, **kwargs) -> Colorbar
def _fig_from_ax(ax: Axes) -> Figure
# REVTeX presets: "single"=(3.375, 2.086), "double"=(7.0, 4.327)
```

From master_thesis_code/plotting/_colors.py (all tokens already exist — Phase F/2 landed):
```python
CMAP = "cividis"           # sequential house cmap (already migrated)
NO_DATA = "#D9D9D9"        # set_bad neutral gray
DIVERGING_CMAP = "RdBu_r"
TRUTH = "#C2451E"          # truth / injected rule ONLY
MEAN = "#D55E00"; EDGE = "#1a1a1a"; REFERENCE = "#4F4F4F"; ACCENT = "#E69F00"
VARIANT_NO_MASS = "#1B2A4A"  # navy, "without M_z" headline
VARIANT_WITH_MASS = "#E8A317"  # gold, "with M_z"
PLANCK = "#3E7CB1"; SH0ES = "#9A6FB0"  # reserved cosmology-band colors
CYCLE: list[str]  # category cycle
```

From master_thesis_code/plotting/_labels.py (the SINGLE provider — VR-ANNO-06 target):
```python
LABELS: dict[str, str] = {
    "M": r"$M_\bullet \, [M_\odot]$", "d_L": r"$d_L \, [\mathrm{Mpc}]$",
    "qS": r"$\theta_S \, [\mathrm{rad}]$", "phiS": r"$\phi_S \, [\mathrm{rad}]$",
    "z": ..., "SNR": r"$\rho$", "H0": ..., "h": r"$h$", "t": ..., ...
}
# label_key(name) maps internal param names -> LABELS keys (lives in fisher_plots/_data).
```

From master_thesis_code/plotting/sky_plots.py (reuse target for the sky 3D->2D):
```python
def plot_sky_localization_mollweide(theta_s, phi_s, snr, *, covariances=None,
                                    n_sigma=1.0, ax: Axes | None = None) -> tuple[Figure, Axes]
```

From master_thesis_code/plotting/fisher_plots.py:
```python
def plot_fisher_diagnostics(... , output_dir: str) -> None   # <- VIOLATOR: returns None, saves internally
def plot_fisher_corner(covariance, param_values, params=None, *, overlay_events=None,
                       n_samples=5000, seed=42, ax: None = None) -> tuple[Figure, ndarray]
# corner 2.2.3 is imported and available. arviz/chainconsumer are NOT — do NOT add them.
```
</interfaces>

<!-- SURVEYED CALL SITES (line numbers verified this session) -->
<!-- tight_layout to REMOVE (VR-ANNO-03): -->
<!--   convergence_plots.py:280   convergence_analysis.py:955 -->
<!--   paper_figures.py:381 (h_pad=0.3)  592  699 (w_pad=1.0)  917 -->
<!--   evaluation_plots.py:408   fisher_plots.py:153  621 -->
<!--   single_event_detail.py:378 (rect=...)   main.py:1315 -->
<!--   KEEP: fisher_plots.py:428 is a COMMENT; corner uses tight_layout INTERNALLY, handled via rc_context (line 430) — VR-ANNO-07. -->
<!-- per-point markers to DROP on smooth curves (VR-ANNO-02): -->
<!--   convergence_plots.py:190 "o-" 237 "s--"   convergence_analysis.py:706 "o-" 717 "s--" 833 "o-" 853 "s-" -->
<!--   paper_figures.py:350 "o-" 356 "s-"   (model_plots/catalog_plots markers are DISCRETE scatter — KEEP) -->
<!-- convention violators (VR-ANNO-04): -->
<!--   fisher_plots.plot_fisher_diagnostics (returns None + internal save) -->
<!--   plt.figure(figsize=...): evaluation_plots:105 (3D) 215   simulation_plots:234 (3D)   paper_figures:337 -->
<!-- 3D scatters (VR-ANNO-05): evaluation_plots:105 plot_sky_localization_3d ; simulation_plots:234 plot_cramer_rao_coverage -->
<!-- Mpc/Gpc (VR-ANNO-06): LABELS["d_L"]=Mpc ; literal Gpc labels at fisher_plots:616 paper_figures:686 evaluation_plots:531 selection_plots:168 catalog_plots:105/148 -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip layout-fighting tight_layout + drop per-point markers on smooth curves (VR-ANNO-02, VR-ANNO-03)</name>
  <files>master_thesis_code/plotting/convergence_plots.py, master_thesis_code/plotting/convergence_analysis.py, master_thesis_code/plotting/paper_figures.py, master_thesis_code/plotting/evaluation_plots.py, master_thesis_code/plotting/fisher_plots.py, master_thesis_code/plotting/single_event_detail.py, master_thesis_code/main.py</files>
  <action>
  VR-ANNO-03 — remove every `fig.tight_layout(...)` call that fights the stylesheet's
  `constrained_layout`, at these EXACT surveyed sites:
    - convergence_plots.py:280  `fig.tight_layout()`
    - convergence_analysis.py:955  `fig.tight_layout()`
    - paper_figures.py:381  `fig.tight_layout(h_pad=0.3)`  (small-multiples grid; the
      `fig.align_ylabels(axes[:, 0])` line above it STAYS)
    - paper_figures.py:592  `fig.tight_layout()`
    - paper_figures.py:699  `fig.tight_layout(w_pad=1.0)`
    - paper_figures.py:917  `fig.tight_layout()`
    - evaluation_plots.py:408  `fig.tight_layout()`
    - fisher_plots.py:153  `fig.tight_layout()` (plot_fisher_ellipses)
    - fisher_plots.py:621  `fig.tight_layout()` (inside plot_fisher_diagnostics — Task 2 rewrites
      this body; removing here is safe and idempotent, Task 2 re-touches the same function)
    - single_event_detail.py:378  `fig.tight_layout(rect=(0.0, 0.0, 1.0, 0.96))` — the rect
      reserved headroom for `fig.suptitle`; constrained_layout packs the suptitle automatically,
      so delete the call. If regen shows the suptitle overlapping the top row, use
      `fig.set_constrained_layout_pads(h_pad=...)` — NEVER reintroduce tight_layout.
    - main.py:1315  `fig.tight_layout()` (fig16 inline composite in _gen_catalog_completeness)
  DO NOT touch fisher_plots.py:428 — that is a COMMENT noting `corner.corner` calls tight_layout
  INTERNALLY; the existing `matplotlib.rc_context({"figure.constrained_layout.use": False})`
  wrapper (line 430) is correct handling and belongs to VR-ANNO-07 (Task 4). The grep gate
  matches only the real call token `fig.tight_layout(`, which the comment does not contain.
  For any site that passed h_pad/w_pad/rect: after removal, regenerate and confirm spacing is
  acceptable. If a panel genuinely needs manual spacing use `fig.set_constrained_layout_pads(...)`
  (NOT tight_layout); document any such case in the SUMMARY.

  VR-ANNO-02 — drop per-point markers on smooth / near-delta CONTINUOUS curves, converting the
  format strings to plain lines (preserve color/linestyle/linewidth/label; remove the now-unused
  `markersize=` kwarg on those calls):
    - convergence_plots.py:190 `"o-"` -> `"-"`;  237 `"s--"` -> `"--"`
    - convergence_analysis.py:706 `"o-"` -> `"-"`;  717 `"s--"` -> `"--"`;  833 `"o-"` -> `"-"`;
      853 `"s-"` -> `"-"`  (drop the four `markersize=4` kwargs)
    - paper_figures.py:350 `"o-"` -> `"-"`;  356 `"s-"` -> `"-"`  (drop both `markersize=2.5`)
  PRESERVE the variant LINESTYLE law: navy solid (`"-"`) vs gold dashed (`"--"`) — when you strip
  the marker from `"s--"` keep the dashed `"--"`; from `"s-"`/`"o-"` keep solid `"-"`.
  DISTINGUISH and KEEP (discrete scatter, markers are correct): model_plots.py:100/110/121,
  catalog_plots.py:184/193/209, and any single-MAP diamond marker.
  </action>
  <verify>
  <automated>cd /home/jasper/Repositories/MasterThesisCode && ! grep -rn "fig\.tight_layout(" master_thesis_code/plotting/ master_thesis_code/main.py && uv run ruff check master_thesis_code/plotting/ master_thesis_code/main.py && uv run pytest -m "not gpu and not slow" master_thesis_code_test/plotting/test_convergence_plots.py master_thesis_code_test/plotting/test_paper_figures.py master_thesis_code_test/plotting/test_evaluation_plots.py master_thesis_code_test/plotting/test_fisher_plots.py -q</automated>
  </verify>
  <done>Zero `fig.tight_layout(` calls remain in plotting/ + main.py; the marker format strings on the listed smooth curves are plain lines with the dashed/solid variant law preserved; the four affected test modules pass; ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore convention-violating factories to (fig,ax)+presets and replace the two 3D scatters with 2D views (VR-ANNO-04, VR-ANNO-05)</name>
  <files>master_thesis_code/plotting/fisher_plots.py, master_thesis_code/plotting/evaluation_plots.py, master_thesis_code/plotting/simulation_plots.py, master_thesis_code/plotting/paper_figures.py, master_thesis_code/main.py, master_thesis_code/bayesian_inference/bayesian_statistics.py, master_thesis_code_test/plotting/test_fisher_plots.py, master_thesis_code_test/plotting/test_simulation_plots.py, master_thesis_code_test/plotting/test_evaluation_plots.py</files>
  <behavior>
    - plot_fisher_diagnostics(...) returns `tuple[Figure, npt.NDArray[np.object_]]` (the (fig, axes)
      it already builds via get_figure preset="double"); it NO LONGER calls save_figure internally.
    - bayesian_statistics.py caller receives (fig, axes) and itself calls
      save_figure(fig, os.path.join("simulations", "fisher_quality_diagnostic")).
    - plot_sky_localization_3d is replaced by a 2D view (NOT projection="3d"): a (fig, ax) factory
      using get_figure / sky_plots.plot_sky_localization_mollweide (color = sky_error via cividis +
      make_heatmap_norm); no plt.figure(figsize=...) direct call.
    - plot_cramer_rao_coverage is replaced by a 2D view (NOT scatter3D / projection="3d"): a 2x2 /
      pair-panel 2D-marginal layout of (M, qS, phiS) via get_figure preset="double"; no
      plt.figure(figsize=(16,9)).
    - paper_figures.py:337 `figsize=(3.375, 6.0)` and evaluation_plots.py:215 `figsize=(7.0, 2.8*nrows)`
      route through get_figure (keep computed figsize where row-count is dynamic — pass it to
      get_figure's figsize= rather than plt.figure; prefer preset only where the size matches a preset).
    - Smoke tests assert each touched factory returns (Figure, axes) and the 2D replacements have
      NO 3D axes (e.g. `ax.name != "3d"` / not isinstance of Axes3D).
  </behavior>
  <action>
  VR-ANNO-04 — restore the factory contract:
    1. fisher_plots.plot_fisher_diagnostics: change signature to return
       `tuple[Figure, npt.NDArray[np.object_]]`; remove the internal
       `save_figure(fig, os.path.join(output_dir, "fisher_quality_diagnostic"))` (line 622) and the
       `fig.tight_layout()` (line 621, already removed in Task 1 — confirm gone); `return fig, axes_arr`.
       Update the docstring (drop "Saved as ... in *output_dir*"). The `output_dir` param can stay for
       back-compat but is now unused for saving — KEEP it (caller still passes it) OR drop it and
       update the caller; CHOOSE: keep `output_dir` so the caller diff is minimal, mark it
       `# retained for call-site compatibility; saving is now the caller's responsibility`.
    2. bayesian_statistics.py:669-682 caller: capture `fig, _axes = plot_fisher_diagnostics(...)` then
       `from master_thesis_code.plotting._helpers import save_figure; save_figure(fig, os.path.join("simulations", "fisher_quality_diagnostic"))`.
    3. Direct `plt.figure(figsize=...)` -> get_figure:
       - evaluation_plots.py:215 (plot_injected_vs_recovered, dynamic nrows): replace
         `fig = plt.figure(figsize=(7.0, 2.8 * nrows))` with
         `fig, _ = get_figure(figsize=(7.0, 2.8 * nrows))` then keep the existing GridSpec on `fig`
         (GridSpec(fig=fig, ...) or fig.add_gridspec); do NOT change the panel layout, only the
         figure-creation primitive.
       - paper_figures.py:337: this is already `get_figure(nrows=4, ncols=2, figsize=(3.375, 6.0), sharex=True)`
         (NOT a plt.figure) — leave as-is (acceptable per the task note "pass figsize into get_figure
         is acceptable"); no change needed beyond confirming.
       - convergence_analysis.py:693 + single_event_detail.py:233 already pass figsize into get_figure
         (acceptable) — leave as-is.
    DO NOT change DIVERGING/sequential cmaps, norms, or any computed value.

  VR-ANNO-05 — replace the TWO 3D scatters with 2D views:
    a) evaluation_plots.plot_sky_localization_3d (lines 99-123): rewrite to a 2D Mollweide. Convert
       (theta, phi) -> the same lat/lon transform sky_plots uses, color by `sky_error` (cividis +
       make_heatmap_norm robust), via get_figure(preset="double", subplot_kw={"projection":"mollweide"})
       — OR simply delegate: build snr-like color array and call
       sky_plots.plot_sky_localization_mollweide(theta, phi, sky_error) (note: that factory colors by
       its 3rd arg and labels it SNR; for sky_error you want your own colorbar label "Sky-loc error",
       so prefer the direct Mollweide rewrite with `make_colorbar(sc, fig, ax, label="Sky localization error [sr]")`).
       Keep the function NAME and (Figure, Any) return so main.py:fig05 wiring is unaffected (fig05
       actually uses plot_sky_localization_mollweide already; this function is the standalone 3D one).
       Return (fig, ax) with a 2D Mollweide ax.
    b) simulation_plots.plot_cramer_rao_coverage (lines 223-243): rewrite from scatter3D to 2D
       marginals — a 1x3 (or 2x2) panel grid via get_figure(nrows=1, ncols=3, preset="double") showing
       the three pairwise projections M-qS, M-phiS, qS-phiS (ax.scatter, cividis-neutral or EDGE points,
       small s, alpha). Set axis labels via LABELS ("M","qS","phiS") and apply the *_limits to the
       matching axes. Keep the signature (M, qS, phiS, M_limits, qS_limits, phiS_limits, *, ax=None) and
       return (Figure, axes-array). main.py:1213 already passes the limits — keep them honored.
    Update main.py fig14 wiring (line 1213) only if the return shape changed (axes array vs single ax):
       the manifest just returns `(fig, axes)`; the figure registry handles either — confirm it renders.

  VR-ANNO-04/05 tests: extend test_fisher_plots.py (assert plot_fisher_diagnostics returns
  (Figure, ndarray) and does NOT write a file), test_simulation_plots.py:62 (assert the new 2D return
  + no 3D axes), test_evaluation_plots.py:168 (assert plot_sky_localization_3d ax is 2D Mollweide, not
  Axes3D). Use `from mpl_toolkits.mplot3d import Axes3D; assert not isinstance(ax, Axes3D)` for the
  no-3D assertion. Complete type annotations on every new/changed signature.
  </action>
  <verify>
  <automated>cd /home/jasper/Repositories/MasterThesisCode && ! grep -rn "projection=\"3d\"\|projection='3d'\|scatter3D\|plt\.figure(figsize" master_thesis_code/plotting/evaluation_plots.py master_thesis_code/plotting/simulation_plots.py && uv run ruff check master_thesis_code/plotting/ master_thesis_code/bayesian_inference/bayesian_statistics.py && uv run mypy master_thesis_code/plotting/fisher_plots.py master_thesis_code/plotting/evaluation_plots.py master_thesis_code/plotting/simulation_plots.py && uv run pytest -m "not gpu and not slow" master_thesis_code_test/plotting/test_fisher_plots.py master_thesis_code_test/plotting/test_simulation_plots.py master_thesis_code_test/plotting/test_evaluation_plots.py -q</automated>
  </verify>
  <done>plot_fisher_diagnostics returns (fig, axes) with no internal save; the caller saves; both 3D scatters are 2D (grep finds no projection="3d"/scatter3D in those two files); no stray plt.figure(figsize=...) in the two replaced factories; tests assert the new contracts + no-3D; ruff + mypy clean on the three files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route all axis/legend text through LABELS + reconcile the Mpc/Gpc inconsistency (VR-ANNO-06)</name>
  <files>master_thesis_code/plotting/_labels.py, master_thesis_code/plotting/evaluation_plots.py, master_thesis_code/plotting/fisher_plots.py, master_thesis_code/plotting/paper_figures.py, master_thesis_code/plotting/selection_plots.py, master_thesis_code/plotting/catalog_plots.py, master_thesis_code/plotting/simulation_plots.py, master_thesis_code_test/plotting/test_labels.py</files>
  <behavior>
    - LABELS gains an explicit Gpc entry `"d_L_gpc": r"$d_L \, [\mathrm{Gpc}]$"` alongside the existing
      Mpc `"d_L"`; both are correct and unit-explicit.
    - Every literal `r"$d_L\,[\mathrm{Gpc}]$"` / `r"$d_L$ [Gpc]"` / `"d_L [Gpc]"` axis label in the
      surveyed sites is replaced by `LABELS["d_L_gpc"]`; every `"theta"`/`"phi"`/`"M"`/`"qS"`/`"phiS"`
      raw-string axis label in touched factories routes through LABELS.
    - A unit-consistency test asserts: LABELS["d_L"] contains "Mpc", LABELS["d_L_gpc"] contains "Gpc",
      and a grep-style check that the listed factory files no longer contain a hardcoded `[Gpc]` /
      `[\mathrm{Gpc}]` axis-label literal (comments documenting the unit may remain).
  </behavior>
  <action>
  VR-ANNO-06 reconciliation. The injection-CSV `luminosity_distance` is in Gpc (fig20/selection/fisher
  scatter/paper closure) while per-source recovery plots are in Mpc — BOTH are physically correct for
  their data; the defect is the inconsistent UNTYPED labeling. Resolve by giving LABELS a second
  unit-explicit key (NOT by mutating the data):
    1. _labels.py: add `"d_L_gpc": r"$d_L \, [\mathrm{Gpc}]$"` to LABELS (keep `"d_L"` = Mpc). Update the
       module docstring's note to mention both d_L (Mpc, per-source CRB recovery) and d_L_gpc (Gpc,
       injection-CSV pooled values).
    2. Replace the literal Gpc axis labels with `LABELS["d_L_gpc"]` at the SURVEYED sites:
       - fisher_plots.py:616  `ax_scatter.set_xlabel(r"$d_L$ [Gpc]")` -> `LABELS["d_L_gpc"]`
       - paper_figures.py:686  `ax_scat.set_xlabel(r"$d_L$ [Gpc]")` -> `LABELS["d_L_gpc"]`
       - evaluation_plots.py:531  `ax.set_xlabel(r"$d_L\,[\mathrm{Gpc}]$")` -> `LABELS["d_L_gpc"]`
         (keep the explanatory comment block at 526-530 — it documents WHY this data is Gpc)
       - selection_plots.py:168  `ax_left.set_xlabel(r"$d_L\,[\mathrm{Gpc}]$")` -> `LABELS["d_L_gpc"]`
       - catalog_plots.py:105 and 148  `r"$d_L\,[\mathrm{Gpc}]$"` -> `LABELS["d_L_gpc"]`
    3. Route remaining raw-string axis labels in touched factories through LABELS:
       - simulation_plots.plot_cramer_rao_coverage (rewritten in Task 2): use LABELS["M"]/["qS"]/["phiS"]
         (already specified in Task 2 — confirm, do not duplicate).
       - evaluation_plots.plot_sky_localization_3d (rewritten in Task 2): the Mollweide needs no x/y
         param labels; keep the colorbar label as a plain descriptive string (no LABELS key exists for
         sky-loc error — that is acceptable; LABELS is for axis PARAM text).
       - fisher_plots.py:616/617 y-label `r"$M$ [$M_\odot$]"` -> `LABELS["M"]`.
    DO NOT touch forest_plot.py / interactive.py "km/s/Mpc" strings (those are H0-axis correct and
    out of this scope) and DO NOT change any data values or unit conversions.
  Add the unit-consistency test to a new test_labels.py: assert "Mpc" in LABELS["d_L"], "Gpc" in
  LABELS["d_L_gpc"], and that the six listed source files contain no hardcoded `[Gpc]` axis-label
  literal (read each file, assert `r"[\mathrm{Gpc}]"` and `"[Gpc]"` not in any `set_xlabel`/`set_ylabel`
  line — a simple substring scan over the file text is sufficient).
  </action>
  <verify>
  <automated>cd /home/jasper/Repositories/MasterThesisCode && uv run python -c "from master_thesis_code.plotting._labels import LABELS; assert 'Mpc' in LABELS['d_L']; assert 'Gpc' in LABELS['d_L_gpc']; print('labels ok')" && ! grep -rn "set_xlabel(r\"\$d_L\$ \[Gpc\]\|set_xlabel(r\"\$d_L\\\\,\[\\\\mathrm{Gpc}\]\$\"" master_thesis_code/plotting/fisher_plots.py master_thesis_code/plotting/paper_figures.py master_thesis_code/plotting/evaluation_plots.py master_thesis_code/plotting/selection_plots.py master_thesis_code/plotting/catalog_plots.py && uv run ruff check master_thesis_code/plotting/ && uv run pytest -m "not gpu and not slow" master_thesis_code_test/plotting/test_labels.py master_thesis_code_test/plotting/test_selection_plots.py master_thesis_code_test/plotting/test_catalog_plots.py master_thesis_code_test/plotting/test_paper_figures.py -q</automated>
  </verify>
  <done>LABELS has both d_L (Mpc) and d_L_gpc (Gpc); the six surveyed Gpc literal axis labels route through LABELS["d_L_gpc"]; the Mpc/Gpc test passes; ruff clean; affected factory tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Modernize the corner plot with filled/gradient HORIZON contours (VR-ANNO-07)</name>
  <files>master_thesis_code/plotting/fisher_plots.py, master_thesis_code_test/plotting/test_fisher_plots.py</files>
  <action>
  VR-ANNO-07 — modernize plot_fisher_corner (fisher_plots.py:372-458) using `corner` 2.2.3's OWN
  options ONLY. corner 2.2.3 is available; arviz / chainconsumer are NOT — do NOT add them, do NOT
  add any dependency. Replace the dated thin-orange / green-crosshair look with filled/gradient
  HORIZON contours by passing these kwargs to the existing `corner.corner(...)` call (line 431):
    - `fill_contours=True`  (filled credible regions, the ChainConsumer/arviz look)
    - `plot_datapoints=False`  (drop the scatter speckle)
    - `plot_density=False`  (the filled contours carry the density; avoids the muddy density image —
      judge: keep True only if the fill alone looks too sparse; document the choice in SUMMARY)
    - `smooth=1.0`  (gaussian-smoothed 2D histograms -> smooth contours)
    - `smooth1d=1.0`  (smooth the 1D marginals)
    - `levels=(0.393, 0.865, 0.989)`  (the 1/2/3-sigma 2D enclosed-mass levels — corner's convention)
    - `color=VARIANT_NO_MASS`  (HORIZON navy as the primary series color, replacing CYCLE[0] orange)
    - `contourf_kwargs={"colors": None, "cmap": "cividis"}` OR a graded-alpha navy fill — PREFER
      `contour_kwargs={"colors": EDGE, "linewidths": 0.8}` + `contourf_kwargs={"alpha": 0.7}` so the
      fill is navy-gradient with a near-black edge (CB + grayscale safe via the edge line). Judge the
      exact contourf recipe by rendering; document the final recipe in the SUMMARY.
    - keep `truths=list(sub_mean)`, `truth_color=TRUTH` (vermillion truth rule — the HORIZON law)
    - keep `quantiles=[0.16, 0.5, 0.84]`, `show_titles=True`, `title_fmt=".3f"`
    - `hist_kwargs={"color": VARIANT_NO_MASS, "edgecolor": EDGE}`  (navy 1D marginals)
  For the overlay_events loop (line 443-455): switch overlay color from CYCLE[(ev_idx+1)%len] to a
  distinguishable HORIZON sequence (VARIANT_WITH_MASS gold for the first overlay, then CYCLE), keep
  fill_contours + plot_datapoints=False consistent so overlays read as a second filled family.
  KEEP the `matplotlib.rc_context({"figure.constrained_layout.use": False})` wrapper (line 430) — this
  is the documented, ACCEPTABLE handling for corner making its own figure with internal tight_layout
  (VR-ANNO-07 explicitly allows corner's own fig; document in SUMMARY that this is intentional and is
  NOT a stray fig.tight_layout call). DO NOT change the sampling, seed, params, or covariance math —
  presentation only, no computed-value change.
  Extend test_fisher_plots.py: the existing corner smoke tests (116-150) must still pass; add an
  assertion that the call does not raise with the new kwargs and still returns (Figure, ndarray) of
  shape (n, n). A visual/style assertion is not feasible in a unit test — rely on the smoke test +
  the SUMMARY render-verify.
  </action>
  <verify>
  <automated>cd /home/jasper/Repositories/MasterThesisCode && grep -n "fill_contours" master_thesis_code/plotting/fisher_plots.py && uv run ruff check master_thesis_code/plotting/fisher_plots.py && uv run mypy master_thesis_code/plotting/fisher_plots.py && uv run pytest -m "not gpu and not slow" master_thesis_code_test/plotting/test_fisher_plots.py -q</automated>
  </verify>
  <done>plot_fisher_corner passes fill_contours/smooth/HORIZON-color/plot_datapoints=False to corner.corner; the rc_context corner-internal-layout wrapper is retained and documented; corner smoke tests pass; ruff + mypy clean; no new dependency added.</done>
</task>

<task type="auto">
  <name>Task 5: Apply the annotation layer (active titles, named reference rules, scaling-law labels) to headline/paper figures (VR-ANNO-01)</name>
  <files>master_thesis_code/plotting/paper_figures.py, master_thesis_code/plotting/convergence_analysis.py, master_thesis_code/plotting/convergence_plots.py, master_thesis_code/main.py</files>
  <action>
  VR-ANNO-01 — install the annotation layer JUDICIOUSLY on the headline + paper figures (NOT every
  figure; prioritize the ones a reader/referee looks at first). The four-part layer (proposal §1 #5,
  §3.2/§3.3): (i) active declarative titles that STATE the finding; (ii) named reference rules
  (Planck/SH0ES/truth labeled AT the line, not only in a legend); (iii) scaling-law labels on the
  curve; (iv) direct end-of-line color-matched labels replacing legend boxes WHERE it reads better.
  Apply, with best-effort HORIZON/Dispatch wording (document EVERY string in the SUMMARY for the
  user to confirm):

    A. Scaling-law label (the N^-1/2 convergence law): convergence_analysis.py:733 already labels the
       reference line `r"$\propto N^{-1/2}$"` in the LEGEND. Upgrade to a DIRECT on-curve label:
       `ax_w.annotate(r"$\propto N^{-1/2}$", xy=(sizes[-1], ref_line[-1]), xytext=(4, 0),
       textcoords="offset points", color=REFERENCE, fontsize=7, va="center")` and remove that entry
       from the legend (`label="_nolegend_"` or drop the legend label) so the law reads at the line.
       Do the SAME for convergence_plots.py:265 `r"$1/\sqrt{N}$ ref"` -> on-curve annotate (unify the
       wording to `$\propto N^{-1/2}$` across both for consistency; note the wording choice in SUMMARY).

    B. Named reference rules on H0/posterior paper figures: wherever a Planck/SH0ES/truth axvline or
       axvspan is drawn in paper_figures (the closure-test / posterior-comparison panels) and the
       convergence figures, add a small text label at the rule using the reserved colors
       (PLANCK / SH0ES / TRUTH) e.g. `ax.text(x_planck, ax.get_ylim()[1], "Planck", color=PLANCK,
       fontsize=7, ha="center", va="bottom", rotation=0)`. Survey paper_figures for existing
       `axvline(... TRUTH ...)` / `axvspan` calls and label them; if a figure has NO reference rules
       (pure diagnostic), SKIP it. Do not invent new reference lines — only LABEL existing ones.

    C. Active declarative titles on the headline + paper figures: replace generic titles with a
       finding-stating sentence. Survey set_title / suptitle in paper_figures.py and the fig01/fig08
       generators in main.py. Best-effort defaults (CONFIRM in SUMMARY), e.g.:
         - posterior comparison: "Adding the redshifted BH mass tightens the $H_0$ posterior"
         - closure test: "Closure test: the pipeline recovers each injected $H_0$" (already exists at
           paper_figures.py:707 — keep/refine)
         - convergence: "$H_0$ uncertainty shrinks as $N^{-1/2}$ with the event count"
       Only set a title where it stating-the-finding adds value; leave neutral diagnostic panels alone.
       Keep titles within REVTeX width (fontsize "small"/"medium"; no overflow).

    D. Direct end-of-line labels WHERE it reads better: on the two-variant convergence/HDI-width
       panels, the "Without $M_z$" / "With $M_z$" series can be labeled at the right end of each curve
       (color-matched via VARIANT_NO_MASS/VARIANT_WITH_MASS) and the legend dropped — apply ONLY where
       there are <=3 series and the right margin has room; otherwise keep the legend. Document each
       per-figure choice in the SUMMARY.

  Keep all changes presentation-only (text/annotation/legend), preserve the (fig, ax) contract, the
  variant color+linestyle law, and REVTeX vector output. Complete type annotations on any touched line.
  TASTE: this task is the most subjective — the SUMMARY MUST list every active-title string, every
  named-rule label, and every legend->direct-label conversion as a best-effort default for the user.
  </action>
  <verify>
  <automated>cd /home/jasper/Repositories/MasterThesisCode && uv run ruff check master_thesis_code/plotting/ master_thesis_code/main.py && uv run mypy master_thesis_code/plotting/paper_figures.py master_thesis_code/plotting/convergence_analysis.py master_thesis_code/plotting/convergence_plots.py && uv run pytest -m "not gpu and not slow" master_thesis_code_test/plotting/test_paper_figures.py master_thesis_code_test/plotting/test_convergence_plots.py -q</automated>
  </verify>
  <done>The N^-1/2 law is annotated on-curve in both convergence factories; existing reference rules carry named labels; headline/paper figures have finding-stating titles where they add value; direct end-of-line labels applied where ≤3 series + margin allows; every taste choice is enumerated in the SUMMARY; ruff + mypy clean; affected tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This is a presentation-only refactor of plotting factory bodies. No new untrusted input crosses any boundary; data flows from already-validated canonical loaders / CSVs into matplotlib. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | label/annotation text rendering a wrong physical unit (the Mpc/Gpc fix could mislabel an axis) | mitigate | LABELS gains explicit d_L (Mpc) AND d_L_gpc (Gpc) keys; each Gpc site is mapped to d_L_gpc by surveyed line number; a unit-consistency test asserts "Mpc"/"Gpc" membership and no residual hardcoded `[Gpc]` axis literal. No data value is converted. |
| T-05-02 | Denial of Service | corner.corner new kwargs raising on edge covariances (singular / overlay events) | accept | corner 2.2.3 is the existing, exercised path; smoke tests cover default + custom-params + multi-event; the rc_context wrapper is unchanged. Low risk, no external trigger. |
| T-05-03 | Information disclosure | (n/a) | accept | No secrets, no PII; figures consume public simulation outputs only. |
</threat_model>

<verification>
- grep gate: `! grep -rn "fig\.tight_layout(" master_thesis_code/plotting/ master_thesis_code/main.py` returns clean (corner-internal handling is via rc_context, not this token).
- grep gate: no `projection="3d"` / `scatter3D` in evaluation_plots.py or simulation_plots.py.
- LABELS unit check: `d_L`→Mpc, `d_L_gpc`→Gpc; no hardcoded `[Gpc]` axis-label literal in the six surveyed factory files.
- Full check gate: `uv run ruff check master_thesis_code/ master_thesis_code_test/` + `uv run mypy master_thesis_code/ master_thesis_code_test/` + `uv run pytest -m "not gpu and not slow"` all green.
- Render-verify (no traceback) the affected figures against the canonical data dirs:
  - posteriors / paper figures: `uv run python -m master_thesis_code simulations/_archive_v2_1_baseline --generate_figures` (or the project's figure-generation entrypoint) — confirm fig01/fig08/paper posterior + closure + convergence + corner + fig14 (CRB coverage) + fig05/sky + fig12 (uncertainty) PDFs render.
  - others: same against `results/figures_seed200/`.
  - Confirm regenerated paper PDFs stay VECTOR (open one PDF, confirm text is selectable / not rasterized) and font ≥7pt.
- Visual taste read (HORIZON/Dispatch): corner now filled-contour navy + vermillion truth; convergence law reads at the curve; no marker speckle on smooth curves; 3D figures are now 2D. Capture each taste choice in the SUMMARY.
</verification>

<success_criteria>
1. Annotation layer applied to the headline/paper figures (active titles, named reference rules,
   on-curve N^-1/2 scaling-law label, direct end-of-line labels where it reads better) — affected
   figures regenerate with no traceback. [VR-ANNO-01]
2. Zero `fig.tight_layout()` calls fighting constrained_layout (grep gate clean); per-point markers
   dropped on smooth/near-delta curves. [VR-ANNO-02, VR-ANNO-03]
3. Convention-violating factories return (fig, ax) + use get_figure presets; fisher_quality_diagnostic
   saved by its caller; the two 3D scatters replaced by 2D marginal/Mollweide views. [VR-ANNO-04, VR-ANNO-05]
4. All touched axis/legend text routes through LABELS; Mpc/Gpc reconciled via explicit d_L + d_L_gpc;
   corner plot modernized with filled/gradient HORIZON contours (no new dependency). [VR-ANNO-06, VR-ANNO-07]
5. Check gate green; regenerated paper PDFs stay vector + ≥7pt.
6. Every active-title wording + corner color/style choice + legend→direct-label conversion is
   documented in the SUMMARY as a best-effort HORIZON/Dispatch default for the user to confirm.
</success_criteria>

<git_discipline>
- Stage with explicit `git add <paths>` per task — NEVER `git add -A`/`git add .`.
- Do NOT stage `.planning/debug/*` (another session's working files) or any `results/`/`simulations/`
  output. Stage only the source/test files in this plan's files_modified plus the SUMMARY/ROADMAP.
- One atomic commit per task (5 commits): subject `viz(05): <task summary>` (GSD presentation work,
  NOT a physics change — no `[PHYSICS]` prefix). End commit messages with the Co-Authored-By trailer.
- Run the `/check` gate (ruff + mypy + pytest "not gpu and not slow") before EACH commit.
</git_discipline>

<output>
After completion, create `.planning/phases/05-annotation-decluttering/05-01-SUMMARY.md`.
The SUMMARY MUST include a dedicated "Taste choices for user confirmation" section enumerating:
- Every active-title string set (figure → wording).
- Every named-reference-rule label and its placement.
- Every legend→direct-end-of-line-label conversion (which figures, which series).
- The final corner-plot recipe (fill_contours / plot_density / smooth / level set / contourf vs
  contour colors / overlay color sequence) and a note that corner's own figure + internal layout
  (via rc_context) is intentional and accepted.
- The N^-1/2 scaling-law wording chosen and that it was unified across both convergence factories.
Also record: the exact tight_layout sites removed, the two 3D→2D replacements chosen, any panel that
needed `set_constrained_layout_pads`, and confirmation the paper PDFs stayed vector + ≥7pt.
</output>
