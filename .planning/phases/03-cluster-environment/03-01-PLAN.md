---
phase: 03-cluster-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cluster/modules.sh
  - cluster/setup.sh
autonomous: false
requirements:
  - ENV-01
  - ENV-02
  - ENV-03

must_haves:
  truths:
    - "source cluster/modules.sh loads 4 modules (compiler/gnu, devel/cuda, numlib/gsl, devel/python) and exports $WORKSPACE"
    - "cluster/setup.sh installs uv if missing, allocates workspace if missing, runs uv sync --extra gpu, and verifies imports"
    - "All output paths use $WORKSPACE (resolved via ws_find), never $HOME"
  artifacts:
    - path: "cluster/modules.sh"
      provides: "Module loading and workspace export"
      contains: "_load_or_die"
    - path: "cluster/setup.sh"
      provides: "First-time cluster setup"
      contains: "uv sync --extra gpu"
  key_links:
    - from: "cluster/setup.sh"
      to: "cluster/modules.sh"
      via: "source"
      pattern: 'source.*modules\.sh'
    - from: "cluster/modules.sh"
      to: "ws_find emri"
      via: "workspace resolution"
      pattern: 'ws_find emri'
---

<objective>
Create and verify a repeatable environment setup for bwUniCluster 3.0.

Purpose: Enable a fresh cluster account to go from zero to a working .venv with all GPU dependencies installed via two shell scripts.
Output: cluster/modules.sh (sourceable module loader), cluster/setup.sh (first-time setup)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cluster-environment/03-CONTEXT.md
@.planning/phases/03-cluster-environment/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cluster/modules.sh (ENV-01, ENV-03)</name>
  <files>cluster/modules.sh</files>
  <read_first>
    - .planning/phases/03-cluster-environment/03-CONTEXT.md (locked decisions D-01 through D-06)
    - .planning/phases/03-cluster-environment/03-RESEARCH.md (Pattern 1: Sourceable Module Script example)
    - .editorconfig (indentation: 4-space, LF, UTF-8, trim trailing whitespace)
  </read_first>
  <action>
Create directory `cluster/` and file `cluster/modules.sh`. The script is designed to be `source`d (not executed directly) by setup.sh and all future Phase 4 SLURM job scripts.

Per D-01, the module load sequence is exactly:
```
compiler/gnu/14.2
devel/cuda/12.8
numlib/gsl
devel/python/3.13.3-gnu-14.2
```

Per D-02, implement a `_load_or_die` helper function that:
- Attempts `module load "$1"`
- On failure: prints `ERROR: Failed to load module '$1'` to stderr, prints hint `Run 'module spider ${mod%%/*}/' to see available versions.` to stderr, returns 1
- On success: prints `  Loaded: $mod`

Per D-05, resolve workspace path:
- Run `ws_find emri 2>/dev/null || true` and capture result
- If empty: print `WARNING: Workspace 'emri' not found. Run cluster/setup.sh first.` to stderr (do NOT fail -- modules.sh must still work for setup.sh which creates the workspace)
- If non-empty: `export WORKSPACE`

Export convenience variables (Claude's discretion):
- `PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"`
- `VENV_PATH="$PROJECT_ROOT/.venv"`
- `PATH="$HOME/.local/bin:$PATH"` (so uv is findable)

Use `set -euo pipefail` at the top. Add a shebang `#!/usr/bin/env bash`. Add header comment: `# cluster/modules.sh -- Source this file; do not execute directly.`

Add inline comments:
- Before the module list: `# Module names verified against bwUniCluster 3.0 wiki (2026-03).`
- After the module list: `# If a module fails to load, verify with: module spider <category>/`

Mark the file executable: `chmod +x cluster/modules.sh`

Use 4-space indentation per .editorconfig. LF line endings. Trim trailing whitespace. Insert final newline.
  </action>
  <verify>
    <automated>bash -n /home/jasper/Repositories/MasterThesisCode/cluster/modules.sh && echo "SYNTAX OK"</automated>
  </verify>
  <acceptance_criteria>
    - cluster/modules.sh exists and is executable
    - File contains `_load_or_die()` function definition
    - File contains exactly these 4 module load calls: `_load_or_die compiler/gnu/14.2`, `_load_or_die devel/cuda/12.8`, `_load_or_die numlib/gsl`, `_load_or_die devel/python/3.13.3-gnu-14.2`
    - File contains `ws_find emri` for workspace resolution
    - File contains `export WORKSPACE`
    - File contains `export PROJECT_ROOT`
    - File contains `export VENV_PATH`
    - File contains `export PATH="$HOME/.local/bin:$PATH"`
    - File contains `set -euo pipefail`
    - File contains `module spider` in error message hint
    - `bash -n cluster/modules.sh` exits 0 (valid syntax)
  </acceptance_criteria>
  <done>cluster/modules.sh passes bash -n syntax check and contains all 4 module loads, _load_or_die function, workspace resolution via ws_find, and exported convenience variables</done>
</task>

<task type="auto">
  <name>Task 2: Create cluster/setup.sh (ENV-02)</name>
  <files>cluster/setup.sh</files>
  <read_first>
    - cluster/modules.sh (just created in Task 1 -- must understand what it exports)
    - .planning/phases/03-cluster-environment/03-CONTEXT.md (locked decisions D-03, D-04, D-06)
    - .planning/phases/03-cluster-environment/03-RESEARCH.md (Pattern 2: Idempotent Setup Script example, Pitfall 4 and 6)
    - .editorconfig (indentation rules)
  </read_first>
  <action>
Create `cluster/setup.sh`. This is an executable script (not sourced) for first-time cluster setup. It must be idempotent -- safe to re-run.

Structure the script as 4 numbered steps with clear echo banners:

**Step 1/4: Loading modules**
- Resolve `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`
- Resolve `PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"`
- `source "$SCRIPT_DIR/modules.sh"` (this loads all modules and exports WORKSPACE if it exists)

**Step 2/4: Checking uv (per D-03: check-and-skip)**
- `if command -v uv &>/dev/null; then` print version and skip
- `else` run `curl -LsSf https://astral.sh/uv/install.sh | sh`
- After install: `export PATH="$HOME/.local/bin:$PATH"` (per Pitfall 4)
- Print installed version

**Step 3/4: Workspace (per D-04: check-and-allocate)**
- `WORKSPACE="$(ws_find emri 2>/dev/null || true)"`
- If empty: `ws_allocate emri 60`, then re-resolve `WORKSPACE="$(ws_find emri)"`
- `export WORKSPACE`
- Print workspace path

**Step 4/4: Python environment (per D-06: use cluster Python module)**
- `cd "$PROJECT_ROOT"`
- Run `uv sync --extra gpu` (this uses the module-loaded Python per D-06)
- Print venv path

**Verification section:**
- `echo "=== Verification ==="`
- `uv run python -c "import master_thesis_code; print('  master_thesis_code importable')"`
- `uv run python -c "import few; print('  few (fastemriwaveforms) importable')"`
- `uv run python -c "import cupy; print(f'  cupy {cupy.__version__} with CUDA')"`
- `echo "Setup complete. Workspace: $WORKSPACE"`

Use `set -euo pipefail` at the top. Shebang: `#!/usr/bin/env bash`. Header: `# cluster/setup.sh -- First-time cluster environment setup. Idempotent: safe to re-run.`

Mark executable: `chmod +x cluster/setup.sh`

Use 4-space indentation per .editorconfig. LF line endings. Trim trailing whitespace. Insert final newline.
  </action>
  <verify>
    <automated>bash -n /home/jasper/Repositories/MasterThesisCode/cluster/setup.sh && echo "SYNTAX OK"</automated>
  </verify>
  <acceptance_criteria>
    - cluster/setup.sh exists and is executable
    - File contains `source "$SCRIPT_DIR/modules.sh"` (or equivalent sourcing of modules.sh)
    - File contains `command -v uv` check (D-03 check-and-skip)
    - File contains `curl -LsSf https://astral.sh/uv/install.sh | sh` (D-03 installer)
    - File contains `ws_find emri` (D-04 check)
    - File contains `ws_allocate emri 60` (D-04 allocate)
    - File contains `uv sync --extra gpu` (D-06 uses module Python)
    - File contains `import master_thesis_code` smoke test
    - File contains `import few` smoke test
    - File contains `import cupy` smoke test
    - File contains `set -euo pipefail`
    - File contains 4 step banners (Step 1/4 through Step 4/4)
    - `bash -n cluster/setup.sh` exits 0 (valid syntax)
  </acceptance_criteria>
  <done>cluster/setup.sh passes bash -n syntax check, is idempotent (check-and-skip for uv, check-and-allocate for workspace), sources modules.sh, runs uv sync --extra gpu, and includes import smoke tests</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify scripts on bwUniCluster 3.0</name>
  <files>cluster/modules.sh, cluster/setup.sh</files>
  <action>
Human verifies both scripts on bwUniCluster 3.0 login node. No automated action -- this is a manual verification checkpoint because the scripts require the cluster Lmod module system and bwHPC workspace tools which are not available on the dev machine.
  </action>
  <what-built>
Two shell scripts for cluster environment setup:
1. `cluster/modules.sh` -- loads 4 environment modules (compiler/gnu/14.2, devel/cuda/12.8, numlib/gsl, devel/python/3.13.3-gnu-14.2), exports $WORKSPACE via ws_find
2. `cluster/setup.sh` -- installs uv, allocates workspace, runs uv sync --extra gpu, verifies imports
  </what-built>
  <how-to-verify>
On a bwUniCluster 3.0 login node:

1. Clone/update repo and cd into it
2. Test modules.sh:
   ```bash
   source cluster/modules.sh
   echo "WORKSPACE=$WORKSPACE"
   python3 --version  # should show 3.13.x
   nvcc --version     # should show CUDA 12.8
   ```
3. If any module name is wrong:
   - Run `module spider <category>/` (e.g., `module spider devel/python/`)
   - Note the correct version string
   - Report it so we can update modules.sh
4. Test setup.sh (full first-time setup):
   ```bash
   bash cluster/setup.sh
   ```
   - Should complete all 4 steps + verification
   - Should print "Setup complete" with workspace path
5. Verify workspace:
   ```bash
   echo "$WORKSPACE"  # should be /pfs/work9/ws/...
   ls "$WORKSPACE"    # should exist
   ```
  </how-to-verify>
  <verify>
    <automated>bash -n cluster/modules.sh && bash -n cluster/setup.sh && echo "SYNTAX OK (full verification requires cluster)"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms `source cluster/modules.sh` completes without errors on bwUniCluster 3.0
    - User confirms `python3 --version` shows 3.13.x after sourcing modules.sh
    - User confirms `bash cluster/setup.sh` completes all 4 steps
    - User confirms $WORKSPACE is set to a valid path under /pfs/work9/ws/
  </acceptance_criteria>
  <done>User confirms modules load correctly on bwUniCluster 3.0, setup.sh completes all 4 steps, and $WORKSPACE points to a valid workspace path</done>
  <resume-signal>Type "approved" if all modules load and setup completes, or describe which module names need updating</resume-signal>
</task>

</tasks>

<verification>
- `bash -n cluster/modules.sh` exits 0
- `bash -n cluster/setup.sh` exits 0
- cluster/modules.sh contains all 4 module load calls from D-01
- cluster/setup.sh sources modules.sh and runs uv sync --extra gpu
- Both files are executable
- Both files use set -euo pipefail
- $WORKSPACE is exported from modules.sh via ws_find emri (ENV-03)
- Human checkpoint confirms on-cluster functionality
</verification>

<success_criteria>
- ENV-01: cluster/modules.sh defines all required environment modules and is sourceable
- ENV-02: cluster/setup.sh automates first-time setup (uv, workspace, venv) idempotently
- ENV-03: $WORKSPACE exported via ws_find emri; all downstream scripts use this variable
- Both scripts pass bash -n syntax validation on dev machine
- Human confirms scripts work on bwUniCluster 3.0 (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/03-cluster-environment/03-01-SUMMARY.md`
</output>
