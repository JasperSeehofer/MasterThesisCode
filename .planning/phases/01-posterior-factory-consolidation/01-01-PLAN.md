---
phase: 01-posterior-factory-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - master_thesis_code_test/plotting/test_canonical_map_consistency.py
  - master_thesis_code/plotting/bayesian_plots.py
  - master_thesis_code/plotting/paper_figures.py
  - master_thesis_code/plotting/convergence_analysis.py
  - master_thesis_code/plotting/convergence_plots.py
  - master_thesis_code/main.py
autonomous: true
requirements: [VR-CONS-01, VR-CONS-02]

must_haves:
  truths:
    - "All five combined-H0-posterior render paths draw their posterior curve(s) via plot_combined_posterior"
    - "Every consolidated path reports the same MAP for identical input data (to sub-grid tolerance)"
    - "paper_h0_posterior, paper_h0_posterior_kde, plot_posterior_convergence, plot_m_z_improvement_panels keep their (data_dir)/(bank) -> (fig, ax) signatures"
    - "fig01 renders area-normalized OR clean peak-norm-no-band overlay per the documented §1.3 reconciliation, with theme passthrough wired"
    - "KDE variant still smooths via gaussian_kde before the canonical factory draws it"
    - "Check gate is green (ruff + mypy + pytest 'not gpu and not slow')"
    - "fig01, paper_h0_posterior, paper_h0_posterior_kde, fig08, paper_m_z_improvement regenerate against the v2.1 archive with no traceback and stay vector >=7pt"
  artifacts:
    - path: "master_thesis_code/plotting/bayesian_plots.py"
      provides: "Single canonical plot_combined_posterior factory with kde + linestyle + truth-style + xlim options"
      contains: "def plot_combined_posterior"
    - path: "master_thesis_code/plotting/paper_figures.py"
      provides: "plot_h0_posterior_comparison + plot_h0_posterior_kde delegating to plot_combined_posterior"
      contains: "plot_combined_posterior"
    - path: "master_thesis_code/plotting/convergence_analysis.py"
      provides: "plot_m_z_improvement_panels top-middle panel routed through plot_combined_posterior"
      contains: "plot_combined_posterior"
    - path: "master_thesis_code/plotting/convergence_plots.py"
      provides: "fig08-left posterior panel routed through plot_combined_posterior"
      contains: "plot_combined_posterior"
    - path: "master_thesis_code_test/plotting/test_canonical_map_consistency.py"
      provides: "Cross-path rendered-MAP regression covering all five figure paths"
      contains: "class TestRenderedMapAgreesAcrossFigurePaths"
  key_links:
    - from: "master_thesis_code/plotting/paper_figures.py"
      to: "plot_combined_posterior"
      via: "import + delegate in plot_h0_posterior_comparison / plot_h0_posterior_kde"
      pattern: "plot_combined_posterior\\("
    - from: "master_thesis_code/main.py"
      to: "plot_combined_posterior"
      via: "_gen_h0_posterior_combined fig01 manifest entry"
      pattern: "plot_combined_posterior\\("
    - from: "master_thesis_code/plotting/convergence_analysis.py"
      to: "plot_combined_posterior"
      via: "ax_p top-middle panel delegate"
      pattern: "plot_combined_posterior\\("
    - from: "master_thesis_code/plotting/convergence_plots.py"
      to: "plot_combined_posterior"
      via: "ax_post left panel delegate"
      pattern: "plot_combined_posterior\\("
---

<objective>
Collapse the five duplicate combined-H0-posterior render paths into ONE canonical
factory — `bayesian_plots.plot_combined_posterior` — so every recolor/annotation
edit lands in one place and the quadruplicate-drift hazard (different MAPs from
copy-pasted plotting code) is gone for good.

This is GSD software/design work. It touches plotting factory BODIES only; it
changes NO formula, constant, PSD coefficient, waveform parameter, or frequency
limit, and it leaves the canonical data loaders (`load_canonical_combined_posterior`,
`compute_m_z_improvement_bank`) untouched. **Never** route to `/physics-change`.

Purpose: VR-CONS-01 (one canonical factory; wrappers + manifest delegate while
preserving `(data_dir) -> (fig, ax)` signatures) and VR-CONS-02 (all paths agree on
the MAP, regression-anchored; fig01 wired to the headline treatment + theme
passthrough).

Output: an extended `plot_combined_posterior` that all five paths call; an extended
`test_canonical_map_consistency` that pins cross-path rendered-curve MAP agreement;
a green check gate; clean regen of the affected figures against the v2.1 archive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/quick/260621-npe-horizon-viz-quick-wins/260621-npe-SUMMARY.md
@CLAUDE.md

# This is the canonical factory (already has normalize / show_credible /
# show_references / annotate_map / show_truth / color / ax switches — extend it).
@master_thesis_code/plotting/bayesian_plots.py

# The two paper wrappers to turn into delegates (KDE + comparison).
@master_thesis_code/plotting/paper_figures.py

# The regression anchor to extend (currently only covers loader paths, NOT renders).
@master_thesis_code_test/plotting/test_canonical_map_consistency.py

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do not re-explore. -->

Canonical factory signature (master_thesis_code/plotting/bayesian_plots.py:68):
```python
def plot_combined_posterior(
    h_values: npt.NDArray[np.float64],
    posterior: npt.NDArray[np.float64],
    true_h: float,
    *,
    label: str | None = None,
    normalize: str = "peak",          # "peak" | "density"
    show_credible: bool = True,        # nested 68/95% HDI bands
    show_references: bool = True,      # Planck + SH0ES bands
    annotate_map: bool = True,         # inline MAP +/- 68% HDI text
    show_truth: bool = True,           # dashed TRUTH line + legend entry
    color: str | None = None,          # defaults to VARIANT_NO_MASS
    ax: Axes | None = None,
) -> tuple[Figure, Axes]:
```
It computes the 68/95% HDI from the RAW posterior via `compute_hdi_interval`,
then fills under the NORMALIZED curve — already the right pattern. It sets
`ax.set_xlabel(LABELS["h"])`, `ax.set_ylabel(r"$p(h|\mathrm{data})$")`, `ax.legend()`.

Shared helpers already available:
- `master_thesis_code/plotting/_helpers.py`:
  `compute_hdi_interval(h, posterior, level)`, `compute_credible_interval(...)`,
  `get_figure(preset=...)`, `_fig_from_ax(ax)`,
  `load_canonical_combined_posterior(data_dir, variant) -> (h, posterior, meta)`.
- `master_thesis_code/plotting/paper_figures.py`:
  `_kde_smooth_posterior(h, posterior, n_fine=500) -> (h_fine, kde_fine)` (Scott's rule;
  returns copies when norm<=0). `_shade_nested_hdi(...)`, `_annotate_inline_map(...)`
  (these become redundant once the factory does the shading — see Task 2).
- `master_thesis_code/plotting/_colors.py`: `VARIANT_NO_MASS` (navy `#1B2A4A`),
  `VARIANT_WITH_MASS` (gold `#E8A317`), `TRUTH` (vermillion `#C2451E`), `PLANCK`, `SH0ES`.

Per-event matrix shapes (for the test, from convergence_analysis loaders):
`_load_per_event_no_mass(dir) -> (h_values, {event_id: np.ndarray})`.
</interfaces>

<current_divergence_map>
<!-- EXACT per-path behavior the executor must reproduce through the delegate.
     Source: read of all 5 paths on 2026-06-21. -->

| # | Path (file:loc) | normalize | KDE | line style | HDI band | truth line | refs | MAP text | xlim |
|---|---|---|---|---|---|---|---|---|---|
| 1 | fig01 `main.py:910 _gen_h0_posterior_combined` -> `plot_combined_posterior` x2 | peak | no | solid both | **none** (`show_credible=False`) | dashed `TRUTH`, primary only | yes, primary only | yes, primary only | default |
| 2 | `paper_figures.plot_h0_posterior_comparison` (`paper_h0_posterior`) | density | no | navy SOLID / gold DASHED | nested 68/95 BOTH variants | dotted `:` `TRUTH` "Injected" | **NO** Planck/SH0ES | yes, no-mass only | 0.59–0.87 |
| 3 | `paper_figures.plot_h0_posterior_kde` (`paper_h0_posterior_kde`) | density (on KDE curve) | **yes** (Scott) | navy SOLID / gold DASHED, lw 1.4 | nested 68/95 BOTH (from KDE) | dotted `:` "Injected" | **NO** | yes, no-mass only | 0.59–0.87 |
| 4 | `convergence_analysis.plot_m_z_improvement_panels` top-mid `ax_p` (lines 748-789) | peak | no | navy SOLID / gold DASHED lw 1.4 | none | dotted `:` "Injected" | no | no | 0.59–0.87 + ylim(-0.05,1.15) |
| 5 | fig08-left `convergence_plots.plot_h0_convergence` `ax_post` (lines 152-202, 237-242) | density | no | primary solid / alt DASHED | none | dashed `--` `TRUTH` "Truth" | no | no | default; ylabel "Posterior density" |

Key consolidation facts the executor MUST honor:
- The factory ALREADY supports the on/off switches for HDI / refs / truth / MAP / color / ax.
- What's MISSING from the factory and must be ADDED in Task 1:
  (a) `linestyle` (paths 2,3,4,5 use dashed for the secondary/alt variant);
  (b) `truth_linestyle` + `truth_label` (path 2/3 use dotted ":" + "Injected";
      path 5 uses dashed "--" + "Truth"; path 1 uses dashed + "True $h = 0.73$");
  (c) `kde` smoothing option (path 3) — thread a `kde: bool = False` that pre-smooths
      via `_kde_smooth_posterior` (or accept a pre-smoothed curve; see Task 1 action);
  (d) `xlim` passthrough (paths 2,3,4 set 0.59–0.87);
  (e) `ylabel` override (path 5 says "Posterior density"; default is `p(h|data)`);
  (f) `linewidth` passthrough (paths 3,4 use 1.4; path 5/default use stylesheet default).
- Paper figures (single-variant headline) KEEP the area-norm + nested-HDI + inline-MAP
  treatment from VR-F3 — `TestHeadlinePosteriorNormalization` (test_paper_figures.py:148)
  pins area≈1, ylabel-not-"peak-normalized", >=2 HDI fill collections. These MUST stay green.
</current_divergence_map>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend test_canonical_map_consistency to pin cross-path RENDERED MAP (RED), then extend plot_combined_posterior to the canonical superset</name>
  <files>master_thesis_code_test/plotting/test_canonical_map_consistency.py, master_thesis_code/plotting/bayesian_plots.py</files>
  <behavior>
    TDD anchor — write the failing render-MAP test FIRST so it pins cross-path agreement
    before any refactor lands:
    - New class `TestRenderedMapAgreesAcrossFigurePaths`:
      - Reuse the module's `_write_synthetic_posteriors` to write a 27-pt h-grid,
        ~80-event posteriors peaked at map_h=0.74 into tmp_path/posteriors and
        tmp_path/posteriors_with_bh_mass.
      - Define a helper that extracts the MAP of the FIRST plotted posterior line from
        a returned Axes: `h_line, y_line = ax.lines[0].get_xdata()/get_ydata()`, then
        `map = float(np.asarray(h_line)[int(np.argmax(np.asarray(y_line)))])`.
        Use `np.asarray(...)` so `len()`/indexing type-checks (matplotlib stub returns).
      - `test_paper_comparison_rendered_map_matches_canonical`: render
        `plot_h0_posterior_comparison(tmp_path)`, extract line-0 MAP, assert it equals
        the canonical discrete_map (from `compute_canonical_combined_posterior`) within
        one grid step (< 0.012).
      - `test_paper_kde_rendered_map_matches_canonical`: render `plot_h0_posterior_kde`,
        extract the KDE line-0 MAP, assert within KDE tolerance (< 0.02; KDE smooths
        sub-grid — match the existing one-grid-spacing tolerance used by the KDE warn).
      - `test_combined_posterior_rendered_map_matches_canonical`: call
        `plot_combined_posterior(h, posterior, 0.73)` with the canonical (h, posterior)
        and assert line-0 MAP == canonical discrete_map exactly.
    - Keep `from __future__ import annotations` (file already has it; do not remove).
    These tests FAIL or are vacuous until the factory exposes a single render path; they
    pass after Tasks 1-3 route every path through `plot_combined_posterior`.

    Then extend `plot_combined_posterior` to the canonical SUPERSET (GREEN). Add these
    keyword-only params with backward-compatible defaults (existing callers unchanged):
    - `linestyle: str = "-"` (used as the curve linestyle; secondary variants pass "--").
    - `linewidth: float | None = None` (None -> stylesheet default; paths pass 1.4).
    - `truth_linestyle: str = "dashed"` (path 2/3 pass ":"; path 5 "dashed").
    - `truth_label: str | None = None` (None -> existing f"True $h = {true_h}$"; paths
      pass "Injected" / "Truth").
    - `xlim: tuple[float, float] | None = None` (paths 2,3,4 pass (0.59, 0.87)).
    - `ylim: tuple[float, float] | None = None` (path 4 passes (-0.05, 1.15)).
    - `ylabel: str | None = None` (None -> existing `p(h|data)`; path 5 -> "Posterior density").
    - `kde: bool = False` (path 3). When True, smooth (h_values, posterior) with
      `_kde_smooth_posterior` BEFORE normalizing/plotting; compute HDI/MAP from the
      smoothed curve so the rendered MAP matches what the panel shows. Import
      `_kde_smooth_posterior` lazily INSIDE the function (local import) to avoid a
      circular import (paper_figures imports from convergence_analysis, and bayesian_plots
      must not import paper_figures at module top). Alternatively accept the smoothing as
      a pre-step — but threading `kde=True` keeps ONE factory, which is the goal.
    - `legend: bool = True` (so overlay callers can suppress the per-call legend and the
      manifest sets it once — fig01 currently lets the last call own the legend).
    Wire the new params: pass `linestyle`/`linewidth` to `ax.plot`; gate `ax.set_xlim`/
    `ax.set_ylim` on the new args; use `ylabel`/`truth_linestyle`/`truth_label` where the
    function currently hardcodes them; gate `ax.legend()` on `legend`.
    Type rules (CLAUDE.md): complete annotations, `X | None`, `tuple[float, float]`, no
    `from __future__ import annotations` in this file, `npt.NDArray[np.float64]` for arrays.
    Update the docstring Parameters block for every new switch.
  </behavior>
  <action>
    Edit `test_canonical_map_consistency.py` to add `TestRenderedMapAgreesAcrossFigurePaths`
    per the behavior block (render-MAP extraction from `ax.lines[0]`). Run it now; the
    combined-posterior render test should pass immediately (the factory already plots
    line-0 as the posterior), the paper tests pass after the factory superset lands.

    Then edit `plot_combined_posterior` in `bayesian_plots.py` to add the keyword-only
    params above with backward-compatible defaults, threading them into the existing
    `ax.plot` / `fill_between` / reference / truth / annotation / legend blocks. For
    `kde=True`, do `from master_thesis_code.plotting.paper_figures import _kde_smooth_posterior`
    locally, smooth, and compute the curve/HDI/MAP from the smoothed arrays. Run
    `ruff format` + `ruff check --fix` on the touched files.

    GSD software change only — no formula/constant touched. Do NOT run /physics-change.
  </action>
  <verify>
    <automated>uv run pytest -m "not gpu and not slow" master_thesis_code_test/plotting/test_canonical_map_consistency.py master_thesis_code_test/plotting/test_bayesian_plots.py -x -q</automated>
  </verify>
  <done>
    `plot_combined_posterior` accepts linestyle/linewidth/truth_linestyle/truth_label/
    xlim/ylim/ylabel/kde/legend with backward-compatible defaults; existing
    test_bayesian_plots tests still pass; the new combined-posterior render-MAP test
    passes; mypy clean on bayesian_plots.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delegate paper_figures.plot_h0_posterior_comparison and plot_h0_posterior_kde to plot_combined_posterior</name>
  <files>master_thesis_code/plotting/paper_figures.py</files>
  <action>
    Rewrite the BODIES of `plot_h0_posterior_comparison` and `plot_h0_posterior_kde` to
    delegate to `plot_combined_posterior`, PRESERVING their `(data_dir) -> (fig, ax)`
    public signatures (tests + main.py call them by name).

    For `plot_h0_posterior_comparison`:
    - Keep the existing data load: `_load_combined_posterior("posteriors", data_dir)` and
      `_load_combined_posterior("posteriors_with_bh_mass", data_dir)`; build h_no/post_no/
      h_with/post_with arrays exactly as today (NO data-plumbing change).
    - First (headline, no-mass) call:
      `fig, ax = plot_combined_posterior(h_no, post_no, 0.73, label=r"Without $M_z$",`
      `normalize="density", color=VARIANT_NO_MASS, linestyle="-", show_credible=True,`
      `show_references=False, annotate_map=True, show_truth=True, truth_linestyle=":",`
      `truth_label="Injected", xlim=(0.59, 0.87), legend=False)`.
      (show_references=False reproduces today's paper figure which draws NO Planck/SH0ES.)
    - Second (with-mass) call on the same `ax`: same but `label=r"With $M_z$"`,
      `color=VARIANT_WITH_MASS`, `linestyle="--"`, `annotate_map=False`, `show_truth=False`,
      `show_references=False`, `legend=False`, `ax=ax`.
    - After both: `ax.legend(loc="upper right")`. Do NOT call tight_layout (constrained
      layout is on; §1.8). Confirm xlabel/ylabel match today (`r"$h$"`, `r"$p(h \mid \mathrm{data})$"`)
      — pass `ylabel=r"$p(h \mid \mathrm{data})$"` so the delegate matches the prior label,
      and override `ax.set_xlabel(r"$h$")` after the call if the factory's `LABELS["h"]`
      differs (verify LABELS["h"] resolves to "$h$"; if so no override needed).

    For `plot_h0_posterior_kde`:
    - Keep the load + the grid-spacing auto-detect + the KDE-MAP-drift warning logging
      EXACTLY as today (that diagnostic stays).
    - Delegate with `kde=True` so the factory does the smoothing:
      headline call `plot_combined_posterior(h_no, post_no, 0.73, label=r"Without $M_z$",`
      `normalize="density", kde=True, color=VARIANT_NO_MASS, linestyle="-", linewidth=1.4,`
      `show_references=False, truth_linestyle=":", truth_label="Injected",`
      `xlim=(0.59, 0.87), legend=False)`; secondary with-mass call as the comparison
      figure (linestyle="--", color=VARIANT_WITH_MASS, annotate_map=False, show_truth=False,
      show_credible=True, legend=False, ax=ax). Then `ax.legend(loc="upper right")`.
    - The local `_shade_nested_hdi` / `_annotate_inline_map` helpers become unused by these
      two functions. If NO other caller uses them (grep first), remove them; if other code
      still calls them, leave them. Do not break `plot_closure_test_overlay` or other paths.

    Guard against MAP drift: the area-norm + HDI + inline-MAP look must match today closely
    enough that `TestHeadlinePosteriorNormalization` (area≈1, ylabel-not-peak, >=2 HDI fills)
    stays green. Run that class explicitly.
    GSD software only — no /physics-change.
  </action>
  <verify>
    <automated>uv run pytest -m "not gpu and not slow" master_thesis_code_test/plotting/test_paper_figures.py master_thesis_code_test/plotting/test_canonical_map_consistency.py -x -q</automated>
  </verify>
  <done>
    Both paper functions delegate to `plot_combined_posterior` (grep shows the call), keep
    `(data_dir) -> (fig, ax)`; `TestHeadlinePosteriorNormalization` + the new paper render-MAP
    tests pass; KDE drift-warning logging preserved; mypy clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delegate convergence_analysis top-middle panel + fig08-left panel to plot_combined_posterior; verify/handle the fig08 duplicate</name>
  <files>master_thesis_code/plotting/convergence_analysis.py, master_thesis_code/plotting/convergence_plots.py</files>
  <action>
    Two embedded posterior panels remain; route both through the canonical factory using
    the `ax=` injection so the surrounding multi-panel layout is untouched.

    (A) `convergence_analysis.plot_m_z_improvement_panels` top-middle panel `ax_p`
    (lines ~748-789): when `canonical_combined_no_mass`/`with_mass` are provided, REPLACE
    the two inline `ax_p.plot(...)` peak-normalize calls with two `plot_combined_posterior`
    calls drawing onto `ax_p`:
    - no-mass: `plot_combined_posterior(h_no, p_no, bank.h_true, label=r"Without $M_z$",`
      `normalize="peak", color=VARIANT_NO_MASS, linestyle="-", linewidth=1.4,`
      `show_credible=False, show_references=False, annotate_map=False, show_truth=False,`
      `xlim=(0.59, 0.87), ylim=(-0.05, 1.15), ylabel="Posterior (peak-norm.)", legend=False,`
      `ax=ax_p)`.
    - with-mass: same on `ax=ax_p` with `label=r"With $M_z$"`, `color=VARIANT_WITH_MASS`,
      `linestyle="--"`, `show_truth=False`.
    - Keep the existing truth `ax_p.axvline(bank.h_true, ... "Injected")`, the
      `posterior_title` logic, `ax_p.set_title(...)`, `ax_p.legend(...)`. The factory's own
      truth/legend are suppressed (`show_truth=False`, `legend=False`) so the panel keeps
      its current single "Injected" line + small-font legend. Leave the legacy
      `elif bank.representative_posteriors_*` fallback branch as-is (old-fixture path; it
      draws a single bootstrap draw and is not a canonical-posterior path).
    - Import `plot_combined_posterior` lazily INSIDE the function (local import) to avoid a
      module-level circular import (convergence_analysis <-> paper_figures <-> bayesian_plots).
    - NOTE: this file uses `from __future__ import annotations` already — do NOT remove it
      and do NOT add new module-level type forms that conflict; new local code keeps full
      annotations.

    (B) fig08-left: `convergence_plots.plot_h0_convergence` DOES embed a duplicate posterior
    panel (`ax_post`, lines ~152-202 + styling 237-242) — confirmed present, so fold it in.
    Its primary content is the right panel (CI-width-vs-N); the left panel is the duplicate.
    Replace the two `ax_post.plot(...)` posterior draws (primary + alt) with
    `plot_combined_posterior` calls on `ax=ax_post`:
    - primary: when `canonical_no_mass` is set use `(h_left, combined)`; else keep the
      existing bootstrap-subset `combined`/`h_left` computation, then call
      `plot_combined_posterior(h_left, combined, true_h if true_h is not None else 0.73,`
      `label=label, normalize="density", color=color, linestyle="-", show_credible=False,`
      `show_references=False, annotate_map=False, show_truth=False, ylabel="Posterior density",`
      `legend=False, ax=ax_post)`.
    - alt (when `event_posteriors_alt`): same on `ax=ax_post` with `combined_alt`/`h_alt_left`,
      `label=label_alt`, `color=color_alt`, `linestyle="--"`, `show_truth=False`.
    - PRESERVE the existing truth handling: keep `ax_post.axvline(true_h, color=TRUTH,`
      `linestyle="--", label="Truth")` after the delegate calls (factory truth suppressed),
      and keep `ax_post.legend(fontsize="small")`. Do NOT change the right panel `ax_ci`,
      the bootstrap band, or the 1/sqrt(N) ref. Keep the normalization math (the existing
      `np.trapezoid` area-normalize of `combined`/`combined_alt`) by passing the
      already-normalized arrays with `normalize="density"` (idempotent) OR pass the raw
      `combined` and let the factory area-normalize once — pick the form that keeps the
      rendered curve identical to today (density-normalized). Local import of
      `plot_combined_posterior` inside the function to avoid circulars.
    GSD software only — no /physics-change.
  </action>
  <verify>
    <automated>uv run pytest -m "not gpu and not slow" master_thesis_code_test/plotting/test_convergence_plots.py master_thesis_code_test/plotting/test_canonical_map_consistency.py -x -q</automated>
  </verify>
  <done>
    Both embedded panels call `plot_combined_posterior` (grep shows two new call sites);
    fig08 right panel + bootstrap band + truth lines unchanged; convergence tests pass;
    mypy clean on both files.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire fig01/main.py to the canonical factory + reconcile §1.3 vs area-norm; run full gate + regen against the v2.1 archive</name>
  <files>master_thesis_code/main.py, master_thesis_code_test/plotting/test_canonical_map_consistency.py</files>
  <action>
    (1) fig01 reconciliation (success criterion 3): fig01 is a TWO-variant overlay. The
    VR-F4 foundation deliberately made it a CLEAN peak-norm, NO-shaded-band overlay (§1.3:
    "never shade a band under a many-variant overlay"). The ROADMAP also asks fig01 be
    "wired to the area-normalized headline treatment (VR-F3)". These two pull opposite ways
    on the band, NOT on normalization. RESOLUTION to implement and DOCUMENT in the SUMMARY:
    - Set `normalize="density"` on BOTH fig01 calls (honor VR-F3 area-norm; honest PDF).
    - Keep `show_credible=False` on BOTH (honor §1.3: no shaded HDI band under the
      two-variant overlay — the single-variant paper figures carry the HDI bands).
    - The headline (Without $M_z$) call keeps the inline MAP annotation + truth + references;
      the secondary (With $M_z$) call suppresses truth/MAP/references as today.
    This delivers the area-norm headline treatment WITHOUT a muddy multi-variant band, and
    is the explicit call the SUMMARY must record.
    Edit `_gen_h0_posterior_combined` (main.py ~910): change `show_credible=False` calls to
    additionally pass `normalize="density"` on BOTH. Keep the rest (color, label, the
    secondary call's `show_references=False, annotate_map=False, show_truth=False, ax=ax`).

    (2) Theme passthrough (success criterion 3 "theme= switch passing through"): verify
    `apply_style()` is the single style entrypoint in `generate_figures` (main.py ~817-824).
    The factory inherits the active rcParams from `apply_style(theme=...)` — so theme already
    "passes through" because the manifest renders under whatever `apply_style` set. Confirm
    `plot_combined_posterior` hardcodes NO figsize/fontsize that would defeat a theme (it
    uses `get_figure(preset=...)` + stylesheet); if any literal fontsize in the reference
    band / annotation blocks would fight a theme, leave them (they are intentional small
    inline labels) but NOTE in the SUMMARY that theme scaling is governed by apply_style.
    Do NOT add a new CLI theme flag in this phase (out of scope; arguments.py untouched).

    (3) Final regression coverage: add to `TestRenderedMapAgreesAcrossFigurePaths` two
    tests that render the convergence panels with synthetic canonical data and assert their
    line MAP matches the canonical discrete_map:
    - `test_convergence_panel_rendered_map_matches_canonical`: build a tiny
      `ImprovementBank` via `compute_m_z_improvement_bank(tmp_path, h_true=0.73)` on the
      synthetic dirs (or construct the minimal bank fields the panel reads), call
      `plot_m_z_improvement_panels(bank, canonical_combined_no_mass=(h, p), ...)`, grab the
      top-middle axes (`fig.axes[1]`), extract line-0 MAP, assert within one grid step.
    - `test_fig08_left_rendered_map_matches_canonical`: call `plot_h0_convergence(...)` with
      `canonical_no_mass=(h, p)`, grab `axes[0]` (ax_post), extract line-0 MAP, assert match.
    Use the existing `_write_synthetic_posteriors` helper for the data. If constructing a
    full bank is heavy, prefer the direct `plot_h0_convergence` + `plot_m_z_improvement_panels`
    calls with `canonical_*` kwargs (the panels short-circuit to the canonical branch).

    (4) Full check gate, then regen against the v2.1 archive:
    - ruff check --fix + ruff format on all touched files; mypy; pytest "not gpu and not slow".
    - Regenerate: `uv run python -m master_thesis_code simulations/_archive_v2_1_baseline --generate_figures simulations/_archive_v2_1_baseline`.
      Confirm fig01_h0_posterior_combined, paper_h0_posterior, paper_h0_posterior_kde,
      fig08_h0_convergence, paper_m_z_improvement regenerate with NO traceback. The 3
      PRE-EXISTING archive failures noted in the quick-wins SUMMARY (fig09_detection_efficiency,
      paper_single_event, paper_convergence — stale-data-availability errors NOT touched here)
      are acceptable; do NOT let any of the FIVE consolidated figures be among the failures.
    - Spot-check the regenerated paper PDF stays vector + fonts >=7pt (it inherits the
      unchanged mplstyle; no rasterization introduced).
    GSD software only — no /physics-change.
  </action>
  <verify>
    <automated>uv run ruff check master_thesis_code/ master_thesis_code_test/ && uv run mypy master_thesis_code/ master_thesis_code_test/ && uv run pytest -m "not gpu and not slow" master_thesis_code_test/plotting/ -q</automated>
  </verify>
  <done>
    fig01 renders area-norm (normalize="density") with the clean no-band overlay (§1.3) and
    the §1.3-vs-VR-F3 reconciliation documented in the SUMMARY; all five paths call
    `plot_combined_posterior` (grep across main.py + the 3 plotting modules shows 5 call
    sites for the consolidated paths); `TestRenderedMapAgreesAcrossFigurePaths` covers all
    five and passes; check gate green; the five figures regenerate against
    simulations/_archive_v2_1_baseline with no traceback; paper PDF stays vector >=7pt.
  </done>
</task>

</tasks>

<verification>
- All five combined-H0-posterior render paths import and call `plot_combined_posterior`:
  `grep -rn "plot_combined_posterior(" master_thesis_code/ | grep -v "def plot_combined_posterior"`
  must show call sites in main.py (fig01 x2), paper_figures.py (comparison + kde),
  convergence_analysis.py (top-mid x2), convergence_plots.py (left panel x2).
- `test_canonical_map_consistency.py::TestRenderedMapAgreesAcrossFigurePaths` covers
  comparison, kde, combined, convergence panel, and fig08-left, all asserting line-MAP ==
  canonical discrete_map to tolerance.
- `(data_dir) -> (fig, ax)` signatures unchanged on `plot_h0_posterior_comparison`,
  `plot_h0_posterior_kde`; `(bank, *, ...)` on `plot_m_z_improvement_panels`; the
  `plot_h0_convergence` signature unchanged.
- Canonical loaders (`load_canonical_combined_posterior`, `compute_m_z_improvement_bank`,
  `_load_combined_posterior`) NOT modified — `git diff --stat` shows no change to
  `_helpers.py` and no change to the loader bodies.
- Check gate green: `uv run ruff check master_thesis_code/ master_thesis_code_test/`,
  `uv run mypy master_thesis_code/ master_thesis_code_test/`,
  `uv run pytest -m "not gpu and not slow"`.
- No physics file changed: diff confined to the two plotting-factory files, two
  convergence files, main.py manifest, and the one test file. `/physics-change` NOT triggered.
</verification>

<success_criteria>
1. The five identified paths all delegate to `plot_combined_posterior` while preserving
   their public `(data_dir)/(bank) -> (fig, ax)` signatures (VR-CONS-01).
2. `test_canonical_map_consistency` is extended to cover all five consolidated render paths
   and passes — every path's plotted curve reports the same MAP to tolerance (VR-CONS-02).
3. fig01 is wired to `normalize="density"` (VR-F3 area-norm) with the §1.3 clean no-band
   overlay, the reconciliation documented; theme passthrough confirmed via `apply_style`;
   regenerating it against `simulations/_archive_v2_1_baseline/posteriors/` produces a PDF
   with no traceback.
4. Check gate (ruff + mypy + pytest "not gpu and not slow") green; regenerated paper PDF
   stays vector and fonts >=7pt.
</success_criteria>

<output>
After completion, create `.planning/phases/01-posterior-factory-consolidation/01-01-SUMMARY.md`.
Record explicitly: (a) the §1.3-vs-VR-F3 fig01 reconciliation call (area-norm + no shaded
band on the two-variant overlay; single-variant paper figures keep the HDI bands);
(b) the final canonical superset of `plot_combined_posterior` kwargs; (c) whether the local
`_shade_nested_hdi`/`_annotate_inline_map` helpers were removed or retained; (d) the exact
grep of the five consolidated call sites; (e) the theme-passthrough mechanism (governed by
`apply_style`, no new CLI flag this phase).
</output>
