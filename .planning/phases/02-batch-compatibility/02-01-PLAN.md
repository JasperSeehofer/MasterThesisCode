---
phase: 02-batch-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/merge_cramer_rao_bounds.py
  - scripts/prepare_detections.py
  - pyproject.toml
  - master_thesis_code_test/scripts/__init__.py
  - master_thesis_code_test/scripts/test_merge_cramer_rao_bounds.py
  - master_thesis_code_test/scripts/test_prepare_detections.py
autonomous: true
requirements:
  - BATCH-01
  - BATCH-02

must_haves:
  truths:
    - "merge_cramer_rao_bounds.py runs without any interactive prompt regardless of flags"
    - "merge_cramer_rao_bounds.py --delete-sources silently deletes source CSVs after merge"
    - "merge_cramer_rao_bounds.py without --delete-sources keeps source files intact"
    - "prepare_detections.py has a main() function callable from a batch script"
    - "Both scripts accept --workdir to resolve paths relative to a given directory"
    - "emri-merge and emri-prepare console entry points are registered and callable after uv sync"
  artifacts:
    - path: "scripts/merge_cramer_rao_bounds.py"
      provides: "Non-interactive merge script with argparse CLI"
      contains: "argparse"
    - path: "scripts/prepare_detections.py"
      provides: "Batch-callable prepare script with main() and argparse CLI"
      contains: "def main"
    - path: "pyproject.toml"
      provides: "Console entry point registration"
      contains: "emri-merge"
    - path: "master_thesis_code_test/scripts/test_merge_cramer_rao_bounds.py"
      provides: "Tests for merge script batch behavior"
      min_lines: 40
    - path: "master_thesis_code_test/scripts/test_prepare_detections.py"
      provides: "Tests for prepare script batch behavior"
      min_lines: 20
  key_links:
    - from: "pyproject.toml"
      to: "scripts/merge_cramer_rao_bounds.py"
      via: "console entry point emri-merge"
      pattern: 'emri-merge.*=.*scripts\.merge_cramer_rao_bounds:main'
    - from: "pyproject.toml"
      to: "scripts/prepare_detections.py"
      via: "console entry point emri-prepare"
      pattern: 'emri-prepare.*=.*scripts\.prepare_detections:main'
---

<objective>
Make post-simulation scripts run unattended in SLURM batch jobs by removing all interactive
prompts, adding argparse CLIs with --workdir and --delete-sources flags, and registering
console entry points so Phase 4 SLURM job scripts can invoke them.

Purpose: Scripts currently block on `input()` calls and lack proper CLI interfaces, making
them unusable in non-interactive batch jobs on the cluster.

Output: Two refactored scripts with argparse, two console entry points in pyproject.toml,
and tests proving batch compatibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-batch-compatibility/02-CONTEXT.md

@scripts/merge_cramer_rao_bounds.py
@scripts/prepare_detections.py
@master_thesis_code/constants.py
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor merge_cramer_rao_bounds.py for batch use (BATCH-01)</name>
  <files>scripts/merge_cramer_rao_bounds.py, scripts/__init__.py, pyproject.toml, master_thesis_code_test/scripts/__init__.py, master_thesis_code_test/scripts/test_merge_cramer_rao_bounds.py</files>
  <read_first>
    - scripts/merge_cramer_rao_bounds.py (current state with 4 input() calls)
    - master_thesis_code/constants.py (path constants: CRAMER_RAO_BOUNDS_PATH, CRAMER_RAO_BOUNDS_OUTPUT_PATH, UNDETECTED_EVENTS_PATH, UNDETECTED_EVENTS_OUTPUT_PATH)
    - pyproject.toml (to find where to add [project.scripts] section)
  </read_first>
  <behavior>
    - Test 1: Calling main(["--workdir", tmpdir]) with per-index CSVs in tmpdir/simulations/ merges them into cramer_rao_bounds.csv and source files still exist (no --delete-sources)
    - Test 2: Calling main(["--workdir", tmpdir, "--delete-sources"]) merges CSVs and source files are deleted
    - Test 3: Calling main(["--workdir", tmpdir]) with no per-index CSVs prints message and exits cleanly (exit code 0)
    - Test 4: No calls to input() exist anywhere in the module (grep check)
    - Test 5: Undetected events CSVs are also merged alongside cramer rao bounds
  </behavior>
  <action>
    Refactor scripts/merge_cramer_rao_bounds.py per D-01, D-02, D-03, D-04, D-05:

    1. Create scripts/__init__.py (empty file, makes scripts a package for entry points).

    2. Add argparse to merge_cramer_rao_bounds.py:
       - Create `def parse_args(argv: list[str] | None = None) -> argparse.Namespace` that defines:
         - `--workdir` (type=str, default=".", help="Working directory; paths resolved relative to this")
         - `--delete-sources` (action="store_true", help="Delete per-index source CSVs after successful merge")
       - Returns `parser.parse_args(argv)`

    3. Refactor `main()` signature to `def main(argv: list[str] | None = None) -> None`:
       - Calls `parse_args(argv)` to get args
       - Passes `args.workdir` (as a `pathlib.Path`) and `args.delete_sources` to merge functions

    4. Refactor `merge_cramer_rao_bounds()` signature to accept `workdir: Path, delete_sources: bool`:
       - Replace all bare constant paths with `workdir / CRAMER_RAO_BOUNDS_OUTPUT_PATH` etc.
       - Remove ALL `input()` calls (lines 54-56 and 69-71 in current code)
       - Instead: if `delete_sources` is True, call `delete_files(used_files)` unconditionally after successful merge
       - If `delete_sources` is False, skip deletion entirely (per D-01)

    5. Refactor `merge_undetected_events()` identically:
       - Accept `workdir: Path, delete_sources: bool`
       - Remove `input()` calls (lines 102-104 and 117-119)
       - Same conditional delete logic

    6. Update `main()` to call both functions with workdir and delete_sources.

    7. Add `[project.scripts]` section to pyproject.toml (before or after [tool.ruff]):
       ```
       [project.scripts]
       emri-merge = "scripts.merge_cramer_rao_bounds:main"
       emri-prepare = "scripts.prepare_detections:main"
       ```

    8. Create master_thesis_code_test/scripts/__init__.py (empty).

    9. Create master_thesis_code_test/scripts/test_merge_cramer_rao_bounds.py with tests using tmp_path fixture:
       - Create helper that writes dummy CSV files (with header row + 1-2 data rows) into tmp_path/simulations/
       - Test no-delete: assert source files exist after merge, output file exists with all rows
       - Test delete: assert source files removed after merge
       - Test empty: no source files, exits cleanly
       - Test no-input: grep scripts/merge_cramer_rao_bounds.py for "input(" and assert zero matches
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/MasterThesisCode && uv run pytest master_thesis_code_test/scripts/test_merge_cramer_rao_bounds.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - scripts/merge_cramer_rao_bounds.py contains `import argparse`
    - scripts/merge_cramer_rao_bounds.py contains `def parse_args(`
    - scripts/merge_cramer_rao_bounds.py contains `--workdir`
    - scripts/merge_cramer_rao_bounds.py contains `--delete-sources`
    - scripts/merge_cramer_rao_bounds.py does NOT contain `input(`
    - scripts/__init__.py exists (can be empty)
    - pyproject.toml contains `emri-merge = "scripts.merge_cramer_rao_bounds:main"`
    - pyproject.toml contains `emri-prepare = "scripts.prepare_detections:main"`
    - master_thesis_code_test/scripts/test_merge_cramer_rao_bounds.py exists with at least 4 test functions
    - All tests in test_merge_cramer_rao_bounds.py pass
  </acceptance_criteria>
  <done>merge_cramer_rao_bounds.py has zero input() calls, accepts --workdir and --delete-sources via argparse, and all tests pass proving batch compatibility</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor prepare_detections.py for batch use (BATCH-02)</name>
  <files>scripts/prepare_detections.py, master_thesis_code_test/scripts/test_prepare_detections.py</files>
  <read_first>
    - scripts/prepare_detections.py (current state with all logic in __main__ block)
    - master_thesis_code/constants.py (CRAMER_RAO_BOUNDS_OUTPUT_PATH, PREPARED_CRAMER_RAO_BOUNDS_PATH)
    - master_thesis_code/datamodels/detection.py (Detection class used by the script)
  </read_first>
  <behavior>
    - Test 1: main(["--workdir", tmpdir]) with a valid cramer_rao_bounds.csv produces prepared_cramer_rao_bounds.csv in tmpdir/simulations/
    - Test 2: main(["--workdir", tmpdir]) with missing input CSV raises SystemExit or prints error and exits cleanly
    - Test 3: main() function exists and is callable with argv parameter
  </behavior>
  <action>
    Refactor scripts/prepare_detections.py per D-02, D-03, D-04, D-05:

    1. Add argparse:
       - Create `def parse_args(argv: list[str] | None = None) -> argparse.Namespace`:
         - `--workdir` (type=str, default=".", help="Working directory; paths resolved relative to this")
       - Returns `parser.parse_args(argv)`

    2. Extract `__main__` block into `def main(argv: list[str] | None = None) -> None`:
       - Calls `parse_args(argv)` to get args
       - Constructs `workdir = Path(args.workdir)`
       - Resolves input path: `workdir / CRAMER_RAO_BOUNDS_OUTPUT_PATH` (i.e., `workdir / "simulations/cramer_rao_bounds.csv"`)
       - Resolves output path: `workdir / PREPARED_CRAMER_RAO_BOUNDS_PATH` (i.e., `workdir / "simulations/prepared_cramer_rao_bounds.csv"`)
       - Move all existing logic from `if __name__` block into this function, replacing hardcoded constant paths with the resolved Path objects
       - Add error handling: if input CSV does not exist, print an error message and call `sys.exit(1)`

    3. Update `if __name__ == "__main__":` block to simply call `main()`.

    4. Add type annotations to main() and parse_args() per project conventions.

    5. Create master_thesis_code_test/scripts/test_prepare_detections.py:
       - Test that main() function exists and is callable
       - Test with missing input CSV exits with non-zero code (use pytest.raises(SystemExit))
       - Test with a minimal valid CSV: create a CSV with the columns that Detection expects (at minimum: M, luminosity_distance, phiS, qS, delta_phiS_delta_phiS with a positive value), run main(), verify output CSV exists
       - Note: The Detection class import comes from cosmological_model (backward-compat re-export); test should verify the import works
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/MasterThesisCode && uv run pytest master_thesis_code_test/scripts/test_prepare_detections.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - scripts/prepare_detections.py contains `def main(`
    - scripts/prepare_detections.py contains `def parse_args(`
    - scripts/prepare_detections.py contains `import argparse`
    - scripts/prepare_detections.py contains `--workdir`
    - scripts/prepare_detections.py `if __name__` block contains only `main()` call
    - master_thesis_code_test/scripts/test_prepare_detections.py exists with at least 2 test functions
    - All tests in test_prepare_detections.py pass
  </acceptance_criteria>
  <done>prepare_detections.py has a proper main() with argparse --workdir, callable from batch scripts and via emri-prepare entry point, and all tests pass</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `uv run pytest master_thesis_code_test/scripts/ -x -v` — all script tests pass
2. `uv run pytest -m "not gpu and not slow"` — full CPU test suite still passes (no regressions)
3. `grep -r "input(" scripts/merge_cramer_rao_bounds.py` — returns empty (no interactive prompts)
4. `grep "emri-merge\|emri-prepare" pyproject.toml` — both entry points registered
5. `uv sync --extra cpu --extra dev && which emri-merge && which emri-prepare` — entry points are on PATH
</verification>

<success_criteria>
- Both scripts run without any interactive `input()` prompts
- `emri-merge --workdir /path --delete-sources` merges CSVs and deletes sources silently
- `emri-merge --workdir /path` merges CSVs and keeps sources
- `emri-prepare --workdir /path` converts detection parameters to best-guess parameters
- All new tests pass, existing test suite has no regressions
- Console entry points are installed and callable after `uv sync`
</success_criteria>

<output>
After completion, create `.planning/phases/02-batch-compatibility/02-01-SUMMARY.md`
</output>
