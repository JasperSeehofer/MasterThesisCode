---
context: phase
phase: 40-verification-gate
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-04-24T11:07:27Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: MAP=0.86 bias — v2.2 combined posterior peaks at h=0.86 (expected 0.73). Root cause not yet confirmed. Do NOT mark Phase 40 PASS or advance to Phase 41/42 until the D(h) hypothesis is tested and angle-mapping is independently verified.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| extract_baseline as MAP proxy | `extract_baseline` sums Σ log L without D(h) denominator — gives biased MAP at high h. DO NOT use for SC-3 verification. | blocking | Read MAP from `--evaluate` log output or from combined_posterior.json `h_values[argmax(posterior)]` only. |
| event_likelihoods.csv deduplication | CSV accumulates rows across eval runs; latest rows may be zero-filled (off-grid events). Using `keep='last'` silently corrupts log-posterior sums. | advisory | Use per-h `simulations/posteriors/h_*.json` files for any re-aggregation; CSV is for diagnostics only. |
| STAT-04 WARNING location | STAT-04 WARNINGs are NOT in individual per-h `master_thesis_code_*.log` files. They appear only in the consolidated sweep log (`verify03_sweep_20260423T172607Z.log`) with `=== BEGIN h=X ===` section markers. | advisory | Always grep the sweep log, not individual per-h logs, for STAT-04 data. |
| Worktree + git-ignored simulations/ | `simulations/` is git-ignored — worktree agents cannot access it. | blocking | All `--evaluate`, `--combine`, `--generate_*` commands must run from `/home/jasper/Repositories/MasterThesisCode` (main repo root), not from any worktree path. |

<current_state>
Phase 40 plan 40-06 is paused at a human checkpoint (autonomous: false).

Agent a34f7d3dac1aa3d8e executed Tasks 1+2 (assembled verify_gate index + 40-VERIFICATION.md, committed as f249ede). Tasks 3–5 (STATE.md / ROADMAP.md / REQUIREMENTS.md updates + phase-close commit) await user decisions.

User has provided the following decisions — these must be passed to the continuation agent:

DECISION Q1 (VERIFY-03 MAP=0.86): **Option B — Insert fix phase.**
- Independently verify that all angles/sky-positions are now correctly mapped after the COORD fix (equatorial→ecliptic + BallTree polar embedding fix).
- Then check whether including D(h) in `--combine`/`extract_baseline` removes the MAP=0.86 bias.
- User's key question: "If angles were always wrong in v2.1, the bias should have been self-consistent (wrong in both sim+eval), so why does MAP=0.86 appear only after the COORD fix?" This puzzle must be answered by the fix phase.

DECISION Q2 (VERIFY-05 borderline Phase 41): **Accept/Skip for now.**
- Note: 19 of 542 events (3.5%) are 100% off-grid at h=0.73; mean off-grid weight lower bound = 0.041 (threshold 0.05). Potential consequence: small systematic bias from P_det extrapolation, but below auto-trigger threshold.
- Phase 41 skipped; document as known limitation.

DECISION Q3 (VERIFY-04 Phase 42): **Wait for Q1 resolution.**
- Phase 42 routing deferred until the MAP=0.86 bias is diagnosed. The anisotropy result (STAGE-2-TRIGGER on Q3) may be a symptom of the same D(h)/COORD issue rather than genuine sky-dependent P_det.
</current_state>

<completed_work>
Phase 40, Wave 1–3 complete:
- Plan 40-00: v2.1 archive created at simulations/_archive_v2_1_baseline/ ✓
- Plan 40-01: VERIFY-01 PASS — 544 tests, 5/5 D-06 regression items present ✓
- Plan 40-02: VERIFY-02 PASS — MAP shift 0.0000% at h=0.73, abort gate not triggered ✓
- Plan 40-03: VERIFY-03 FAIL — SC-3 MAP=0.860 (expected 0.730±0.010); 37/37 h-values swept, figures regenerated ✓
- Plan 40-04: VERIFY-04 STAGE-2-TRIGGER — Q3 |ΔMAP|=0.020 >> σ=0.0037; anisotropy_audit artifact committed ✓
- Plan 40-05: VERIFY-05 PHASE-41-TRIGGER-BORDERLINE — mean_lb=0.041, 19 events 100% off-grid; quadrature summary committed ✓
- Plan 40-06: Tasks 1+2 committed (f249ede) — verify_gate_20260423T172607Z.md + 40-VERIFICATION.md ✓

Wave 4, 40-06 is paused at checkpoint before phase-close commit.
</completed_work>

<remaining_work>
IMMEDIATE (this session or next):
1. Resume 40-06 agent (a34f7d3dac1aa3d8e) with user decisions above → it will:
   - Record decisions in 40-VERIFICATION.md
   - Update STATE.md: next focus = insert fix phase for D(h) bias + angle verification
   - Update ROADMAP.md: Phase 40 = PARTIAL PASS / GAPS_FOUND; Phase 41 = skipped; Phase 42 = deferred pending Q1
   - Flip REQUIREMENTS.md checkboxes (VERIFY-01..05)
   - Commit phase-close atomically
   - Write 40-06-SUMMARY.md

NEXT PHASE WORK (requires a new phase/plan):
2. Fix phase for VERIFY-03 SC-3 bias (to be planned):
   a. Independent coordinate round-trip audit: verify ecliptic angles in CRBs match what the COORD-fixed galaxy catalog now uses. Check that (qS, phiS) in cramer_rao_bounds.csv are in ecliptic SSB frame (as the v2.2 catalog now expects).
   b. Diagnose D(h) in --combine: check if combined_posterior.json is produced without D(h) denominator. If so, this is the same bug as Phase 32 (L_comp denominator), but in the `--combine`/`_combine_posteriors` code path rather than in `BayesianStatistics.evaluate()`.
   c. If D(h) fix removes bias → [PHYSICS] commit + re-run VERIFY-03.
   d. Understand why v2.1 was unaffected: v2.1 had wrong COORD mapping in both sim+eval (self-consistent). v2.2 COORD fix changed galaxy catalog coordinates — if CRBs (computed pre-fix) still store equatorial angles as (qS, phiS) but the galaxy catalog now expects ecliptic angles, there's a coordinate mismatch in evaluation even though each step is internally consistent.
</remaining_work>

<decisions_made>
- Q1 (VERIFY-03 MAP=0.86): Insert fix phase (Option B). Two independent checks: (1) angle mapping audit, (2) D(h) in --combine.
- Q2 (VERIFY-05 borderline Phase 41): Skip Phase 41 for now. Note 19 off-grid events as known limitation.
- Q3 (VERIFY-04 Phase 42): Defer Phase 42 until Q1 resolved — the anisotropy may be a symptom of the same bug.
- All Wave 3 plans ran sequentially on main working tree (no worktree isolation) due to git-ignored simulations/ directory.
- Shared timestamp 20260423T172607Z used across all Wave 3/4 artifacts.
</decisions_made>

<blockers>
- VERIFY-03 SC-3 FAIL: MAP=0.86 in combined posterior. Working hypothesis: D(h) missing from --combine step AND/OR coordinate mismatch between pre-fix CRBs (equatorial qS/phiS) and post-fix galaxy catalog (ecliptic). Needs a fix phase before Phase 41/42.
- 81 deleted .claude/skills/gsd-* files in working tree (not committed). Pre-existing issue unrelated to Phase 40 work — do not restore or commit these.
</blockers>

## Required Reading (in order)
1. `.planning/phases/40-verification-gate/40-06-PLAN.md` — remaining tasks 3–5 for the continuation agent
2. `.planning/debug/verify_gate_20260423T172607Z.md` — full D-20 verdict table
3. `.planning/phases/40-verification-gate/40-VERIFICATION.md` — SC-1..SC-5 status
4. `.planning/phases/40-verification-gate/40-03-SUMMARY.md` — VERIFY-03 root cause analysis
5. `.planning/debug/anisotropy_audit_20260423T172607Z.md` — VERIFY-04 quartile detail
6. `.planning/debug/pdet_quadrature_summary_20260423T172607Z.md` — VERIFY-05 quadrature detail
7. `master_thesis_code/bayesian_inference/posterior_combination.py` — to understand --combine D(h) handling

## Infrastructure State
- No running servers or watchers.
- `simulations/` is git-ignored: all eval outputs live only in the local working tree.
- 81 deleted `.claude/skills/gsd-*` files in working tree — pre-existing, do not touch.
- 40-06 checkpoint agent a34f7d3dac1aa3d8e is NOT active; resume by spawning a fresh continuation agent with the decisions above and pointing it at the remaining tasks in 40-06-PLAN.md.

<context>
Phase 40 Verification Gate ran clean through Waves 1–3. The surprise is the combined posterior MAP=0.86 after the full h-sweep (VERIFY-03 SC-3 FAIL). This is almost certainly caused by one or both of:
(A) The D(h) completeness-correction denominator being missing from the `--combine`/`_combine_posteriors` code path (same class of bug as the Phase 32 L_comp fix, but in a different code path)
(B) A coordinate frame mismatch: CRBs (computed pre-COORD-fix) store equatorial sky angles as (qS, phiS), but after the COORD fix the galaxy catalog now stores ecliptic angles — so the evaluation code, which queries the fixed catalog using CRB sky angles, finds the wrong galaxies at every h-value. At h=0.86 the distance scale is different enough that some galaxies happen to match, giving a spuriously high likelihood.

The user's insight is key: "If angles were always wrong in v2.1 it should have been self-consistent" — v2.1 sim used wrong COORD in both sim+eval → internally consistent. v2.2 COORD fix changed the galaxy catalog (eval side) but NOT the CRB angles (sim side, already on disk). This breaks the self-consistency.

Both hypotheses need independent verification. The fix phase should start with a coordinate round-trip check (inspect a few CRB rows and verify their qS/phiS against what the v2.2 galaxy catalog expects) before touching any code.
</context>

<next_action>
1. Resume 40-06: spawn a fresh gsd-executor agent for the continuation of plan 40-06. Brief it with: the three user decisions (Q1=B, Q2=skip, Q3=defer), the remaining tasks (3–5: STATE.md/ROADMAP.md/REQUIREMENTS.md updates + phase-close commit + SUMMARY.md), and the files to read listed above.
2. After 40-06 closes: plan the fix phase (angle audit + D(h) in --combine diagnosis + [PHYSICS] fix).
</next_action>
