---
phase: 38-statistical-correctness
plan: 03
type: execute
wave: 3
routing: GSD
depends_on:
  - "38-01-PLAN.md ([PHYSICS] commit for STAT-01/STAT-02 landed)"
  - "38-02-PLAN.md ([PHYSICS] commit for STAT-03/STAT-04 landed)"
files_modified:
  - .planning/phases/38-statistical-correctness/38-SUMMARY.md
  - .planning/STATE.md

interactive: false

conventions:
  units: "N/A (smoke test + state update; no physics computation)"

contract:
  scope:
    question: "Do all 5 Phase 38 Success Criteria pass end-to-end, and is project state advanced to Phase 39?"
  claims:
    - id: claim-sc5-pass
      statement: "--evaluate smoke run on ≤5 events completes without NaN/Inf in the posterior and the diagnostic CSV is non-empty."
      deliverables: [deliv-smoke-result]
      acceptance_tests: [test-smoke-run]
    - id: claim-all-sc-pass
      statement: "All 5 SC-1..SC-5 are verifiably satisfied (grep + unit test + smoke)."
      deliverables: [deliv-summary]
      acceptance_tests: [test-sc1-grep, test-sc2-pytest, test-sc3-grep, test-sc4-grep, test-smoke-run]
    - id: claim-state-advanced
      statement: "STATE.md records Phase 38 as COMPLETE and Phase 39 as NEXT."
      deliverables: [deliv-state-update]
      acceptance_tests: [test-state-grep]
  deliverables:
    - id: deliv-smoke-result
      kind: log
      path: "simulations/h_0_73/ (existing small dataset used as input)"
      description: "stdout of --evaluate run on N≤5 events; no NaN/Inf; diagnostic CSV non-empty"
    - id: deliv-summary
      kind: doc
      path: ".planning/phases/38-statistical-correctness/38-SUMMARY.md"
      description: "Phase summary with SC-1..SC-5 pass/fail table, commit hashes, and regression status"
    - id: deliv-state-update
      kind: doc
      path: ".planning/STATE.md"
      description: "Phase 38 marked COMPLETE; Phase 39 marked NEXT"
  acceptance_tests:
    - id: test-sc1-grep
      subject: claim-all-sc-pass
      kind: smoke
      procedure: "grep -n 'np.mean(ratios_without_bh)\\|np.mean(ratios_with_bh)\\|1908.06050' master_thesis_code/bayesian_inference/bayesian_statistics.py"
      pass_condition: "≥3 matches (2× np.mean, ≥1× arXiv ref). Old ratio-of-sums grep returns 0."
    - id: test-sc2-pytest
      subject: claim-all-sc-pass
      kind: test
      procedure: "uv run pytest master_thesis_code_test/test_l_cat_equivalence.py -v"
      pass_condition: "3/3 tests pass."
    - id: test-sc3-grep
      subject: claim-all-sc-pass
      kind: smoke
      procedure: "grep -n 'detection_probability_without_bh_mass_interpolated[^_]' master_thesis_code/bayesian_inference/bayesian_statistics.py | grep -v zero_fill | grep -E 'numerator_integrant|denominator_integrant'"
      pass_condition: "Zero matches inside single_host_likelihood function body."
    - id: test-sc4-grep
      subject: claim-all-sc-pass
      kind: smoke
      procedure: "grep -n 'quadrature_weight_outside_grid_numerator\\|quadrature_weight_outside_grid_denominator' master_thesis_code/bayesian_inference/bayesian_statistics.py"
      pass_condition: "≥2 matches."
    - id: test-smoke-run
      subject: claim-sc5-pass
      kind: integration
      procedure: "uv run python -m master_thesis_code simulations/smoke_test_38/ --evaluate --h_value 0.73 [with N≤5 events from simulations/h_0_73]"
      pass_condition: "Exits 0; no NaN/Inf in posterior output; diagnostic CSV non-empty."
    - id: test-state-grep
      subject: claim-state-advanced
      kind: smoke
      procedure: "grep -n 'Phase 38\\|Phase 39' .planning/STATE.md"
      pass_condition: "Phase 38 shows COMPLETE; Phase 39 shows NEXT or IN-PROGRESS."
  references:
    - id: ref-roadmap-phase38
      kind: internal
      locator: ".planning/ROADMAP.md §Phase 38 SC-1..SC-5"
    - id: ref-plan-38-01
      kind: internal
      locator: ".planning/phases/38-statistical-correctness/38-01-PLAN.md"
    - id: ref-plan-38-02
      kind: internal
      locator: ".planning/phases/38-statistical-correctness/38-02-PLAN.md"
  forbidden_proxies:
    - "Marking Phase 38 COMPLETE without running the end-to-end smoke test."
    - "Skipping the NaN/Inf check in the smoke run output."
    - "Writing a SUMMARY that says 'SC-5 assumed pass' without actually running the smoke run."
    - "Using from __future__ import annotations."
  uncertainty_markers:
    weakest_anchors:
      - "The smoke run path simulations/h_0_73 may need a symlink or copy for the --evaluate flag. Check the actual data directory layout before running."
      - "The --evaluate flag may require a minimum number of events or a completed simulation directory structure. Read main.py's evaluate() function to confirm what is needed."
    disconfirming_observations:
      - "If --evaluate produces NaN in the posterior for all events (not just some), it likely means the P_det zero-fill change from STAT-03 is truncating too aggressively. Record the fraction of events with non-zero posterior and escalate if <50%."
      - "If the diagnostic CSV is empty, the STAT-04 return value augmentation may not have been propagated to the CSV writer. Check BayesianStatistics.evaluate() for where single_host_likelihood results are collected."
---

<objective>
Verify all 5 Phase 38 Success Criteria pass end-to-end, run the --evaluate smoke test, write the phase SUMMARY, and advance project state to Phase 39.

Purpose: SC-5 (end-to-end smoke run), regression verification (Phase 36 roundtrip tests, Phase 37 regressions), and project bookkeeping. No physics changes — pure verification + state update.
Output: 38-SUMMARY.md with SC-1..SC-5 pass table; STATE.md updated; commit closing Phase 38.
</objective>

<execution_context>
This is Wave 2 of Phase 38. GSD routing — no /physics-change protocol needed (no formula changes).
Depends on both Plan 38-01 and Plan 38-02 being committed.

Commit strategy: ONE chore(38) commit for SUMMARY + STATE update.
Commit prefix: chore(38) — documentation and state update only.

Pre-commit hooks (ruff + mypy + pytest) run automatically.
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-statistical-correctness/38-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SC-1..SC-4 with grep and pytest</name>
  <files></files>
  <action>
    Run each acceptance test in sequence and record pass/fail:

    **SC-1 verification (L_cat formula + reference):**
      grep -n "np.mean(ratios_without_bh)\|np.mean(ratios_with_bh)\|1908.06050" \
        master_thesis_code/bayesian_inference/bayesian_statistics.py
      Expected: ≥3 matches.

      grep -n "likelihood_without_bh_mass / selection_effect_correction_without_bh_mass\|likelihood_with_bh_mass / selection_effect_correction_with_bh_mass" \
        master_thesis_code/bayesian_inference/bayesian_statistics.py
      Expected: ZERO matches.

    **SC-2 verification (unit test):**
      uv run pytest master_thesis_code_test/test_l_cat_equivalence.py -v
      Expected: 3/3 PASS.

    **SC-3 verification (P_det symmetry):**
      grep -n "detection_probability_without_bh_mass_interpolated[^_]" \
        master_thesis_code/bayesian_inference/bayesian_statistics.py
      Check output: any matches inside single_host_likelihood (lines ~1185-1340) must be ZERO.
      (Matches outside single_host_likelihood, e.g., in precompute functions, are expected and acceptable.)

      grep -n "detection_probability_without_bh_mass_interpolated_zero_fill" \
        master_thesis_code/bayesian_inference/bayesian_statistics.py
      Expected: ≥2 matches in single_host_likelihood.

    **SC-4 verification (diagnostic columns):**
      grep -n "quadrature_weight_outside_grid_numerator\|quadrature_weight_outside_grid_denominator" \
        master_thesis_code/bayesian_inference/bayesian_statistics.py
      Expected: ≥2 matches.

    Record each result as PASS or FAIL with the exact grep output.
    If any check fails, stop and report — do NOT proceed to SC-5 until SC-1..SC-4 are GREEN.
  </action>
  <verify>
    All four SC-1..SC-4 checks return expected results:
    - SC-1: ≥3 np.mean/arXiv matches; 0 old-ratio matches.
    - SC-2: 3/3 pytest tests pass.
    - SC-3: 0 bare _interpolated calls inside single_host_likelihood.
    - SC-4: ≥2 diagnostic field matches.
  </verify>
  <done>SC-1..SC-4 all PASS. Proceeding to SC-5 smoke run.</done>
</task>

<task type="auto">
  <name>Task 2: SC-5 — End-to-end --evaluate smoke run</name>
  <files></files>
  <action>
    Run the --evaluate pipeline on a small subset of events.

    First, identify a suitable small dataset. Check:
      ls simulations/h_0_73/
    If this contains Cramer-Rao bound CSV files, use them. If the directory is large,
    create a symlink to a subset or identify the smallest available directory under simulations/.

    Run the smoke test:
      uv run python -m master_thesis_code simulations/h_0_73/ --evaluate --h_value 0.73

    If the above fails due to too many events or memory, run on a subset:
      # Copy/symlink just 5 CSV files to a temp directory and run from there
      mkdir -p /tmp/smoke_38/
      ls simulations/h_0_73/*.csv | head -5 | xargs -I{} cp {} /tmp/smoke_38/
      uv run python -m master_thesis_code /tmp/smoke_38/ --evaluate --h_value 0.73

    Check the output for:
    1. Exit code 0 (no crash).
    2. No NaN or Inf in posterior output: the final posterior log line should show finite values.
       grep -i "nan\|inf" in the output — must return nothing (or only in debug log, not the result).
    3. Diagnostic CSV: check that the output directory contains a diagnostics CSV with
       quadrature_weight columns. If the columns are absent, the STAT-04 return-value propagation
       to the CSV writer was incomplete.

    Record: exact command run, exit code, first/last 20 lines of stdout, whether NaN/Inf appeared,
    whether diagnostic CSV exists and is non-empty, number of events processed.
  </action>
  <verify>
    1. Exit code 0.
    2. No NaN/Inf in posterior output lines (grep confirms).
    3. Diagnostic CSV exists and has ≥1 data row.
    4. If diagnostic CSV lacks quadrature_weight columns: FAIL — escalate with exact CSV headers.
    5. uv run pytest -m "not gpu and not slow" -q — all tests pass (regression check).
  </verify>
  <done>--evaluate smoke run exits 0; posterior is finite; diagnostic CSV non-empty; all pytest regressions GREEN. SC-5 PASS.</done>
</task>

<task type="auto">
  <name>Task 3: Write 38-SUMMARY.md and update STATE.md</name>
  <files>
    .planning/phases/38-statistical-correctness/38-SUMMARY.md
    .planning/STATE.md
  </files>
  <action>
    **Write 38-SUMMARY.md:**

    Create .planning/phases/38-statistical-correctness/38-SUMMARY.md with:
    - Phase 38 summary header (date, phase name, status: COMPLETE)
    - SC-1..SC-5 pass/fail table with evidence (commit hash, grep output, test result)
    - Commit hashes for [PHYSICS] commits from Plan 38-01 and 38-02 (git log --oneline -5)
    - Regression status: Phase 36 roundtrip tests (9/9 GREEN), Phase 37 regressions (GREEN)
    - Known limitations: if with-BH-mass zero_fill variant was not available (TODO comment added), record it
    - Next: Phase 39

    **Update STATE.md:**

    Read the current .planning/STATE.md. Find the Phase 38 entry (or current active phase entry).
    Update to reflect:
    - Phase 38: COMPLETE (date: today)
    - Current active phase: Phase 39 (or whatever ROADMAP.md says is next)
    - Update "last completed" and "next phase" fields per the STATE.md format already in use.

    Read .planning/ROADMAP.md to confirm what Phase 39 is named/targeted at before writing.
  </action>
  <verify>
    1. ls .planning/phases/38-statistical-correctness/38-SUMMARY.md — file exists.
    2. grep "SC-1\|SC-2\|SC-3\|SC-4\|SC-5" .planning/phases/38-statistical-correctness/38-SUMMARY.md — 5 matches.
    3. grep "COMPLETE" .planning/phases/38-statistical-correctness/38-SUMMARY.md — ≥1 match.
    4. grep -n "Phase 38\|Phase 39" .planning/STATE.md — shows Phase 38 COMPLETE, Phase 39 NEXT.
  </verify>
  <done>38-SUMMARY.md written with SC-1..SC-5 table; STATE.md shows Phase 38 COMPLETE and Phase 39 NEXT.</done>
</task>

<task type="auto">
  <name>Task 4: Commit phase bookkeeping as chore(38)</name>
  <files>
    .planning/phases/38-statistical-correctness/38-SUMMARY.md
    .planning/STATE.md
  </files>
  <action>
    Run pre-commit:
      uv run pre-commit run --all-files
    If fails, fix (likely a mypy or ruff issue from prior tasks — resolve before staging).

    Run pytest:
      uv run pytest -m "not gpu and not slow" -x -q
    Must pass.

    Stage only the two files:
      git add .planning/phases/38-statistical-correctness/38-SUMMARY.md
      git add .planning/STATE.md

    Commit message:
    ```
    chore(38): Phase 38 complete — all 5 SCs pass, state advanced to Phase 39

    SC-1: L_cat = (1/N)Σ(N_g/D_g) per Gray et al. (2020) Eq. 24-25 ✓
    SC-2: test_l_cat_equivalence.py 3/3 PASS (counterexample: 2/3 ≠ 3/4) ✓
    SC-3: P_det zero-fill symmetric in single_host_likelihood and D(h) denom ✓
    SC-4: quadrature_weight_outside_grid diagnostic + >5% WARNING ✓
    SC-5: --evaluate smoke run exits 0; posterior finite; diagnostic CSV non-empty ✓

    Phase 36 roundtrip tests: 9/9 GREEN
    Phase 37 regressions: GREEN
    Total pytest: ≥517 passing

    REQ-IDs: STAT-01, STAT-02, STAT-03, STAT-04
    Milestone: v2.2 Pipeline Correctness (phases 35-42, partial)
    ```

    Do NOT amend existing commits. Do NOT use --no-verify.
  </action>
  <verify>
    1. git log -1 --format=%s shows "chore(38): Phase 38 complete..." subject line.
    2. git show --stat HEAD lists exactly the two files above.
    3. uv run pytest -m "not gpu and not slow" -q — all tests pass.
    4. grep "COMPLETE" .planning/STATE.md — Phase 38 COMPLETE confirmed.
  </verify>
  <done>chore(38) commit landed. Phase 38 fully closed. All 5 SCs verified. STATE.md points at Phase 39. v2.2 milestone partially advanced.</done>
</task>

</tasks>

<verification>
- No physics formula changes in this plan — chore(38) commit prefix correct.
- SC-1..SC-4 verified by grep and pytest before smoke run.
- SC-5 smoke run: --evaluate on ≤5 events; exit code 0; no NaN/Inf; diagnostic CSV non-empty.
- Phase 36 regression: 9 roundtrip tests must remain GREEN throughout.
- Phase 37 regression: test_parameter_space_h.py and other Phase 37 tests must remain GREEN.
- SUMMARY.md records commit hashes for both [PHYSICS] commits so the fix is traceable.
</verification>

<success_criteria>
- chore(38) commit landed with all 4 REQ-IDs in message.
- `uv run pytest -m "not gpu and not slow"` passes (≥517 tests, all GREEN).
- 38-SUMMARY.md exists with SC-1..SC-5 all marked PASS.
- STATE.md: Phase 38 COMPLETE, Phase 39 NEXT.
- --evaluate smoke run completed without NaN/Inf and with non-empty diagnostic CSV.
- Milestone v2.2 Pipeline Correctness: phases 35-38 complete.
</success_criteria>

<output>
Phase 38 complete. Next: Phase 39 per .planning/ROADMAP.md.
Run /gsd:execute-phase 39 (or /gpd:execute-phase 39 if physics work) after clearing context.
</output>
