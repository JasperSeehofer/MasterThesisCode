---
phase: 17-injection-physics-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gpd/phases/17-injection-physics-audit/audit-parameter-consistency.md
  - .gpd/phases/17-injection-physics-audit/audit-cosmological-model.md
  - .gpd/phases/17-injection-physics-audit/test_round_trip.py
interactive: false

conventions:
  units: "SI (distances in Gpc, masses in solar masses, h dimensionless)"
  cosmology: "Flat LambdaCDM, Omega_m=0.25, Omega_DE=0.75, w0=-1, wa=0 (WMAP-era, internally consistent)"
  hubble: "Dimensionless h = H0/(100 km/s/Mpc), fiducial h=0.73"

dimensional_check:
  d_L: "[Gpc]"
  z: "[dimensionless]"
  M: "[solar masses, source-frame]"
  round_trip_error: "[dimensionless, relative]"

approximations:
  - name: "Analytic LambdaCDM distance via hypergeometric 2F1"
    parameter: "Omega_m=0.25, Omega_DE=0.75"
    validity: "z in [0, ~10] for flat LambdaCDM"
    breaks_when: "wCDM (w0 != -1 or wa != 0) -- known bug, wCDM params silently ignored"
    check: "dist(z=0, h) = 0 for all h; dist proportional to 1/h at fixed z"

contract:
  scope:
    question: "Are the injection campaign parameter distributions and cosmological model consistent with the simulation pipeline, and is the d_L-to-z round-trip inversion numerically accurate?"

  claims:
    - id: claim-param-identical
      statement: "injection_campaign() and data_simulation() draw from identical EMRI parameter distributions (M, z, spin, eccentricity) via the shared Model1CrossCheck instance"
      deliverables: [deliv-param-report]
      acceptance_tests: [test-param-linebyline]
      references: [ref-design-decisions, ref-injection-code, ref-simulation-code]
    - id: claim-cosmo-consistent
      statement: "dist() is called with identical cosmological parameters (Omega_m, Omega_DE) in both pipelines; the only h difference is intentional per D-04"
      deliverables: [deliv-cosmo-report]
      acceptance_tests: [test-cosmo-defaults, test-h-intentional]
      references: [ref-design-decisions, ref-dist-function]
    - id: claim-roundtrip-accurate
      statement: "The d_L-to-z round-trip z -> dist(z, h) -> dist_to_redshift(d_L, h) -> z_recovered is accurate to 1e-4 relative error for z in [0.001, 0.5] and all 7 h-values"
      deliverables: [deliv-roundtrip-test]
      acceptance_tests: [test-roundtrip-accuracy]
      references: [ref-dist-function, ref-hogg1999]

  deliverables:
    - id: deliv-param-report
      kind: report
      path: ".gpd/phases/17-injection-physics-audit/audit-parameter-consistency.md"
      description: "Line-by-line comparison of parameter flow in injection_campaign() vs data_simulation(), citing specific code lines for every comparison"
      must_contain:
        - "Model1CrossCheck instance sharing verification"
        - "M sampling comparison (injection direct vs simulation via galaxy catalog)"
        - "Extrinsic parameter (phiS, qS) handling comparison"
        - "z_cut=0.5 documentation"
        - "Each difference categorized as intentional (D-XX) or unintentional"
    - id: deliv-cosmo-report
      kind: report
      path: ".gpd/phases/17-injection-physics-audit/audit-cosmological-model.md"
      description: "Cosmological model consistency check: dist() default arguments, h-handling in both pipelines, ParameterSample.get_distance() call-site audit"
      must_contain:
        - "dist() default argument values (Omega_m, Omega_DE, w_0, w_a)"
        - "All call sites of dist() in injection path with h argument"
        - "All call sites of dist() in simulation path with default h"
        - "get_distance() call-site grep confirming no injection usage"
        - "Impact assessment of h-handling difference"
    - id: deliv-roundtrip-test
      kind: code
      path: ".gpd/phases/17-injection-physics-audit/test_round_trip.py"
      description: "Standalone Python script testing d_L-to-z round-trip for 7 h-values x 100 z-points, reporting max relative error per h"
      must_contain:
        - "h_values = [0.60, 0.65, 0.70, 0.73, 0.80, 0.85, 0.90]"
        - "z_test = np.linspace(0.001, 0.5, 100)"
        - "dist() and dist_to_redshift() round-trip"
        - "Assertion: relative error < 1e-4"
        - "Edge case: z=0.001 (low z fsolve convergence)"

  references:
    - id: ref-design-decisions
      kind: prior_artifact
      locator: ".planning/phases/11.1-simulation-based-detection-probability/11.1-CONTEXT.md"
      aliases: ["Phase 11.1 D-01 through D-09"]
      role: definition
      why_it_matters: "Defines intentional differences between injection and simulation pipelines -- audit must verify implementation matches these decisions"
      applies_to: [claim-param-identical, claim-cosmo-consistent]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-injection-code
      kind: prior_artifact
      locator: "master_thesis_code/main.py (injection_campaign function)"
      role: definition
      why_it_matters: "Primary audit target -- the injection code path"
      applies_to: [claim-param-identical]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-simulation-code
      kind: prior_artifact
      locator: "master_thesis_code/main.py (data_simulation function)"
      role: definition
      why_it_matters: "Reference code path -- the ground truth simulation pipeline"
      applies_to: [claim-param-identical]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-dist-function
      kind: prior_artifact
      locator: "master_thesis_code/physical_relations.py (dist and dist_to_redshift functions)"
      role: method
      why_it_matters: "The cosmological distance function whose consistency is being audited"
      applies_to: [claim-cosmo-consistent, claim-roundtrip-accurate]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-hogg1999
      kind: paper
      locator: "Hogg (1999) astro-ph/9905116 Eq. 16"
      role: benchmark
      why_it_matters: "Reference formula for d_L(z, h) -- dist(z=0) = 0 and low-z scaling d_L ~ cz/H0"
      applies_to: [claim-roundtrip-accurate]
      must_surface: false
      required_actions: [cite]

  acceptance_tests:
    - id: test-param-linebyline
      subject: claim-param-identical
      kind: consistency
      procedure: "For each of the 14 EMRI parameters, trace the value-setting code path in both injection_campaign() and data_simulation(). Document the source of each parameter value (Model1CrossCheck, ParameterSpace.randomize_parameters, galaxy catalog, or direct assignment) with specific line numbers."
      pass_condition: "All parameters except d_L and sky angles (phiS, qS) originate from the same source; d_L difference is intentional per D-04; sky angle difference (random vs catalog) is intentional per design; any unexpected difference is flagged with impact assessment"
      evidence_required: [deliv-param-report, ref-design-decisions, ref-injection-code, ref-simulation-code]
      automation: automated
    - id: test-cosmo-defaults
      subject: claim-cosmo-consistent
      kind: consistency
      procedure: "Inspect dist() function signature for default values of Omega_m, Omega_de, w_0, w_a. Grep all call sites in injection and simulation paths. Verify no call overrides these defaults (except h)."
      pass_condition: "All dist() calls use Omega_m=0.25, Omega_DE=0.75, w_0=-1, w_a=0 (matching constants.py). Only h varies between paths."
      evidence_required: [deliv-cosmo-report, ref-dist-function]
      automation: automated
    - id: test-h-intentional
      subject: claim-cosmo-consistent
      kind: consistency
      procedure: "Verify that injection calls dist(z, h=h_value) with variable h and simulation calls dist(z) with default h=0.73. Cross-reference with D-04."
      pass_condition: "The h-handling difference matches D-04 exactly. No other dist() argument differs."
      evidence_required: [deliv-cosmo-report, ref-design-decisions]
      automation: automated
    - id: test-roundtrip-accuracy
      subject: claim-roundtrip-accurate
      kind: benchmark
      procedure: "Run test_round_trip.py: for each h in {0.60, 0.65, 0.70, 0.73, 0.80, 0.85, 0.90}, for z in linspace(0.001, 0.5, 100), compute d_L = dist(z, h), z_rec = dist_to_redshift(d_L, h), rel_error = |z - z_rec|/z."
      pass_condition: "max(rel_error) < 1e-4 for all h-values. If any edge case fails, document the (z, h) pair and the actual error."
      evidence_required: [deliv-roundtrip-test, ref-dist-function]
      automation: automated

  forbidden_proxies:
    - id: fp-looks-similar
      subject: claim-param-identical
      proxy: "Claiming parameters are identical because the code 'looks similar' without citing specific line numbers"
      reason: "The contract explicitly requires line-by-line comparison. Vague similarity claims miss subtle wiring differences (e.g., galaxy catalog intermediary)."
    - id: fp-skip-galaxy-catalog
      subject: claim-param-identical
      proxy: "Comparing only direct parameter assignments without tracing through the galaxy catalog intermediary in the simulation path"
      reason: "The simulation routes parameters through GalaxyCatalogueHandler which may modify M, phiS, qS, z. Skipping this step misses the largest structural difference."
    - id: fp-roundtrip-qualitative
      subject: claim-roundtrip-accurate
      proxy: "Saying the round-trip 'works' without reporting actual numerical errors per h-value"
      reason: "Quantitative error bounds are required. A qualitative statement provides no evidence for the 1e-4 threshold."

  links:
    - id: link-param-to-report
      source: claim-param-identical
      target: deliv-param-report
      relation: supports
      verified_by: [test-param-linebyline]
    - id: link-cosmo-to-report
      source: claim-cosmo-consistent
      target: deliv-cosmo-report
      relation: supports
      verified_by: [test-cosmo-defaults, test-h-intentional]
    - id: link-roundtrip-to-test
      source: claim-roundtrip-accurate
      target: deliv-roundtrip-test
      relation: computes
      verified_by: [test-roundtrip-accuracy]

  uncertainty_markers:
    weakest_anchors:
      - "Phase 11.1 design decisions D-01 through D-09 may not cover every intentional difference -- undocumented intentional differences would be flagged as bugs"
    disconfirming_observations:
      - "A dist() call site in the injection path that overrides Omega_m or Omega_DE (not just h) would indicate a cosmological model inconsistency"
      - "Round-trip error exceeding 1e-4 at low z would indicate fsolve convergence issues requiring a code fix"

estimated_execution:
  total_minutes: 50
  breakdown:
    - task: 1
      minutes: 30
      note: "Code reading and line-by-line comparison of two ~200-line functions plus dependency tracing"
    - task: 2
      minutes: 20
      note: "Round-trip test implementation, dist() call-site audit, and cosmological model comparison"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made: []
---

<objective>
Verify that the injection campaign parameter distributions and cosmological model are consistent with the simulation pipeline, and that the d_L-to-z round-trip inversion is numerically accurate.

Purpose: This is the foundation for the entire v1.2.2 milestone. If injection parameters or cosmological models differ from the simulation pipeline (beyond intentional D-04 h-handling), the P_det grids built from injection data would be inconsistent with the evaluation pipeline, leading to biased H0 posteriors.

Output: Parameter consistency report (AUDT-01), cosmological model consistency report (AUDT-02), round-trip numerical test script with results.
</objective>

<execution_context>
@/home/jasper/.claude/get-physics-done/workflows/execute-plan.md
@/home/jasper/.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/17-injection-physics-audit/17-RESEARCH.md
@.planning/phases/11.1-simulation-based-detection-probability/11.1-CONTEXT.md
@master_thesis_code/main.py
@master_thesis_code/physical_relations.py
@master_thesis_code/cosmological_model.py
@master_thesis_code/datamodels/parameter_space.py
@master_thesis_code/galaxy_catalogue/handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Line-by-line parameter distribution comparison (AUDT-01)</name>
  <files>
    .gpd/phases/17-injection-physics-audit/audit-parameter-consistency.md
  </files>
  <action>
    Perform a systematic line-by-line comparison of parameter flow in injection_campaign() (main.py:396-572) vs data_simulation() (main.py:188-383).

    For EACH of the following audit axes, read the relevant code, cite specific line numbers, and document the comparison:

    1. **Model1CrossCheck instance sharing:** Verify that main() creates one Model1CrossCheck instance and passes it to both functions. Confirm _apply_model_assumptions() runs once at construction, setting M in [10^4.5, 10^6], a=0.98, mu=10, e0_max=0.2, max_z=1.5. Cite the lines in main() where the model is created and passed.

    2. **M sampling path:** In injection: trace sample_emri_events() -> ParameterSample.M -> parameter_space.M.value = sample.M. In simulation: trace sample_emri_events() -> ParameterSample -> GalaxyCatalogueHandler.get_hosts_from_parameter_samples() -> HostGalaxy -> set_host_galaxy_parameters() -> parameter_space.M.value = host_galaxy.M. Document the galaxy catalog intermediary step where M may be modified.

    3. **Redshift/d_L computation:** In injection: dist(sample.redshift, h=h_value). In simulation: set_host_galaxy_parameters() calls dist(host_galaxy.z) with default h=0.73. Categorize the h difference as intentional per D-04.

    4. **Extrinsic parameters (phiS, qS):** In injection: randomize_parameters(rng=rng) sets all extrinsic params; phiS and qS come from the uniform random distribution. In simulation: set_host_galaxy_parameters() sets phiS = host_galaxy.phi, qS = host_galaxy.theta from the GLADE galaxy catalog. Document this difference and confirm it is intentional (injection marginalizes over sky angles for P_det).

    5. **Eccentricity and spin:** Verify both paths use the same randomize_parameters() for e0 (uniform in [0, 0.2]) and the same fixed a=0.98 from _apply_model_assumptions().

    6. **z_cut = 0.5:** Document that injection_campaign() applies z_cut = 0.5 (cite the line) and data_simulation() has no such cut. Categorize as intentional for P_det grid efficiency.

    7. **Error handling:** Compare exception types caught in both paths (TimeoutError, ParameterOutOfBoundsError, etc.).

    Write the report as a structured markdown document with a summary table at the top:
    | Parameter | Injection Source | Simulation Source | Match? | Category |
    followed by detailed per-axis analysis with line number citations.

    CRITICAL: Read handler.py to understand how GalaxyCatalogueHandler modifies parameters. Do NOT skip the galaxy catalog intermediary.
  </action>
  <verify>
    1. Every one of the 14 EMRI parameters has an entry in the comparison table
    2. Every code citation includes a specific line number (not just "main.py")
    3. Every difference is categorized as "intentional per D-XX" or "unintentional discrepancy"
    4. The galaxy catalog intermediary (GalaxyCatalogueHandler) is explicitly traced
    5. The z_cut=0.5 is documented with its line number
    6. get_distance() call sites are grepped to confirm none appear in the injection path (research question 3)
  </verify>
  <done>Parameter consistency report is complete with line-by-line citations for all 14 parameters, every difference categorized against D-01 through D-09, and the galaxy catalog intermediary fully traced</done>
</task>

<task type="auto">
  <name>Task 2: Cosmological model consistency and d_L round-trip test (AUDT-02)</name>
  <files>
    .gpd/phases/17-injection-physics-audit/audit-cosmological-model.md
    .gpd/phases/17-injection-physics-audit/test_round_trip.py
  </files>
  <action>
    Part A: Cosmological model consistency audit.

    1. Read physical_relations.py and document the dist() function signature with all default argument values: Omega_m, Omega_de, h, w_0, w_a.
    2. Grep all call sites of dist() across the entire codebase. For each call site, record: file, line number, arguments passed (especially whether h is explicit or default).
    3. Grep all call sites of get_distance() and ParameterSample.get_distance(). Verify NONE appear in the injection_campaign() code path. If any do, flag as a potential bug (would use default h=0.73 instead of candidate h).
    4. Verify that the dist() defaults match constants.py values: Omega_m=OMEGA_M, h=H.
    5. Document the intentional h-handling difference per D-04: injection uses dist(z, h=h_value), simulation uses dist(z) with default h=0.73.
    6. Assess impact: for h=0.60 vs h=0.90, compute d_L(z=0.3, h) to show the magnitude of the h-dependence. This quantifies how much the injection d_L differs from the simulation d_L at a representative redshift.

    Part B: d_L-to-z round-trip numerical test.

    Write test_round_trip.py that:
    1. Imports dist and dist_to_redshift from physical_relations
    2. Tests h_values = [0.60, 0.65, 0.70, 0.73, 0.80, 0.85, 0.90]
    3. Tests z_test = np.linspace(0.001, 0.5, 100)
    4. For each (h, z) pair: d_L = dist(z, h=h), z_rec = dist_to_redshift(d_L, h=h), rel_error = abs(z - z_rec) / z
    5. Reports: max relative error per h-value, worst-case (z, h) pair, overall pass/fail at 1e-4 threshold
    6. Tests edge cases: z=0.001 (fsolve convergence at low z), z=0.5 (upper bound of injection range)
    7. Tests dist(z=0, h) = 0 for all h (limiting case)
    8. Tests low-z scaling: dist(z=0.001, h) approx c*z/(100*h) in Gpc (Hubble law)

    Run the test script and include the output in the cosmological model report.

    Write the cosmological model report documenting both Part A findings and Part B results.
  </action>
  <verify>
    1. dist(z=0, h) = 0 for all 7 h-values (limiting case)
    2. Round-trip relative error < 1e-4 for all 700 test points (7 h x 100 z)
    3. Low-z scaling: dist(0.001, h) is within 5% of c*0.001/(100*h) Gpc (Hubble law approximation)
    4. All dist() call sites documented with file:line and argument list
    5. No get_distance() call found in injection path
    6. Impact assessment includes numerical d_L values showing h-dependence magnitude
  </verify>
  <done>Cosmological model report complete with all dist() call sites audited, h-handling difference documented per D-04, round-trip test passes at 1e-4 for all 7 h-values, and edge cases verified</done>
</task>

</tasks>

<verification>
- Parameter distributions: all 14 parameters traced through both code paths with line numbers
- Cosmological model: dist() arguments identical except intentional h-handling per D-04
- Round-trip: z -> d_L(z, h) -> z_recovered accurate to 1e-4 across full (z, h) grid
- Limiting cases: dist(z=0) = 0, low-z Hubble law scaling
- Design decisions D-01 through D-09 are explicitly referenced for every intentional difference
- No get_distance() usage in injection path (would bypass h-dependence)
</verification>

<success_criteria>
1. Parameter consistency report documents all 14 EMRI parameters with line-by-line citations; every difference categorized as intentional or unintentional
2. Cosmological model report confirms dist() called with identical defaults (except h) in both pipelines
3. Round-trip test script runs and reports max relative error < 1e-4 for all 7 h-values across z in [0.001, 0.5]
4. dist(z=0, h) = 0 verified for all h
5. No unintentional parameter discrepancies found (or all are flagged with impact assessment)
</success_criteria>

<output>
After completion, create `.gpd/phases/17-injection-physics-audit/17-01-SUMMARY.md`.
</output>
