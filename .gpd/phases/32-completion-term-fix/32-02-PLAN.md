---
phase: 32-completion-term-fix
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - master_thesis_code/main.py
  - master_thesis_code/bayesian_inference/bayesian_statistics.py
interactive: true

conventions:
  units: "SI (distances in Gpc, masses in solar masses, h dimensionless)"
  metric: "mostly-plus"
  coordinates: "spherical"
  distance: "luminosity distance d_L in Gpc"

dimensional_check:
  MAP_h: "[dimensionless] -- peak of posterior p(h|data)"
  bias: "[dimensionless] -- (MAP_h - 0.73) / 0.73"

approximations:
  - name: "P_det = 0 beyond injection grid"
    parameter: "N/A"
    validity: "Always"
    breaks_when: "Never"
    check: "Inherited from Plan 01"
  - name: "4-sigma local window for numerator"
    parameter: "sigma_dL / d_L << 1"
    validity: "Fisher matrix accuracy at SNR >= 20"
    breaks_when: "Non-Gaussian posteriors"
    check: "Inherited from Plan 01"

estimated_execution:
  total_minutes: 45
  breakdown:
    - task: 1
      minutes: 25
      note: "Run evaluation with fix, extract MAP, compare before/after, bias-vs-N convergence"
    - task: 2
      minutes: 20
      note: "Per-event L_comp decomposition analysis, checkpoint for user review"

contract:
  schema_version: 1
  scope:
    question: "Does the full-volume denominator fix reduce the H0 posterior bias toward the true value h=0.73, with bias shrinking as ~1/sqrt(N)?"
  claims:
    - id: claim-map-shift
      statement: "MAP H0 shifts closer to h=0.73 for both 'without BH mass' and 'with BH mass' channels after the fix"
      deliverables: [deliv-map-comparison]
      acceptance_tests: [test-map-shift]
      references: [ref-gray2020, ref-production-baseline]
    - id: claim-bias-convergence
      statement: "Bias shrinks as ~1/sqrt(N) when combining more events, confirming the per-event systematic is removed"
      deliverables: [deliv-bias-vs-n]
      acceptance_tests: [test-bias-vs-n]
      references: [ref-gray2020]
    - id: claim-lcomp-decomposition
      statement: "Per-event L_comp is less h-dependent after the fix (reduced U-shaped anti-correlation observed in debug investigation)"
      deliverables: [deliv-lcomp-decomp]
      acceptance_tests: [test-lcomp-shape]
      references: [ref-debug-investigation]
  deliverables:
    - id: deliv-map-comparison
      kind: data
      path: ".gpd/phases/32-completion-term-fix/validation/map_comparison.json"
      description: "MAP and CI values before and after fix for both channels"
      must_contain: ["MAP_before", "MAP_after", "bias_before", "bias_after"]
    - id: deliv-bias-vs-n
      kind: data
      path: ".gpd/phases/32-completion-term-fix/validation/bias_vs_n.json"
      description: "Bias values at N=10,50,100,200,531 events for both channels"
      must_contain: ["N_values", "bias_without_bh", "bias_with_bh"]
    - id: deliv-lcomp-decomp
      kind: data
      path: ".gpd/phases/32-completion-term-fix/validation/lcomp_decomposition.json"
      description: "Per-event L_comp, L_cat, f_i values across h grid for representative events"
      must_contain: ["events", "h_values", "L_comp", "L_cat", "f_i"]
  references:
    - id: ref-gray2020
      kind: paper
      locator: "Gray et al. (2020), arXiv:1908.06050, Eqs. 9, A.19"
      aliases: ["Gray2020"]
      role: definition
      why_it_matters: "Defines the correct full-volume normalization whose implementation this plan validates"
      applies_to: [claim-map-shift, claim-bias-convergence]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [compare, cite]
    - id: ref-production-baseline
      kind: prior_artifact
      locator: "cluster_results/eval_corrected_full"
      aliases: ["production-baseline"]
      role: benchmark
      why_it_matters: "Current baseline MAP h=0.66/0.68 with 531/527 events -- the before comparison"
      applies_to: [claim-map-shift]
      carry_forward_to: [verification]
      must_surface: true
      required_actions: [compare]
    - id: ref-debug-investigation
      kind: prior_artifact
      locator: ".gpd/debug/h0-posterior-bias-worsening.md"
      aliases: ["bias-debug"]
      role: background
      why_it_matters: "Documents the U-shaped L_comp anti-correlation that should be reduced after fix"
      applies_to: [claim-lcomp-decomposition]
      carry_forward_to: []
      must_surface: true
      required_actions: [compare]
  acceptance_tests:
    - id: test-map-shift
      subject: claim-map-shift
      kind: benchmark
      procedure: "Run full evaluation with fixed code on same production data (531 events). Compare MAP before and after."
      pass_condition: "MAP shifts toward 0.73 by at least 0.01 (i.e., new MAP > 0.67 for 'without BH mass' channel). If MAP shift < 0.01, the fix is not targeting the dominant bias source."
      evidence_required: [deliv-map-comparison, ref-production-baseline]
      automation: automated
    - id: test-bias-vs-n
      subject: claim-bias-convergence
      kind: convergence
      procedure: "Compute posterior for subsets of N=10,50,100,200,531 events (cumulative from the first N). Extract MAP for each N. Compute bias = (MAP - 0.73) / 0.73. Check that |bias| decreases roughly as 1/sqrt(N)."
      pass_condition: "Bias at N=531 is smaller in magnitude than bias at N=50. No reversal (bias improving then worsening)."
      evidence_required: [deliv-bias-vs-n]
      automation: automated
    - id: test-lcomp-shape
      subject: claim-lcomp-decomposition
      kind: consistency
      procedure: "Extract L_comp(h) for 5-10 representative events from diagnostic CSV. Compare shape across h values."
      pass_condition: "L_comp variation across h is reduced compared to pre-fix diagnostic (U-shaped anti-correlation should be weaker or absent)"
      evidence_required: [deliv-lcomp-decomp, ref-debug-investigation]
      automation: hybrid
  forbidden_proxies:
    - id: fp-normalization-bug-map
      subject: claim-map-shift
      proxy: "MAP shift that comes from breaking the f_i=1 limiting case (catalog-only regression failure)"
      reason: "A MAP shift from accidentally modifying L_cat would be false progress"
    - id: fp-single-n-bias
      subject: claim-bias-convergence
      proxy: "Bias that improves at small N but worsens at large N"
      reason: "A correct fix must monotonically reduce bias with N; non-monotonic behavior indicates a compensating error"
    - id: fp-map-away
      subject: claim-map-shift
      proxy: "MAP moves AWAY from 0.73 (bias gets worse)"
      reason: "If the fix makes bias worse, it is introducing a new systematic, not correcting the old one"
  uncertainty_markers:
    weakest_anchors:
      - "L_cat itself carries bias (catalog-only shows -17.8% bias at 60 events). The L_comp fix addresses the 79%-weight completion term but cannot fix L_cat bias."
      - "The production data baseline (cluster_results/eval_corrected_full) was generated with SNR_THRESHOLD=15 and a specific set of detections. The fix must be validated on the same data."
    disconfirming_observations:
      - "If MAP shift < 0.01, the completion term normalization is NOT the dominant bias source -- investigate L_cat."
      - "If bias worsens at large N, a new per-event systematic was introduced by the fix."
      - "If >10% of events produce zero or NaN L_comp after the fix, the P_det boundary handling is broken."
---

<objective>
Validate the completion term fix by running the evaluation pipeline on production data and checking that the H0 posterior bias is reduced.

Purpose: Confirm that the full-volume denominator D(h) reduces the systematic low-h bias per the contract acceptance criteria. Produce the three user-requested validation outputs: MAP comparison, bias-vs-N convergence, and per-event L_comp decomposition.

Output: MAP comparison (before vs after), bias-vs-N convergence data, per-event L_comp decomposition, validation JSON files in `.gpd/phases/32-completion-term-fix/validation/`.
</objective>

<execution_context>
@/home/jasper/.claude/get-physics-done/workflows/execute-plan.md
@/home/jasper/.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/phases/32-completion-term-fix/32-CONTEXT.md
@.gpd/phases/32-completion-term-fix/32-RESEARCH.md
@.gpd/phases/32-completion-term-fix/32-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run evaluation and extract MAP comparison + bias-vs-N convergence</name>
  <files>
    master_thesis_code/main.py
    master_thesis_code/bayesian_inference/bayesian_statistics.py
  </files>
  <action>
    Run the evaluation pipeline with the fixed code on the existing production data. The evaluation uses `--evaluate` CLI flag which calls BayesianStatistics.evaluate().

    **Step 1: Locate production data**
    - Find the prepared CRB data: check PREPARED_CRAMER_RAO_BOUNDS_PATH in constants.py
    - Find the injection data: check INJECTION_DATA_DIR in constants.py
    - Verify data exists and has the expected number of detections (~531)
    - If data is not locally available (cluster-only), check cluster_results/eval_corrected_full for pre-computed posteriors and use the existing evaluation infrastructure

    **Step 2: Run full evaluation with the fix**
    - Run: `uv run python -m master_thesis_code <working_dir> --evaluate --h_value 0.73`
    - This evaluates the posterior at h=0.73 and surrounding h values from the grid
    - The diagnostic CSV will be written to simulations/diagnostics/event_likelihoods.csv
    - Posterior JSONs will be written to simulations/posteriors/ and simulations/posteriors_with_bh_mass/

    **Step 3: Extract MAP comparison**
    - Load the posterior data (JSON files in simulations/posteriors/)
    - Find MAP = argmax over h of the combined posterior
    - Compare with baseline: MAP_before = 0.66 (without BH mass), 0.68 (with BH mass) from cluster_results/eval_corrected_full
    - Compute bias_before = (MAP_before - 0.73) / 0.73 and bias_after = (MAP_after - 0.73) / 0.73
    - Save to .gpd/phases/32-completion-term-fix/validation/map_comparison.json

    **Step 4: Bias-vs-N convergence**
    - Add a temporary diagnostic mode (or modify the evaluation to support it):
      - Run evaluation with first N=10, 50, 100, 200, 531 detections
      - For each N, compute the combined posterior and extract MAP
      - Compute bias(N) = (MAP(N) - 0.73) / 0.73
    - Implementation options (choose the most practical):
      a) Add a `--max_detections N` CLI flag that truncates cramer_rao_bounds after filtering
      b) Or: post-process by multiplying posterior contributions for subsets
      c) Or: modify BayesianStatistics.p_D to accept a max_count parameter
    - Save to .gpd/phases/32-completion-term-fix/validation/bias_vs_n.json
    - Red flag: if |bias| at N=531 > |bias| at N=50, the fix is not working

    **Step 5: Check stop conditions**
    - If MAP moves AWAY from 0.73 (bias > -10% in magnitude, i.e., worse than -0.10): STOP and report
    - If >10% of events produce zero or NaN L_comp: STOP and report
    - If catalog-only output changes: STOP and report (regression)

    **IMPORTANT:** If the production CRB data is not available locally, this task must adapt:
    - Check if cluster_results/ contains the needed CSVs
    - If not, create a synthetic test with a small number of events to verify the pipeline runs correctly, and document that a full production run on the cluster is needed
    - The key deliverable in that case is: the code runs without errors, D(h) is precomputed correctly, and the pipeline produces output
  </action>
  <verify>
    1. Evaluation pipeline runs without errors with the fixed code
    2. D(h) values are logged and look reasonable (positive, varying smoothly with h, not varying >10x)
    3. MAP_after is closer to 0.73 than MAP_before for at least one channel
    4. Bias-vs-N shows decreasing |bias| with increasing N (no reversal)
    5. No events produce NaN or zero L_comp (or < 10% if some do)
    6. Diagnostic CSV is written with L_comp, L_cat, f_i columns
    7. map_comparison.json and bias_vs_n.json written to validation directory
  </verify>
  <done>Evaluation complete. MAP comparison extracted (before vs after). Bias-vs-N convergence data for N=10,50,100,200,531. Stop conditions checked. Validation JSONs written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review validation results and per-event L_comp decomposition</name>
  <what-built>
    Full validation of the completion term fix:
    1. MAP comparison: before vs after fix for both channels
    2. Bias-vs-N convergence: bias at N=10,50,100,200,531
    3. Per-event L_comp decomposition from diagnostic CSV
    4. D(h) precomputed values across h grid

    Key results:
    - MAP shift: [reported by Task 1]
    - Bias reduction: [reported by Task 1]
    - D(h) range: [reported by Task 1]
    - Flagged events (grid coverage): [reported by Task 1]
  </what-built>
  <how-to-verify>
    Review the following:

    1. **MAP comparison:** Does MAP move toward 0.73? By how much? Is the improvement significant (> 0.01)?
    2. **Bias-vs-N:** Does the bias shrink monotonically with N? Is the rate consistent with ~1/sqrt(N)?
    3. **Per-event L_comp:** Sample 5-10 events from the diagnostic CSV. Plot L_comp(h) across h values. Is the U-shaped anti-correlation reduced compared to the pre-fix behavior documented in .gpd/debug/h0-posterior-bias-worsening.md?
    4. **D(h) values:** Are they physically reasonable? Do they vary smoothly with h?
    5. **Flagged events:** How many events have 4-sigma windows exceeding the P_det grid? Is it < 10%?

    If MAP shift < 0.01: The completion term normalization is NOT the dominant bias source. Consider investigating L_cat bias (galaxy search window, MVN issues).

    If bias worsens at large N: The fix introduced a new systematic. Revert and investigate.

    If MAP overcorrects (bias flips sign): The full-volume normalization may be overweighting high-z. Consider rate-weighted prior (deferred).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the fix, or describe issues and next steps</resume-signal>
</task>

</tasks>

<verification>
- MAP shifts toward 0.73 for both channels (primary acceptance criterion)
- Bias-vs-N shows ~1/sqrt(N) convergence (secondary)
- Per-event L_comp is less h-dependent (reduced systematic per-event bias)
- No regression in catalog-only mode
- D(h) values are physically reasonable
- < 10% of events flagged for grid coverage issues
</verification>

<success_criteria>
MAP H0 shifts closer to h=0.73 by at least 0.01 for at least one channel. Bias-vs-N shows monotonically decreasing |bias|. Per-event L_comp decomposition confirms reduced h-dependence. User approves validation results at checkpoint.
</success_criteria>

<output>
After completion, create `.gpd/phases/32-completion-term-fix/32-02-SUMMARY.md`
</output>
