---
phase: 45-p-det-first-bin-asymptote-fix
plan: 03
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - cluster/submit_phase45_eval.sh
  - .gpd/phases/45-p-det-first-bin-asymptote-fix/45-03-ACCEPTANCE.md
interactive: true
gap_closure: false

conventions:
  units: "natural (Gpc, h_100)"

dimensional_check:
  MAP_h: "[dimensionless] ∈ [0.5, 1.0] (h_100 grid range)"
  interval_68: "[dimensionless] tuple (lo, hi) ∈ [0.5, 1.0]^2 with lo ≤ hi"
  delta_MAP_vs_phase44: "[dimensionless] expected ∈ [-0.045, -0.025] (Plan 45-02 prediction)"
  cluster_walltime_hours: "[time] expected ≤ 4 h per evaluate task on gpu_h100 (matches Phase 44)"

approximations:
  - name: "Bootstrap MAP uncertainty"
    parameter: "B=1000 resamples; σ_MAP = 0.0114 (Phase 45 Step 0 baseline)"
    validity: "412 events; per-event likelihoods are independent"
    breaks_when: "If post-fix N_events differs > 5% from pre-fix → posterior set integrity issue"
    check: "post-fix bootstrap σ_MAP within 2× of pre-fix (≤ 0.025)"

key_links:
  - from: "Plan 45-02 [PHYSICS] commit"
    to: "cluster/submit_phase45_eval.sh"
    via: "Cluster job picks up the post-fix master branch from git pull on the cluster login node."
    check: "Pre-flight check verifies the cluster checkout's HEAD matches the local [PHYSICS] commit hash."
  - from: "cluster/submit_phase45_eval.sh"
    to: "results/phase45_posteriors/combined_posterior.json"
    via: "evaluate.sbatch array (38 h-values) + combine.sbatch."
    check: "MAP_h, interval_68, h_truth=0.73, n_events_used recorded."

contract:
  scope:
    question: "Does the Plan 45-02 anchor lift produce MAP ∈ [0.72, 0.74] with the 68% bootstrap interval containing 0.73 on the production seed200 (412 events) cluster posterior?"
  claims:
    - id: claim-cluster-map-in-range
      statement: "Cluster re-eval on production seed200 (412 events, truth h=0.73, SNR≥20) produces MAP ∈ [0.72, 0.74]."
      deliverables: [deliv-submit-script, deliv-acceptance-doc, deliv-cluster-posterior]
      acceptance_tests: [test-map-in-range, test-bootstrap-coverage, test-zero-strategies-equal]
      references: [ref-handoff, ref-research, ref-phase44-script]
    - id: claim-bootstrap-coverage
      statement: "68% equal-tailed bootstrap interval contains truth h=0.73."
      deliverables: [deliv-cluster-posterior]
      acceptance_tests: [test-bootstrap-coverage]
      references: [ref-handoff]
    - id: claim-strategies-still-equivalent
      statement: "All 4 zero-handling strategies (naive, exclude, per-event-floor, physics-floor) produce identical MAPs (Phase 44 invariant)."
      deliverables: [deliv-cluster-posterior]
      acceptance_tests: [test-zero-strategies-equal]
      references: [ref-handoff]
  deliverables:
    - id: deliv-submit-script
      kind: code
      path: "cluster/submit_phase45_eval.sh"
      description: "Cloned from cluster/submit_phase44_eval.sh with three changes: (1) NEW_RUN dir name 'run_phase45_<date>', (2) source CRBs unchanged ('/pfs/.../run_20260401_seed200/simulations'), (3) console message updated ('Phase 45 post-fix re-eval; expected MAP shift 0.7650 → ~0.73')."
      must_contain:
        - "Phase 45"
        - "run_phase45"
        - "evaluate.sbatch"
        - "combine.sbatch"
    - id: deliv-acceptance-doc
      kind: doc
      path: ".gpd/phases/45-p-det-first-bin-asymptote-fix/45-03-ACCEPTANCE.md"
      description: "Acceptance gate documentation: pre-flight checklist, exact accept/reject criteria, fallback escalation tree (RESEARCH.md §4 hybrid 4c if 4a-(ii) overshoots or undershoots)."
      must_contain:
        - "MAP ∈ [0.72, 0.74]"
        - "68% interval contains 0.73"
        - "naive == exclude == per-event-floor == physics-floor"
        - "Wilson CI [0.806, 1.000]"
    - id: deliv-cluster-posterior
      kind: data
      path: "results/phase45_posteriors/combined_posterior.json"
      description: "Cluster output (rsynced after combine.sbatch finishes). Contains MAP, posterior, interval, n_events_used, D_h_per_h, strategy comparison."
      must_contain:
        - "map_h"
        - "interval_68"
        - "h_values"
        - "posterior"
  references:
    - id: ref-handoff
      path: ".gpd/HANDOFF-phase45-diagnosis.md"
      kind: handoff
    - id: ref-research
      path: ".gpd/phases/45-p-det-first-bin-asymptote-fix/45-RESEARCH.md"
      kind: research
    - id: ref-phase44-script
      path: "cluster/submit_phase44_eval.sh"
      kind: code
  acceptance_tests:
    - id: test-pre-flight-commit-match
      subject: claim-cluster-map-in-range
      kind: integration
      procedure: "On the cluster login node after the user pulls: `git rev-parse HEAD` must equal the local Plan 45-02 [PHYSICS] commit hash."
      pass_condition: "Hashes match."
      evidence_required: [deliv-submit-script]
    - id: test-pre-flight-tests-pass
      subject: claim-cluster-map-in-range
      kind: integration
      procedure: "On the cluster login node: `uv run pytest -m 'not gpu and not slow' master_thesis_code_test/bayesian_inference/test_simulation_detection_probability.py`."
      pass_condition: "Pass; specifically `TestPhase45EmpiricalAnchor` (6 tests) and `TestZeroFillBoundaryConvention` (4 tests) all pass."
      evidence_required: [deliv-submit-script]
    - id: test-map-in-range
      subject: claim-cluster-map-in-range
      kind: empirical
      procedure: "Read `results/phase45_posteriors/combined_posterior.json`. Inspect `map_h`."
      pass_condition: "0.72 ≤ map_h ≤ 0.74."
      evidence_required: [deliv-cluster-posterior]
    - id: test-bootstrap-coverage
      subject: claim-bootstrap-coverage
      kind: empirical
      procedure: "Run `uv run python scripts/bias_investigation/test_08_bootstrap_map.py` after rsyncing the post-fix posteriors to `results/phase45_posteriors/`. Inspect the JSON's `interval_68`."
      pass_condition: "interval_68[0] ≤ 0.73 ≤ interval_68[1]."
      evidence_required: [deliv-cluster-posterior]
    - id: test-zero-strategies-equal
      subject: claim-strategies-still-equivalent
      kind: regression
      procedure: "Inspect the cluster log output for the 4 strategies (naive, exclude, per-event-floor, physics-floor) reported in combine.sbatch's stdout."
      pass_condition: "All 4 MAPs are identical (Phase 44 invariant)."
      evidence_required: [deliv-cluster-posterior]
    - id: test-bootstrap-sigma-stable
      subject: claim-cluster-map-in-range
      kind: empirical
      procedure: "Compare post-fix bootstrap σ_MAP to Phase 45 Step 0 baseline σ_MAP = 0.0114."
      pass_condition: "post-fix σ_MAP ≤ 0.025 (2× the baseline; allows for moderate posterior re-shaping)."
      evidence_required: [deliv-cluster-posterior]
  forbidden_proxies:
    - "Skipping the cluster re-eval and claiming success on local-only proxy MAP from `simulations/prepared_cramer_rao_bounds.csv` (60 events) — RESEARCH.md §5 explicitly requires the 412-event seed200 cluster posterior."
    - "Re-using `results/phase44_posteriors/` and just re-bootstrapping — those posteriors were generated with the pre-Plan-45 interpolator and are stale."
    - "Submitting before the [PHYSICS] commit hash matches on the cluster login node — pre-flight gate must pass first."
    - "Accepting MAP=0.7650 (or any value outside [0.72, 0.74]) as 'close enough' — the acceptance window is locked in RESEARCH.md §5 and HANDOFF-phase45-diagnosis.md."
    - "Treating MAP slightly above 0.74 as a conservative-bound side-effect of using 0.806 instead of 1.0 — this is real overshoot data and triggers escalation to hybrid 4c (RESEARCH.md §4)."
  uncertainty_markers:
    weakest_anchors:
      - "Wilson 95% CI on `p_max_empirical` is [0.806, 1.000]. The conservative 0.806 anchor may not lift L_comp enough to reach 0.73; if MAP lands in [0.74, 0.76], escalate to 4c (hybrid: anchor + (c_0/2, p̂_split=1.0) intermediate point). If MAP lands in [0.70, 0.72], recheck (overshoot is unlikely with conservative anchor but possible)."
      - "Cluster wallclock variance: 38 h-values × per-event posterior is GPU-bound; expect 2–4 h per array task on gpu_h100, total ≤ 6 h with combine."
    disconfirming_observations:
      - "If MAP lands at 0.7650 (unchanged from Phase 44) → patch did not take effect on cluster; investigate (commit hash mismatch? caching issue? wrong branch?)."
      - "If 4 strategies produce DIFFERENT MAPs → unrelated regression introduced; investigate before accepting."
      - "If interval_68 EXCLUDES 0.73 but MAP ∈ [0.72, 0.74] → bias is fixed but posterior is too narrow; this is acceptable per RESEARCH.md §5 IF MAP is in range, but flag as INFO for Phase 46."
      - "If pre-flight tests fail on cluster → do NOT submit; reconcile environment first."
---

<objective>
Run the cluster re-evaluation that decides Phase 45's success. Submit the production seed200 (412 events) posterior re-computation under the Plan 45-02 [PHYSICS] anchor lift, monitor the array job, rsync results, run the bootstrap on the post-fix posterior, and check off the locked acceptance criteria from RESEARCH.md §5.

Purpose: This is the binding gate. Local tests in Plan 45-02 confirm the patch behaves correctly; only the cluster re-eval can confirm the patch achieves the Phase 45 goal (MAP shift from 0.7650 toward 0.73, 68% interval covering truth, all four zero-handling strategies still equivalent).

Output: A new cluster submission script `cluster/submit_phase45_eval.sh`; an acceptance-gate document `45-03-ACCEPTANCE.md`; the rsynced cluster posteriors at `results/phase45_posteriors/`; a final SUMMARY.md recording MAP, interval, σ_MAP, strategy comparison, and the accept/reject decision.

Order-of-magnitude prediction (locked in Plan 45-02 SUMMARY.md): MAP shift from 0.7650 to ≈ 0.72–0.74. Magnitude: −0.025 to −0.045 (3-σ_boot residual was +0.0350; the conservative anchor 0.806 should remove most of it; the point-estimate anchor 1.0 would over-correct to ≈0.71 — hence Plan 45-02 chose the conservative variant).
</objective>

<execution_context>
@/home/jasper/.claude/get-physics-done/workflows/execute-plan.md
@/home/jasper/.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/phases/45-p-det-first-bin-asymptote-fix/45-RESEARCH.md
@.gpd/HANDOFF-phase45-diagnosis.md
@.gpd/phases/45-p-det-first-bin-asymptote-fix/45-02-SUMMARY.md
@cluster/submit_phase44_eval.sh
@scripts/bias_investigation/test_08_bootstrap_map.py
@scripts/bias_investigation/outputs/phase45/bootstrap_summary.json
@scripts/bias_investigation/outputs/phase45/p_max_h_independence.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone submit_phase44_eval.sh into submit_phase45_eval.sh with surgical edits</name>
  <files>cluster/submit_phase45_eval.sh</files>
  <action>
    Copy `cluster/submit_phase44_eval.sh` to `cluster/submit_phase45_eval.sh`. Apply ONLY these edits:

    1. Header comment (lines 1–4 region): replace `Phase 44` references with `Phase 45`. New header:
       ```
       #!/usr/bin/env bash
       # Re-evaluate production CRBs (run_20260401_seed200, ecliptic-migrated)
       # under the Phase 45 P_det first-bin asymptote anchor (commit <hash>).
       # Same data + same script structure as Phase 44; expects MAP shift from
       # 0.7650 (Phase 44 post-fix) to ~0.73 (Phase 45 post-anchor-lift).
       # Acceptance window: MAP ∈ [0.72, 0.74]; 68% bootstrap interval ∋ 0.73.
       ```

    2. `NEW_RUN` line: change `run_phase44_$(date +%Y%m%d)` → `run_phase45_$(date +%Y%m%d)`.

    3. Console message block: change "Phase 44 post-fix re-evaluation" → "Phase 45 post-anchor-lift re-evaluation"; change "Expected: MAP shifts from 0.86 (pre-fix) to ~0.73 (post-fix)" → "Expected: MAP shifts from 0.7650 (Phase 44 post-fix) to ∈ [0.72, 0.74] (Phase 45 post-anchor)".

    4. Final rsync hint: change `phase44_posteriors` → `phase45_posteriors`.

    5. Source CRB path UNCHANGED: `/pfs/work9/workspace/scratch/st_ac147838-emri/run_20260401_seed200`. Same data, different code.

    6. Make executable: `chmod +x cluster/submit_phase45_eval.sh`.

    Do NOT modify `cluster/evaluate.sbatch` or `cluster/combine.sbatch`. Those are picked up by the script as-is and operate on the post-fix master branch checked out on the cluster.

    Per CLAUDE.md / MEMORY.md `feedback_no_adhoc_scripts.md`: this is a permanent cluster re-eval template, not an ad-hoc script.
  </action>
  <verify>
    1. File exists, executable, parses with `bash -n cluster/submit_phase45_eval.sh`.
    2. `diff cluster/submit_phase44_eval.sh cluster/submit_phase45_eval.sh` shows ONLY the 4 surgical text changes (header, NEW_RUN, console messages, rsync hint). No structural changes.
    3. The source CRB path is unchanged (same `/pfs/.../run_20260401_seed200/simulations`).
    4. The array index range is unchanged (`--array="0-37"`, 38 h-values).
  </verify>
  <done>
    `cluster/submit_phase45_eval.sh` ready; differs from Phase 44 only in nominal labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 45-03-ACCEPTANCE.md (pre-flight checklist + accept/reject tree)</name>
  <files>.gpd/phases/45-p-det-first-bin-asymptote-fix/45-03-ACCEPTANCE.md</files>
  <action>
    Create the acceptance-gate document. Required sections:

    ### 1. Pre-flight checklist (must pass before submitting `submit_phase45_eval.sh`)
    - Local: Plan 45-02 [PHYSICS] commit landed. `git log -1 --format="%H %s"` recorded here.
    - Local: `uv run pytest -m "not gpu and not slow"` is green.
    - Cluster: `git pull` brings cluster checkout to the same hash. `git rev-parse HEAD` matches local.
    - Cluster: `uv run pytest -m "not gpu and not slow" master_thesis_code_test/bayesian_inference/test_simulation_detection_probability.py` is green (specifically the new `TestPhase45EmpiricalAnchor` and the 4 `TestZeroFillBoundaryConvention` regression tests).
    - Cluster: `WORKSPACE` env var is set; source CRB path `/pfs/work9/workspace/scratch/st_ac147838-emri/run_20260401_seed200/simulations` exists and contains `cramer_rao_bounds.csv`.

    ### 2. Submission
    - Run `bash cluster/submit_phase45_eval.sh` from the cluster login node. Record EVAL_JOB and COMBINE_JOB IDs.

    ### 3. Monitoring
    - `sacct -j <EVAL_JOB>,<COMBINE_JOB> --format=JobID,State,Elapsed,ExitCode`.
    - Expected wallclock: ≤ 4 h per evaluate task on gpu_h100; ≤ 30 min combine.

    ### 4. Rsync
    - `rsync -avz bwunicluster:$NEW_RUN/simulations/posteriors/ ./results/phase45_posteriors/`.

    ### 5. Acceptance criteria (locked from RESEARCH.md §5)
    - **Primary:** `combined_posterior.json` reports `map_h ∈ [0.72, 0.74]`. (test-map-in-range)
    - **Coverage:** `uv run python scripts/bias_investigation/test_08_bootstrap_map.py` (pointed at `results/phase45_posteriors/`) reports `interval_68` containing 0.73. (test-bootstrap-coverage)
       - NOTE: edit `POSTERIORS_DIR` in `test_08_bootstrap_map.py` from `phase44_posteriors` → `phase45_posteriors` for this run, OR copy the script as test_08_bootstrap_map_phase45.py with the path edit.
    - **Strategy invariance:** combine.sbatch's stdout shows MAPs from naive/exclude/per-event-floor/physics-floor strategies are all equal. (test-zero-strategies-equal; Phase 44 invariant)
    - **Bootstrap σ_MAP stability:** post-fix σ_MAP ≤ 0.025 (≤ 2× pre-fix 0.0114). (test-bootstrap-sigma-stable)

    ### 6. Outcome → next step (escalation tree)
    | Outcome | Action |
    |---|---|
    | All 4 acceptance tests PASS | Phase 45 SUCCESS. Mark phase complete. Update STATE.md. Close any GitHub issues tagged `paper-blocker` related to MAP=0.86 / 0.7650 with reference to commit. |
    | MAP ∈ [0.74, 0.76] (under-corrected) | Conservative anchor 0.806 was too low. Escalate to RESEARCH.md §4c (hybrid: anchor at (0, 1.0) + (c_0/2, 1.0) intermediate point). Open Plan 45-04 with the new fix family. |
    | MAP ∈ [0.70, 0.72] (over-corrected) | Conservative anchor 0.806 was too high (unlikely; flag for investigation). Open Plan 45-04 with anchor=pooled_p_hat instead of CI lower bound, OR re-derive p_max from a tighter d_L bin. |
    | MAP unchanged at 0.7650 | Patch did not take effect on cluster. CHECKPOINT: investigate commit hash mismatch, environment difference, caching, OR wrong injection_data_dir on cluster. Do NOT proceed; ask researcher. |
    | 4 strategies disagree | Unrelated regression introduced in Plan 45-02. CHECKPOINT: revert Plan 45-02, reapply with fewer changes, or investigate the call-site invariant. |
    | Interval_68 excludes 0.73 but MAP ∈ [0.72, 0.74] | INFO only — MAP is in range, posterior is just narrower than the bias. Document and accept. |

    ### 7. Reproducibility
    - `submit_phase45_eval.sh` is committed; `evaluate.sbatch` and `combine.sbatch` unchanged from Phase 44.
    - The post-fix master commit hash is recorded in this doc, in `45-03-SUMMARY.md`, and in `combined_posterior.json`'s `git_commit` field (auto-recorded per CLAUDE.md `--seed` policy).
  </action>
  <verify>
    1. File exists with all 7 sections.
    2. The 4 acceptance criteria from RESEARCH.md §5 are quoted verbatim (MAP ∈ [0.72, 0.74]; 68% interval ∋ 0.73; 4 strategies equal; σ_MAP ≤ 0.025).
    3. Escalation tree covers: under-correction, over-correction, no-effect, strategy disagreement, narrow posterior. (5 branches.)
    4. Section 5 includes the explicit POSTERIORS_DIR edit for `test_08_bootstrap_map.py`.
  </verify>
  <done>
    Acceptance gate document is on disk and complete; researcher has a deterministic flowchart for the cluster outcome.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Researcher submits the cluster job (only operation requiring human credentials)</name>
  <what-built>
    `cluster/submit_phase45_eval.sh` (Task 1) + `45-03-ACCEPTANCE.md` (Task 2). Submission step requires SSH to bwUniCluster — no automated equivalent.
  </what-built>
  <how-to-verify>
    Researcher runs the pre-flight checklist on the cluster login node, then `bash cluster/submit_phase45_eval.sh`, captures EVAL_JOB and COMBINE_JOB IDs, and posts back here. The agent then resumes with monitoring + rsync.

    Pre-flight checklist (from 45-03-ACCEPTANCE.md):
    1. SSH to cluster: `ssh bwunicluster`.
    2. `cd <repo_path> && git pull && git rev-parse HEAD` — must equal local Plan 45-02 [PHYSICS] commit hash.
    3. `uv run pytest -m "not gpu and not slow" master_thesis_code_test/bayesian_inference/test_simulation_detection_probability.py` — must be green.
    4. `echo $WORKSPACE` — must be set.
    5. Verify `/pfs/work9/workspace/scratch/st_ac147838-emri/run_20260401_seed200/simulations/cramer_rao_bounds.csv` exists.
    6. `bash cluster/submit_phase45_eval.sh` — record both job IDs from stdout.

    Post results (job IDs + any pre-flight failures) here so the agent can monitor.
  </how-to-verify>
  <resume-signal>
    Researcher posts: `EVAL_JOB=<id>, COMBINE_JOB=<id>` (or `BLOCKER: <reason>` if a pre-flight gate failed).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Monitor the cluster job, rsync posteriors, run bootstrap, evaluate acceptance</name>
  <files>results/phase45_posteriors/combined_posterior.json, scripts/bias_investigation/outputs/phase45/post_fix_bootstrap_summary.json, .gpd/phases/45-p-det-first-bin-asymptote-fix/45-03-SUMMARY.md</files>
  <action>
    1. **Monitor:** `ssh bwunicluster sacct -j <EVAL_JOB>,<COMBINE_JOB> --format=JobID,State,Elapsed,ExitCode` polled until COMBINE_JOB shows COMPLETED or FAILED. (Expected 2–6 h total.)

    2. **Rsync results:** `rsync -avz bwunicluster:$NEW_RUN/simulations/posteriors/ ./results/phase45_posteriors/`.

    3. **Read the cluster MAP:** parse `results/phase45_posteriors/combined_posterior.json`. Record `map_h`, `interval_68`, `n_events_used`, `D_h_per_h`, `git_commit`. Sanity check: `git_commit` must equal the Plan 45-02 [PHYSICS] commit hash; otherwise the cluster ran a stale checkout — abort with CHECKPOINT.

    4. **Run bootstrap:** Copy `scripts/bias_investigation/test_08_bootstrap_map.py` to a sibling file `test_08_bootstrap_map_phase45.py` (or temporarily edit the existing script's `POSTERIORS_DIR`); change `POSTERIORS_DIR = PROJECT_ROOT / "results" / "phase45_posteriors"`. Run: `uv run python scripts/bias_investigation/test_08_bootstrap_map_phase45.py`. Move output to `scripts/bias_investigation/outputs/phase45/post_fix_bootstrap_summary.json`.

    5. **Strategy comparison:** parse the combine.sbatch stdout (in `$NEW_RUN/logs/combine_*.out` rsynced from cluster) for the 4 strategies' MAPs. Confirm equality. Record in SUMMARY.md.

    6. **Evaluate acceptance gate** (from 45-03-ACCEPTANCE.md §5):
       - Primary: 0.72 ≤ map_h ≤ 0.74?
       - Coverage: bootstrap_summary.json `interval_68[0]` ≤ 0.73 ≤ `interval_68[1]`?
       - Strategy invariance: 4 MAPs all equal?
       - σ_MAP stability: post-fix σ_MAP ≤ 0.025?

    7. **Branch on outcome** per the escalation tree in 45-03-ACCEPTANCE.md §6. If all PASS: Phase 45 success. If any branch fires: emit a structured CHECKPOINT/CHECKPOINT-style result and DO NOT mark Phase 45 complete; the researcher decides whether to open Plan 45-04 (escalation) or re-investigate.

    8. **Write SUMMARY:** `45-03-SUMMARY.md` with sections:
       - Pre-fix vs post-fix MAP table (Phase 44: 0.7650; Phase 45: <value>).
       - Bootstrap intervals comparison.
       - 4-strategy table.
       - σ_MAP table.
       - Pass/fail per acceptance criterion.
       - Final verdict + escalation if needed.

    9. **Update STATE.md** if all gates PASS: set `Current Phase: 46` (or `none` if no follow-on phase queued); set `Last Activity Description` to record MAP shift achieved.

    10. **GitHub sync** (if applicable): close any open issues tagged `paper-blocker` related to MAP=0.86 / MAP=0.7650 with a comment referencing the [PHYSICS] commit and Phase 45 SUMMARY.

    11. **Commit:** `gpd commit "docs(phase45): record cluster re-eval acceptance gate result"` covering `45-03-SUMMARY.md`, `post_fix_bootstrap_summary.json`, the cluster posterior JSON, and any `cluster/submit_phase45_eval.sh` cleanup.
  </action>
  <verify>
    1. **`results/phase45_posteriors/combined_posterior.json` exists** and contains `map_h` (float ∈ [0.5, 1.0]), `interval_68` (list of 2 floats), `git_commit` (string matching Plan 45-02 [PHYSICS] hash).
    2. **MAP is in the bootstrap-grid range:** map_h is one of the discrete grid points (h-grid is {0.60, …, 0.86} with 0.005 spacing per Phase 44 script).
    3. **`post_fix_bootstrap_summary.json` exists** with same schema as the pre-fix `bootstrap_summary.json`.
    4. **Acceptance evaluation is recorded** in `45-03-SUMMARY.md` with explicit PASS/FAIL per criterion.
    5. **If any FAIL:** the SUMMARY explicitly says "ESCALATE per 45-03-ACCEPTANCE.md §6 branch <X>"; STATE.md is NOT updated to "Phase 45 complete".
    6. **If all PASS:** STATE.md updated; SUMMARY records the locked MAP value and interval; commit landed.
    7. **σ_MAP stability test:** post_fix_bootstrap_summary.json's `std` field ≤ 0.025.
    8. **Bootstrap quality check:** post-fix `n_events_used` ≥ 0.95 × pre-fix n_events_used (412); a sudden drop signals data integrity issue.
    9. **Order-of-magnitude check:** post-fix MAP shifted in the predicted direction (downward) from 0.7650.
  </verify>
  <done>
    Acceptance gate evaluated; SUMMARY.md records the verdict; STATE.md updated if PASS; CHECKPOINT escalation issued if FAIL. Phase 45 closed (success or escalated).
  </done>
</task>

</tasks>

<verification>
- **Pre-flight integrity:** cluster checkout's HEAD matches Plan 45-02 [PHYSICS] commit; new TestPhase45EmpiricalAnchor + 4 TestZeroFillBoundaryConvention tests all pass on cluster.
- **Cluster execution:** evaluate.sbatch + combine.sbatch unchanged from Phase 44; only the source code (anchor in `_build_grid_1d`) differs.
- **Acceptance criteria** (RESEARCH.md §5, locked):
  - MAP ∈ [0.72, 0.74]
  - 68% bootstrap interval contains 0.73
  - 4 zero-handling strategies produce identical MAPs
  - σ_MAP ≤ 0.025
- **Order-of-magnitude:** predicted MAP shift from 0.7650 to ≈ 0.72–0.74 (Δ ≈ −0.025 to −0.045). Direction: DOWNWARD.
- **Anchor uncertainty bound:** Wilson 95% CI [0.806, 1.000] on `p_max_empirical` corresponds to a bounded MAP shift; the conservative 0.806 anchor is the chosen default. If MAP lands in [0.74, 0.76], escalate to hybrid 4c.
- **Reproducibility:** new script + acceptance doc + commit hash in cluster posterior JSON allow exact replay.
</verification>

<success_criteria>
- `cluster/submit_phase45_eval.sh` committed.
- `45-03-ACCEPTANCE.md` records pre-flight checklist, accept/reject criteria, and 5-branch escalation tree.
- Cluster job IDs recorded; rsync completed; `results/phase45_posteriors/combined_posterior.json` exists with matching git_commit.
- Bootstrap rerun produces `post_fix_bootstrap_summary.json`.
- 4 acceptance criteria evaluated; verdict recorded in `45-03-SUMMARY.md`.
- If PASS: STATE.md updated, GitHub issues closed if any, Phase 45 marked complete.
- If FAIL: structured CHECKPOINT issued, escalation branch identified.
</success_criteria>

<output>
After completion, create `.gpd/phases/45-p-det-first-bin-asymptote-fix/45-03-SUMMARY.md` recording:
- Pre-fix vs post-fix MAP table.
- Bootstrap interval table.
- 4-strategy comparison.
- Acceptance verdict per criterion.
- Final phase status (PASS / ESCALATE / CHECKPOINT).
</output>
