---
phase: 15-code-audit-fix
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - .gpd/phases/15-code-audit-fix/15-validation-results.md
interactive: true

conventions:
  units: "SI (c, G, H0 in km/s/Mpc)"
  parameterization: "h = H0 / (100 km/s/Mpc)"

dimensional_check:
  posterior_value: "[dimensionless] (probability density over h)"

approximations:
  - name: "P_det = 1 (detection probability disabled)"
    parameter: "P_det set to uniform"
    validity: "Removes selection bias -- both channels are equally affected"
    breaks_when: "Realistic P_det needed (Phase 11.1 scope)"
    check: "Both channels show bias relative to true h=0.73 (expected with P_det=1)"
  - name: "MC denominator noise"
    parameter: "N = 10000 MC samples"
    validity: "~1% relative error per galaxy"
    breaks_when: "Single h-value comparison at ~1% level"
    check: "Compare posterior shape across 4 h-values, not single-point values"

estimated_execution:
  total_minutes: 25
  breakdown:
    - task: 1
      minutes: 15
      note: "Run evaluation at 4 h-values (may need cluster; local fallback if data available)"
    - task: 2
      minutes: 10
      note: "Analyze results and write validation summary"

contract:
  scope:
    question: "Does removing /(1+z) shift the 'with BH mass' posterior peak from h=0.600 toward h=0.678?"
  claims:
    - id: claim-posterior-shift
      statement: "The 'with BH mass' posterior peak shifts from h=0.600 toward h=0.678 after removing /(1+z)"
      deliverables: ["deliv-validation-results"]
      acceptance_tests: ["test-direction", "test-without-unchanged"]
      references: ["ref-pre-fix-baseline"]
    - id: claim-without-bh-unchanged
      statement: "The 'without BH mass' channel results are UNCHANGED by the fix"
      deliverables: ["deliv-validation-results"]
      acceptance_tests: ["test-without-unchanged"]
      references: ["ref-pre-fix-baseline"]
  deliverables:
    - id: deliv-validation-results
      kind: report
      path: ".gpd/phases/15-code-audit-fix/15-validation-results.md"
      description: "Quick-validation results showing posterior values at 4 h-values pre and post fix"
      must_contain: ["h=0.678", "posterior peak", "shift direction"]
  acceptance_tests:
    - id: test-direction
      subject: claim-posterior-shift
      kind: benchmark
      procedure: "Compare posterior values at h = {0.652, 0.678, 0.704, 0.730}. The peak should be near h=0.678, not h=0.600."
      pass_condition: "Posterior at h=0.678 is larger than at h=0.652, indicating peak has shifted from h=0.600 toward h=0.678"
      evidence_required: ["deliv-validation-results"]
    - id: test-without-unchanged
      subject: claim-without-bh-unchanged
      kind: reproducibility
      procedure: "Compare 'without BH mass' posterior values before and after the fix"
      pass_condition: "'Without BH mass' values identical (code path was not modified)"
      evidence_required: ["deliv-validation-results"]
  references:
    - id: ref-pre-fix-baseline
      kind: prior_artifact
      locator: "STATE.md: h=0.600 pre-fix 'with BH mass' baseline; h=0.678 'without BH mass' baseline"
      role: benchmark
      why_it_matters: "Pre-fix posterior peak at h=0.600 is the baseline; post-fix must shift toward h=0.678 to confirm the fix works"
      applies_to: ["claim-posterior-shift", "claim-without-bh-unchanged"]
      must_surface: true
      required_actions: ["compare"]
  forbidden_proxies:
    - id: fp-no-run
      subject: claim-posterior-shift
      proxy: "Declaring the fix correct without actually running the evaluation pipeline"
      reason: "Theoretical justification alone is insufficient; the contract requires empirical confirmation via evaluation"
    - id: fp-wrong-direction
      subject: claim-posterior-shift
      proxy: "Accepting a result where the posterior shifts AWAY from h=0.678"
      reason: "A shift away from the expected peak would indicate the fix is wrong or a compensating error was introduced"
    - id: fp-two-errors-cancel
      subject: claim-posterior-shift
      proxy: "Accepting a result where posterior matches h=0.73 exactly"
      reason: "With P_det=1 still disabled, both channels should be biased below h=0.73; exact agreement with true value is suspicious"
  uncertainty_markers:
    weakest_anchors:
      - "Quick validation uses only 4 h-values -- full posterior shape deferred to Phase 16"
      - "MC denominator noise could affect individual h-value comparisons at ~1% level"
    disconfirming_observations:
      - "If posterior peak does NOT shift by at least 0.02 from h=0.600, the /(1+z) fix alone is insufficient and additional bias sources must be investigated"
      - "If posterior peak overshoots past h=0.73, a compensating error may have been introduced"
---

<objective>
Run a quick-validation evaluation to confirm the /(1+z) fix shifts the "with BH mass" posterior peak from h=0.600 toward h=0.678.

Purpose: FIX-02 from the roadmap. This is the empirical confirmation that the code fix is correct. Without this, the fix is theoretically justified but empirically unvalidated.
Output: Validation results document showing posterior values at 4 h-values, confirming directional shift.
</objective>

<execution_context>
@/home/jasper/.claude/get-physics-done/workflows/execute-plan.md
@/home/jasper/.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/15-code-audit-fix/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run quick-validation evaluation at 4 h-values</name>
  <files>.gpd/phases/15-code-audit-fix/15-validation-results.md</files>
  <action>
    Run the evaluation pipeline at h = {0.652, 0.678, 0.704, 0.730} to determine the post-fix posterior peak location.

    PREREQUISITES:
    - Plan 15-01 must be complete (/(1+z) removed from bayesian_statistics.py)
    - The evaluation pipeline requires: Cramer-Rao bounds CSV data (simulations/cramer_rao_bounds.csv), GLADE galaxy catalog, and SimulationDetectionProbability KDE data

    EXECUTION:
    1. Check if evaluation can run locally:
       ```bash
       uv run python -c "from master_thesis_code.bayesian_inference.bayesian_statistics import BayesianStatistics; print('Import OK')"
       ```
       If import fails (likely due to cupy/LISA_configuration dependency), note that cluster execution is needed.

    2. If local execution is possible, run for each h-value:
       ```bash
       uv run python -m master_thesis_code <working_dir> --evaluate --h_value 0.652 --log_level INFO
       uv run python -m master_thesis_code <working_dir> --evaluate --h_value 0.678 --log_level INFO
       uv run python -m master_thesis_code <working_dir> --evaluate --h_value 0.704 --log_level INFO
       uv run python -m master_thesis_code <working_dir> --evaluate --h_value 0.730 --log_level INFO
       ```
       The working_dir must contain the simulation output (cramer_rao_bounds.csv).

    3. If local execution is NOT possible (cluster dependency), prepare a validation script that the researcher can run on the cluster:
       - Create a small shell script `cluster/quick_validation.sh` that runs the 4 h-values
       - Document exactly what output to look for
       - Set `interactive: true` on this task (checkpoint for researcher to run on cluster and report results)

    4. Record the posterior values for each h-value in the validation results document.

    NOTE: The "without BH mass" channel values should be checked too -- they must be UNCHANGED from the pre-fix baseline (since Plan 15-01 did not modify the "without BH mass" code path).

    EXPECTED RESULTS:
    - Pre-fix "with BH mass" peak: h = 0.600
    - Post-fix "with BH mass" peak: h ~ 0.678 (matching "without BH mass" channel)
    - "Without BH mass" peak: h = 0.678 (unchanged)
    - Both channels biased relative to true h = 0.73 (expected with P_det = 1)
  </action>
  <verify>
    1. Evaluation ran successfully at all 4 h-values (or validation script prepared for cluster)
    2. Posterior values recorded for both "with BH mass" and "without BH mass" channels
    3. "Without BH mass" values are unchanged from pre-fix baseline
  </verify>
  <done>Evaluation completed (or cluster script prepared) at h = {0.652, 0.678, 0.704, 0.730}, posterior values recorded for both channels</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify posterior shift direction and write validation summary</name>
  <what-built>
    Quick-validation evaluation results showing "with BH mass" posterior values at 4 h-values after removing the spurious /(1+z) factor. The pre-fix baseline had the posterior peak at h=0.600.
  </what-built>
  <how-to-verify>
    Check the following:
    1. The "with BH mass" posterior peak has shifted from h=0.600 TOWARD h=0.678 (the "without BH mass" baseline)
    2. The shift is at least 0.02 in h (from 0.600 to at least 0.620)
    3. The "without BH mass" channel values are UNCHANGED
    4. Neither channel peaks at h=0.73 exactly (P_det=1 bias is expected)
    5. The posterior shape is reasonable (monotonically increasing toward peak, then decreasing)

    RED FLAGS:
    - Peak did not shift at all -> fix not applied correctly, or bias has different root cause
    - Peak shifted AWAY from 0.678 -> fix is wrong, re-examine derivation
    - Peak at exactly h=0.73 -> suspicious, possible compensating error
    - "Without BH mass" channel changed -> wrong code path was modified
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 15, or describe issues if the validation fails</resume-signal>
</task>

</tasks>

<verification>
- Posterior peak shift from h=0.600 toward h=0.678 confirms the /(1+z) fix addresses the bias
- "Without BH mass" channel unchanged confirms no regression
- Both channels biased relative to h=0.73 confirms no compensating error introduced
</verification>

<success_criteria>
FIX-02: Quick-validation evaluation confirms "with BH mass" posterior shifts from h=0.600 toward h=0.678 (directional confirmation). Full numerical validation deferred to Phase 16.
</success_criteria>

<output>
After completion, create `.gpd/phases/15-code-audit-fix/15-02-SUMMARY.md`
</output>
