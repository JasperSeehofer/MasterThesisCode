---
phase: 15-code-audit-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - master_thesis_code/bayesian_inference/bayesian_statistics.py
interactive: false

conventions:
  units: "SI (c, G, H0 in km/s/Mpc)"
  coordinates: "spherical (phi, theta for sky location)"
  parameterization: "fractional (d_L_frac = d_L/d_L_det, M_z_frac = M*(1+z)/M_z_det)"

dimensional_check:
  numerator_integrand: "[dimensionless] (probability density products with matching measure)"
  denominator_integrand: "[dimensionless] (p_det * p_gal(z) * p_gal(M) with matching measure)"

approximations:
  - name: "Gaussian GW measurement errors"
    parameter: "SNR >= 20"
    validity: "Fisher matrix approximation valid for high SNR"
    breaks_when: "SNR < ~8"
    check: "All detections in dataset have SNR >= 20 (enforced by SNR_THRESHOLD)"
  - name: "MC importance sampling for denominator"
    parameter: "N = 10000 samples"
    validity: "Relative MC error ~ 1% if integrand variance is O(1)"
    breaks_when: "p_det varies rapidly over galaxy prior support"
    check: "Compute np.std(weights)/np.sqrt(N) and compare to np.mean(weights)"

patterns_consulted:
  insights: []
  error_patterns: []
  adjustments_made: []

estimated_execution:
  total_minutes: 35
  breakdown:
    - task: 1
      minutes: 20
      note: "Term-by-term audit of 12 code terms + add reference comments + remove /(1+z)"
    - task: 2
      minutes: 15
      note: "Denominator consistency check + MC convergence audit"

contract:
  scope:
    question: "Does every term in bayesian_statistics.py match the Phase 14 derivation, and is the spurious /(1+z) removed?"
  claims:
    - id: claim-1z-removed
      statement: "The spurious /(1+z) at line 646 is removed, justified by Eq. (14.21) Jacobian absorption identity"
      deliverables: ["deliv-fixed-code"]
      acceptance_tests: ["test-1z-gone", "test-testing-fn-fixed"]
      references: ["ref-phase14-derivation"]
    - id: claim-sky-weight
      statement: "Sky localization weight (phi, theta) appears in exactly one factor: inside gaussian_3d_marginal.pdf() at line 624"
      deliverables: ["deliv-fixed-code"]
      acceptance_tests: ["test-sky-single"]
      references: ["ref-phase14-derivation"]
    - id: claim-denom-consistent
      statement: "Denominator (lines 656-685) uses same physics as numerator: p_det * p_gal(z) * p_gal(M), with no /(1+z) and no mz_integral"
      deliverables: ["deliv-fixed-code"]
      acceptance_tests: ["test-denom-match"]
      references: ["ref-phase14-derivation"]
    - id: claim-mc-convergence
      statement: "MC denominator with N=10000 has quantified relative error, documented as acceptable or flagged for future work"
      deliverables: ["deliv-fixed-code"]
      acceptance_tests: ["test-mc-error"]
      references: ["ref-mc-standard"]
    - id: claim-ref-comments
      statement: "Every audited term has a reference comment citing the corresponding derivation equation"
      deliverables: ["deliv-fixed-code"]
      acceptance_tests: ["test-ref-comments"]
      references: ["ref-phase14-derivation"]
  deliverables:
    - id: deliv-fixed-code
      kind: code
      path: "master_thesis_code/bayesian_inference/bayesian_statistics.py"
      description: "Corrected bayesian_statistics.py with /(1+z) removed and reference comments added"
      must_contain: ["Eq. (14.32)", "Eq. (14.33)", "Eq. (14.21)"]
  acceptance_tests:
    - id: test-1z-gone
      subject: claim-1z-removed
      kind: existence
      procedure: "Verify line 646 no longer contains '/ (1 + z)'. The return statement of numerator_integrant_with_bh_mass should be: p_det * gw_3d * mz_integral * galaxy_redshift_normal_distribution.pdf(z)"
      pass_condition: "No '/ (1 + z)' in the return statement of numerator_integrant_with_bh_mass"
      evidence_required: ["deliv-fixed-code"]
    - id: test-testing-fn-fixed
      subject: claim-1z-removed
      kind: existence
      procedure: "Verify line 866 (testing function) also has '/ (1 + z)' removed"
      pass_condition: "No '/ (1 + z)' in the return statement of single_host_likelihood_integration_testing's numerator integrand"
      evidence_required: ["deliv-fixed-code"]
    - id: test-sky-single
      subject: claim-sky-weight
      kind: consistency
      procedure: "Search for all uses of phi and theta in the numerator integrand. Confirm they appear only in gaussian_3d_marginal.pdf() and p_det (where p_det is a separate physics factor, not a double-counting of sky weight)"
      pass_condition: "phi, theta used only in p_det call (line 619) and gaussian_3d_marginal.pdf() (line 624)"
      evidence_required: ["deliv-fixed-code"]
    - id: test-denom-match
      subject: claim-denom-consistent
      kind: consistency
      procedure: "Compare denominator_integrant_with_bh_mass_vectorized term-by-term against Eq. (14.33). Verify: p_det(d_L, M_z, phi, theta, h) * p_gal(z) * p_gal(M), no /(1+z), no mz_integral"
      pass_condition: "Denominator matches Eq. (14.33) with no extraneous factors"
      evidence_required: ["deliv-fixed-code", "ref-phase14-derivation"]
    - id: test-mc-error
      subject: claim-mc-convergence
      kind: convergence
      procedure: "From code lines 672-685: weights = p_det after importance sampling cancellation. Relative MC error = np.std(weights) / (np.sqrt(N_SAMPLES) * np.mean(weights)). Document the expected magnitude."
      pass_condition: "MC relative error quantified; if > 10%, flagged for future work with a code comment"
      evidence_required: ["deliv-fixed-code"]
    - id: test-ref-comments
      subject: claim-ref-comments
      kind: existence
      procedure: "Verify reference comments citing derivation equations exist above: the fix (Eq. 14.21 and 14.32), mz_integral (Eq. 14.31), denominator (Eq. 14.33), conditional decomposition (Eqs. 14.23-14.28)"
      pass_condition: "At least 4 reference comments present linking code to derivation"
      evidence_required: ["deliv-fixed-code"]
  references:
    - id: ref-phase14-derivation
      kind: prior_artifact
      locator: "derivations/dark_siren_likelihood.md Sections 8-14"
      role: definition
      why_it_matters: "Provides the reference formula for every term in the code; Section 14.1 contains the term-by-term mapping that identified /(1+z) as spurious"
      applies_to: ["claim-1z-removed", "claim-sky-weight", "claim-denom-consistent", "claim-ref-comments"]
      must_surface: true
      required_actions: ["read", "compare"]
    - id: ref-mc-standard
      kind: other
      locator: "Standard MC error analysis: relative error = sigma/sqrt(N)"
      role: method
      why_it_matters: "Provides the formula for quantifying MC convergence of the denominator estimate"
      applies_to: ["claim-mc-convergence"]
      must_surface: true
      required_actions: ["use"]
  forbidden_proxies:
    - id: fp-no-derivation
      subject: claim-1z-removed
      proxy: "Removing /(1+z) 'because it looks wrong' without citing Eq. (14.21)"
      reason: "Every code change must trace to a specific derivation equation; ad-hoc removal risks introducing or masking errors"
    - id: fp-adhoc-fix
      subject: claim-1z-removed
      proxy: "Adding fudge factors or rescaling to make posterior match without term-by-term justification"
      reason: "Numerical tuning without mathematical backing produces fragile code that breaks under parameter changes"
    - id: fp-modify-correct-1z
      subject: claim-1z-removed
      proxy: "Removing (1+z) from lines 633-634 (mu_gal_frac, sigma_gal_frac) instead of line 646"
      reason: "Lines 633-634 are CORRECT coordinate transformations per Eq. (14.22); removing them would break the Gaussian rescaling"
    - id: fp-modify-without-bh
      subject: deliv-fixed-code
      proxy: "Modifying the 'without BH mass' code path (lines 536-561)"
      reason: "This channel is the unbiased baseline (h=0.678); modifying it invalidates the reference for validating the fix"
    - id: fp-modify-denominator
      subject: claim-denom-consistent
      proxy: "Modifying the denominator (lines 656-685) beyond adding comments"
      reason: "Denominator confirmed correct by Phase 14 derivation; changes would introduce new errors"
  uncertainty_markers:
    weakest_anchors:
      - "MC convergence depends on actual p_det variance across galaxy catalog -- can only estimate, not bound exactly without running code"
    disconfirming_observations:
      - "If /(1+z) removal does NOT shift the posterior (tested in Plan 02), the bias has a different root cause and the Phase 14 derivation must be re-examined"
---

<objective>
Audit every term in the "with BH mass" likelihood code against the Phase 14 derivation, remove the spurious /(1+z) factor, add reference comments, and check denominator/MC convergence.

Purpose: Implement AUDT-01 through AUDT-04 and FIX-01 from the roadmap. The Phase 14 derivation proved /(1+z) at line 646 is spurious (Jacobian absorbed by Gaussian rescaling, Eq. 14.21). This plan applies that finding to the code.
Output: Corrected bayesian_statistics.py with reference comments linking each term to the derivation.
</objective>

<execution_context>
@/home/jasper/.claude/get-physics-done/workflows/execute-plan.md
@/home/jasper/.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/14-first-principles-derivation/14-02-SUMMARY.md
@derivations/dark_siren_likelihood.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Term-by-term audit of numerator + remove spurious /(1+z) + add reference comments</name>
  <files>master_thesis_code/bayesian_inference/bayesian_statistics.py</files>
  <action>
    This task audits the "with BH mass" numerator integrand and applies the single-line fix identified by Phase 14.

    PHYSICS CHANGE PROTOCOL (required by CLAUDE.md):
    - Old formula (line 646): `p_det * gw_3d * mz_integral * galaxy_redshift_normal_distribution.pdf(z) / (1 + z)`
    - New formula: `p_det * gw_3d * mz_integral * galaxy_redshift_normal_distribution.pdf(z)`
    - Reference: derivation Eq. (14.21) -- Jacobian |dM/dM_z_frac| = M_z_det/(1+z) is absorbed by Gaussian rescaling when transforming p_gal(M) to M_z_frac coordinates. No leftover /(1+z). See also Eq. (14.32) for the complete numerator.
    - Dimensional analysis: /(1+z) is dimensionless, so dimensional analysis cannot detect this bug. The verdict rests on the Jacobian algebra chain (Eqs. 14.15-14.21).
    - Limiting case: sigma_Mz -> infinity should recover d_L-only likelihood (Eq. 14.40). The spurious /(1+z) violates this (it would survive the limit since it's outside the mass-related terms).

    Step-by-step:

    1. READ derivations/dark_siren_likelihood.md Section 14.1 (term-by-term mapping table). This is the authoritative mapping between code lines and derivation equations.

    2. CONFIRM each term marked CORRECT in the mapping:
       - Line 583: `norm(loc=possible_host.M, scale=possible_host.M_error)` matches p_gal(M) from Sec. 8.1
       - Lines 586-611: Bishop conditional decomposition matches Eqs. (14.23)-(14.28)
       - Lines 619-621: p_det call uses detection.M (INFO-level observation: this is the ML mass estimate, not M_gal*(1+z) at trial z -- document this but do NOT change it)
       - Lines 624-626: gaussian_3d_marginal.pdf() is p_GW^(3D) from Eq. (14.25) -- sky weight (phi, theta) is INSIDE this factor. This resolves TODO at line 535.
       - Lines 629-630: mu_cond matches Eq. (14.27)
       - Line 633: mu_gal_frac = M*(1+z)/M_z_det matches Eq. (14.22) -- DO NOT remove (1+z) here
       - Line 634: sigma_gal_frac = sigma_M*(1+z)/M_z_det matches Eq. (14.22) -- DO NOT remove (1+z) here
       - Lines 640-643: mz_integral matches Eq. (14.31) (analytic Gaussian product)

    3. REMOVE the spurious factor: On line 646, delete `/ (1 + z)` from the return statement. The line should become:
       ```python
       return (
           p_det * gw_3d * mz_integral * galaxy_redshift_normal_distribution.pdf(z)
       )
       ```

    4. ADD reference comment above the return statement:
       ```python
       # Eq. (14.32) in derivations/dark_siren_likelihood.md
       # No /(1+z) factor: Jacobian absorbed by Gaussian rescaling (Eq. 14.21)
       ```

    5. ADD reference comments to other key terms (if not already present):
       - Above the conditional decomposition block (~line 586): cite Eqs. (14.23)-(14.28), Bishop (2006) PRML Eq. 2.81-2.82
       - Above mz_integral (~line 640): cite Eq. (14.31)
       - Above mu_gal_frac/sigma_gal_frac (~line 632): cite Eq. (14.22), note that (1+z) here is correct

    6. FIX the testing function: At line 866, remove `/ (1 + z)` from the return statement of the numerator integrand in `single_host_likelihood_integration_testing()`. Add the same reference comment.

    7. RESOLVE TODO at line 535 ("KEEP IN MIND SKYLOCALIZATION WEIGHT IS IN THE GW LIKELIHOOD ATM. possible source of error"): Update this comment to state that the sky localization weight placement has been verified correct by the Phase 14 derivation (Sec. 2.7) and is NOT a source of error.

    8. Similarly resolve any TODO at line 718 about sky localization weight, if present.

    9. Document the p_det(detection.M) INFO-level observation: Add a comment near line 619-621 noting that p_det uses the ML mass estimate (detection.M) rather than M_gal*(1+z) at trial z. Note this as a known approximation, not a bug, per Phase 14 analysis.

    SAFETY CHECKS:
    - Do NOT modify lines inside the `if not evaluate_with_bh_mass:` block (the "without BH mass" path, lines 536-561)
    - Do NOT modify the denominator function (lines 656-670) -- confirmed correct by Phase 14
    - Do NOT modify the MC sampling code (lines 672-685)
    - Do NOT remove (1+z) from lines 633-634
    - After making changes, run `uv run ruff check master_thesis_code/bayesian_inference/bayesian_statistics.py` and `uv run ruff format master_thesis_code/bayesian_inference/bayesian_statistics.py` to ensure code quality
  </action>
  <verify>
    1. Code inspection: line 646 no longer contains `/ (1 + z)` -- the return is `p_det * gw_3d * mz_integral * galaxy_redshift_normal_distribution.pdf(z)`
    2. Code inspection: line 866 (testing function) also has `/ (1 + z)` removed
    3. Code inspection: lines 633-634 STILL contain `(1 + z)` -- confirming the correct (1+z) factors were preserved
    4. Code inspection: "without BH mass" path (lines 536-561) is UNCHANGED
    5. Code inspection: reference comments present citing Eq. (14.21), (14.22), (14.31), (14.32)
    6. Code inspection: TODO at line 535 resolved with derivation reference
    7. Ruff check passes with no errors
    8. Mypy check passes (if type annotations were affected)
  </verify>
  <done>Spurious /(1+z) removed from production function (line 646) and testing function (line 866), reference comments added citing derivation equations, TODOs about sky weight resolved, p_det(detection.M) documented, code passes ruff/mypy</done>
</task>

<task type="auto">
  <name>Task 2: Denominator consistency audit + MC convergence analysis</name>
  <files>master_thesis_code/bayesian_inference/bayesian_statistics.py</files>
  <action>
    This task completes AUDT-03 (denominator consistency) and AUDT-04 (quadrature-vs-MC asymmetry).

    1. DENOMINATOR AUDIT (lines 656-670):
       Read `denominator_integrant_with_bh_mass_vectorized` and confirm term-by-term against Eq. (14.33):
       - d_L = dist_vectorized(z, h=h) -- correct: luminosity distance at trial z and h
       - M_z = M * (1 + z) -- correct: detector-frame mass for p_det
       - p_det(d_L, M_z, phi, theta, h=h) -- correct: detection probability using M_z (contrast with numerator which uses detection.M)
       - galaxy_redshift_normal_distribution.pdf(z) -- correct: p_gal(z)
       - galaxy_mass_normal_distribution.pdf(M) -- correct: p_gal(M)
       - NO /(1+z) -- correct: denominator integrates over source-frame (z, M) directly
       - NO mz_integral -- correct: denominator has no GW likelihood or conditional decomposition

       Add a reference comment above the function: `# Eq. (14.33) in derivations/dark_siren_likelihood.md`

    2. MC IMPORTANCE SAMPLING AUDIT (lines 672-685):
       Trace the importance sampling algebra:
       - Samples drawn from galaxy priors: z ~ p_gal(z), M ~ p_gal(M)
       - Integrand evaluated at samples: f(z, M) = p_det * p_gal(z) * p_gal(M)
       - Proposal density: q(z, M) = p_gal(z) * p_gal(M) (same as galaxy priors)
       - Weights: w = f(z, M) / q(z, M) = p_det (after cancellation of p_gal terms)
       - MC estimate: E[f] = mean(w) = mean(p_det)

       This is correct. The importance sampling uses the galaxy priors as proposal distribution, and the weights simplify to just p_det.

    3. MC CONVERGENCE ASSESSMENT:
       Estimate the expected MC error:
       - N_SAMPLES = 10000
       - weights = p_det values (each in [0, 1])
       - Relative MC error = std(weights) / (sqrt(N_SAMPLES) * mean(weights))
       - For typical p_det values:
         - If p_det is mostly near 0 or 1 (bimodal): std ~ 0.5, mean ~ 0.5, relative error ~ 0.5/(100*0.5) = 1%
         - If p_det varies smoothly: std ~ 0.3, mean ~ 0.5, relative error ~ 0.3/(100*0.5) = 0.6%
       - Conclusion: N=10000 gives ~1% relative error per galaxy, which is acceptable.

       Add a comment near line 672 documenting this analysis:
       ```python
       # MC importance sampling: proposal = p_gal(z) * p_gal(M), so weights = p_det
       # Relative MC error ~ std(p_det) / (sqrt(N_SAMPLES) * mean(p_det)) ~ 1%
       ```

    4. QUADRATURE-VS-MC ASYMMETRY NOTE:
       The numerator uses fixed_quad (n=50) while the denominator uses MC (N=10000).
       This is a methodology asymmetry but not an error:
       - Numerator is 1D integral over z (galaxy mass analytically marginalized) -> quadrature is appropriate
       - Denominator is 2D integral over (z, M) -> MC is a reasonable choice for 2D
       - Alternative: scipy.integrate.dblquad exists in the testing function (lines 773-789, commented out) for cross-checking
       Document this asymmetry with a comment but do NOT change the integration method.

    5. FINAL CONSISTENCY CHECK:
       Verify that numerator and denominator use the same physics:
       - Both use same galaxy priors: p_gal(z) = galaxy_redshift_normal_distribution, p_gal(M) = galaxy_mass_normal_distribution
       - Both use p_det (though with different M arguments: detection.M in numerator vs M_gal*(1+z) in denominator -- this is the INFO-level observation)
       - Numerator has additional GW likelihood and mass integral terms (p_GW, mz_integral) that correctly DO NOT appear in denominator
       - Neither has the spurious /(1+z) after the fix
  </action>
  <verify>
    1. Denominator function matches Eq. (14.33): p_det * p_gal(z) * p_gal(M), no extra factors
    2. MC importance sampling algebra traced: weights = p_det after cancellation (confirmed by reading lines 676-683)
    3. MC convergence estimate documented: relative error ~ 1% for N=10000
    4. Quadrature-vs-MC asymmetry documented as numerical choice, not physics error
    5. Reference comment added above denominator function citing Eq. (14.33)
    6. No code logic was modified in the denominator or MC sampling (only comments added)
    7. Ruff check still passes after comment additions
  </verify>
  <done>Denominator confirmed correct per Eq. (14.33), MC importance sampling algebra verified (weights = p_det), MC convergence estimated at ~1% relative error for N=10000, quadrature-vs-MC asymmetry documented, reference comments added</done>
</task>

</tasks>

<verification>
- Every term in the "with BH mass" numerator (lines 613-647) has been audited against Section 14.1 mapping
- Spurious /(1+z) removed from both production (line 646) and testing (line 866) functions
- Correct (1+z) factors preserved at lines 633-634
- "Without BH mass" code path (lines 536-561) unchanged
- Denominator (lines 656-670) confirmed correct per Eq. (14.33)
- MC sampling (lines 672-685) confirmed correct with quantified convergence
- Sky localization weight verified in exactly one factor (gaussian_3d_marginal.pdf at line 624)
- Reference comments link code to derivation equations throughout
</verification>

<success_criteria>
AUDT-01: /(1+z) verdict documented with reference to Eq. (14.21), code fixed at lines 646 and 866
AUDT-02: Sky weight verified inside gaussian_3d_marginal.pdf() only, TODO resolved
AUDT-03: Denominator matches Eq. (14.33), no extraneous factors
AUDT-04: MC convergence quantified (~1%), quadrature-vs-MC asymmetry documented
FIX-01: All changes follow physics change protocol, reference comments added
</success_criteria>

<output>
After completion, create `.gpd/phases/15-code-audit-fix/15-01-SUMMARY.md`
</output>
