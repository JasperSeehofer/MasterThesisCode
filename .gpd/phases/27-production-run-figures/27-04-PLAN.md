---
phase: 27-production-run-figures
plan: 04
type: execute
wave: 3
depends_on: ["27-02", "27-03"]
files_modified:
  - paper/sections/results.tex
  - paper/sections/conclusions.tex
  - paper/main.tex
interactive: true

conventions:
  units: "SI (km/s/Mpc for H0, Gpc for distances)"
  hubble_parameter: "h = H0 / (100 km/s/Mpc), dimensionless"
  true_value: "h_true = 0.73"
  significant_figures: "3 for MAP/CI values, 1 for precision/bias percentages"

contract:
  scope:
    question: "Are all pending markers in the paper filled with production values and are all figure references pointing to generated PDFs?"
  claims:
    - id: claim-markers-filled
      statement: "All 25 pending markers and 4 figure todo markers are resolved with production values or figures"
      deliverables: [deliv-results-tex, deliv-conclusions-tex, deliv-main-tex]
      acceptance_tests: [test-no-pending, test-values-consistent]
      references: [ref-extracted-results]
    - id: claim-figures-included
      statement: "All 4 figure environments have includegraphics pointing to generated PDFs"
      deliverables: [deliv-results-tex]
      acceptance_tests: [test-figures-included]
      references: []
  deliverables:
    - id: deliv-results-tex
      kind: other
      path: "paper/sections/results.tex"
      description: "Results section with all pending markers replaced by production values"
      must_contain: ["MAP estimate", "precision", "includegraphics"]
    - id: deliv-conclusions-tex
      kind: other
      path: "paper/sections/conclusions.tex"
      description: "Conclusions with production precision values"
      must_contain: ["precision"]
    - id: deliv-main-tex
      kind: other
      path: "paper/main.tex"
      description: "Abstract with production precision values"
      must_contain: ["precision"]
  references:
    - id: ref-extracted-results
      kind: prior_artifact
      locator: ".gpd/phases/27-production-run-figures/extracted_results.json"
      role: definition
      why_it_matters: "Single source of truth for all numerical values in the paper"
      applies_to: [claim-markers-filled]
      must_surface: true
      required_actions: [read]
  acceptance_tests:
    - id: test-no-pending
      subject: claim-markers-filled
      kind: existence
      procedure: "Search all paper .tex files for \\pending{ -- none should remain"
      pass_condition: "grep -r '\\\\pending{' paper/ returns zero matches"
      evidence_required: [deliv-results-tex, deliv-conclusions-tex, deliv-main-tex]
    - id: test-values-consistent
      subject: claim-markers-filled
      kind: consistency
      procedure: "Verify that MAP and precision values in abstract, results, and conclusions are identical"
      pass_condition: "Same numbers appear in all three locations"
      evidence_required: [deliv-results-tex, deliv-conclusions-tex, deliv-main-tex]
    - id: test-figures-included
      subject: claim-figures-included
      kind: existence
      procedure: "Check that all 4 figure environments have uncommented \\includegraphics lines"
      pass_condition: "4 figures point to existing PDF files in paper/figures/"
      evidence_required: [deliv-results-tex]
  forbidden_proxies:
    - id: fp-no-baseline-values
      subject: claim-markers-filled
      proxy: "Using thesis baseline values where production values are available"
      reason: "Production values are the actual results of this work"
    - id: fp-no-leftover-markers
      subject: claim-markers-filled
      proxy: "Leaving any \\pending{} or figure \\todo{} markers unresolved"
      reason: "All markers must be filled for paper completion (except acknowledgments \\todo{} which is Phase 28)"
  uncertainty_markers:
    weakest_anchors:
      - "If the with-BH-mass grid is too coarse, CI/precision values may be grid-limited rather than physics-limited"
    disconfirming_observations:
      - "If production MAP is further from h_true than thesis baseline, the completeness correction worsened the bias"

estimated_execution:
  total_minutes: 30
  breakdown:
    - task: 1
      minutes: 20
      note: "Fill all pending markers with values from extracted_results.json"
    - task: 2
      minutes: 10
      note: "Verify consistency and build paper"
---

<objective>
Fill all 25 \pending{} markers and 4 \todo{} figure markers in the paper with production values and figure references.

Purpose: This is the final step that turns the paper draft into a complete manuscript with real numbers and figures.
Output: Updated results.tex, conclusions.tex, and main.tex with no remaining \pending{} markers.
</objective>

<execution_context>
@/home/jasper/.claude/get-physics-done/workflows/execute-plan.md
@/home/jasper/.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/phases/27-production-run-figures/27-02-SUMMARY.md
@.gpd/phases/27-production-run-figures/27-03-SUMMARY.md
@.gpd/phases/27-production-run-figures/extracted_results.json
@paper/sections/results.tex
@paper/sections/conclusions.tex
@paper/main.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all pending markers with production values</name>
  <files>paper/sections/results.tex, paper/sections/conclusions.tex, paper/main.tex</files>
  <action>
    Load the extracted results from `.gpd/phases/27-production-run-figures/extracted_results.json`.

    Replace ALL \pending{} markers in the paper with production values. The mapping is:

    **results.tex numerical values:**
    - Line 48: N_det after sigma cut -> n_events_used (from extracted_results.json, without_bh_mass variant)
    - Line 132: h^(no mass) MAP -> without_bh_mass.map_h (format: "0.XXX")
    - Line 134: Update Eq. h^(no mass) = MAP +/- sigma_h
    - Line 138: precision -> without_bh_mass.precision_pct (format: "$\\sim X\\%$")
    - Line 143: h^(mass) MAP -> with_bh_mass.map_h
    - Line 146: Update Eq. h^(mass) = MAP +/- sigma_h
    - Line 149: precision -> with_bh_mass.precision_pct
    - Line 177: N_det for caption -> n_events_used
    - Line 213: subset size -> largest subset size used in convergence figure (from Plan 03)
    - Line 227: N_subsets -> 50 (from Plan 03 convergence analysis)

    **results.tex qualitative paragraphs:**
    - Line 250: P_det results paragraph -> Write 2-3 sentences describing that the current results use P_det=1 baseline; realistic P_det evaluation is deferred to future work due to the remaining p_det normalization issues documented in STATE.md
    - Line 295: Completeness correction effect -> Write 2-3 sentences: The completeness-corrected MAP is h=X.XX (without mass) and h=X.XX (with mass), compared to the P_det=1 thesis baseline of h=0.712 and h=0.742. The correction has [shifted/not shifted] the posterior toward h_true=0.73. Discuss the bias direction honestly.
    - Line 339: P_det normalization effect -> Write 1-2 sentences: Quantification of the residual P_det effect requires a production run with realistic P_det, which is left for future work.

    **results.tex summary table (lines 356-368):**
    - MAP estimate: without_bh_mass.map_h, with_bh_mass.map_h
    - 1-sigma width: without_bh_mass.sigma_h, with_bh_mass.sigma_h
    - Precision (%): without_bh_mass.precision_pct, with_bh_mass.precision_pct
    - Bias (%): without_bh_mass.bias_pct, with_bh_mass.bias_pct
    - N_det used: n_events_used (same for both)

    **results.tex figure \todo{} markers:**
    - Line 55: Uncomment the \includegraphics line and point to `figures/snr_distribution.pdf`. Remove the \todo{} text. If the SNR figure was a placeholder (from Plan 03), keep the \todo{} but update it to note the data is not yet available.
    - Line 173: Uncomment and point to `figures/h0_posterior_comparison.pdf`. Remove the \todo{}.
    - Line 198: Uncomment and point to `figures/single_event_likelihoods.pdf`. Remove the \todo{}.
    - Line 223: Uncomment and point to `figures/posterior_convergence.pdf`. Remove the \todo{}.

    **conclusions.tex:**
    - Line 11: Replace \pending{} with production precision values

    **main.tex abstract:**
    - Line 53: Replace both \pending{} values with production precision percentages

    **Table caption:** Update the \pending{} in the table caption (line 348) to remove the marker.

    IMPORTANT: Use the EXACT values from extracted_results.json. Do NOT round differently than what was computed. Use 3 significant figures for h values and sigma, 1 decimal for percentages.

    IMPORTANT: The acknowledgments \todo{} on main.tex line 98 is for Phase 28, NOT this phase. Leave it untouched.

    IMPORTANT: If the production results show the completeness correction WORSENED the bias (MAP moved further from h_true compared to thesis baseline), state this honestly in the text. This is a valid scientific result, not a bug to hide.
  </action>
  <verify>
    1. `grep -c '\\pending{' paper/sections/results.tex` returns 0
    2. `grep -c '\\pending{' paper/sections/conclusions.tex` returns 0
    3. `grep -c '\\pending{' paper/main.tex` returns 0
    4. All 4 figure \todo{} markers removed (except acknowledgments \todo{} in main.tex)
    5. Values in abstract, results section, and conclusions are consistent (same MAP, same precision)
    6. Summary table values match inline text values
    7. Figure paths point to existing files in paper/figures/
  </verify>
  <done>All 25 \pending{} markers replaced with production values. 4 figure \todo{} markers replaced with \includegraphics. Values consistent across abstract, results, and conclusions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete paper with all production values and figures. Key results:
  - Without BH mass: MAP = [value], precision = [value]%
  - With BH mass: MAP = [value], precision = [value]%
  - Bias direction: [toward/away from] h_true = 0.73
  - 4 publication figures generated
  </what-built>
  <how-to-verify>
    1. Build the paper: `cd paper && pdflatex main.tex && bibtex main && pdflatex main.tex && pdflatex main.tex`
    2. Check that the PDF compiles without errors
    3. Inspect the figures: do they look publication-quality?
    4. Check the numerical values: are they physically reasonable?
    5. Check the summary table: are all cells filled?
    6. Read the P_det and completeness paragraphs: are they honest about the limitations?
    7. CRITICAL: Decide whether the low MAP bias (h < 0.73) is acceptable for the paper, or whether a different framing/analysis is needed
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Phase 28, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Zero \pending{} markers remain (except acknowledgments \todo{} which is Phase 28)
- All numerical values traceable to extracted_results.json
- Values consistent across all three tex files
- Paper compiles to PDF
</verification>

<success_criteria>
Paper has zero \pending{} markers. All figures included with \includegraphics. Values consistent throughout. Paper compiles. Researcher has reviewed and approved the production results.
</success_criteria>

<output>
After completion, create `.gpd/phases/27-production-run-figures/27-04-SUMMARY.md`
</output>
