---
phase: 27-production-run-figures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - master_thesis_code/main.py
  - cluster_results/eval_corrected_full/posteriors_with_bh_mass/h_0_64.json
interactive: false

conventions:
  units: "SI (km/s/Mpc for H0, Gpc for distances)"
  hubble_parameter: "h = H0 / (100 km/s/Mpc), dimensionless"
  true_value: "h_true = 0.73"
  normalization: "peak-normalized posteriors"
  zero_handling: "physics-floor strategy"

contract:
  scope:
    question: "Are the production data files loadable and the pipeline functional for figure generation and number extraction?"
  claims:
    - id: claim-dir-fix
      statement: "The generate_figures directory name matches the actual data directory"
      deliverables: [deliv-main-py-fix]
      acceptance_tests: [test-dir-loads]
      references: []
    - id: claim-data-integrity
      statement: "All non-corrupted posterior JSON files load correctly and event counts are consistent"
      deliverables: [deliv-data-report]
      acceptance_tests: [test-data-integrity]
      references: []
  deliverables:
    - id: deliv-main-py-fix
      kind: code
      path: "master_thesis_code/main.py"
      description: "Fix posteriors_without_bh_mass -> posteriors on line 724"
      must_contain: ["posteriors"]
    - id: deliv-data-report
      kind: report
      path: ".gpd/phases/27-production-run-figures/data_validation.json"
      description: "JSON report of data integrity checks"
      must_contain: ["n_events", "h_values", "corrupted_files"]
  references: []
  acceptance_tests:
    - id: test-dir-loads
      subject: claim-dir-fix
      kind: existence
      procedure: "Run _load_posteriors('posteriors') and verify it returns non-None data"
      pass_condition: "Returns dict with h_values and event_posteriors arrays"
      evidence_required: [deliv-main-py-fix]
    - id: test-data-integrity
      subject: claim-data-integrity
      kind: consistency
      procedure: "Load all JSON files, count events, verify no additional corrupted files"
      pass_condition: "Without BH mass: 23 files, 539 events each. With BH mass: 8 files (excl h_0_64), consistent event counts."
      evidence_required: [deliv-data-report]
  forbidden_proxies:
    - id: fp-no-skip
      subject: claim-data-integrity
      proxy: "Silently skipping corrupted files without documenting them"
      reason: "Missing data points affect posterior resolution and must be tracked"
  uncertainty_markers:
    weakest_anchors:
      - "With-BH-mass variant has only 8 usable h-values (h_0_64 corrupted); posterior may be too coarsely sampled"
    disconfirming_observations:
      - "If more than 1 additional JSON file is corrupted, the with-BH-mass analysis may be unusable"

estimated_execution:
  total_minutes: 20
  breakdown:
    - task: 1
      minutes: 10
      note: "Fix directory name bug in main.py"
    - task: 2
      minutes: 10
      note: "Validate all data files and produce integrity report"
---

<objective>
Fix the known bugs that block the data pipeline and validate the integrity of all production posterior data files.

Purpose: The generate_figures pipeline cannot run because it looks for a non-existent directory name. A corrupted JSON file will crash the combination step. These must be fixed before any downstream extraction or figure generation.
Output: Fixed main.py, data validation report, confirmed event counts and h-value grids for both variants.
</objective>

<execution_context>
@/home/jasper/.claude/get-physics-done/workflows/execute-plan.md
@/home/jasper/.claude/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/phases/27-production-run-figures/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix directory name bug and handle corrupted JSON</name>
  <files>master_thesis_code/main.py</files>
  <action>
    1. In `master_thesis_code/main.py`, line 724, change `_load_posteriors("posteriors_without_bh_mass")` to `_load_posteriors("posteriors")`. This is the actual directory name used by the evaluation pipeline.

    2. Handle the corrupted JSON file `cluster_results/eval_corrected_full/posteriors_with_bh_mass/h_0_64.json`:
       - Try loading it to confirm it is corrupted (JSONDecodeError at char ~108887040)
       - Remove or rename it (e.g., to `h_0_64.json.corrupted`) so it does not crash batch loading
       - Document the removal in the data validation report

    3. Verify the fix by checking that `_load_posteriors("posteriors")` would find the correct directory path relative to the working directory used by `generate_figures()`.

    This is a SOFTWARE change (directory name string), not a physics change. The /physics-change protocol is NOT required.
  </action>
  <verify>
    1. Line 724 now reads `_load_posteriors("posteriors")` (not `posteriors_without_bh_mass`)
    2. The corrupted h_0_64.json is renamed/removed from the with_bh_mass directory
    3. Run `uv run python -c "from master_thesis_code.main import generate_figures"` to confirm the import works (do not call the function, just import)
  </verify>
  <done>Directory name fixed in main.py, corrupted JSON handled, import confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Validate all production data files and produce integrity report</name>
  <files>.gpd/phases/27-production-run-figures/data_validation.json</files>
  <action>
    Write a validation script (inline, not a new file) that:

    1. Loads every JSON file in `cluster_results/eval_corrected_full/posteriors/` and `cluster_results/eval_corrected_full/posteriors_with_bh_mass/` (excluding combined_posterior.json and any .corrupted files)
    2. For each file, records: filename, number of events, whether it loaded successfully
    3. Extracts the h-value grid from filenames (e.g., h_0_73.json -> h=0.73, h_0_626.json -> h=0.626)
    4. Checks consistency: do all files in the same directory have the same event count?
    5. Loads the existing combined_posterior.json for both variants and records: h_values, map_h, n_events_total, n_events_used, strategy
    6. Checks that MAP is not at grid boundary (would indicate posterior not captured by grid)
    7. Checks the SNR threshold used: read constants.py for SNR_THRESHOLD to determine which value was used (15 or 20)

    Save the report as JSON to `.gpd/phases/27-production-run-figures/data_validation.json` with structure:
    ```json
    {
      "without_bh_mass": {
        "n_files": 23,
        "h_values": [...],
        "events_per_file": 539,
        "all_consistent": true,
        "combined_map_h": 0.66,
        "map_at_boundary": false
      },
      "with_bh_mass": {
        "n_files": 8,
        "h_values": [...],
        "events_per_file": ...,
        "all_consistent": true,
        "corrupted_files": ["h_0_64.json"],
        "combined_map_h": 0.68,
        "map_at_boundary": false
      },
      "snr_threshold": 20,
      "data_ready": true
    }
    ```

    Print the report to stdout as well.
  </action>
  <verify>
    1. All 23 without-BH-mass files load successfully with consistent event counts
    2. All 8 remaining with-BH-mass files load successfully (h_0_64 excluded)
    3. MAP values (0.66 and 0.68) are not at grid boundaries
    4. SNR threshold is documented (expect 20 from constants.py)
    5. Report written to disk and is valid JSON
  </verify>
  <done>Data validation report produced confirming 23 + 8 loadable files, consistent event counts, MAP not at boundary, SNR threshold documented</done>
</task>

</tasks>

<verification>
- main.py directory name matches actual data directory
- No corrupted files remain in the loading path
- Event counts are consistent within each variant
- MAP is interior to the h-grid (not boundary-pegged)
</verification>

<success_criteria>
generate_figures pipeline is unblocked (directory name fixed), corrupted JSON handled, all data files validated with integrity report produced.
</success_criteria>

<output>
After completion, create `.gpd/phases/27-production-run-figures/27-01-SUMMARY.md`
</output>
