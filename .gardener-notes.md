---
title: "Gardener Notes — MasterThesisCode"
generated: 2026-05-01T13:00Z
gardener-run: 2
---

# Gardener Notes — MasterThesisCode

## Preload
| Metric | Value | vs baseline |
|---|---|---|
| Most recent logged preload | ~31.6 k tokens | (2026-04-27, Sonnet 4.6 / 200 k context) — stable since Run 1 |
| p_skills | 88 (hook) = 81 gsd-* symlinks + 7 native | unchanged |
| p_agents | 57 = 23 gpd-* + 33 gsd-* + wiki-librarian | unchanged |
| CLAUDE.md lines | 460 | (target: < 500) ✅ |

## Inventory

### Skills
**Native (7 total):**
- `check` — 1+ invocations (2026-04-27) · active
- `physics-change` — 1+ invocations (2026-04-27) · active
- `gpu-audit` — 0 invocations, age ~24d → unused flag
- `integration-test-eval` — 0 invocations, age ~24d → unused flag (operationally load-bearing: UAT spec eval)
- `known-bugs` — 0 invocations, age ~24d → unused flag
- `pre-commit-docs` — 0 invocations, age ~24d → unused flag
- `run-pipeline` — 0 invocations, age ~24d → unused flag

**gsd-* skills (81 symlinks):** All targets exist in skill-store. Valid.

### Agents (57 total)
**gpd-* native agents (23):** All active — `gpd:plan-phase`, `gpd:debug`, `gpd:execute-phase` all invoked 2026-04-27 to 2026-04-30.
**gsd-* symlinks (33):** All targets valid.
**wiki-librarian:** Symlink to `~/.claude/agent-store/wiki-librarian.md` · verified present.

## Auto-applied
None this run.

## Proposals — requires approval

- **[P-01 unused-skill]** `gpu-audit` — 0 invocations, age 24d. Load-bearing if GPU cluster jobs resume. Recommend keeping; reassess at run 3.
- **[P-02 unused-skill]** `integration-test-eval` — 0 invocations, age 24d. Operationally load-bearing (UAT spec evaluation). Keep.
- **[P-03 unused-skill]** `known-bugs` — 0 invocations, age 24d. Useful reference; human decision on keep/delete.
- **[P-04 unused-skill]** `pre-commit-docs` — 0 invocations, age 24d. Low preload cost; keep unless storage is a concern.
- **[P-05 unused-skill]** `run-pipeline` — 0 invocations, age 24d. Load-bearing for cluster submission workflow. Keep.

## CLAUDE.md ↔ vault drift

- **[orphan]** CLAUDE.md lines 236–263: HPC/GPU Best Practices section — multiple patterns in `cross-project-memory.md` tagged `[Python, HPC, SLURM, simulation]` but no `wiki/concepts/hpc-gpu-patterns.md` concept page exists
- **[orphan]** CLAUDE.md lines 289–329: Math/Physics Validation Workflow (physics-change protocol) — patterns in `cross-project-memory.md` all `Verified: tentative`; no `wiki/concepts/physics-change-protocol.md` concept page
- **[orphan]** CLAUDE.md lines 398–452: GSD→GPD routing table — routing logic documented only in CLAUDE.md; no `wiki/concepts/gsd-gpd-routing.md` concept page (closest is `wiki/analyses/gsd-gpd-coordination.md`, which covers high-level coordination but not the per-command routing table)

No stale or contradicted entries detected.

## Staleness flags touching this repo

None in `staleness-flags.md ## Open`.

## Flow
- Links checked: 0 (`[[wikilinks]]` in CLAUDE.md: 0; no `.claude/rules/` directory)
- Lazy-load candidates: 3 (HPC/GPU Best Practices, Math/Physics Validation Workflow, GSD→GPD Routing Table — all candidates for `.claude/rules/` files with `paths:` globs)

## Next gardener run
- Scheduled: 3 days from now (2026-05-04)
- Triggers for earlier run: preload > 34 k, new skill/agent added, CLAUDE.md growth > 40 lines (close to 500-line limit), Phase 45 cluster jobs resume (would activate gpu-audit + run-pipeline)
