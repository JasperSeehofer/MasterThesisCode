# Physics-change gate ledger

Append-only record of every `/physics-change` hard-gate run. Its purpose is to make gate
compliance **evidence** rather than **inference**: a `[PHYSICS]` commit with no ledger row is a
gate that cannot be shown to have run.

**This ledger starts 2026-07-30.** `[PHYSICS]` commits before that date have no rows and must
not be back-filled — their gate compliance is genuinely unrecorded, and inventing rows would
destroy the property the ledger exists for.

## Row format (stable — do not reorder columns)

```
| YYYY-MM-DD | <commit-ref> | <step> | <verdict> | <target> | <note> |
```

| Field | Values |
|---|---|
| `YYYY-MM-DD` | date the step completed |
| `<commit-ref>` | short SHA once committed, or `pre-commit` if the commit does not exist yet |
| `<step>` | `presented` (the 5-item gate was put to the user) · `implemented` (code written after approval) · `verified` (post-implementation checks reported) |
| `<verdict>` | `APPROVED` · `REJECTED` · `PASS` · `FAIL` · `WAIVED` (with a reason in `<note>`) |
| `<target>` | `file.py:line` or `file.py` — the physics file changed |
| `<note>` | one clause: what changed, or why waived |

Greppable: every ledger row starts with `| 20`.

```bash
grep '^| 20' docs/gates/PHYSICS-GATE-LEDGER.md          # all rows
grep '^| 20' docs/gates/PHYSICS-GATE-LEDGER.md | grep FAIL
git log --oneline --grep='^\[PHYSICS\]'                 # cross-check against commits
```

A complete gate run leaves three rows (`presented` → `implemented` → `verified`) sharing a
target; a run that stopped at `REJECTED` leaves one. `pre-commit` rows should be updated to the
real short SHA when the commit lands — the trailing `<note>` is free text, the first five fields
are not.

## Ledger

| Date | Commit | Step | Verdict | Target | Note |
|---|---|---|---|---|---|
<!-- APPEND NEW ROWS BELOW THIS LINE — newest last -->
| 2026-08-04 | pre-commit | presented | APPROVED | bayesian_statistics.py:4226-4293 | Fix A C7-core: host-z volume_deconv kernel gains f_k(z) selection weight (GATE_PACKAGE_FINAL.md §1.2); author approved with honest framing (rail persists, 1D moves down) |
| 2026-08-04 | pre-commit | presented | APPROVED | bayesian_statistics.py:3296-3331 | Fix B C8 half: 2D completion leg mass density g_i (measure-invariance PROVEN); C9 half NOT presented — blocked on gates ii-b/ii-c, author asked for measurement rationale |
| 2026-08-04 | pre-commit | presented | APPROVED | bayesian_statistics.py (selection stack + mixture) | Fix B path-A joint C9+C8 (FIXB_PATHA_PACKAGE.md §3): S̄_φ replaces fitted S_3D in all three slots, D^φ, g-inside, w̃_G=α_G^φ/D̃^φ. Author decisions: D1=both (S_and now, retire stale p0 bounds next campaign; p0-window onto 2D-bias suspect list), D2=delivered-convention pins primary with MANDATORY promotion to truth once truth Σ4D(h) measured at 41h, D3=point form. Gate (ii) demoted to monitored consistency number; ship-on-correctness |
| 2026-08-04 | pre-commit | implemented | PASS | bayesian_statistics.py:4351 | C7-core: host-z volume_deconv kernel carries the catalogued-host intensity f_k(g)(z)·w_pop(z) in numerator + Z_g/D_g (batched :4351/:4437/:4656, scalar twin :3865); ZoA all-zero-window falls back to the pre-C7 kernel, warn once, no elementwise clamp; ratified in GATE_PACKAGE_FINAL.md §1 (author-approved 2026-08-04) |
| 2026-08-04 | pre-commit | verified | PASS | bayesian_statistics.py:4351 | Sign: γ_f = dln f/dln z ≤ 0 ⇒ the kernel's z-weight and h_eff can only move DOWN, bounded by f ≡ 1 (measured Δ/e² = −0.400 ± 5% over e = 0.04/0.02/0.01, matching the stub's analytic γ_f(0.1) = −0.4). Units: f dimensionless ∈ (0,1] ⇒ ρ_g still a unit-mass density in z; N_g, D_g, Z_g, Σ_glob, w_G unit-unchanged. Limits: f ≡ 1 byte-identical to HEAD (all 3 modes × both channels); σ_z → 0 exact and f-independent (gap monotone, 1.2e-2 → 2.0e-3 over σ_z = 6e-3 → 1.5e-3, residual is the known n=50 numerator-window aliasing floor); kernel h-invariance exact (f_k h-independent to <1e-10 via the m_star cancellation, w_pop·f_k ratio z-separable to <1e-10); w_G = β_G/D bit-identical (pure quadrature) and the #51 point-kernel numerator bit-identical. Pins moved: PIN_VD/PIN_VT/PIN_CLAMP (see the test file's re-pin block); PIN_LR unchanged. Batched/scalar parity rtol 1e-9 with completeness threaded through child_process_init. |

| 2026-08-04 | pre-commit | implemented | PASS | bayesian_statistics.py (selection stack + mixture), dark_siren_injection.py | S̄_φ = ∫φ S_4D dlog10M on the production pooled-2D with-BH object (pdet_wbh_z_resolved=False); new φ-convention tables β_G^φ/β_Ḡ^φ/Σᶲ/D^φ consumed by absolute_marginal ONLY (legacy tables + generator_marginal byte-identical, gate iii-a); mixture w̃_G=α_G^φ/D̃^φ, α_G^φ=β_G^φ·r_Malm; (N8) B_num_wbh with g_i inside the quadrature, 1D B_num unmultiplied; φ exported once from dark_siren_injection (never re-typed); point-mass evaluation (D3); instrumentation (w_G RENAMED to w_G_legacy, 7 s.f. diagnostics, T9 Σ4D band shares, g_i support-exit); monitored gate (ii) under S_and (ρ=0.7305) |
| 2026-08-04 | pre-commit | verified | PASS | bayesian_statistics.py (selection stack + mixture) | Dimensions: S̄_φ dimensionless, β^φ/D^φ in [p_pop dz], r_Malm/r_φ dimensionless, g_i the ONLY x_M density (2D completion only) — no leg gains/loses a mass measure. Signs: S̄_φ ∈ [0,1] monotone in d_L; α_G^φ, D̃^φ > 0; w̃_G ∈ (0,1). Anchors reproduced on the pool/catalogues of record: β_G^φ=1.533228e8, β_Ḡ^φ=8.884038e8, D^φ=1.041727e9, Σᶲ_del=9.5623703e8, r_Malm_del=0.4415122, w̃_G=0.0708023 — all ≤4e-8 rel. Gates: (i) 2D exactly homogeneous in x_M at 1e-14 and h-independent (dMAP/dlnC=0); (iii-a) #51 1D digest + generator_marginal unmoved; (iv) 1D bitwise invariant; T8 r_φ≡1 exact; L4 s=0 tilt vanishes; L5 σ_Mz→0 finite=point form; falsifier (c) window-collapse limit. pytest -m "not gpu and not slow" 1192 passed / 15 skipped, ruff+mypy clean. Gate (ii) NOT evidence (monitored: −0.48 under S_and) |
| 2026-08-12 | 87c6670b | presented | PENDING AUTHOR RATIFICATION | bayesian_statistics.py:dark_mass_density_per_mass | phi(M) two-segment affine swap, 5-item package in PERF_ROADMAP.md §5 + test file docstring |
| 2026-08-12 | 87c6670b | implemented | PASS | bayesian_statistics.py:dark_mass_density_per_mass | ref comments + regression pins + seam test + tripwire test |
| 2026-08-12 | 87c6670b | verified | PASS | bayesian_statistics.py:dark_mass_density_per_mass | sign/units unchanged (same phi, same normalisation); limits: pins at band edges + kink; adversarial verify CONFIRMED 1.8e-15; counterfactual smoke tolerance registered (rel 1e-8, 2D channel) |
| 2026-08-12 | dfedf19c | presented | DIRECTION APPROVED 2026-08-12 / PACKAGE PENDING AUTHOR RATIFICATION | bayesian_statistics.py:completion_mass_factor_g | Route 1 adaptive GH order, package in ROUTE1_GATE_PACKAGE.md |
| 2026-08-12 | dfedf19c | implemented | PASS | bayesian_statistics.py:completion_mass_factor_g | adaptive path + 7 tests incl. fast-order pin + gate doc |
| 2026-08-12 | dfedf19c | verified | PASS | bayesian_statistics.py:completion_mass_factor_g | xhigh adversarial CONFIRMED, bound 2.5e-37, in-support max 9.6e-16, off-support zero-vs-dust divergence registered, smoke rel 1.26e-14, 9.28x cumulative |
| 2026-08-12 | 7c58f31e | ratified | RATIFIED | bayesian_statistics.py:dark_mass_density_per_mass | phi(M) two-segment affine swap 87c6670b — author ratified 2026-08-12 ("all approved"), incl. rel-1e-8 2D tolerance class registered as a divergence |
| 2026-08-12 | 7c58f31e | ratified | RATIFIED | bayesian_statistics.py:completion_mass_factor_g | Route 1 adaptive Hermite dfedf19c — author ratified 2026-08-12 ("all approved"), incl. off-support zero-vs-dust divergence registered |
| 2026-08-17 | 44aa239e | presented | APPROVED | bayesian_statistics.py:4344,:4295 | selection fusion [P1]+[P2]: 5-item package = GATE_PRESENTATION_SELECTION_FUSION_20260817.md (verifier GO-W-AMENDMENTS 44aa239e); approval = ledger rows #117 item 1 + #118 G1 (adaptive+guard) / G2 (ratio+track) / G3 (deferral confirmed) |
| 2026-08-17 | 2b10b8b8 | implemented | PASS | bayesian_statistics.py (completion legs), arguments.py, main.py | [P1] completion_mass_factor_g_sel (new callable; g_i untouched for external callers) + [P2] S_bar_phi default-on via 'fused' cell ('auto' default resolves per normalization mode); G1 adaptive+S-var guard (_G_SEL_S_VAR_TOL); MINOR-1/2/6 folded; ref comments (MFG 2019 Eqs. 5-7) + amended [P5]-1 suite in test_selection_fusion.py; pre-change pins recorded at 4ab5da0e |
| 2026-08-17 | 2b10b8b8 | verified | PASS | bayesian_statistics.py (completion legs) | Sign/bounds: S_4D in [0,1] => 0 <= g_sel <= g_i measured TRUE; monotone suppression only. Units: S dimensionless => g_sel stays a 1/x_M density, same measure as mz_integral (gate (i) addability preserved). Limits: S==1 recovers g_i BIT-exact on both quadrature paths (test); S_bar_phi==1 recovers old B_num (pins); constant S=c => exact c-scaling (closed form, rtol 5e-15); beyond-horizon S->0 zeros classified. G1 recorded bound: adaptive-vs-pinned max rel 6.65e-16 (smooth S, production-regime rows). Byte-identity: off/1d cells reproduce pre-change pins EXACTLY; generator_marginal auto->off; 1506 passed / 15 skipped, ruff+format+mypy clean |
| 2026-08-19 | 4128eab2 | presented | APPROVED | bayesian_statistics.py:5514,:6000 (mz_integral, both kernels) | Counterfactual instrument --catalogue_mass_overlap {production,neutralized,inflated}: gate presentation = PREREGISTRATION_PROD_COUNTERFACTUAL.md v2 §6 (verifier NOT-READY→v2: V1' measure-coherent neutralization, batch-kernel + N-2 anti-no-op gates, V2 pure-width frozen-mu_eff k={0.5,2.0}); author approval verbatim "approved and please go ahead. you can do the cluster steps aswell" (2026-08-19) |
| 2026-08-19 | d1681ed9 | implemented | PASS | bayesian_statistics.py (both kernels + child_process_init), arguments.py, main.py | Default stream untouched (guard else-branch); neutralized = completion_mass_factor_g with completion-leg d_L-only (proj, sigma_cond) threaded via new initargs; inflated scales sigma_gal only, mu_eff frozen at k=1; argparse rejects scale!=1 outside inflated; 22-test suite test_catalogue_mass_overlap.py (scalar↔batch parity rtol 1e-12; k=1 bit-identity; neutralized mass-invariance; 1D bit-identity; mode guards); full suite 1568 passed / 15 skipped; ruff+mypy clean. Local N-0 blocked by cluster-only injection pool (probe_n0_local.py fails fast, documented); registered N-0 runs as cluster V0 probes. N-1 PASS, N-2 engagement 93.9% local smoke |
| 2026-08-19 | 5e232f3c | presented | APPROVED | bayesian_statistics.py:4904-4906 (B_scale) | Gate presentation = docs/derivations/bscale_completion_normalization.md §7 (verdict DEFECT: un-derived two-detection-model ratio on the completion leg, MFG-A2; derived form B_num^phi = B_num); author approval verbatim "approved" (2026-08-19, ledger row #131 →#132) |
| 2026-08-19 | b2e77940 | implemented | PASS | bayesian_statistics.py (path_a_completion_numerators + p_Di), arguments.py, main.py, fixb_pathA erratum | Default = derived (B_scale dropped); legacy preserved via --completion_b_scale legacy (historical reproducibility); mode dispatch factored into testable path_a_completion_numerators; pins: both-mode combine arithmetic + limiting case (equal detection models ⇒ modes degenerate); legacy pin test moved to explicit legacy mode, expected numbers unchanged; validation bed = banked bscale counterfactual (removal = derived-form posterior exactly: 2D 0.6771/0.6788). Full suite 1582 passed / 15 skipped; ruff+mypy clean. Production 2D expected to move +0.054/+0.067 → ≈ −0.05 (base tilt re-exposed; tilt ledger remains open by design) |
| 2026-08-19 | 6d9e21a1 | presented | APPROVED | bayesian_statistics.py:5629-5633 (scalar `_host_M_eff`), :6182-6191 (batch `host_M_eff`); precompute_global_catalog_selection with_bh_mass branch (:2707-2731 pre-edit) | Instruments E (--eddington_m {on,off}) + J (--sigma4d_mass_kernel {point,kernel}); 5-item gate presentation = results/prod2d_closure_20260818/PREREGISTRATION_TILT_BATTERY.md v2 §6 (verifier P1-P5 amendments applied, instrument F withdrawn by derivation); author approval = rows #130-#132 "all approved" [DO] |
| 2026-08-19 | 6d9e21a1 | implemented | PASS | bayesian_statistics.py (both kernels + precompute_global_catalog_selection + new helpers `_eddington_shifted_host_mass_batch`, `_sigma4d_mass_kernel_expectation`, `write_selection_table_json`), arguments.py, main.py | E: single `_host_M_eff`/`host_M_eff` guard-pattern assignment (off ⇒ raw host_M, eddington_shifted_host_mass never called) switches numerator mu_gal + D_g erf-sum together in both kernels; stale -0.020 comment refreshed to cite G7row9_N5 (-0.0022) + this instrument. J: with_bh_mass=True branch of precompute_global_catalog_selection gains a "kernel" mode reusing the erf-sum inner-M machinery of `_bh_mass_denominator_inner_m_integral_batch` (via new `_sigma4d_mass_kernel_expectation`, chunked 200k rows) at M_eff_g = the SAME Eddington-shifted mean (gated by --eddington_m), sigma_g = catalogue BH_MASS_ERROR; NO R_eff/mass_trunc inside the kernel; P4 banked JSON (`write_selection_table_json`) always written per-h alongside the phi-selection log. New tests: test_eddington_m_instrument.py (11 cases: on≡default, scalar↔batch parity, sigma_M→0 collapse, 1D bit-identity, mode guard), test_sigma4d_mass_kernel_instrument.py (12 cases: sigma_g→0 collapse, [0,1] bounds, chunk-invariance, Eddington-batch↔scalar parity, point≡default, mode guards, JSON round-trip). Full suite 1605 passed / 15 skipped / 27 deselected; ruff+format+mypy clean. No pre-existing pins touched (both instruments default byte-identical to production). |
| 2026-08-20 | (next commit) | presented | APPROVED | bayesian_statistics.py:4904-4950 (`completion_numerator_integrand`'s event term) | B-DEN falsifier instrument (--completion_event_measure {ratio,data}); gate presentation = docs/derivations/completion_numerator_data_measure.md §6 (AMENDMENT A-5, results/prod2d_closure_20260818/PREREGISTRATION_1D_CORRESPONDENCE.md); author approval 2026-08-20 |
| 2026-08-20 | 606af0e0 | implemented | PASS | bayesian_statistics.py (`completion_numerator_integrand` + siblings `completion_numerator_integrand_sel_1d`/`completion_numerator_integrand_with_bh_mass`, which derive from it and inherit the switch unmodified), arguments.py, main.py, darksiren_emri/validation/correspondence_1d.py (new "bden" fleet arm, ARM_EVENT_MEASURE registry), results/prod2d_closure_20260818/correspondence_fleet.sbatch (142 tasks, --array=0-141) | Default 'ratio' is the untouched original expression (guard pattern, no always-executed multiply-by-1.0); 'data' evaluates norm.pdf(_comp_det_d_L; loc=d_L, scale=sigma_frac*d_L) -- mapping verified exact since _comp_mean_dLfrac is hardcoded to 1.0 in _means_3d's construction (asserted once per event, not assumed). New tests: test_completion_event_measure_instrument.py (19 cases: ratio≡default bit-identity via p_Di harness, sigma_frac→0 collapse, ∫dd_hat "data"≈1 vs "ratio"≈d_L·(1+3σ²) numerical check via scipy.integrate.quad, invalid-mode guard, CLI/run_metadata plumbing) + harness plumbing tests in test_correspondence_1d.py (ARM_EVENT_MEASURE registry + per-arm forwarding proof). Full suite 1663 passed / 15 skipped / 27 deselected; ruff+format+mypy clean. No pre-existing pins touched (instrument defaults byte-identical to production). |
| 2026-08-20 | pre-commit | presented | PENDING | darksiren_emri/validation/correspondence_1d.py:1965, :2479 (log-space `-1.0e300` sentinel) and :1967 (`np.gradient` moment weights) | Harness numerical-hygiene gate, NOT a physics-formula change: (1) replace the log-space sentinel with production's registered `CombinationStrategy.PHYSICS_FLOOR` (`posterior_combination.py:219-273`) — the sentinel is bit-identical to correct `-inf` when >=1 grid node survives (max\|d mean_h\| = 0.000e+00 over 98 banked seeds) but, when EVERY node is masked, banks a finite normalizable posterior where correct `-inf` would fire the harness's own `isfinite().any()` guard; blast radius 25/123 seeds, catalogue-mode 25/70, population 0/53. (2) replace `np.gradient(grid)` with true trapezoid weights (endpoints doubled: 0.010 vs 0.005; sum 0.27 vs interval 0.26), shifting bsel/bself/bden by ~3.7e-3 (within their 0.005 band, above bsel's 1.7e-3 SE). A third proposed change (`_hpd_contains` early return) was WITHDRAWN — tested against an analytic Gaussian at levels 0.50/0.68/0.90, the code is correct in 6/6 cases. 5-item gate presentation = docs/derivations/GATE_PRESENTATION_SENTINEL_COMBINE_20260820.md; evidence = ledger row #145 + PREREGISTRATION_1D_CORRESPONDENCE.md AMENDMENT A-7. AWAITING AUTHOR RULING. |
| 2026-08-20 | pre-commit | presented | APPROVED | darksiren_emri/validation/correspondence_1d.py:1965, :2479 (log-space `-1.0e300` sentinel) and :1967 (`np.gradient` moment weights) | Author ruling, verbatim: "please continue, approved" — given on the 8-item decision table of docs/derivations/GATE_PRESENTATION_SENTINEL_COMBINE_20260820.md §6, which mixed [DO] and [RULE] tags; the itemisation below is orchestrator-derived. Grants [DO] items 1 (sentinel fix), 2 (trapezoid-weight fix), 7 (open the g_frac=NaN thread next) and [RULE] items 3-6, 8 (A15 adopted). Supersedes the PENDING row above. |
| 2026-08-20 | pre-commit | implemented | PASS | darksiren_emri/validation/correspondence_1d.py (new `combine_log_likelihood` + `moment_weights` helpers; `compute_seed_statistics` and `compute_full_log_posterior_vector` rewired) | Both corrections default ON; superseded behaviours retained behind `zero_handling="legacy_sentinel"` / `weights_convention="legacy_gradient"` SOLELY so the banked fleet stays reproducible (GATE R-0a of AMENDMENT A-7 depends on it). Verified: legacy mode reproduces all **123/123** banked arm seeds bit-exactly, for the registered statistics AND the full 46-node `log_posterior` vectors. New explicit guard: a seed with no finite node now RAISES instead of emitting a number (the superseded path emitted the grid midpoint; correct `-inf` would have emitted NaN — see the correction note in ledger row #145's second addendum, which withdraws an earlier claim that an `isfinite().any()` guard already existed). New tests: darksiren_emri_test/validation/test_sentinel_combine_fix.py (16 cases: float64 absorption, the banked degenerate triple 0.7299999999999999/0.07784719124788758/0.600 pinned under legacy mode, grid-midpoint identity, refusal of an uninformative seed, all-zero-event drop-out equivalence, N-0 bit-identity with no zeros, sentinel≡-inf when a node survives, endpoint-weight doubling, interior-weight agreement, flat-density exactness, textbook uniform-grid weights, two mode guards, and 3 parametrised `_hpd_contains`-vs-analytic-Gaussian pins). Full suite 1684 passed / 15 skipped / 27 deselected; ruff + format + mypy clean. |
| 2026-08-22 | pre-commit | presented | APPROVED | darksiren_emri/validation/correspondence_1d.py:303-323 (PRODUCTION_FLAGS completion-cell pin) | D2 off→fused basis switch for FUTURE runs-of-record. 6-item package = docs/derivations/PROPOSAL_FUSED_BASIS_AND_IMPOSTOR_DIRECTION_20260821.md §1-D2 (old formula = off-cell numerator vs S̄_φ-carrying normalizer, the ratified IMPLEMENTATION-CONVENTION DEFECT rows #157/#158; new = fused, both legs one table — O6-verified object identity; ref Gray 2020 + rows #117-#118; dimensionless S̄_φ; limiting case S̄_φ≡1 recovers off; regression = O6 GATE D6 bit-exactness + F6 anchor + suite green). Materiality measured zero on MAP/width (row #119). Author ruling: row #159 D2 = "A: Fused for future runs". |
| 2026-08-22 | pre-commit | implemented | PASS | darksiren_emri/validation/correspondence_1d.py (pin + docstrings), darksiren_emri/validation/selfgen_control.py (docstring), darksiren_emri_test/validation/test_correspondence_1d.py (pin test re-anchored with provenance) | Pin flipped to "fused"; every banked arm's historical cell stays explicitly registered in ARM_SELECTION_CELL (regeneration is pin-independent — asserted by the updated test); off-cell regeneration requires the explicit "off" kwarg (O6 GATE D6 / P3 GATE R-P3 pattern). Targeted suite 44/44; ruff+mypy clean. Past runs-of-record STAND on off with the row #119 bridge. |
| 2026-08-22 | pre-commit | presented | PENDING | darksiren_emri/bayesian_inference/bayesian_statistics.py (catalogue-leg per-candidate S̄_φ pairing + class-weight re-derivation β_G_φ→β_G) | The [P2]+[P3] completion. 6-item package = docs/derivations/PROPOSAL_P3_MIXTURE_WEIGHT_20260822.md (derivation: per-candidate S̄_φ(z_g) forced inside the sum by the latent model; the global β_G_φ is its ensemble mean — using both double-counts). Evidence rows #159-#164: twin +0.0155±0.0037 (LEVERAGE only — all-impostor venue), shape-null +0.00057, level +0.01493. Recommendation: registered candidate + verification plan (normalizer-completion instrument; decisive catalogued-host responsibility-identity test), NOT production adoption. AWAITING AUTHOR RULING. |
| 2026-08-22 | pre-commit | presented | APPROVED | (same row as above) | Author ruling, verbatim: "please continue and approved" — row #166: §4 items 1-2 granted as recommended (registered candidate + verification plan; production adoption NOT granted; returns as its own gate if the identity test passes). Supersedes the PENDING row above. |
| 2026-08-22 | pre-commit | presented | PENDING | darksiren_emri/bayesian_inference/bayesian_statistics.py:4823/:3826 (no-BH catalogue divisor Σ³ᴰ→Σ^φ) | The fourth Path-A slot. 6-item package = docs/derivations/PROPOSAL_SIGMA_PHI_DIVISOR_20260822.md (derivation = the A20/RPHI review's ratified algebraic-exactness argument: Σ^φ is the unique divisor making β_G_φ·L_cat = A/n̂_w^φ; makes the code's own documented r_φ≡1 invariant true; with-BH channel is the correct internal control). Measured venue effect −0.0043±0.0007 (anti-conservative). Recommendation: measure first (production r_φ + counterfactual) — rows #171-#172. AWAITING AUTHOR RULING. |
| 2026-08-23 | pre-commit | instrumentation | ORCH-RULED (measure-first) | darksiren_emri/bayesian_inference/bayesian_statistics.py (+arguments.py, main.py, correspondence_1d.py) | Autonomous-session orchestrator rulings [ORCH-RULE 1–3], flagged for the author's trace (row #174): Σ^φ proposal §7 ruled as recommended — measure-first; NO formula change adopted; the counterfactual flag `catalogue_global_selection ∈ {s3d,phi}` (default s3d, byte-identical; COUNTERFACTUAL log line; 15 tests + S̄→cS̄ degree test) implements verification-plan item (ii). Item (i) production r_φ deferred to cluster (pool absent locally). Production adoption returns to the author as its own gate after the b0 identity verdict + the production r_φ read. |
| 2026-08-24 | pre-commit | adopted | APPROVED (author: "decisions approved", row #178) | darksiren_emri/bayesian_inference/bayesian_statistics.py:4898 (no-BH catalogue divisor Σ³ᴰ→Σ^φ; default "auto"→"phi" under absolute_marginal; "s3d" = counterfactual) | Supersedes the PENDING row of 2026-08-22. 6-item package = PROPOSAL_SIGMA_PHI_DIVISOR_20260822.md + appended VERIFICATION RESULTS (production r_φ(0.73)=0.885984 row #176; venue-internal 1/r_φ(h) confirmation row #177; S̄→cS̄ degree test on the default path; with-BH leg bit-unchanged; stale 0.9119 comment corrected [A11]). Implementation independently verified COMMIT-READY (one date AMEND applied). Suite 1741 green; zero numeric expectation drift outside the flag's own tests. Pre-row-#178 banked arm artifacts are s3d-slot (code note at PRODUCTION_FLAGS). |
| 2026-08-25 | pre-commit | presented | APPROVED (author, row #195 item 1: "all approved") | darksiren_emri/bayesian_inference/bayesian_statistics.py (catalogue_numerator_survival default "off"→"auto") | Package = docs/derivations/PROPOSAL_CATALOGUE_TWIN_PRODUCTION_20260825.md (evidence chain rows #149–#187: derivation → mechanism → leverage → TWIN-CALIBRATED); mirrors the row-#178 Σ^φ adoption exactly. |
| 2026-08-25 | pre-commit | implemented | PASS | bayesian_statistics.py:3398,:3502-3546; correspondence_1d.py:2700 | "auto"→"phi" under absolute_marginal else "off"; explicit "off" = the COUNTERFACTUAL; new test_catalogue_numerator_survival.py (24 tests); suite 1821 passed; ruff+format+mypy clean. |
| 2026-08-25 | pre-commit | verified | PASS (COMMIT-READY) | bayesian_statistics.py:3398 (single read site) | β_G_φ/D̃_φ/Σ-chain untouched (p_Di-level proof); with-BH channel bit-unchanged; constant-table exact-ratio + varying-table engagement; scalar/batch parity; AMEND-1 applied (score2d pins "off" explicitly + stamps it); AMEND-2 noted (stale COUNTERFACTUAL log-substring gates in the banked p3_twin/p3_b0 instruments — the accepted row-#178 staleness precedent; fix on next use, never re-run banked). |
| 2026-08-25 | pre-commit | presented | APPROVED (author ruling via decision table: "(a) Adopt symmetric (Recommended)") | darksiren_emri/galaxy_catalogue/handler.py (mass_filter_sigma default "asymmetric"→"symmetric") | Package = docs/derivations/PROPOSAL_MASS_FILTER_SYMMETRIC_20260825.md (6-item; evidence chain rows #191–#201: pilot STOP → forensic → Gate-B DEFECT → measure-first grant row #198 → mirror EXCLUSION-MATERIAL row #200 → production read row #201); mirrors the row-#178/#195 adoption pattern. |
| 2026-08-25 | pre-commit | implemented | PASS | handler.py:570 (+ bayesian_statistics.py:3249,3311,3461; correspondence_1d.py:2716 — default declarations only, read-site logic untouched) | Default flip at 5 declaration sites; explicit "asymmetric" = the COUNTERFACTUAL pinning the retired pre-flag window; tests updated (default-symmetric + explicit-asymmetric bit-identity + event-113 retention); suite 1827 passed; ruff+format+mypy clean. |
| 2026-08-25 | pre-commit | verified | PASS (COMMIT-READY after 6 comment AMENDs) | handler.py:655 (single read site) | Independent verification: diff scope exactly the 5 defaults + tests; read/validate logic untouched; explicit-asymmetric counterfactual path intact in all drivers (ARM_MASS_FILTER + wza0/proda0 explicit); end-to-end default trace confirms no package site hardcodes "asymmetric"; 6 stale comment blocks found and corrected pre-commit. |
| 2026-08-27 | pre-commit | implemented | PASS | darksiren_emri/validation/correspondence_1d.py:1737-1760 (`_draw_2d_accepted_latents`: `_M2D_MASS_FLOOR` deleted, `M <= 0` REJECTED in-loop instead of floor-clipped); results/campaign51_20260728/realistic_20260729/p3_2d_fleet.py:`_identity_inputs_2d`/`stage_lhs2d` (dead-row convention PA-2D-1 F16 honoured over ALL F-0-accepted rows, matching `ca_rhs_scorer.py:1315-1324`) | [P3-2D] class-G venue repair, Defects 1+2 of the residual ladder (`p32d_residual_accounting_20260827.md`). `correspondence_1d.py` is a validation instrument, but `_draw_2d_accepted_latents` IS the venue's draw law, so a ledger row is filed either way (PA-2DR-8). Draw-law change is a SUPPORT correction: the venue accepted latents at a spurious point mass `M == 1.0 M_sun` (793/4800 pre-repair) while the companion that DEFINES Σ̃^4D assigns `S_4D(M<=0) := 0` (`p3_2d_companion.py:281`) — clip-then-accept drew from the wrong support. Limiting case + external check: with constant `S_4D` and a shared `(M, M_error)` the accepted masses must follow `N(mu,sigma)|M>0` by rejection sampling's own closed form — new KS gate passes on the repair and FAILS the pre-repair code at `D=0.1103, p=1.2e-21` (218/2000 at the old floor). RNG-stream integrity: new draw-count instrumentation asserts exactly `4 x batch` variates per round on an engineered ~50%-invalid pool, round-1 byte-identical to a verbatim pre-repair reference. Suite 1831 passed / 15 skipped; ruff+format+mypy clean. Run pre-registered in `PREREGISTRATION_P3_2D_REPAIR_20260827.md` REGISTERED DESIGN v2 — note rung 1 (S̄_φ, ×1.1585) is NOT in this change and is NOT authorised. |
| 2026-08-28 | pre-commit | presented | APPROVED (author, ledger row #216: "all approved" against the Runbook 37 Docket + the presentation) | darksiren_emri/bayesian_inference/bayesian_statistics.py (single_host_likelihood / single_host_likelihood_batch / _smeared_global_pdet_expectation host-z kernel) | Package = results/campaign51_20260728/realistic_20260729/PHYSICS_CHANGE_THETA_HOOK_20260828.md ([HIER] θ-hook C1+C2, PA-HIER-28 item 3 GATE): θ=(b,s) affine photo-z reparametrization z̃=z+b(1+z), σ̃=s·σ_eff at sites 2.1/2.2/2.3; literal skip at (0,1); b-after-PV-fold pin; Ma, Hu & Huterer (2006) arXiv:astro-ph/0506614 §2. |
| 2026-08-28 | pre-commit | implemented | PASS | bayesian_statistics.py (sites 2.1/2.2/2.3 + evaluate()/p_Di threading + PA-HIER-23 diagnostics); correspondence_1d.py (docstring fix + _B0I_ZTRUE_GRID_N 401→4001, PA-HIER-30) | Ref comments above each hooked line; regression tests written BEFORE the change (test_theta_hook.py: 3 pre-change value pins captured at e6278c16 bit-exact, θ=(0,1) bit-identity scalar/batch/smeared, closed-form substitution equivalence, engagement + counter + twin-tripwire + defaults-guard); suite 1846 passed / 15 skipped; ruff+format+mypy clean. Site 2.3 registered scope = the smeared kernel only (guard raises if θ requested without smear_global_selection); site 2.7 twin deliberately un-hooked (PA-HIER-11, shared expression shape unchanged). |
| 2026-08-28 | d40fe5c8 | verified | PASS | bayesian_statistics.py (sites 2.1/2.2/2.3) | Sign: b enters additively as Δz=+b(1+z) (Ma-Hu-Huterer convention), s>0 multiplicative on the folded width — consistent at all three sites. Units: all-dimensionless (redshift algebra). Limits verified by test: θ=(0,1) bit-identity (3 pins + explicit-identity scalar/batch/smeared); closed-form substitution equivalence at (0.02, 1.4142) rtol 1e-12; s→0 delta limit inherited from the pre-existing floor comment (:1670), floor re-applied after scaling. |
| 2026-08-29 | a794404c | presented | row #223 (standing grant, charter node B6.1) | bayesian_statistics.py:6370-6381 (site 2.1), :7041-7050 (site 2.2), :1692-1704 (site 2.3) — host_z_error_eff `s`-placement only; `b` unchanged | s-placement alignment gate, appended to `results/campaign51_20260728/realistic_20260729/PHYSICS_CHANGE_THETA_HOOK_20260828.md` §"Appended note 2026-08-29" (row #221 item 4): moves `s` to scale RAW `host_z_error` BEFORE the peculiar-velocity quadrature fold (was: scaling the folded width), matching `PREREGISTRATION_HIER_HTHETA_20260826.md` §1.2's registered arithmetic; bit-identical to the built code at every θ while `SIGMA_V_PEC_KM_S = 0.0` (`constants.py:95`, reverified) — supersedes without editing the 2026-08-28 "pinned order-of-operations" paragraph and its Decision-table item 1. No code written under this row; new nonzero-σ_pv regression tests specified (§6) but not yet authored/run. |
| 2026-08-29 | pre-commit | implemented | PASS | bayesian_statistics.py:6370-6381 (site 2.1), :7041-7050 (site 2.2), :1692-1704 (site 2.3) | Charter node B6.1 [ALIGN]. `host_z_error_eff` (site 2.1/2.2) / `sigma_eff` (site 2.3) now built as `sqrt((theta_s * host_z_error_raw)**2 + sigma_z_pv**2)` inside the θ-engaged branch, with `sigma_z_pv` read from the LOCAL already computed above from the UNSHIFTED host redshift (b's centre-shift line moved to run AFTER this, so it cannot leak into the PV term — a deliberate divergence from the appended note's own §2 formula text, which reads `sigma_z_pv = (1+z̃)·σ_v/c`; the note's prose ("b still shifts the centre; only the fold ORDER for s moves") is what was implemented, not that formula literal — see the B6_1_ALIGN_RECORD.md primary finding). 1e-10 floor re-applied after the combine at site 2.3, unchanged semantics. `single_host_likelihood_integration_testing` (site 2.7 twin) untouched, per PA-HIER-11. Reference comments citing the row #221 item 4 note added at all three sites. New tests in `test_theta_hook.py`: 3 required nonzero-σ_pv discriminators (2.1 scalar, 2.2 batch, 2.3 smeared) per the note's §6(b) plan, a discriminator-sanity check, and one additional b-order regression pin (`test_theta_b_order_unchanged_uses_raw_host_z_for_pv`) added to guard the exact b/s-order divergence identified above. Ruff + ruff-format clean, mypy clean. |
| 2026-08-29 | pre-commit | verified | PASS | bayesian_statistics.py:6370-6381, :7041-7050, :1692-1704 | Sign/units: unchanged from the 2026-08-28 verified row (all-dimensionless redshift algebra; b additive, s multiplicative on a redshift-valued quantity). Limits: θ=(0,1) identity untouched (existing pins green); s=1 no-op preserved (new formula reduces to the un-hooked fold identically, independent of placement); σ_z,raw→0 with σ_pv>0 now correctly leaves an UNSCALED σ_pv floor (note §5 item 3) — pinned by the new discriminator tests. Targeted suite `test_theta_hook.py` + `test_catalog_only_diagnostic.py`: 27 passed. Full suite `pytest -m "not gpu and not slow"`: 1851 passed, 15 skipped, 27 deselected. Ruff/mypy clean. |
| 2026-08-29 | a794404c | presented | row #223 (standing grant, charter node B5.1) | darksiren_emri/galaxy_catalogue/handler.py:648-673 (`mass_filter_sigma`/`sigma_multiplier` mask), bayesian_statistics.py:4797-4798 (call site) — proposed NEW flags `mass_filter_geometry∈{"linear","log"}` (default "linear", byte-identical) + `mass_filter_k` (default 1.5, decoupled from `sigma_multiplier`'s sky-radius role) | 5-item package = `results/campaign51_20260728/realistic_20260729/fanout1_20260829/PHYSICS_CHANGE_MASS_WINDOW_GEOMETRY_20260829.md` (row #221 F-ii pre-code half; log-side σ_lnM derived from existing `BH_MASS_ERROR/BH_MASS` ratio — already an R&V15 ln-space budget, no new derivation; GW-side σ_lnM,z ≈ M_z_sigma/M_z, small-error correspondence; Reines & Volonteri 2015 arXiv:1508.06274; k→∞ reproduces the HB exoneration; log/linear agree to O(σ²) for CV≪1). Companion zero-compute read `b5_window_count.json`: gate (i) linear k=1.5 reproduces the corrected §3.8 n_lin/n_all=0.9577 (PASSED, 4dp); flags TWO caveats needing the node's later registered counterfactual — aggregate pass fraction under log k=3 is LOWER than linear k=1.5 (0.695 vs 0.958, contradicting runbook 37 §5's "cannot add more than 4.2%" framing) via the linear window's vacuous negative-lower-edge heavy-side admission; true-host mass-window retention drops 95.7%→78.9% switching linear k1.5→log k3. NO CODE WRITTEN under this row (presentation only, per row #223's "presentation before code").
| 2026-08-29 | a794404c | presented (revised) | row #223 (standing grant, charter node B5.1, refuter-panel revision) | (same row as above; companion `b5_window_count.py` `gw_window()`) | Refuter panel REFUTED the row-above text (must_fix: `gw_window()` always used the linear formula even under "log" configs, contradicting §2). Fixed + re-run by a DIFFERENT agent from the builder (standing rule 2), with a new companion `b5_window_count_arm_jackknife.py` for the A15 operating-characteristics ask. Appended `## Revision Notes R0-R9` + inline SUPERSEDED markers to `PHYSICS_CHANGE_MASS_WINDOW_GEOMETRY_20260829.md` (append-only; no prior text edited): R1 bug+fix; R2 corrected numbers — every §7 headline figure UNCHANGED at reported precision (full-precision deltas ≤1.3e-6; only config-iii moved, by 3/2,249,231 candidates; configs ii/iv and all true-host retention counts bit-identical); R3 stale docstring citation fixed (handler.py:648-660→598-609; mask citation 654-673 confirmed correct); R4 two A14 falsifiers; R5 A15 operating characteristics via a 24-arm jackknife (arm-to-arm std/SE reported per config; observed drops clear ≥3 arm-SEs); R6 A10 blindness sentence (retention is knowingly non-blind by design); R7 A10 invariants list (4 items); R8 A8 bands (arm-jackknife std/SE as the meaningful uncertainty, full-census point stats otherwise); R9 A11 per-number citations. Standing rule 5 re-checked, no re-litigation collision. NO CODE WRITTEN in production; this remains presentation-only. |
| 2026-08-29 | pre-commit | implemented | PASS | handler.py:558-753 (`get_possible_hosts_from_ball_tree` signature, docstring, mask branch); bayesian_statistics.py class defaults/`__init__`/`evaluate()` signature/read site (:3301-3311,:3373-3376,:3527-3547,:3762-3767)/call site (:4842-4843); correspondence_1d.py `run_mirror_seed_inprocess` signature (:2773-2779)/passthrough (:2949-2951); arguments.py properties (:379-401)/argparse (:1076-1104); main.py `main()` call site (:211-212)/module `evaluate()` signature (:1420-1426)/call site (:1454-1455) | Charter node B5.1 part (B), implementing the row-#223-cited presentation above verbatim (§2). New flags `mass_filter_geometry∈{"linear","log"}` (default "linear") + `mass_filter_k` (default 1.5) threaded end-to-end: handler.py single read/validate site (raises on invalid geometry or non-finite k); `mass_filter_sigma`'s existing "symmetric"/"asymmetric" split reinterpreted as the candidate-side multiplier being `mass_filter_k` vs `1.0` under EITHER geometry (was `sigma_multiplier` vs `1.0`) — the two flags are read independently, disclosed explicitly in-code since §2's illustrative snippet omitted this interaction; `sigma_multiplier` now feeds ONLY the sky-cone radius. Default pairing (`"linear"`, `1.5`) is byte-identical to the pre-flag mask by construction (same arithmetic, `mass_filter_k` literal-defaults to the coupled call-site value). New test module `darksiren_emri_test/test_mass_filter_geometry.py` (19 cases: §6 items 1-3 — default byte-identity incl. sigma_multiplier decoupling, hand-computed log edges at k∈{1.5,2.5,3.0} incl. an asymmetric-vs-symmetric discriminator, MC epsilon test 2Φ(−k) at N=20,000/seed=20260829 — plus invalid-value guards and 2 bonus R7 limiting-case tests, k→∞ and σ→0, for both geometries) + 1 signature-passthrough test appended to `test_correspondence_1d.py`. Independent (non-suite) verification script: NEW production call (defaults only) vs. an independently re-typed pre-change formula, 50 random events × 2,000 random candidates = 100,000 candidate-event pairs, 0 mismatches (`b51_byte_identity_check.py`, scratchpad, not committed). Full suite 1871 passed / 15 skipped / 27 deselected (1851 baseline + 20 new); ruff/ruff-format/mypy clean on all touched files and on the full `darksiren_emri/`+`darksiren_emri_test/` tree. |
| 2026-08-29 | pre-commit | verified | PASS | handler.py:679-745 (single read/validate site) | Builder-run smoke verification (standing rule 2: a registered fleet-level counterfactual re-run by a different agent is node B5.2's job, not this implementation task's; this is the implementer's own smoke pass, disclosed as such). Sign/units: `σ_lnM,z = M_z_sigma/M_z` and `σ_lnM = BH_MASS_ERROR/BH_MASS` both dimensionless ratios of same-unit `[M_sun]` quantities (§4 of the presentation, unchanged); `exp(±k·σ)` a pure number, restores `[M_sun]` on multiply-back. Limits (§5, R7 invariants 1-3): invariant 1 (default byte-identity) PASSED at both unit-test and 100,000-pair independent-script granularity (see implemented row); invariant 2 (`k→∞`) and invariant 3 (`σ→0`) now closed by dedicated automated tests for both geometries (R7's disclosed residual gap — partially closed; invariant 4, first-order `kσ≪1` agreement, is a direct `exp(x)=1+x+O(x²)` consequence and was not separately pinned). `mass_filter_sigma`/`mass_filter_geometry` independence: the asymmetric-vs-symmetric discriminator test under `geometry="log"` confirms the candidate-side multiplier switches correctly (bare `1.0` vs `mass_filter_k`) under the NEW geometry, closing the one genuine design ambiguity in the presentation's illustrative §2 code (which showed no `mass_filter_sigma` interaction at all) — resolved as documented in the implemented row's note, not silently. Falsifier R4 item 2 (re-deriving the real-fleet true-host retention drop against the NOW-existing production flags) is explicitly OUT OF SCOPE for this row (charter node B5.2, wave 2) and NOT attempted here; no fleet-level census was re-run. Full details: `results/campaign51_20260728/realistic_20260729/fanout1_20260829/B5_1_WIN_RECORD.md`.
| 2026-08-29 | dd63fe0c | presented | row #223 (standing grant, charter node B3.2) — PRESENTED WITH A STOP: premise REFUTED by generator provenance; NO CODE authorised by this presentation | bayesian_statistics.py:1262-1298 (`precompute_completion_denominator` integrand, S1), :1403-1450 (`precompute_missing_completion_denominator`, S2), :2120-2124 (`precompute_phi_selection_integrals` p_pop, S3), :5345-5410 (`p_Di` completion numerator, S4) [+ :1607 V_f, :1718 smeared, non-CoR-P] — proposed NEW flag `completion_population_prior ∈ {"comoving","m1"}` (default "comoving", byte-identical); catalogue-leg host-z kernel sites :6511-6519/:7171-7180/:7222-7247/:7567-7589 and `_rate_weight` :1036 explicitly NOT touched | Package = `results/campaign51_20260728/realistic_20260729/fanout1_20260829/PHYSICS_CHANGE_POPULATION_PRIOR_M1_20260829.md` (5 items + §6 F3 registered predictions + §7 A15 + §8 A10 + §9 A14 + §10 [WPOP-TUNING] boundary quoted verbatim). Decisive finding (§F): the production CRB set `seed61000` (md5 9a1f2a14…, generating commit 03cfe80) draws its 1514 dark hosts from `(1−f)·dVc/dz/(1+z)` (`draw_mixture_hosts` → `_draw_dark_redshifts`), i.e. the estimator's OWN completion prior (emri_rate M1-(i), p0=1 ⇒ constant comoving); the z-dependent "M1" of row #138/B3.1 is `Model1CrossCheck`'s extracted dN/dz (M1-(ii)), used ONLY as the p_det pool's stratum-'a' sampling measure. Hence the row-#138 population term is identically zero for production's dark class; B3.1's 98.5 %/103.9 % coverage is a premise-independent statement about the estimator's algebraic response to swapping the prior (ΔN = +0.603 on bins 2–5, registered per bin in §6.1 with a Δ_D denominator read), not evidence of a data-vs-model mismatch. Leg decision: completion family S1–S6 only; K1–K4 (G2b/C7-ratified) untouched. "m1" read from the production generator is NULL-BY-CONSTRUCTION; read from the pool it is a generator-INCONSISTENT counterfactual whose adoption would be a [WPOP-TUNING] collision (§10). L4 pure-completion arm registered direction = UP (away from 0.73), contradicting docket §2 B3 (c) — corrected before any run. Recommendation to the orchestrator: do not spend the 45–69 CPU-h C2 arm; strike B3.2 from L1; return the provenance finding + the paper-facing population-shape sensitivity (G7 row 16, §12). No physics-trigger file edited. |
| 2026-08-29 | pre-commit | dispatch-declined | STOP CONFIRMED (no code written) | bayesian_statistics.py (no site touched) | Wave-2 PREP dispatch for charter node B3.2 instructed "IMPLEMENT the flag exactly as presented ... panel clean after 0 rounds ... you ARE authorized to edit bayesian_statistics.py". The cited presentation and the row directly above (both already on disk before this dispatch) say the opposite: premise REFUTED (§F), "No code under this presentation" (§13 item 2), approval column reads "PRESENTED WITH A STOP ... NO CODE authorised". Builder re-verified §F's provenance citations, re-ran the exoneration grep (standing rule 5, [WPOP-TUNING] register item 5, same hit as the presentation's §10), found no basis to override the STOP, and declined the code-writing instruction as issued rather than implement a refuted premise under a standing grant whose inputs (the refutation) postdate the grant. No file under `darksiren_emri/` touched; no `completion_population_prior` flag exists in the tree; no tests added. Full account: `results/campaign51_20260728/realistic_20260729/fanout1_20260829/B3_2_POP_FLAG_RECORD.md`. Returned to the orchestrator for a re-issued, narrower authorization (counterfactual-instrument-only) or to strike B3.2 from L1 per the docket §13 recommendation. |
| 2026-08-29 | ff230621 | presented | row #223 (standing grant, charter node B7.3) | bayesian_statistics.py:3275,:3278 (class attributes), :3359-3360 (__init__), :3497,:3502 (evaluate() signature defaults), :3701-3725 (2D validation/log block) + plumbing arguments.py:1065,:1082 / main.py:1418-1419 / validation/correspondence_1d.py:2756-2757 — production-default flip catalogue_numerator_survival_2d "off"->"mz_sel", catalogue_numerator_survival_2d_center "unset"->"eff"; kernel read sites (:6823-6846, :6872-6896, :7517-7540) and worker signature defaults (:6274,:6280,:7000,:7005,:7641-7642) NOT touched | Charter node B7.3 [2D-TWIN] adoption gate. 5(+1)-item package = results/campaign51_20260728/realistic_20260729/fanout1_20260829/PHYSICS_CHANGE_2D_TWIN_ADOPTION_20260829.md, built from PROPOSAL_2D_TWIN_ADOPTION_20260829.md (§1-§9, §13, §15) + B7_2_TWIN_CF_READOUT_RECORD.md §6 (row #248) + B7_2_FALSIFIER_I_RECORD.md (row #236). LITERAL default flip (G-3 resolved: no "auto" token — the S_4D accessor exists in every normalization mode; G-1 RAISE already realized by the kernel composition guards :6376-6382/:7316-7322, an early evaluate()-layer guard specified); explicit "off" = the COUNTERFACTUAL (byte-identical to today, R6 1D channel bit-identical: production max_abs 0.0 at all H4 nodes). Stated plainly as a STRUCTURAL-CONSISTENCY change (with-BH catalogue leg gets the same survival treatment as the fused completion leg (rows #117-#118) and the 1D catalogue leg (row #197); with-BH mixture becomes S-degree-matched — falsifier (i) PASS 2.6e-16/1.3e-16 vs coded 1.50/5.67), justified by CONFIRMED-supported (row #216 item 1) + IMMATERIAL-PREDICTED production delta (row #248: Delta-mean_h,pred +0.0025057 <= T_mat/2 0.004; R1 0/6352, R2 982/982, R6 0.0) — NOT by a bias reduction; the C2* 2D identity NOT closed (x2.253±0.082 bt / x2.700±0.101 bc at 33 seeds), calibration status supported-capped, row #211 PARK unchanged, attribution PROVISIONAL until falsifier (ii) (unrun). A14 falsifier registered: wave-3 per-change arm, |Delta-mean_h(2D)| >= 0.008 on either venue falsifies IMMATERIAL-PREDICTED; R1/R6 at full grid = INSTRUMENT-DEFECT. F2: batched into the one wave-3 blind HEAD readout. Regression plan + complete keep-"off" call-site list (8 explicit, 6 default-reliant incl. the live hier_s0_driver.py:374/:386/:590) + 2 tests to re-pin + new pins in §6; suite baseline 1889 passed / 15 skipped / 27 deselected. NO CODE WRITTEN under this row (presentation before code, row #223); implementation serialized behind the local runner (row #248 path decision); implemented/verified rows follow in the [PHYSICS] flip commit. |
| 2026-08-29 | pre-commit | implemented | PASS | bayesian_statistics.py:3272-3279 (class attributes), :3358-3362 (`__init__` instance defaults), :3492-3505 (`evaluate()` signature defaults), :3705-3757 (2D validation/log block, restructured for the three-way INFO/WARNING split); arguments.py:1061-1078 (argparse `--catalogue_numerator_survival_2d` default+help), :1079-1092 (argparse `--catalogue_numerator_survival_2d_center` default+help), :357-368/:370-376 (property docstrings); main.py:1414-1419 (module `evaluate()` signature defaults+comment); validation/correspondence_1d.py:2747-2759 (`run_mirror_seed_inprocess` signature defaults+comment); results/campaign51_20260728/realistic_20260729/fanout1_20260829/hier_s0_driver.py (three `run_mirror_seed_inprocess` call sites pinned explicit `"off"`/`"unset"`, Class-B site B3) | Charter node B7.3, implementing the row-#223-cited presentation (`PHYSICS_CHANGE_2D_TWIN_ADOPTION_20260829.md` §6) verbatim: LITERAL default flip at the nine declaration sites (six in bayesian_statistics.py + arguments.py×2/main.py/correspondence_1d.py), `off`->`mz_sel`, `unset`->`eff`. Kernel read sites (:6823-6846, :6872-6896, :7517-7540), the composition guards (:6376-6382/:7316-7322), and worker signature defaults (single_host_likelihood/`_batch`, `_starmap_host_batches`) untouched — `git diff` over that range is empty. (a-iv) early evaluate()-layer composition guard NOT added (disclosed choice, permitted by the presentation): the existing kernel guards alone already realize G-1 (raise on `mz_sel` + `trunc_lognormal`/non-`production` `catalogue_mass_overlap`); adding a duplicate guard at evaluate() was judged outside the minimal diff. G-2 log block restructured to a three-way split: `mz_sel`+`eff` (new default) -> INFO `[PHYSICS] ... ACTIVE (row #249)`; `mz_sel`+`raw` -> WARNING (instrument-only COUNTERFACTUAL, per §6.1(a-iii)); `off` -> WARNING (COUNTERFACTUAL, no per-candidate survival factor). Class-A call sites (§6.1(a-v), 8 sites across p3_2d_fleet.py/ca_rhs_scorer.py/p3_wbhzero_measure.py/p3_2d_companion.py/wbhzero_probe.py/rhs_inflation_*.py/cluster sbatch set) verified UNCHANGED and still explicit (grepped, no edits needed). Class-B site B3 (hier_s0_driver.py, three `run_mirror_seed_inprocess` calls at the b0i/ft branches and the S0-C full-grid call) pinned explicit `catalogue_numerator_survival_2d="off"`/`catalogue_numerator_survival_2d_center="unset"` so the banked Stage-0/KW-Q1 comparands stay byte-identical (no local runner was active at implementation time, confirmed by the dispatching orchestrator). Tests re-pinned per §6.1(a-vi): `test_evaluate_mz_sel_with_unset_center_raises` and `test_cli_validate_refuses_mz_sel_with_unset_center` now pass `catalogue_numerator_survival_2d_center="unset"`/`--catalogue_numerator_survival_2d_center unset` explicitly rather than relying on the (former) default. `test_cli_flag_defaults_to_off_and_unset` renamed to `test_cli_flag_defaults_to_mz_sel_and_eff` (asserts the new default pair) with a new sibling `test_cli_flag_explicit_off_and_unset_parses_and_validates` pinning the counterfactual. New pin tests (§6.2): `test_six_site_default_trace_is_mz_sel_and_eff` (class attribute/`__init__`/`evaluate()`/argparse/`main.evaluate`/`run_mirror_seed_inprocess` all resolve to `("mz_sel","eff")`; kernel defaults confirmed to stay `("off","unset")`); `test_kernel_default_pair_bit_identical_to_explicit_mz_sel_eff`; `test_evaluate_default_logs_physics_info_line` / `test_evaluate_explicit_off_logs_counterfactual_warning` (caplog, short-circuited via a deliberately invalid unrelated `catalogue_global_selection="bogus"` to avoid driving `evaluate()` into real host/pool computation against `MagicMock()` stand-ins — disclosed technique); `test_r5_sigma_gal_zero_limit_matches_point_s4d_at_host_mass` (R5 of §6.2 item 6: sigma_gal->0 limit against an independently-computed point S_4D query, isolated via `normalization_mode="generator_marginal"`, the code's actual delta-kernel branch — collapsing `host_z_error` under `volume_deconv` was tried first and found to underflow the 50-node host-z quadrature well before the limit is reached, so it was abandoned for the generator_marginal vehicle instead, disclosed); `test_run_mirror_seed_inprocess_defaults_to_mz_sel_and_eff` appended to `test_correspondence_1d.py` in the `:172-195` signature-default pattern. run_metadata: unaffected by construction (`Arguments.to_dict()` serializes the full parsed namespace, arguments.py:123-133; confirmed via `test_cli_flag_defaults_to_mz_sel_and_eff`'s `to_dict()` assertions — no separate stamp-path code change needed). Item 6.2.3 (production-scale cluster pin against the C4/C0 CSVs) is explicitly OUT OF SCOPE for a local implementation task (needs the cluster CRB/catalogue datasets) and was NOT attempted; it rides the wave-3 per-change arm per the presentation. Suite: flag-file directory (`test_catalogue_numerator_survival_2d.py`+`test_survival_2d_homogeneity_falsifier.py`+`test_correspondence_1d.py`) 128 passed; `darksiren_emri_test/bayesian_inference` 572 passed / 6 skipped; `test_arguments.py`+`test_theta_cli_forwarding.py`+`darksiren_emri_test/validation` 432 passed / 1 skipped; full `pytest -m "not gpu and not slow"` **1896 passed / 15 skipped / 27 deselected** (baseline 1889 + 7 net-new: the renamed default-pin test is not a net addition; +6 in the flag file, +1 in test_correspondence_1d.py), 119.75s, coverage 73.48%. ruff check --fix + ruff format clean on all touched files (ruff format additionally reformatted long pre-existing lines throughout `hier_s0_driver.py` that predated this row — disclosed, no semantic change, `git diff` inspected line-by-line); mypy clean on `darksiren_emri/` (70 source files, no issues). |
| 2026-08-29 | pre-commit | verified | PASS | bayesian_statistics.py (six declaration sites + log block only; kernel bodies :6231-7723 untouched) | Builder-run smoke verification (standing rule 2: this row's own dispatch did not provide a separate independent verifier agent; disclosed as such, following the row-#(B5.1) precedent above). Sign/units: the flip changes no arithmetic — S_4D stays a dimensionless survival factor in [0,1] on both sides of the flip (unchanged from the presentation's §3); the with-BH numerator's units are unaffected by which of two pre-existing, already-tested branches is selected by default. Limits: `test_mz_sel_sharp_gw_mass_limit_matches_point_s4d` (sigma_cond->0, pre-existing) and the NEW `test_r5_sigma_gal_zero_limit_matches_point_s4d_at_host_mass` (sigma_gal->0) both green against independently-computed point-`S_4D` queries; `E[S]<=1` eventwise and the homogeneity falsifier (`test_survival_2d_homogeneity_falsifier.py`, 4 tests) both green, unaffected by the flip since they exercise the kernel with explicit flags. Diff scope: `git diff` confirms the bayesian_statistics.py change is confined to :3272-3279/:3358-3362/:3492-3505/:3705-3757 — no line inside :6231-7723 (both kernels + `_starmap_host_batches`) changed; worker signature defaults (`single_host_likelihood`:6274/:6280, `single_host_likelihood_batch`:7000/:7005, `_starmap_host_batches`:7641-7642) confirmed still `"off"`/`"unset"` by `test_six_site_default_trace_is_mz_sel_and_eff`. Six-site default trace: PASSED (class attribute, `__init__`, `evaluate()` signature, argparse, `main.evaluate`, `run_mirror_seed_inprocess` all `("mz_sel","eff")`). Counterfactual reachability: explicit `"off"` (any center) confirmed byte-identical to the pre-flip kernel path by the pre-existing golden tests (unaffected, still green) and at the CLI/evaluate() layer by the new sibling test. Class-A call sites (§6.1(a-v)) re-grepped post-implementation: all 8 still pass their explicit value, zero edits landed on them. Class-B site B3 (hier_s0_driver.py) confirmed pinned at all three call sites by direct read of the diff. Exoneration re-check (standing rule 5): re-ran the mechanism grep of the presentation's §12 against `EXONERATION_REGISTER` — no new entry between a794404c and this implementation covers "survival inside the with-BH catalogue numerator's mass quadrature, paired with the fused completion leg"; unchanged from the presentation's own check. Suite: 1896 passed / 15 skipped / 27 deselected reproduced independently on a second run after `ruff format`; ruff/mypy clean. NOT attempted by this row (explicitly out of scope, disclosed): the production-scale cluster pin (§6.2 item 3, needs cluster datasets) and re-deriving the wave-3 per-change H0 arm (§8's A14 falsifier; that is the end-of-fan-out verifier's job per §10). COMMIT-READY. Full account: `results/campaign51_20260728/realistic_20260729/fanout1_20260829/B7_3_ADOPTION_IMPLEMENTATION_RECORD.md`. |
| 2026-08-30 | ecd33336 | presented | row #255 (standing grant, tree 2 node T1.1) | bayesian_statistics.py:5215-5219 (no-BH L_cat divisor consumer, the only consumer that changes), :4234-4243 (Sigma-phi call site; a NEW theta-ratio pass is appended after it, existing branches untouched), :2906-2915/:3022 (phi-table branch, UNCHANGED, cited as the OLD form), :7119-7129/:7138/:7155/:7183-7209 (site-2.2 kernel the divisor is DEFINED to mirror), :4869 (sky-cone literal 1.5 -> sky_cone_k flag); handler.py:662 (radius consumer, unchanged), optional :668-677 (z_window_k companion knob, orchestrator path choice) | [HIER] theta-consistent no-BH global-selection divisor, site 2.3phi. Package = results/campaign51_20260728/realistic_20260729/tree2_20260830/PHYSICS_CHANGE_THETA_DIVISOR_20260830.md (5 items + score-identity derivation (A12) + cost + regression plan R1-R13 + A10 invariants/blindness + A14 falsifiers F1-F5 + decision table). Registered form: Sigma_phi_reg(theta;h) = Sigma_phi_point(h) x rho(theta;h), rho = sum_g w_g S_tilde_g(theta) / sum_g w_g S_tilde_g(0,1), S_tilde_g = the C7-core site-2.2 kernel (with f_k, +/-4 sigma window, 1e-6 floor, GL-50) transformed by z_g -> z_g + b(1+z_g), sigma_eff = sqrt((s sigma_z)^2 + sigma_pv^2), smeared over the theta-free S_bar_phi table; literal skip at (0,1) => byte-identical (and forced-(0,1) rho == 1.0 exactly); consumed ONLY at the no-BH L_cat divisor (path-A objects alpha_G_phi/D_tilde_phi/w_tilde_G stay at (0,1) so the C-C identity check holds); degenerate transformed windows (b<0, z_g + 4 s sigma_g <= 1e-6 after the shift: 15,618 rows = 0.075 pct at b=-0.02, 51,394 = 0.247 pct at b=-0.033, w-share <= 0.10/0.33 pct; reproduced from the full pool, md5 c52c13b5 PASS) get S_tilde := 0 and are counted. Per-node scalar rho (E11) is EXACT w.r.t. this form (not first-order): re-run == banked L_cat / rho per event (pin R3, rtol 1e-12). New flag theta_phi_divisor {off,on} default off (byte-identical), independent of theta_sites, valid with smear off; sky_cone_k default 1.5. REGISTERED PREDICTION (T1.2 S0-A re-certification, 2.2/unsmeared, as-built +/-0.02, 4 seeds, h=0.73): score_b = -0.27 +/- 0.43, Z_b = -0.62 (band |Z_b| <= 3; exactness |score_b - (-0.268)| <= 0.10); score_s stays at -0.073 +/- 0.012 (Z ~ -6) by the ball-truncation mechanism (ii), AS PREDICTED; with sky_cone_k 3.0 AND z_window_k 4.0: Z_s -> -0.5 +/- 1 (E12). Cost MEASURED this node: C7-kernel pool smear 27.3-31.8 us/row => 569-662 s per full 20.8M-row pass single-core; 2 passes per off-truth node per h (~20.5 min; ~11 min with the (0,1) normaliser cached; ~1.6 min at 14-way row-chunk parallelism, bit-identical by row independence); ZERO extra S_bar_phi table builds; the theta-engaged no-BH cell IS single-core-bound (62 s -> ~21 min, x21) under the reference implementation, x2.5 with row parallelism; T1.2 = ~6.3 h serial / ~1.3 h parallel wall, local. NO CODE WRITTEN under this row (presentation before code); builder != presenter != T1.2 runner. |
| 2026-08-30 | pre-commit | implemented | PASS | bayesian_statistics.py:1625-1631 (site_2_3_phi hook counter), new module functions _phi_divisor_kernel_pass and precompute_phi_divisor_theta_ratio inserted after precompute_global_catalog_selection, write_selection_table_json extended with nine optional instrumentation fields (sigma_phi_theta, sigma_phi_smear_truth, rho_theta, kappa_smear_over_point, n_degenerate_rows, w_share_degenerate, theta_b, theta_s, theta_phi_divisor, all None-default => byte-identical payload when off), class-level defaults _theta_phi_divisor="off"/_sky_cone_k=1.5 (and their __init__ copies), evaluate() signature gains sky_cone_k and theta_phi_divisor, validation/storage block (sky_cone_k finite/>0 guard; theta_phi_divisor token guard; "on" requires self._catalogue_global_selection == "phi", i.e. normalization_mode absolute_marginal), the site-2.3phi ratio build immediately after the existing Sigma_phi call site (independent of theta_sites; literal skip at theta=(0,1) leaves the new dict empty), self._global_cat_selection_phi_theta stored alongside self._global_cat_selection_phi, the sky-cone call site sigma_multiplier=1.5 -> sigma_multiplier=self._sky_cone_k, and the global_denom_no_bh consumer now falls through the new dict via getattr(self, "_global_cat_selection_phi_theta", {}).get(...) (the getattr guard was required because several pre-existing tests construct BayesianStatistics via object.__new__ and never call evaluate(), so the plain attribute is absent on those instances -- matches the codebase's existing getattr(self, "_catalogue_global_selection", "s3d") convention at the same read site). Plumbing: arguments.py gains --theta_phi_divisor {off,on} (default off) and --sky_cone_k (default 1.5) with matching properties; main.py's module-level evaluate() and its call into BayesianStatistics.evaluate() thread both flags; validation/correspondence_1d.py's run_mirror_seed_inprocess gains the same two passthrough kwargs plus a docstring note. handler.py NOT touched (its sigma_multiplier parameter already existed; the presentation's section 2.5 states this explicitly). New tests: darksiren_emri_test/bayesian_inference/test_theta_phi_divisor.py (19 tests covering regression items R1, R2, R4, R6, R7, R8, R9, R13; a two-host hand computation cross-checked against an independent scipy.integrate.quad reimplementation). DISCLOSED SCOPE DECISION: regression items R3, R5 and R11 (byte-identity pins against the banked S0-A CSVs and the correspondence_1d.kernel_smeared_survival harness-parity check) require a full evaluate() pipeline run against the real GLADE catalogue and are integration-level, not fast unit tests; they are deferred to the T1.2 re-certification's own verification pass rather than attempted here (builder != T1.2 runner; see the implementation record for the exact reasoning). DECISIVE DRIVER-GAP FINDING (stated for the orchestrator before T1.2 is launched): results/campaign51_20260728/realistic_20260729/fanout1_20260829/hier_s0_driver.py has no --theta_phi_divisor or --sky_cone_k CLI flag and no passthrough at any of its run_mirror_seed_inprocess call sites (grepped, confirmed absent); --theta-sites 2.2 alone does NOT engage the new divisor (theta_phi_divisor is independent of theta_sites by design, section 2.2 of the presentation). No new theta_sites site label is needed or was added. Full details, exact file list, and the exact T1.2 command (with the required driver addition made explicit) are in results/campaign51_20260728/realistic_20260729/tree2_20260830/T1_1_DIVISOR_IMPLEMENTATION_RECORD.md. Suite: ruff check --fix and ruff format clean on all touched files; mypy clean on darksiren_emri/ (70 source files); full pytest -m "not gpu and not slow" (split in two halves per the 600s foreground limit) 1915 passed / 15 skipped / 27 deselected (baseline 1896 + 19 net-new), reproduced across both halves with no failures. Cluster inactive this node; local only. Launched under row #255 -- tree 2 node T1.1. |
| 2026-08-30 | pre-commit | verified | PASS | bayesian_statistics.py (new functions + the eleven read/write sites listed in the implemented row above; all other lines of precompute_global_catalog_selection, single_host_likelihood/_batch, path_a_mixture_objects and the smeared-kernel branch confirmed untouched by a targeted git diff read) | Builder-run smoke verification (this node's own dispatch did not provide a separate independent verifier agent; disclosed as such, following the row-#(B7.3) precedent above). Byte-identity (GATE T-ID): test_rho_is_exactly_one_at_theta_identity asserts rho == 1.0 and the two internal kernel passes bit-identical at theta=(0,1) (exact equality, not approx); test_default_instrument_attributes_are_byte_identical pins the "off"/1.5 class defaults; test_arguments_defaults_are_byte_identical pins the CLI defaults. Sign/units: dimensional analysis unchanged from the presentation's section 4 (rho is a dimensionless ratio of two like sums; the divisor's units are untouched); test_positive_b_lowers_rho_negative_b_raises_it confirms the sign statement of section 5.3 on a monotone-decreasing synthetic S_bar_phi. Limiting case: test_narrow_kernel_recovers_point_evaluation confirms the s -> 0 delta limit (section 5.2) to rtol 1e-4 against an independently-computed point sum. Hand-derivation cross-check: test_two_host_hand_computation_via_scipy_quad reproduces _phi_divisor_kernel_pass to rtol 1e-6 against a from-scratch scipy.integrate.quad implementation of the same kernel, independently typed (regression R6). Chunk invariance (R8): test_chunk_invariance_bit_identical_sums confirms exact equality across chunk sizes 97/1000/n on a 250-row synthetic pool. Degenerate windows (R9, section 2.4): two tests confirm a collapsed window contributes exactly 0.0 and is counted, and that an all-degenerate pool returns total == 0.0 exactly. Guards (R7): six tests cover the theta_phi_divisor token guard, both routes to the "requires catalogue_global_selection to resolve to phi" guard (auto-resolved and explicit s3d), the sky_cone_k finite/>0 guard (zero and NaN), and a plumbing-reach test proving self._sky_cone_k is stored before a later validation deliberately aborts the call (mirrors the existing _reach_catalogue_global_selection pattern in test_catalogue_global_selection.py). Regression sweep: re-ran test_theta_hook.py, test_smear_global_selection.py, test_catalogue_global_selection.py and test_mass_filter_geometry.py alongside the new file -- 85 passed, zero regressions (one AttributeError was caught and fixed during this pass: several pre-existing tests build BayesianStatistics via object.__new__ without ever calling evaluate(), so the new self._global_cat_selection_phi_theta attribute was absent on those instances until the getattr fallback was added at the consumer site). Full suite re-run independently after ruff format: 1915 passed / 15 skipped / 27 deselected, reproduced. Diff scope: git status confirms exactly four modified source files (bayesian_statistics.py, arguments.py, main.py, validation/correspondence_1d.py) plus one new test file; handler.py and every other physics-trigger file NOT touched. NOT attempted by this row (explicitly out of scope, disclosed): R3/R5/R11 (needs the real GLADE catalogue and a full evaluate() run; deferred to T1.2), the T1.2 S0-A re-certification itself (builder != runner, row #255 charter), and any driver (hier_s0_driver.py) edit (owned by the T1.2 builder per the presentation's regression item R13; this row only documents the gap). Full account: results/campaign51_20260728/realistic_20260729/tree2_20260830/T1_1_DIVISOR_IMPLEMENTATION_RECORD.md. Launched under row #255 -- tree 2 node T1.1. |
| 2026-08-30 | pre-commit | instrumentation | PASS | bayesian_statistics.py:3660-3665 (`_candidate_dump_dir` class attribute), :3744-3747 (`__init__` instance defaults `_candidate_dump_dir`/`_candidate_dump_rows`/`_candidate_dump_event_rows`/`_candidate_dump_warned`), :3962 (`evaluate()` signature `candidate_dump_dir` kwarg), :3980-3982 (store + `os.makedirs`), :5134-5136 (per-h reset inside the `h_values is not None` grid-mode block), :5305-5471 (`_collect_candidate_dump_rows`, new read-only method), :5478-5506 (`_write_candidate_dump_csvs`, new method), :5670-5682 (call site inside `p_D`'s event loop, after `p_Di` returns), :5199-5204 (write-out call site alongside the existing diagnostics-CSV write); arguments.py (new `--candidate_dump_dir` argparse entry + `candidate_dump_dir` property, pattern 0b308828); main.py (module `evaluate()` signature + both call sites); validation/correspondence_1d.py:`run_mirror_seed_inprocess` (new kwarg + docstring, forwarded verbatim); results/campaign51_20260728/realistic_20260729/fanout1_20260829/hier_s0_driver.py (`run_theta_node`, `run_arm_seed_s0a`/`run_arm_seed_s0r`, `_run_one_seed_worker`, `run_arm`, `main()` -- new `--candidate-dump` CLI flag, per-(seed, node) subdirectory) | A10 (row #255: "the per-candidate p_Di instrumentation hook is an instrumentation change (no computed value changes; byte-identity guard + gate-ledger row), not a full /physics-change gate") -- APPROVED "row #255 A10 (instrumentation guard)". Design = B4_3_MIXTURE_WEIGHT_DERIVATION_20260830.md section 6 (placement: read-only serialiser in evaluate(), strictly AFTER p_Di returns; no change inside p_Di; the per-candidate/per-event column lists of section 6.2). Default `candidate_dump_dir=None` is BYTE-IDENTICAL (GATE BI): no directory created, no attribute read differently by any existing branch, nothing appended, nothing written. When set, a read-only method assembles rows from state `p_Di` already computed for a normal run (`candidate_hosts`/`candidate_hosts_with_bh_mass`, `self.posterior_data_with_bh_mass[GALAXY_LIKELIHOODS/ADDITIONAL_GALAXIES_WITHOUT_BH_MASS]`, `self._phi_survival_table`, `self._diagnostic_rows[-1]`, `_rate_weight`, `dist`, `detection_probability_with_bh_mass_interpolated`, `completeness.f_bar`/`f_k`/`ang2pix`) and writes `per_candidate_h_<label>.csv` / `per_event_h_<label>.csv` per h (overwrite, `write_selection_table_json` naming convention) -- never writes into anything the likelihood consumes; wrapped end-to-end in try/except so a diagnostic failure can never fail the run (logged once). GATE BI (byte-identity, new test `darksiren_emri_test/integration/test_candidate_dump_instrumentation.py::test_candidate_dump_on_is_byte_identical_to_off`): both posterior JSONs and the `event_likelihoods.csv` diagnostics CSV byte-for-byte identical (`==` on raw file bytes) between a `candidate_dump_dir=None` run and a `candidate_dump_dir=<tmp>` run on the same deterministic synthetic fixture (seeded galaxy catalogue, seeded `Model1CrossCheck`, single h=0.73) -- PASS, and the dump files were confirmed actually written (comparison is not vacuous). GATE SCHEMA (`test_candidate_dump_schema`): both dump CSVs exist, columns match section 6.2's list exactly (17 per-candidate / 13 per-event fields, `sky_mahalanobis` kept as an always-NaN placeholder -- optional per section 6.2, not computed by this hook), one event row per event that reached `p_Di`, `batch` in {with_bh, no_bh_only}, `is_true_host` parses as bool, `z_g`/`N_g_used`/`D_g` finite on every row. `test_candidate_dump_off_is_default` confirms omitting the kwarg writes zero dump files anywhere under the run tree. Suite: ruff check + ruff format clean on all five touched files; mypy clean on `darksiren_emri/`/`darksiren_emri_test/` (216 source files); `hier_s0_driver.py` syntax-checked (`ast.parse`) and ruff-clean (not under mypy's configured path). Full `pytest -m "not gpu and not slow"` **1915 passed / 15 skipped / 30 deselected** (unchanged pass count from the T1.1 baseline -- the three new tests are `slow`-marked, matching this repo's convention for every other full-`evaluate()`-pipeline test, so they sit in the deselected count, not the fast default run); the three new tests independently run and PASS under `pytest -m slow darksiren_emri_test/integration/test_candidate_dump_instrumentation.py`. Full account + exact 3.4 CPU-h instrumented-run command: results/campaign51_20260728/realistic_20260729/tree2_20260830/T2_2_CANDIDATE_HOOK_RECORD.md. Launched under row #255 -- tree 2 node T2.2. |
| 2026-08-30 | ecd33336 | presented (revised) | APPROVED "row #255 (standing grant, tree 2 node T1.1; panel revision)" | results/campaign51_20260728/realistic_20260729/tree2_20260830/PHYSICS_CHANGE_THETA_DIVISOR_20260830.md, Revision note 1 (lines 724-876), superseding section 3 lines 308-311 only | Refuter-panel must_fix (severity major, verdict not refuted -- no registered number or plan item changes): (1) section 3's score-identity derivation used the UNnormalised per-host kernel k_g^theta(z) in L_cat's numerator and asserted INTEGRAL[numerator] dd = Sigma_phi_smear(theta) directly, dimensionally inconsistent with the section-4 units table given section 2.1's own S_tilde_g = INTEGRAL k_g^theta S_bar_phi dz / Z_g^theta; corrected chain restores the normalised numerator numerator_g(theta) = (1/Z_g^theta) INTEGRAL k_g^theta(z) S_bar_phi(z) p(d\|z) dz -- matching the code (bayesian_statistics.py:8000-8008 _z_prior_pdf_at "base * w_pop / z_prior_norm", :8011-8013 prior_num; scalar mirror :7291-7295/:7272-7290) -- under which the Z_g^theta cancels EXACTLY on d-integration (INTEGRAL_d numerator_g dd = S_tilde_g(theta)), reproducing sum_g w_g S_tilde_g(theta) = Sigma_phi_smear(theta) and A12 (E_gen[d/dtheta ln L_cat]_(0,1) = 0) unchanged; (2) section 1(e)'s code excerpt (:7117-:7366) omitted the intervening normalisation lines, now cited explicitly above. Units re-checked term-by-term against the section-4 table (k_g^theta and Z_g^theta both Mpc^3 sr^-1, cancelling in numerator_g(theta) to leave [p(d\|z)] units matching the table's L_cat row; INTEGRAL_d numerator_g dd dimensionless matching the table's S_tilde_g row; sum_g w_g S_tilde_g in events/year matching the table's Sigma_phi_smear row) -- no table row changes. Section 2.1's definitions (S_tilde_g, Sigma_phi_smear, rho), the falsifiers F1-F5 (section 9), the regression items R1-R13 (section 7), and the numeric predictions of sections 5.6/6/9 (score_b = -0.27 +/- 0.43, score_s = -0.073 +/- 0.012, the E12 enlarged-ball prediction, the section-6 cost anchors) are all UNAFFECTED -- none of them was derived from the flawed section-3 prose step; they read from measured rho/C_b/timing values computed by the already-normalised implementation. Append-only: section 3 left as originally written, marked SUPERSEDED in the revision note rather than edited in place. No code change; no re-run; T1.2 command and registered predictions unchanged. |
| 2026-08-30 | pre-commit | presented | row #255 (standing grant, tree 2 node T2.3 — instrument only) | bayesian_statistics.py:8079-8089 (site N1: batch no-BH numerator per-candidate survival factor, point + quadrature paths), :7319-7325/:7366-7372 (site N1 scalar twin), :5936-5942/:6019-6021 (site D1: no-BH catalogue global divisor consumer), :6521 (site W1: 1D mixture class weight), :4064-4105 (flag-resolution block pattern) — working-tree line numbers at HEAD ecd33336 + the uncommitted T1.1/T2.2 edits; NEW instrument flag catalogue_leg_1d_mass_aware in {off,on}, default off, byte-identical | Mass-aware 1D catalogue leg: under on, per candidate S_4D(d_L(z;h), M_g(1+z)) (Sigma_4D point convention, raw catalogue BH_MASS, isotropic sky, the same accessor and z rider as :3022-3038 and the T2.2 hook :5382-5385) replaces S_bar_phi(z;h) inside the no-BH numerator z-quadrature, Sigma_4D (global_denom_with_bh, already in hand) replaces Sigma_phi as the divisor, alpha_G_phi (already in hand, :6501) replaces beta_G_phi as the weight; all three toggled together (the divisor/weight re-booking is the exact n_hat_w identity alpha_G_phi/Sigma_4D = beta_G_phi/Sigma_phi, so combined_no_bh changes by exactly one factor S_4D/S_bar_phi per candidate); Z = integral p dd = (alpha_G_phi + beta_Gbar_phi)/D_tilde_phi = 1 identically (coded Z = 1.099921 at h = 0.73, d ln Z/dh = −0.18895 per unit h per event, derivation B4_3 section 4.3) and the 1D class share = the generator's w_tilde_G = 0.0619668; the kernel form (E over N(M_eff_g, BH_MASS_ERROR^2) via _sigma4d_mass_kernel_expectation :6784) is the paired alternative slaved to sigma4d_mass_kernel, REPORTED-ONLY. Package = results/campaign51_20260728/realistic_20260729/tree2_20260830/PHYSICS_CHANGE_MASS_AWARE_1D_LEG_20260830.md (5 items + F3 registered predictions with two-sided bands and A15: (i) mirror FT paired Delta mean_h = +0.05 band [+0.03, +0.10], MATERIAL >= +0.03, NULL <= +0.008, 4 seeds for a 3-sigma point read and 12 for the lower band edge on the banked per-seed SDs 0.0176/0.0268; (ii) production 1D at h = 0.730 dark-class impostor score −0.193 -> −0.074 band [−0.097, −0.048] over rho in [0.25, 0.5], pooled −0.265 -> −0.144; (iii) production 1D MAP 0.60 -> about 0.675 band [0.64, 0.72], cluster arm queued behind the OST 5 recovery; A14: MAP not leaving the 0.60 floor (map_h <= 0.605, dark-only pure arm unchanged) REFUTES the Z(h) attribution; cost: mirror cell 65 s and production 5-7 min per h-point, overhead <= 1.01x by the C4 STEP-2 pin 0.99x for the 24-node with-BH quadrature of which this point form is 1/24 per host-node; regression R1-R14 incl. byte-identity at off, the Z = 1 unit test with a can-fail control, and the 1D/2D symmetry test R10). GOVERNANCE: the flag modifies the row #169 Appendix B (i)/(ii) pairing — instrument + counterfactual arms proceed under row #255; the production-default flip returns to the author as a fresh [RULE] with the arm numbers. NO CODE WRITTEN under this row (presentation before code); builder != presenter; end-of-tree-2 verifier scope. |
| 2026-08-30 | pre-commit | presented (revised) | row #255 (standing grant, tree 2 node T2.3 — instrument only; panel revision) | results/campaign51_20260728/realistic_20260729/tree2_20260830/PHYSICS_CHANGE_MASS_AWARE_1D_LEG_20260830.md, Revision note 2026-08-30b (section 15, appended; section 16 records this row), superseding inline spots in sections 6.2, 6.3, 8 (F-3) and 13 only — nothing above rewritten | Refuter-panel must_fix (both severity: must_fix; both verdict: refuted=false; APPLIED, evidence-based). (1) The in-catalogue "minus 130 to about minus 117 nats over 76 events" transform (section 6.2's minus 1.707 to about minus 1.54, its band [minus 1.7, minus 1.4], the pooled prediction's band [minus 0.166, minus 0.120], the F-3 falsifier band, and section 6.3's MAP band [0.64, 0.72] and dark-only-full band [0.60, 0.67], all of which use this input via derivation B4_3 section 7's remedy-(d) row) was attempted from actual data as directed rather than left asserted: the only per-candidate dump on disk (T2.2, candidate_dump_run, 4 FT-mirror seeds 900101-900104, h = 0.73 only, 606571 candidate rows total) was read column-by-column for is_true_host; the result is 0 True rows out of 606571 across all 4 seeds (126215/135174/158958/186224 False rows respectively, 128/130/117/111 events per seed) — structural, not incidental, because that arm's registered population (derivation section 6.5) is the dark, q1 class by construction, so it carries no in-catalogue true host to serialise; a further search of the full results tree found no production/iiib per-candidate dump (T2.2b has not been run). The minus 117 input therefore cannot be derived from any data that exists today in either direction; it is relabelled from ARITH to REPORTED-ONLY/UNSUBSTANTIATED at every location named above (inline markers in the target file plus the full account in its section 15.1), pending a T2.2b production run or a T2.2-style hook run on an in-catalogue-inclusive event set (neither performed or authorised by this revision — no code, no arms run). The dark-class halves of predictions (i) and (ii), which scale by the anchored ARITH rho = 0.383 and do not depend on minus 117, are unaffected and remain ARITH. (2) The dark-class band upper edge minus 0.097 (section 6.2) was recomputed directly: minus 0.192564 times 0.5 = minus 0.09628, which rounds to minus 0.096, not minus 0.097; found to be intentional outward rounding consistent with this document's own convention (section 6.3 explicitly rounds its own band edges outward), stated but left implicit at section 6.2 — the value minus 0.097 is confirmed correct under that stated convention and is NOT changed; the convention is now made explicit at section 6.2 via an inline marker (section 15.2). Nothing in sections 1-5, 6.1, 7, 9-12, or the untouched section 13 rows was found affected and none of it was changed. |
| 2026-08-30 | pre-commit | presented (revised) | row #255 (standing grant, tree 2 node T2.3 — instrument only; second panel revision) | results/campaign51_20260728/realistic_20260729/tree2_20260830/PHYSICS_CHANGE_MASS_AWARE_1D_LEG_20260830.md, Revision note 2026-08-30c (section 17, appended; section 18 records this row), superseding/restoring inline spots in sections 6.3, 11 and 13 only — nothing above rewritten | Second refuter-panel must_fix (three items; must_fix; refuted=false; all addressed). (1) ACCEPTED — added an explicit hard sequencing rule (section 17.1, amending section 11's arm (c) bullet): arm (c), the 41-node production MAP array testing prediction (iii), is BLOCKED and must not be submitted or judged against section 6.3's Z-CONFIRMED/MIXED/REFUTED bands until arm (b), T2.2b on production iiib, has produced a derived ARITH in-catalogue S_4D(z_true,M_true)/S_bar_phi(z_true) transform for the 76 true in-catalogue hosts, superseding section 15.1's REPORTED-ONLY placeholder — the OST-recovery queue named in section 11 is necessary but not sufficient; arm (b) remains unblocked and DO-covered. (2) REFUTED with evidence, no correction applied — re-grepped the working tree today at every cited site (site W1 lines 6520/6521/6523/6524, site D1 lines 5936-5942 and 6019-6021, site N1 lines 8079-8089 in bayesian_statistics.py) and found every citation byte-exact against the current file; the panel's claimed one-line offsets (6521/6524 to 6520/6523; 5936-5942 to 5936-5941; 6019-6021 to 6018-6020) do not reproduce, so sections 0, 1, 2.2, 10, 13 and 14 stand unedited. (3) ACCEPTED, RESTORED to ARITH — independently recomputed the dark-only-full band from the derivation of record's own section 7 table row (d), column "production dark-only full mixture (1514 events)" (built only from the dark-scaled impostor term at anchor rho and the dark-only pure-arm score, with no reference anywhere to the disputed minus 117 in-catalogue figure): rho 0.25 gives MAP 0.657, rho 0.383 gives MAP 0.627 (about 0.63), rho 0.5 gives MAP 0.601, reproducing the stated band [0.60, 0.67] and point value exactly; section 15.1's inclusion of this band in the superseded/relabelled list was an over-cautious error and is un-superseded by this note (section 6.3's clause and the corresponding section 13 provenance-table clause both revert to ARITH); every other item section 15.1 relabelled genuinely does use the minus 117 input (confirmed by the same table row) and remains REPORTED-ONLY, now additionally sequencing-gated by item (1) above. No code, no arms run, no git by this node. |
| 2026-08-30 | pre-commit | presented (revised, final) | APPROVED | row #255 (standing grant, tree 2 node T2.3 — instrument only; production flip returns as a fresh [RULE]) | Panel must_fix (minor, one item): the "production dark-only full mixture" event count read 1514 at PHYSICS_CHANGE_MASS_AWARE_1D_LEG_20260830.md sections 6.3/17.3 and at B4_3_MIXTURE_WEIGHT_DERIVATION_20260830.md section 7, against the validated 1512 (=1588−76) at the gate doc's section 6.2. Reconciled: 1512 is the validated count (host_galaxy_index join, T2.2b); 1514 (=1590−76) came from the unvalidated CRB-row assumption-join (crb_rows=1590, fanout1_20260829/b4_imp_stage1_production_o2.json). Numeric effect <= 0.4 nats of Sigma ~ −264 (~0.15 percent), invisible at reported precision; the registered band [0.60, 0.67] and point value "about 0.63" for the dark-only-full-mixture prediction (remedy (d)) are UNCHANGED. Dated revision notes appended, append-only, to both documents (gate doc section 19; derivation section 13); no prior line in either document was edited — only SUPERSEDED-by-note pointers recorded, naming the affected lines. No code, no arms run, no git by this node. |
| 2026-08-30 | pre-commit | presented | row #255 (standing grant, tree 2 node T1.3) | galaxy_catalogue/handler.py:668-677 (redshift_filter_mask: galaxy-side term z_g +/- 1 sigma_g -> theta-transformed z_g + b(1+z_g) +/- z_window_k * sqrt((s sigma_g)^2 + sigma_pv^2), GW side unchanged) + bayesian_statistics.py:5636-5650 (single ball call site: theta_b/theta_s pass-through when theta_zwindow="on"), :3659/:3753/:3945 (new evaluate() defaults theta_zwindow="off", z_window_k=1.0), :1625-1633 (hook counter site_2_2_window) + plumbing arguments.py / main.py / validation/correspondence_1d.py / fanout1_20260829/hier_s0_driver.py (--theta_zwindow, --z_window_k, "_zwin<k>" node-dir suffix); physical_relations.py:546-567 NOT touched (dead sigma_multiplier disclosed) | Tree 2 node T1.3 [HIER] gate presentation, presentation BEFORE code, no code under this node: results/campaign51_20260728/realistic_20260729/tree2_20260830/PHYSICS_CHANGE_THETA_ZWINDOW_20260830.md (5 items + A15/cost/regression/A10/A14/decision table). Resolves T1.1 decision-table item 3 per ledger row #266 path decision. Mechanism: the candidate z-window is built from the quoted sigma_g without theta, so under theta != (0,1) the kernel-mass fraction the window captures, C(theta), is theta-dependent and the score identity acquires d/dtheta ln C != 0 (negative in s); zero-compute capture model on the drawn hosts (t1_3_gate_work/t13_out.json) reproduces the T1.2 s-residual (model -0.074 to -0.105 vs measured -0.0997 +/- 0.0123 in PA-HIER-32 debiased form; raw -0.0720 +/- 0.0121, Z -5.97) and its structure (entirely in the narrowest-window quartile, hw < 1.08 sigma_g: -0.254 +/- 0.037; sky-excluded class null -0.019 +/- 0.029). Registered form: flag theta_zwindow {off,on} default off byte-identical + knob z_window_k default 1.0; derivation shows the transform alone (k=1) removes 55 percent of the s-term (the GW envelope still cuts the kernel at zeta/s sigmas), while k=4 = integration_limit_sigma_multiplier makes the selection interval the site-2.2 window W_g^theta itself (capture 1.0000 at every node, 99.9 percent removed). Divisor NOT re-windowed (data-independent by definition; T1.1 form stands); sky cone carries no theta (no sigma_z in it; sky_cone_k stays a separate knob). Registered decisive arm P1 = on, k=4.0, sky 1.5, divisor on, sites 2.2, smear off, h=0.73, 4 seeds x {truth, s_plus, s_minus} (12 cells, ~2.5 h wall at 14 cores; off-truth cell 800-860 s from the T1.2 anchor 698 s + count growth x2.5-3.6); prediction (PA-HIER-32 debiased, c-weighted convention fixed before unblinding): score_s = -0.026 +/- 0.012 (Z_s ~ -2.1), point band [-0.031, +0.005], BAND |Z_s| <= 3; raw form Z_s in [0, +2.5] with E12 raw -0.005 +/- 0.011 (Z -0.5 +/- 1.0, sky 3.0) as the measured analogue; b-axis not re-run (byte-identical at default; if re-run at k=4: E12-calibrated -0.78 +/- 0.47, Z_b -1.7). Control arm P2 (k=1): PARTIAL predicted, Z_s in [-4.8, -3.4], REPORTED-ONLY. Falsifier F1: |Z_s| > 3 at P1 refutes the window-truncation attribution; next candidates named: the S_bar_phi table s own sigma_z dependence (T1.1 kappa(h) diagnostic first) and the V2 mixture-weight covariance (c > 0.84 class, -0.338 +/- 0.031). Row #255 A17 standing grant is the approval; builder != presenter; runner != builder. |
| 2026-08-30 | pre-commit | implemented | PASS | bayesian_statistics.py: new helper catalogue_leg_1d_mass_aware_factor (point/kernel mirror of Sigma_4D's own with-BH branch), class default + __init__ default (catalogue_leg_1d_mass_aware="off"), evaluate() kwarg + guard block (raises unless catalogue_numerator_survival and catalogue_global_selection both resolve to "phi" and theta_phi_divisor="off"), site D1 (global_denom_no_bh/global_denom_with_bh: "on" substitutes Sigma_4D, already in hand), site W1 (combined_without_bh_mass: "on" substitutes alpha_G_phi for beta_G_phi, the identical float the 2D assembly consumes), _starmap_host_batches (+2 trailing kwargs, jobs tuple), single_host_likelihood_batch and single_host_likelihood (+2 trailing params each; site N1 point path and quadrature path both replaced under "on"); arguments.py (--catalogue_leg_1d_mass_aware choices off/on default off + matching property); main.py (evaluate() wrapper param + CLI dispatch); validation/correspondence_1d.py (run_mirror_seed_inprocess passthrough param, forwarded to bs.evaluate); results/campaign51_20260728/realistic_20260729/fanout1_20260829/hier_s0_driver.py (--catalogue-leg-1d-mass-aware choices off/on default off, "_ma1d" node-dir suffix in _node_dir_suffix, threaded through run_theta_node / run_arm_seed_s0a / run_arm_seed_s0r / run_seed_s0c / _run_one_seed_worker / run_arm / gather_node_results_from_disk / main) | row #255 (standing grant, tree 2 node T2.3 -- instrument only) |
| 2026-08-30 | pre-commit | verified | PASS | bayesian_statistics.py:8079-8089 (site N1 batch), :7319-7325/:7366-7372 (site N1 scalar), :5936-5943/:6019-6021 (site D1), :6520-6525 (site W1) | Sign/units: S_4D and S_bar_phi both dimensionless in [0,1] (same replaced-factor dimension, section 4 of the gate doc); the Malmquist sign (heavier host at least as detectable at an unsaturated d_L/horizon ratio) verified directly on catalogue_leg_1d_mass_aware_factor. Limit checks: (L1) mass-flat survival recovers the coded "phi" numerator to rtol 1e-6 (interpolation-table accuracy); (L7) sigma_g -> 1e-8 in the "kernel" sub-form recovers the "point" sub-form to rtol 1e-6 (a finer 4000-knot M-grid needed to clear this bound -- disclosed, an interpolation-discretisation effect of the "kernel" erf-sum, not a "point"-form defect); the with-BH channel (columns 2/3, both worker kernels) is bit-unchanged between "off" and "on" at every parametrisation tried. Z = 1 identity: a self-contained 200-galaxy synthetic fixture (r_Malm = 0.850, an informative can-fail-control value under the R2 bound r_Malm <= 0.9) confirms integral p_i dd = 1.0 to atol 1e-10 under "on" and equals D_phi/D_tilde_phi != 1 under "off", using the real path_a_mixture_objects function; the n_hat_w re-booking identity (alpha_G_phi/Sigma_4D == beta_G_phi/Sigma_phi == 1/n_hat_w_phi) is pinned separately at 3 parametrisations including the r_Malm=1 limit. Scalar/batch parity confirmed to rtol 1e-9 under "on" at both host-z-kernel modes. Byte-identity at "off" confirmed (kwarg-omitted vs explicit "off", scalar and batch). Guards: all 4 (catalogue_numerator_survival != "phi", catalogue_global_selection != "phi", theta_phi_divisor != "off", unknown token) raise at both evaluate() and worker level; "off" unaffected by the other three flags' values; "on" logs COUNTERFACTUAL only, never [PHYSICS] ACTIVE. New test file darksiren_emri_test/bayesian_inference/test_catalogue_leg_1d_mass_aware.py, 26 tests, all passing. Full suite: darksiren_emri_test/bayesian_inference (617 passed / 6 skipped), darksiren_emri_test/validation (402 passed / 1 skipped), darksiren_emri_test/test_arguments.py (27 passed), then the complete tree in two directory halves (845 passed / 6 skipped / 15 deselected; 1096 passed / 9 skipped / 15 deselected) -- 1941 passed total (the T2.2 baseline 1915 + the 26 new tests), zero regressions. ruff check --fix darksiren_emri/: clean; ruff format: 1 file reformatted (bayesian_statistics.py, cosmetic); mypy darksiren_emri/: clean after widening catalogue_leg_1d_mass_aware_factor's M_g/M_g_error annotations to float or ndarray (the scalar kernel passes bare floats). Not verified by this row (out of instrument-only scope, per governance section 11 of the gate doc): the F3 registered predictions (arms (a)-(c), none run by this node), the R13 live-catalogue engagement gate, and the REPORTED-ONLY kernel-form cost. | row #255 (standing grant, tree 2 node T2.3 -- instrument only) |
| 2026-08-30 | pre-commit | presented (revised) | row #255 (standing grant, tree 2 node T1.3; panel revision) | results/campaign51_20260728/realistic_20260729/tree2_20260830/PHYSICS_CHANGE_THETA_ZWINDOW_20260830.md, Revision note 1 (appended after the provenance section), superseding section 2.2's z_window_k guard-site attribution and section 1(c)'s bayesian_statistics.py line citations only -- nothing above rewritten | Refuter panel returned two must_fix items on the T1.3 gate doc (refuted=false; neither changes a registered number, band, prediction, or decision-table item). (1) CONFIRMED and fixed: section 2.2 contradicted itself on where z_window_k is read/validated (the flag-declaration line said handler.py; the next sentence implied the bayesian_statistics.py call site, citing the mass_filter_k precedent). Re-verified the actual precedent in the working tree: mass_filter_k's only raise site is handler.py:692-694 (isfinite guard) inside get_possible_hosts_from_ball_tree; bayesian_statistics.py:4225/:5649 only casts and passes it through, with no guard. Corrected registered plumbing: z_window_k's ValueError guard goes in handler.py alongside the existing mass_filter_k guard (the single raise site, mirroring the precedent exactly); theta_zwindow's token guard and the theta_b/theta_s _validate_theta call correctly stay on the bayesian_statistics.py side at the call site (:5636-5650), as originally stated -- so there is exactly one raise site per guard, and R5's guard test for z_window_k now targets handler.py, not the call site. (2) CONFIRMED and fixed, but the panel's own suggested replacement (:8105-8107) was independently re-derived and found itself off by 2 lines: a fresh cat -n re-grep of bayesian_statistics.py shows the batch site-2.2 block's true lines are :8099 (integration_limit_sigma_multiplier), :8103 (sigma_z_pv), :8104 (host_z_error_eff), :8105 (the if line), :8113-8115 (the theta-hook body), :8124 (den_hi), :8141-8143 (den_lo) -- not the document's original :8101/:8107-8109/:8115-8117/:8126/:8143-8144, and not the panel's proposed :8105-8107 (which mislabels two comment lines as sigma_z_pv/host_z_error_eff). The offset is 4 lines for the sigma_z_pv/host_z_error_eff/if triple and 2 lines for the rest of the same excerpt, traced to two comment lines inserted between the multiplier line and sigma_z_pv since the citations were written. The scalar-twin parenthetical (:7321, :7407-7408, :7418, :7426-7427) was checked on the same basis and corrected to :7319, :7403-7404, :7406, :7424-7426. Every other citation in the document (sections 1(a)/1(b)/1(d), 2.2's :1625-1633/:1640-1643/:5636-5650, section 8) was independently re-grepped fresh and reproduces byte-for-byte -- confirming the panel's own finding that those were already exact; no other line number in the document changes. Append-only: revision note 1 appended, nothing above it edited. No code, no arms run, no git by this node. |
| 2026-08-30 | pre-commit | presented (revised) | row #255 (standing grant, tree 2 node T1.3-zwin; relabeled this row; second panel revision) | results/campaign51_20260728/realistic_20260729/tree2_20260830/PHYSICS_CHANGE_THETA_ZWINDOW_20260830.md, Revision note 2 (appended after Revision note 1), narrowing the operative reading of bayesian_statistics.py:7391-7392's in-code comment, relabeling this node T1.3 -> T1.3-zwin, and downgrading section 5.6's "c-weighted primary" clause from settled to proposed-pending-[RULE] -- nothing above rewritten | Second refuter panel returned three must_fix items (refuted=false; none changing a registered number, band, prediction, or falsifier threshold). (1) RECONCILED, not superseded as a measurement: bayesian_statistics.py:7391-7392 ("the ball-tree candidate window... intentionally keep the bare catalogue z_error -- a +/-1sigma, second-order candidate-list effect") re-verified present (site 2.1 only, not duplicated at site 2.2); git log -S traced it to commit 8568d9fc (2026-07-03, issue #16, PV-marginalization), seven weeks before theta existed -- the comment concerns the (now identically-zero, SIGMA_V_PEC_KM_S=0.0) PV-fold term, not the theta_b/theta_s transform, so it is an unverified scope-narrow assertion about a different term, not a superseded measurement; but it reads, unqualified, as covering this node's finding that the same window choice is DOMINANT (up to 99.9-100 percent of the s-term removed at k=4) under theta != (0,1) -- narrowed disclosure added plus a builder instruction (R7c, documentation-only) to re-scope the comment's wording when the registered code change lands at that site. (2) CONFIRMED, node relabeled: TREE2_CHARTER_20260830.md's own table reserves "T1.3" for the S0-B/C1 production-venue launch gated on a passing T1.2 (quoted verbatim, re-grepped); this document's reuse of "T1.3" for the z-window gate (inherited from row #266's own path-naming, not invented here) collides with it. Resolved by relabeling this node "T1.3-zwin" effective this note (the panel's own "alternatively relabel" option), rather than editing the charter or the ledger, both outside this presentation's scope; a charter disambiguation note is recommended to the orchestrator, not performed here. (3) CONFIRMED, F1's operative status corrected: row #266 (re-quoted, read-only) calls the raw-vs-c-weighted Es_null_det convention "disclosed, not adjudicated, a fresh reconciliation item for the author/orchestrator," but section 9's F1 and section 5.6 named the c-weighted form PRIMARY for the decisive pass/fail read without an author/orchestrator [RULE] adopting it. Downgraded to PROPOSED pending [RULE]; both convention numbers in section 5.6 stand unchanged; the P1 reader must report both side by side and must not declare F1 CONFIRMED/REFUTED on one convention alone -- that declaration is itself the fresh [RULE] this document cannot pre-empt. Append-only: revision note 2 appended, nothing above it (including revision note 1) edited. No code, no arms run, no git by this node; BIAS_HISTORY_LEDGER.md and TREE2_CHARTER_20260830.md read-only, not written. |
| 2026-08-30 | pre-commit | presented (revised, final) | APPROVED | row #255 (standing grant, tree 2 node T1.3-zwin) | Third refuter panel returned four must_fix items (documentation only; none changing a registered number, band, prediction, or falsifier threshold). (1) Section 0's hw_sig-quartile numbers (-0.254/-0.042/+0.011/-0.006) labelled explicitly as the RAW ss_lin statistic (t13_out.json by_hw_sig_quartile), distinguished from section 3's debiased ss_deb_c quartile numbers (-0.290/-0.074/-0.013/-0.026, *_model_vs_measured_by_hw_sig_quartile "measured" column) -- the two sit a few sections apart with the same quartiling and no convention label, inviting a same-statistic misread. (2) Section 8(b)'s capture-model-approximations list disclosed as omitting the envelope-linearity assumption: f_lo = z_min/z, f_hi = z_max/z (section 2.3) are held FIXED at each event's own observed z_min_ball/z_max_ball/z_GW (f7_events.csv, E6-exact ball bounds) while z_s varies inside the capture-fraction integral -- the model does not re-evaluate the GW envelope at the varying drawn redshift; added as a disclosed fifth item, no registered number changed. (3) Section 3 item (ii)'s -0.031 +/- 0.011 figure (E12's own debiased residual, quoted unchanged in section 8(a) as the P1 floor) given an inline pointer to section 5.6's own footnote ("c_i changes with the candidate set, so the c-weighted convention needs the ARM's own truth node") -- E12's -0.005 c-weighted point was debiased using T1.2/baseline c_i, not a truth node at E12's own (enlarged-ball) configuration, so the floor inherits an un-quantified config-dependence beyond its stated SEM. (4) The header's "No backtick characters in this record" claim (line 14) WITHDRAWN: re-counted fresh this note (python3, character-exact) at 182 backtick characters, all 182 within Revision note 1, 0 in Revision note 2 -- not the 46 supplied in this presenter's own launch instruction, which this note does not reproduce uncorrected, on the same fresh-verification basis Revision note 1 item 2 used for the panel's own imprecise line-number replacement. Append-only: Revision note 3 appended to PHYSICS_CHANGE_THETA_ZWINDOW_20260830.md (lines 925-1097) after Revision note 2; nothing above it (including Revision notes 1-2) edited. No code, no arms run, no git by this node; BIAS_HISTORY_LEDGER.md, TREE2_CHARTER_20260830.md, the T1_2_*/T2_2_* files and the HIER prereg read-only, not written. |
| 2026-08-30 | pre-commit | implemented | PASS | galaxy_catalogue/handler.py:558-573 (get_possible_hosts_from_ball_tree signature: +theta_zwindow="off", +z_window_k=1.0, +theta_b=0.0, +theta_s=1.0), :714-761 (redshift_filter_mask rewritten: single read/validate site for z_window_k, isfinite-and-positive guard alongside the mass_filter_k guard; theta_zwindow token guard; "off" branch bare z_g/sigma_g at k=z_window_k; "on" branch literal-skips at theta=(0,1), else z_g^theta=z_g+b(1+z_g), sigma_g^theta=sqrt((s*sigma_g)^2+sigma_pv^2) with sigma_pv=(1+z_g)*SIGMA_V_PEC_KM_S/SPEED_OF_LIGHT_KM_S), imports SIGMA_V_PEC_KM_S; bayesian_statistics.py:3659-3671/3765-3768 (class + __init__ defaults theta_zwindow="off", z_window_k=1.0), :3945-3970 (evaluate() kwargs + docstring), :4256-4269 (token guard + opaque-cast storage, the mass_filter_k/theta_phi_divisor precedent), :5646-5658 (single call site: theta_zwindow/z_window_k passed unconditionally, theta_b/theta_s passed ONLY when theta_zwindow=="on" else the identity defaults), :7391-7397 (Revision-note-2-directed comment update at the site-2.1 "second-order" remark, scoped to the zeroed PV fold only); arguments.py (--theta_zwindow choices off/on default off, --z_window_k default 1.0, matching properties); main.py (evaluate() wrapper params + CLI dispatch, both call sites); validation/correspondence_1d.py (run_mirror_seed_inprocess passthrough params + docstring, forwarded to bs.evaluate); results/campaign51_20260728/realistic_20260729/fanout1_20260829/hier_s0_driver.py (--theta-zwindow choices off/on default off, --z-window-k default 1.0, "_zwin"/"_zk<k>" node-dir-suffix parts in _node_dir_suffix, threaded through run_theta_node / run_arm_seed_s0a / run_arm_seed_s0r / run_seed_s0c / _run_one_seed_worker (+2 args-tuple slots) / run_arm / gather_node_results_from_disk / score_only_payload's CLI call sites / main; PA-HIER-32(d) scorer: new _es_null_det_closed_form / _es_null_det_kernel / compute_es_null_det_table / _write_es_null_det_cache / _read_es_null_det_cache, wired into run_arm_seed_s0a/s0r (compute+cache once per seed, merged into every node's ln_l via read_event_ln_l's new es_null_det kwarg) and gather_node_results_from_disk (cache read-back, no venue reconstruction); compute_scores rewritten to emit score_b (unchanged), score_lns (PA-HIER-4's ln-s-centred secant, denominator ln 2), score_s_raw (the old linear secant, renamed, kept for continuity) and score_s (PA-HIER-32(d) corrected primary = score_lns - Es_null_det, with score_s_available disclosed False/n_pooled=0 when no cache is present, never silently substituting an uncorrected number); write_score_markdown extended to print score_s_raw/score_lns/score_s_available alongside score_s | row #255 (standing grant, tree 2 node T1.3-zwin) |
| 2026-08-30 | pre-commit | verified | PASS | galaxy_catalogue/handler.py:716-761 (z-filter mask); bayesian_statistics.py:4256-4269 (guards/storage), :5646-5658 (call site), :7391-7397 (comment) | R1 byte-identity: "off" at default k=1.0 reproduces the bare +/- 1 sigma_g mask exactly on a synthetic 6-row fixture spanning inside/edge/outside/wide-sigma cases, and is unmoved by engaged theta_b/theta_s passed alongside "off" (the flag must never leak theta into a disabled window). R2/GATE T-ID: "on" at theta=(0,1) is bit-for-bit identical to "off" at every k in {1,2,4}, and the z_window_k=2.0 "off" mask independently reproduces its own hand-computed +/- 2 sigma_g form. R3/R4: k=1 candidate set is a strict subset of the k=4 set at the identity (nested, non-trivial on the fixture). Engagement: s=3.0 admits a new host a s=1.0 window cannot reach (superset, non-trivial); a centre shift b=0.15 moves the admitted set; "on" with theta engaged differs from "off" at the same k. Theta-consistency: "on" reproduces the section-2.1 closed form (z_g+b(1+z_g) +/- k*sqrt((s*sigma_g)^2+sigma_pv^2), sigma_pv=0 since SIGMA_V_PEC_KM_S=0.0 today) exactly at three independent (b,s,k) triples. Guards: invalid theta_zwindow token and non-finite/non-positive z_window_k (0, -1, nan, +-inf) all raise, including at theta_zwindow="off" (the guard is unconditional). Mass-filter inheritance: the with-BH set is a subset of (and, under a permissive mass window, equal to) the widened no-BH set at k=4 -- membership only ever shrinks. Plumbing (evaluate()/CLI): the theta_zwindow token guard raises with the theta_phi_divisor pattern before any floating op; a deliberate downstream abort (theta_phi_divisor="bogus") proves theta_zwindow/z_window_k reached self._theta_zwindow/self._z_window_k before that later guard fired; BayesianStatistics()'s bare class defaults and Arguments.create([".", "--evaluate"])'s CLI defaults are both "off"/1.0; --theta_zwindow on --z_window_k 4.0 forwards correctly; an invalid --theta_zwindow choice raises SystemExit at argparse. Driver scorer: compute_scores's score_s_raw and score_lns hand-verified against independently computed (s_plus-s_minus)/(sqrt2-1/sqrt2) and /ln(2) respectively on a synthetic 2-event fixture; score_s (corrected) hand-verified as score_lns-Es_null_det per event THEN pooled, on a 3-event fixture with a planted es_null_det column, matching an independent by-hand mean; score_s_available correctly reports False/n_pooled=0 with no es_null_det column present anywhere (score_s never silently equals score_lns); score_b unaffected. _es_null_det_closed_form smoke-tested end-to-end (real GL/trapz numerics, no mocking) on a single synthetic host under a flat completeness, returning a finite, sign-correct value. New test files: darksiren_emri_test/test_theta_zwindow.py (22 tests, handler-level R1-R6 plus mass-filter inheritance) and darksiren_emri_test/bayesian_inference/test_theta_zwindow.py (9 tests, evaluate()/CLI plumbing + the driver scorer, loaded directly from its results/ path). Full suite: darksiren_emri_test/bayesian_inference (626 passed / 6 skipped), then the complete tree in two directory halves (923 passed / 6 skipped / 21 deselected; 1106 passed / 9 skipped / 15 deselected), zero regressions. ruff check --fix darksiren_emri/ results/.../hier_s0_driver.py darksiren_emri_test/test_theta_zwindow.py darksiren_emri_test/bayesian_inference/test_theta_zwindow.py: clean (one B023 loop-variable-capture finding in the new _es_null_det_closed_form, fixed by extracting the per-host kernel to a module-level function with no closure over loop variables); ruff format: unchanged in darksiren_emri/, 2 test files + the driver reformatted (cosmetic); mypy darksiren_emri/: clean, no new annotations needed. Not verified by this row (out of this node's registered scope): any actual P1/P2/P1b arm (no evaluate() call was made against the real GLADE catalogue by this node); the F1/F2 falsifier reads (require P1 data); the Es_null_det closed form's numerical agreement with the archived f4_mechanism.py/f4_out.json figures on the real production catalogue (both implement the same math independently but were not cross-run here). | row #255 (standing grant, tree 2 node T1.3-zwin) |
| 2026-08-30 | pre-commit | implemented (addendum) | PASS | results/campaign51_20260728/realistic_20260729/fanout1_20260829/hier_s0_driver.py:1158-1219 (compute_scores' node-completeness gate, rewritten from all-4-required to per-axis), :1817-1873 (run_arm's requested/produced gate, same relaxation), :2016-2039 (score_only_payload's need_all_four gate, same relaxation) | Found while drafting this node's own registered P1 command: the pre-existing gate in all three functions hard-required ALL FOUR of b_plus/b_minus/s_plus/s_minus to be present before computing any score, so the registered P1 arm's own node list (theta_sites 2.2, nodes truth/s_plus/s_minus, NO b-nodes -- section 5.6's own text, "b-axis: NOT re-run under P1") would have raised ValueError out of compute_scores and never produced score_s at all, silently defeating this node's entire scorer implementation for its own use case. Relaxed to per-axis (b, s each independently "ready" iff BOTH its nodes are present and non-empty): an axis simply never requested is now fine (that axis's score reports n_pooled=0/NaN, score_b_available/score_s_available False, never a crash); a PARTIALLY present axis (one of the two nodes missing while the other succeeded -- a genuine broken pair, e.g. one seed's worker for b_minus raised) still raises the original, now axis-specific, diagnostic; both axes absent still raises. gate_eng/gate_parity were already per-node tolerant (unchanged). No change to score_b's or score_s's own formula; verdict_s0a/verdict_s0r read the same keys and already degrade to their INSTRUMENT-DEFECT branch on a NaN Z (np.isfinite guard, pre-existing), so they needed no further change. Two new regression tests (test_compute_scores_handles_the_registered_p1_node_list_with_no_b_axis, using the exact node-dict shape run_arm produces for --nodes truth,s_plus,s_minus; test_compute_scores_raises_on_broken_axis_pair / test_compute_scores_raises_when_neither_axis_present for the two failure modes) added to darksiren_emri_test/bayesian_inference/test_theta_zwindow.py (12 tests total in that file after this addendum); full driver-scorer test file re-run clean; ruff/mypy re-run clean; the complete darksiren_emri_test tree re-run in two halves after this change (926 passed / 6 skipped / 21 deselected; 1106 passed / 9 skipped / 15 deselected), zero regressions (this file is outside darksiren_emri/ and darksiren_emri_test/, so it carries no mypy obligation, but ruff/tests were re-run anyway). | row #255 (standing grant, tree 2 node T1.3-zwin) |
| 2026-08-30 | pre-commit | verified (revised) | PASS | results/campaign51_20260728/realistic_20260729/fanout1_20260829/hier_s0_driver.py:512-624 (`_es_null_det_closed_form` secant denominator) | Independent verifier MUST_FIX (T1_3_ZWINDOW_VERIFIER_REPORT.md item 3): PA-HIER-32(d) (PREREGISTRATION_HIER_HTHETA_20260826.md, PA-HIER-32(d)) defines `Es_null_det_i` as the closed-form expectation of `score_lns_i` (ln(2) secant denominator), but the code computed it with `score_s_raw`'s denominator (`sqrt2 - 1/sqrt2`), a `(sqrt2-1/sqrt2)/ln(2) = 1.02014` factor error (the code's raw-denominator value undercounts the registered ln(2)-denominator value by that factor). Fixed: `denom_lns = math.log(2.0)` replaces the single `denom_s` use site inside `_es_null_det_closed_form` (window-selection logic and the raw `score_s_raw` path elsewhere in the driver are untouched -- confirmed only one use site existed). Two new regression tests added (`test_es_null_det_closed_form_uses_the_ln2_secant_denominator`, `test_score_s_equals_score_lns_minus_es_null_det_by_construction`, darksiren_emri_test/bayesian_inference/test_theta_zwindow.py, 36 tests total in the two zwindow test files, all passing) pinning the ratio `Es_null_det(ln2)/Es_null_det(raw) = (sqrt2-1/sqrt2)/ln(2)` and `score_s == score_lns - Es_null_det`. `ruff check`/`ruff format --check` clean on the driver + test; `mypy --ignore-missing-imports` clean except 8 pre-existing `run_mirror_seed_inprocess` kwarg-splat errors at lines 484/500 (reproduced identically at HEAD, unrelated to this fix, out of scope). Recert re-run (`--score-only`, seeds 900101-900104, out-root tree2_20260830/hier_s0_recert_run) confirms `score_b`/`score_s_raw` bit-identical to the banked values (Z_b=-0.6762222336551854, raw Z_s=-5.971118248467597); the corrected `score_s`/Z_s remain reported unavailable for this specific recert_run (no `es_null_det.csv` cache present -- this run predates PA-HIER-32(d), and the design never silently substitutes an uncorrected number); a full P1 arm (which computes+caches `Es_null_det` fresh per seed from the real GLADE catalogue) is outside this node's foreground/600s budget, matching the verifier's own disclosure that the defect "WILL be exercised the moment the registered P1 arm runs". | row #255 (standing grant, tree 2 node T1.3-zwin; verifier MUST_FIX) |
| 2026-08-31 | pre-commit | implemented | PASS | bayesian_statistics.py:3690-3695 (class default "auto" + comment), :3779-3783 (__init__ "auto"), :4024-4031 (evaluate kwarg "auto" + docs), :4403-4460 (token guard widened to auto/off/on; NEW auto-resolution block — "on" iff numerator/global-selection both "phi" AND theta_phi_divisor "off", else "off" silently; explicit "off" now warns COUNTERFACTUAL; resolved "on" logs [PHYSICS] ACTIVE info instead of the stale COUNTERFACTUAL warning), :7310-ish/:8092-ish/:8774-ish (worker fallbacks kept "off", comments state evaluate() threads the resolved token); main.py:1460-1465 ("auto"); validation/correspondence_1d.py:3199-3204 ("auto"); arguments.py:1333-1352 (choices auto/off/on, default "auto", help rewritten). Production-default flip of the mass-aware 1D catalogue leg per §6.3 Z-CONFIRMED (arm (c) job 6747032: 1D map 0.665 / mean 0.66699 in band; C-C pin exact-zero) + §11's fresh-[RULE] chain rows #278(3)/#284/#286; AMENDMENT G-EXT grid; the auto→engaged form (row #197/#253 pattern) chosen because a blind default "on" would raise the §2 guard in every non-phi-stack evaluate() call (caught by the pre-existing R14/off-default tests before commit). Test amendments: R14 inverted + 3 new auto/off-logging tests; unknown-token match string updated; R1 worker byte-identity tests unchanged by design. | rows #284-#286 (author cross-check ratification of the row #278(3) conditional; Z-CONFIRMED measured) |
| 2026-08-31 | pre-commit | implemented (addendum) | PASS | results/campaign51_20260728/realistic_20260729/fanout1_20260829/hier_s0_driver.py:1447-1500 (gate_eng: all_nodes.get("truth", []) guard; empty-truth path reports eng_available=False/NaN/pass=False per off-truth node instead of KeyError, mirroring the 2026-08-30 per-axis relaxation that fixed compute_scores/run_arm/score_only_payload but missed this function; truth-present path stamps eng_available=True) | Found by runner-11's b-only instrument run (row #287): 8/8 compute cells succeeded, the crash was purely post-hoc scoring. Regression test test_gate_eng_handles_a_b_only_node_dict_with_no_truth_node added to darksiren_emri_test/bayesian_inference/test_theta_zwindow.py (15/15 pass); ruff check/format clean both files; call sites unchanged (they already store the payload dict; write_score_markdown reads keys still present). Builder = subagent, reviewer = orchestrator, runner (score-only) = same subagent on banked CSVs (zero compute). | rows #280/#287 (standing grant; instrument-driver repair) |
| 2026-09-02 | a26959b4 | presented | APPROVED | cosmological_model.py:388 | h admissibility decoupled from host-window bound: h_grid_admissibility_max=1.00, guard max(); design DECOUPLING_DESIGN.md, author word row #308 |
| 2026-09-02 | a26959b4 | implemented | PASS | cosmological_model.py:388+bayesian_statistics.py:4655 | ref comment + regression tests (old-behavior tests written first) |
