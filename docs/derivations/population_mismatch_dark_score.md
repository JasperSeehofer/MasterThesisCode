# The dark-class score bias from population misspecification — derivation memo

**Date:** 2026-08-20 · **Status:** derivation + quantitative post-hoc confirmation; the
PRE-REGISTERED confirmation (B-OUT, job 6385173) is in flight. Authored under the author's
autonomy grant; the fork in §6 returns as a [RULE]. **No production change proposed here.**

**Object:** the production base tilt — the 1D posterior railed at 0.601 (2D −0.053 after the
B_scale removal), shown in `PREREG_COMPLETION_CLASS_DECOMPOSITION.md` to be carried by the
pure-completion (dark) class with a per-event score at truth of **−0.635 ± 0.017** (37σ).

## 1. The identity being violated

For data generated by the model itself, the score has zero mean at truth:
E_true[∂_h ln p(d|h)]|_{h_true} = ∂_h ∫ p(d|h) dd = ∂_h 1 = 0. A large non-zero mean score
therefore means the data are NOT drawn from the estimator's model. For dark events the
estimator's model is

    p(d_obs|h) ∝ (1/D̃^φ(h)) ∫ dz  w_model(z;h) (1−f(z)) S(z;h) p_gw(d_obs | d_L(z;h))

with **w_model = constant comoving number density × dV_c/dz/(1+z)** — an explicit modelling
assumption (`bayesian_statistics.py:1192`, used at :1590/:1666). The events, however, are
injected from the **Barausse (2012) M1 EMRI rate** (arXiv:1201.5888), whose redshift shape
w_true(z) differs.

## 2. First-order prediction of the induced score bias

σ_dL/d_L ≈ 10⁻³–10⁻¹, so the z-integral is dominated by the saddle z*(h) with
a(z*) = h·d_obs (a ≡ h·d_L). Writing the slowly varying factors as
g(z) = w(z)(1−f(z))S(z)·|dz/dd_L|, ln p ≈ ln g(z*(h)) − ln D̃^φ(h), so the score difference
between analysing with w_model and with the true w_true is

    **Δscore(z) ≈ [ d ln(w_model/w_true)/dz ]_(z) × (dz*/dh),   dz*/dh = a(z*)/(h · da/dz)**

— the log-slope of the population ratio times the redshift lever arm. Only the ratio's
SHAPE enters (normalisation cancels), which is why a self-consistent harness sees nothing.

## 3. Quantitative test (post-hoc; B-OUT is the pre-registered confirmation)

w_true measured from the pool's stratum-'a' rows (the population-measure stratum, issue #51
bookkeeping); w_model computed as dV_c/dz/(1+z) at h = 0.73; the score measured on the
banked production baseline (iiib, dark class, n = 605):

| z bin | n | measured score | predicted (§2) | ratio |
|---|---|---|---|---|
| 0.075–0.392 | 121 | +0.014 | −0.095 | — (both ≈ 0) |
| 0.392–0.559 | 121 | −0.465 | −0.329 | 1.41 |
| 0.559–0.659 | 122 | −0.743 | −0.818 | 0.91 |
| 0.659–0.753 | 120 | −0.902 | −0.659 | 1.37 |
| 0.753–1.018 | 121 | −1.081 | −0.592 | 1.83 |
| **ensemble** | **605** | **−0.635** | **−0.555** | **1.14** |

A one-line, first-order formula with no free parameters reproduces **87%** of the measured
ensemble score bias and its monotone redshift growth. Residual discrepancy is expected at
this order: the derivation drops the z-dependence of (1−f)S relative to the population term,
uses a saddle-point approximation, and takes w_true from a finite pool histogram.

## 4. Why four campaigns of elimination could not see this

The validation harnesses (`pp_coverage`, `calibration_gate`, `venue_transfer`) **generate
events from the same population they analyse**, so w_model ≡ w_true by construction and
Δscore ≡ 0: a self-consistent harness is STRUCTURALLY BLIND to a data-vs-model population
mismatch, no matter how many arms it runs. This is the same blindness class as the
un-derived B_scale surviving every counterfactual that held it fixed (rows #130–#131).

## 5. Consequence for the systematics budget (returns to the author)

`docs/gates/G7_systematics_budget.md` **row 16** records the M1 population-shape choice as
affecting "rates/shape, **not estimator calibration** (P–P closes at injected truth)". The
measurement above contradicts that clause in the dark-dominated regime: the population-shape
mismatch is not a rate nuisance, it is **the leading term of the production H₀ bias** (≈87%
of the dark-class score, and the dark class carries ~195% of the full-sample slope). The
parenthetical is exactly the §4 blindness — P–P closes because the harness is
self-consistent. Row 16 should be re-graded from QUOTED-forecast-assumption to a measured,
dominant, calibration-affecting systematic.

## 6. Fork (author [RULE]; none pre-decided)

- **(a) Self-consistent mock:** give the estimator the injected M1 dN/dz as its dark-class
  population prior. Removes the mismatch by construction; makes the mock a clean closure
  test but assumes the population is known — which a real analysis cannot.
- **(b) Hierarchical marginalisation (principled, matches the literature):** marginalise
  over population/rate-evolution parameters, so H₀ is not conditioned on an assumed rate
  shape. Largest scope; the honest form for a real dark-siren analysis.
- **(c) Document as the dominant systematic:** keep the comoving prior, quote the measured
  score/H₀ sensitivity and the z-resolved decomposition as the systematic's size.
- Any of these is a *modelling* decision, not a bug fix: the estimator's mathematics is not
  defective here — it is being conditioned on a population that is not the one that
  generated the data. Consistent with the binding value (correctness over bias-removal).

## 7. Pending confirmation

**B-OUT** (registered A-2, job 6385173, 15 seeds) draws dark hosts from the estimator's OWN
comoving population and analyses them with the same estimator: it must be UNBIASED
(|bias| ≤ max(0.005, 2·SE)) if §1–§3 are right. A biased B-OUT would instead indicate a
residual internal misnormalization and would falsify this memo's attribution.

---

## Appended note, 2026-08-29 (orchestrator decision, charter node B3; append-only)

**Launched under rows #222/#223 — charter node B3.** Nothing above this note is altered.

**§F of `results/campaign51_20260728/realistic_20260729/fanout1_20260829/PHYSICS_CHANGE_POPULATION_PRIOR_M1_20260829.md`
refutes this memo's §1 premise at zero compute, at the generating commit.** §1 above states "the
events … are injected from the Barausse (2012) M1 EMRI rate" and takes `w_true(z)` from
`Model1CrossCheck.emri_distribution`. §F shows this is the wrong generator object: the production
event set (generating commit `03cfe80`, `dark_siren_injection.py:328`) draws dark hosts from
`density = (1.0 - f_z) * _redshift_population_weight(z_grid, h)`, i.e. `(1−f)·dVc/dz/(1+z)` —
**byte-identical to the estimator's own completion-leg prior** (`bayesian_statistics.py:1192`,
this memo's `w_model`). `Model1CrossCheck.emri_distribution` (the z-dependent M1 curve this memo
calls `w_true`) is, in this codebase, **the p_det survival pool's sampling law** (stratum 'a',
`main.py:injection_campaign`) — never the production CRB event set's law. The two objects share
the name "M1" but are not the same generator; §3's post-hoc 87% coverage confirms only that the
estimator's algebraic response to swapping in the pool's M1 curve has the right size and shape,
not that such a mismatch exists between the production data and the estimator's prior.

**Consequence for row #138 (`BIAS_HISTORY_LEDGER.md:1367-1392`):** row #138 **stays on the record**
as measured (the score-at-truth numbers in §3 above and its ledger entry are unchanged); its
**production-venue causal premise is refuted** — the dark-class score bias measured on the
production venue is not attributable to a data-vs-estimator population-shape mismatch, because no
such mismatch exists there. The re-measurement on fused HEAD diagnostics
(`B3_1_POP_RECORD.md`, charter node B3.1) that reported 98.5–129.9% coverage of this memo's
predicted term carries the same superseding note for the same reason. Fork (a) of §6 above
(give the estimator the injected M1 dN/dz) is now understood to already describe the as-built
system for the production z-shape — there is no comoving-vs-M1 gap to close by adopting it; fork
(b) (hierarchical marginalisation over rate-evolution parameters) and fork (c) (document the
model-scope systematic) remain live, and are re-scoped: the open lever is the astrophysical
*uncertainty* in the true EMRI rate's redshift shape (unknown for real data), not a mismatch
between this codebase's generator and its own estimator.

**M1-(i)/M1-(ii) labelling recommendation (§12 of the physics-change presentation):** the repository
carries two functions both called "M1" — `emri_rate.R_EMRI` (M1-(i), the `p0 = 1` constant-comoving
surrogate that drives the production generator's dark-host draw and the estimator's completion
prior) and `Model1CrossCheck.emri_distribution` (M1-(ii), the extracted z-dependent Barausse curve
that drives only the p_det pool's stratum-'a' sampling). §12 recommends a docstring cross-reference
in both files naming which pipeline each feeds, so a future reader does not repeat this memo's
substitution. Docs-only; no gate required.

**Branch verdict of record: B3 CLOSED — PREMISE-REFUTED (provenance, zero compute).**

## Addendum, 2026-08-30 (launched under row #255 -- tree 2 node A5; append-only)

G7 row 16 re-graded exactly as ruled in ledger row #255 item A5:

mock: zero by construction (the injected dark-host law is the estimator prior, section F of
results/campaign51_20260728/realistic_20260729/fanout1_20260829/PHYSICS_CHANGE_POPULATION_PRIOR_M1_20260829.md);
real data: O(1) degeneracy with the population z-evolution -- hierarchical marginalisation
required (shape sensitivity: r(z) 0.53 to 1.39 moves the dark-class score by -0.60 on bins 2-5).

Rows #137/#138 are retired as citations (kept on the record).
