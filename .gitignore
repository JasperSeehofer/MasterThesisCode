*.log
.venv/
.coverage
coverage.xml
docs/build/
docs/source/_generated/
.vscode/
**/__pycache__/
env/
evaluation/
!darksiren_emri_test/fixtures/evaluation/
test-artifacts/

# Data files (regenerable, live on $WORKSPACE on cluster)
cluster_results/
simulations/
results/h_sweep_*/
few_data/
lisa_files/
GLADE+.txt
reduced_galaxy_catalogue.csv

# Figures (regenerable)
saved_figures/
*.png
# Exception: committed baseline audit histograms in .planning/
!.planning/*.png
# Exception: committed documentation gallery figures (served on GitHub Pages)
!docs/source/figures/*.png
# Campaign-report figures are tracked deliberately: the A7 report renders them
# inline, so the PNG must resolve in-repo. Narrow allowlist, matching the
# pattern above, rather than a force-add that leaves the file tracked while the
# global rule says otherwise (2026-08-14).
!results/mechanism_study_20260813/*.png

# Run metadata (per-run)
run_metadata.json

# Claude Code worktrees (embedded git repos)
.claude/worktrees/

# Reference documents (large PDFs, not tracked)
references/*.pdf

# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.out
paper/*.synctex.gz
paper/*.synctex(busy)

# Credentials
cluster/vpn.sh

# --- Internal agent/workflow state (kept local by design) ---
# Durable project state is TRACKED in ROADMAP.md, STATE.md, and docs/ (a transparency virtue).
# The noisy, ephemeral working state below is intentionally local-only; it syncs across the
# author's machines via the same discipline, not by cluttering the public tree.
.planning/
.gpd/
.gardener-notes.md
.claude/agents/
.commission*.yaml
.cartographer-notes.md
docs/coauthor_meeting_*.md
docs/reviews/

# Campaign #51/#53 bulk data mirrors (regenerable; canonical copies live on the
# bwUniCluster workspace — see results/campaign51_20260728/RUNBOOK_NEXT_SESSION_5.md)
results/campaign51_20260728/pool_mix200k/
results/campaign51_20260728/pilot/injections/
results/campaign51_20260728/pilot3/injections/
results/campaign51_20260728/run_seed*/prepared_*.csv

# Campaign #53 realistic-run mirrors: the prepared CRB table is IDENTICAL across a
# seed's 5 realizations (only the observed catalogue varies), so only the
# seed-level copy is tracked. score_realistic.py falls back to it.
results/campaign51_20260728/realistic_20260729/seed*/real_r*/prepared_cramer_rao_bounds.csv

# D1 S_and re-weight: the staged injection pool of record and the two derived
# pools built from it (74 MB of CSV, regenerable byte-for-byte from
# d1_sand/make_sand_pools.py). The sha256 MANIFESTS are tracked instead — they
# are the provenance record; the bulk rows are not.
results/campaign51_20260728/realistic_20260729/gate_b_20260730/injection_pool_mix200k_20260728/
results/campaign51_20260728/realistic_20260729/d1_sand/pool_p0kept/
results/campaign51_20260728/realistic_20260729/d1_sand/pool_p0window/

# Commission research notes (propose-only, not tracked)
.commission-notes.md

# Cluster-staging realization data (multi-GB, seed-reproducible; provenance in *.meta.json)
results/campaign51_20260728/realistic_20260729/realizations_staged/*.csv

# retrieved cluster data (multi-GB, cluster is the copy of record) — 2026-08-28
results/campaign51_20260728/realistic_20260729/headreadout_20260827/
results/_archive/

# wave-2/3 retrieved 2D posterior JSONs are >100 MB each (per-event arrays) — keep local, not in git
results/campaign51_20260728/realistic_20260729/wave*_2026*/*/posteriors_with_bh_mass/
