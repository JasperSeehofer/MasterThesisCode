*.log
.venv/
.coverage
coverage.xml
docs/build/
docs/source/_generated/
.vscode/
**/__pycache__/
env/
evaluation/
!master_thesis_code_test/fixtures/evaluation/
test-artifacts/

# Data files (regenerable, live on $WORKSPACE on cluster)
cluster_results/
simulations/
results/h_sweep_*/
few_data/
lisa_files/
GLADE+.txt
reduced_galaxy_catalogue.csv

# Figures (regenerable)
saved_figures/
*.png
# Exception: committed baseline audit histograms in .planning/
!.planning/*.png
# Exception: committed documentation gallery figures (served on GitHub Pages)
!docs/source/figures/*.png

# Run metadata (per-run)
run_metadata.json

# Claude Code worktrees (embedded git repos)
.claude/worktrees/

# Reference documents (large PDFs, not tracked)
references/*.pdf

# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.out
paper/*.synctex.gz
paper/*.synctex(busy)

# Credentials
cluster/vpn.sh

# --- Internal agent/workflow state (kept local by design) ---
# Durable project state is TRACKED in ROADMAP.md, STATE.md, and docs/ (a transparency virtue).
# The noisy, ephemeral working state below is intentionally local-only; it syncs across the
# author's machines via the same discipline, not by cluttering the public tree.
.planning/
.gpd/
.gardener-notes.md
.claude/agents/
.commission*.yaml
.cartographer-notes.md
docs/coauthor_meeting_*.md
docs/reviews/
