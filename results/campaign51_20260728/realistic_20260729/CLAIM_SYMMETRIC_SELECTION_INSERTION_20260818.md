# CLAIM — Symmetric selection insertion (stage-0 intake; front NOT opened)

**Date:** 2026-08-18 · **Status:** admitted to the record by author [RULE], ledger row #122
item 5. This is an intake card for a FUTURE front; no work is authorized beyond what row #122
grants (the V-flat cell of AMENDMENT-2, whose exploratory symmetric run is non-verdict-bearing).

## The claim

> **[C-SYM]** The exact latent-thresholded conditional places the detection-survival factor in
> BOTH numerator legs of the per-event mixture — S̄_φ(z_gal;h) on each catalogue candidate
> (latent mass marginalized at pinned z) exactly as S̄_φ(z;h) inside the completion integral —
> paired against the same α(h). Production's landed completion-leg-only form ([P2], `2b10b8b8`)
> is therefore a scoped approximation whose error is governed by the variation of S̄_φ across
> the per-event host-hypothesis support: safe where S̄ is flat (production's regime, measured:
> counterfactual zero MAP motion, +0.0155 nats/h/event tilt), unsafe where S̄ varies strongly
> (measured: −0.030 confidently-wrong displacement at the prodcal V-deep venue).

Tags: the both-legs form is [INFER] (from the [P2] derivation's own logic, per the instrument
audit); the boundary-condition evidence is [LOCAL] (prodcal ladder, `6f3a8c3a`): symmetric
+0.008 / completion-only −0.029 / catalogue-only +0.041 (V-deep, n=250, 20 reals, exploratory
audit variant).

**Refute by:** the AMENDMENT-2 V-flat cell failing to calibrate under the symmetric variant
(registered form: the asymmetric cell is verdict-bearing; the symmetric run exploratory), OR a
derivation showing the catalogue leg's S̄ factor cancels exactly against the in-catalogue
normalization under the production convention (this would collapse [C-SYM] to the already-ruled
[P3] presentation question).

## Relation to standing record (exoneration/deferral check)

- NOT a re-open of #66/#67: those are venue-scoped bias-mechanism verdicts on `p_det(d_L)` in a
  mass-free harness (four-axis reconciliation, row #120 item 3, ratified).
- Overlaps the **[P3] catalogue-leg fork** (rows #110/#117/#119: deferred to the Gray-convention
  paper task, M-4 attached): [C-SYM] is [P3]'s estimator-side counterpart. Any production change
  would route `/physics-change` and must reconcile with the [P3] paper presentation first.
- The Gray-convention finding (denominator-only exact only under data-deterministic detection)
  is this claim's parent: [C-SYM] extends it from "keep p_det inside" to "keep it inside BOTH
  legs".

## What opening the front would take (not authorized)

Stage-1 sizing off the prodcal record; a `symmetric` selection_cell instrument knob (+tests);
stage-2 prereg with the V-flat and V-deep venues; production counterfactual only if the harness
result warrants. Expected production materiality: LOW (flat-S̄ regime) — the claim's value is
correctness of form and the paper's story, not a bias fix.
