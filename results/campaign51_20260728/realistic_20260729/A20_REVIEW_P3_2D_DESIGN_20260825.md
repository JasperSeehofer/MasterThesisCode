# A20-STYLE PRE-EXECUTION DESIGN REVIEW — [P3-2D] registration (banked verbatim, 2026-08-25)

**Reviewer:** clean-context adversarial agent (inherit-model, xhigh). **Verdict: REGISTRATION-READY conditional on folding F2, F4, F7–F16 verbatim** (+ the F3 line-cite fix). Every stage-0 [AGENT] number independently re-derived (F17). **Disposition:** PA-2D-1 adopts all named findings; the three delegated open items (ρ₂ → C₂\*, M2-LINK form, B2-R/D_C₂ formulas) are supplied in registered form below.

All probes and re-derivations complete. Final review below.

---

# ADVERSARIAL PRE-EXECUTION DESIGN REVIEW — [P3-2D] (2026-08-25)

**Subject:** `results/campaign51_20260728/realistic_20260729/PREREGISTRATION_P3_2D_20260825.md` · **Method:** independent re-derivation (own code, not the probe's logic) on the 24 banked artifacts + code reading at all cited sites + written-out derivations. Zero `evaluate()` calls.

## Item 1 — the mz_sel object

**F1. CONFIRMED — the stage-0 derivation of mz_sel is right.** Written out: in the coded numerator (`:6245-6335` scalar, `:6859-6910` batch) the mass factor is `mz(z) = ∫dx N(x; μ_cond(z), σ_cond)·p_gal(x;z)dx` in x = M_z/M_z,det (analytic product `:6322-6329` = N(μ_cond; μ_gal, √(σ²_cond+σ²_gal)) ✓). Under the banked latent-thresholded premise, the accepted density per candidate is ∝ ∫dz∫dM ρ_g·p_gal(M|g)·S_4D(d_L(z), M(1+z))·p(d|z,M); since M(1+z) = x·M_z,det, S at the latent is exactly `S_4D(d_L(z), x·M_z,det)` inside the single ∫dx weighted by the candidate's own p_gal — the prereg §1 object. Sharp limit: mz_sel = mz(z)·E_{x~N(μ_post,σ_post)}[S_4D], μ_post→μ_cond, σ_post→σ_cond for σ_cond≪σ_gal ✓ (production σ_cond p50 = 8.8e-8, `:2283-2285`). NOT point-S, NOT S̄_φ (p_gal ≠ φ), NOT a z-constant — the claim §1.3 exclusions hold.

**F2. AMEND (the centering ruling the prereg delegates to this review) — exact text:**
> "Centering RULED: the twin inserts S_4D inside the branch-resolved mass kernel the coded path already evaluates, byte-shared — gaussian branch: μ_gal_frac from `_host_M_eff` (`:6223-6231`; = `eddington_shifted_host_mass` under the production configuration volume_deconv ∧ eddington_m="on" ∧ ¬mass_trunc, raw host_M under "off"); mass_trunc branch: the full truncated-lognormal×R_eff kernel at raw host_M with S_4D per existing GH node. Rationale: the mixture's own latent host-mass law is the rate-weighted prior N(M;M_g,σ_M)R_eff/Z_M = the shifted Gaussian (G2d, `:6204-6215`) — the latent model wants `_host_M_eff`, and kernel identity is what makes W̃₂ ≤ 1 eventwise (S∈[0,1] inside an otherwise identical positive integrand), which D_C₂'s sign and the D_C-collapse proof consume. The gaussian-branch quadrature is over the product posterior N(μ_post, σ_post), σ²_post = σ²_condσ²_gal/(σ²_cond+σ²_gal), with analytic prefactor mz(z), 2-node GH escalating under the G1-style S-variation guard; the MINOR-6 non-positive-mass guard (S:=0 at M_z ≤ 0, `:2322-2335` pattern) is carried. The VENUE's latent-mass draw binds to the SAME resolved kernel (shifted Gaussian in the production configuration, untruncated; non-positive-mass latents auto-rejected by defining S(M≤0)=0 in the thinning) — a raw-centered venue against a shifted estimator would manufacture a false miscalibration component."

## Item 2 — the identity, C₂*, ρ₂, F-0

**F3. CONFIRMED — w₂'s numerator weight is α_G_φ, with a line-cite correction.** The Path-A with-BH combination is `combined_with_bh = (α_G_φ·L_cat_with_bh + B_num_wbh_phi)/D̃_φ` at **`:5481-5483`** (NOT :5402-5404 — those lines are the generator_marginal comment block; fold the correct cite). B_num_wbh_phi = B_num_wbh under "derived" — verified at the data level: my independent W-closure recompute of (α·L_cat_wbh + B_num_wbh)/D̃_φ against the CSV `combined_with_bh` column gives **max rel dev 4.2e-9 over all 24 artifacts**. The Σ^4D cancellation: A₂ = α·(Σ_ball w_gN_g)/Σ^4D with α = Σ^4D·β_G_φ/Σ^φ (`:2451-2453`, fed `global_denom_with_bh` at `:5458` — the SAME object as the L_cat divisor `:4903/:4987`) ⟹ A₂ = β_G_φ·(Σ_ball w_gN_g)/Σ^φ **exactly, algebraically** — my independent measurement of the CSV α column vs β·Σ^4D/Σ^φ: **max rel dev 6.942e-8** (= the claimed ≤6.9e-8; this is 7-sig-fig CSV storage, not a physics residual).

**F4. AMEND — the ρ₂ convention, FIXED (the prereg's open item): NEITHER banked reading is exact.** Derivation: A₂^twin = β_G_φ·J·Σ̃^4D·p̄_gen,2/Σ^φ, so for bounded φ: C₂*·E_G[φ·R₂] = E_Ḡ[φ] with

**C₂\* = β_G_φ·Σ̃^4D / (Σ^φ·β̄_Ḡ_φ)** — Σ^4D cancels completely; the ρ₂/r_Malm factorization is presentational only.

Exact replacement text:
> "ρ₂ RESOLVED: C₂\* ≡ β_G_φ·Σ̃^4D/(Σ^φ·β̄_Ḡ_φ), with β_G_φ, Σ^φ, β̄_Ḡ_φ the banked seed-invariant JSON scalars (verified identical across all 24 artifacts) and Σ̃^4D = Σ_g w_g S̃_4D,g the ONE new number: the venue draw-law contraction at h=0.73 (the same resolved p_gal kernel, windows, and S_4D table the harness draws from), computed by the registered zero-`evaluate()` companion pass BEFORE the σ freeze; C₂\* is frozen at companion-pass landing. Neither ρ₂=ρ nor ρ₂=1 is exact (both are venue-vs-table ratios of distinct contractions; 1D ρ=0.98777≠1 proves the class); the two banked LHS₂ readings (0.025475/0.025790) are DEMOTED to bracket references. §6's ρ₂-sensitivity falsifier is superseded by: the companion Σ̃^4D verified by an independent recompute at review, and C₂\* frozen pre-verdict."

**F5. CONFIRMED — F-0 in 2D needs no factorization at all.** `use_detection` (`:5540-5554`) reads ONLY d_L_uncertainty/d_L (mass-blind; the sky term is computed but unused — note it). The identity is exact for the member φ(d) = w₂(d)·1_acc(d) for ANY 1_acc that is a measurable function of the drawn data+context: C₂*·E_G[(1−w₂)1_acc] = E_Ḡ[w₂·1_acc] under the PA-CA-1 drawn-count normalization, mass-blind or not. The joint (d̂,M̂_z) draw leaves d̂'s per-(g,z) marginal unchanged (Gaussian marginal), so §5(v)'s disclosed worry is discharged by derivation — fold that discharge into §5.

**F6. CONFIRMED (with stated premise) — J and the completion mass.** J = d̂·M̂_z is exact pointwise (exponent invariant under D⁻¹-rescale, det D from normalization, mean↔argument by Gaussian symmetry; the code builds Σ_frac = D⁻¹Σ_absD⁻¹ per `:6059`), cancels in R₂ since g_sel is a density in the same x_M measure (gate (i), `:5212-5219`). ∫B̃₂dd = β̄_Ḡ_φ rides on the tower identity S̄_φ = ∫φS_4D dM, true by construction on the production table (`:1961-1970`, r_φ≡1) **and on the fused completion cell being ON**.

**F7. AMEND — the A22 stamp set omits the flag whose "off" voids the tower identity.** §3 registers FOUR resolved values but drops `selection_in_completion_numerator` (∈{auto,off,1d,2d,fused}, `:3427-3437`) — the claim's own "three-flag hazard" (§2.5). Text: "A22 stamps: FIVE resolved values — `catalogue_global_selection="phi"`, `catalogue_numerator_survival` (1D twin production default), `selection_in_completion_numerator="fused"`, the NEW with-BH flag, and the acceptance-model version; never 'auto'."

## Item 3 — B2-R and D_C₂/κ̂₂ (the RE-DERIVE deliverables)

**F8. AMEND — registered D_C₂/κ̂₂ formulas (derivation written out).** For ψ = (1−w₂,BC)·1_acc/R₂^BT = 1_acc·A₂^BT/(A₂^BC+B₂) = W̃₂·w₂,BC·1_acc ∈ [0,1] (bounded because W̃₂ = L_cat^wbh,twin/L_cat^wbh,coded ≤ 1 under F2's kernel identity):
> "LHS₂(B-C) = E_Ḡ[W̃₂·w₂,BC·1_acc] exactly; REGISTERED collapsed criterion |LHS₂(B-C) − E_Ḡ[W̃₂·w₂,BC·1_acc]| ≤ band; D_C₂ = E_Ḡ[(W̃₂−1)·w₂,BC·1_acc] (< 0 by W̃₂ ≤ 1); κ̂₂ = E_Ḡ[W̃₂·w₂,BC·1_acc]/E_Ḡ[w₂,BT·1_acc], frozen pre-verdict; TWIN2-MISCALIBRATED ⇔ |T_w₂(B-T)| > band AND |(T_w₂(B-C)−D_C₂) − κ̂₂·T_w₂(B-T)| ≤ band; VENUE-MISSPEC ⇔ the B-C deviation inconsistent with that coherence relation. W̃₂ model-side = the same synthetic draw scored under both arrangements; α cancels in W̃₂."

**F9. AMEND — GATE B2-R exact form.** Rescale A₂→r₂A₂ ⟹ 1−w₂′ = (1−w₂)/(1+(r₂−1)w₂):
> "Banked side (deterministic, on the FRESH B2-T frames when they land): LHS₂,BR = (C₂\*/200)·Σ_acc(1−w₂,e)/(1+(r₂−1)w₂,e). Model side: RHS₂,BR = E_Ḡ[w₂/(1+(r₂−1)w₂)·1_acc], same synthetic set. C₂\* un-rescaled both sides (PA-CA-4 convention). REGISTERED r₂ = 1/r_Malm(0.73) = Σ^φ/Σ^4D = 2.6124925 — pre-derived, banked, and 2D-meaningful: it rescales A₂ from the α-pairing to the β-pairing, i.e. it directly guards the most likely 2D scorer defect (α↔β mix-up); it moves the banked-frame core −13.6% (0.390399 → 0.337246 ± 0.008766, review-computed venue-drift reference; the 1D r=1.5155 alternative moves it only −5.9%). PASS iff |LHS₂,BR − RHS₂,BR| ≤ max(3.35·σ_comb,BR, ε₂), σ numbers frozen at the σ freeze. Jensen-gap warning (scalar transforms of RHS(twin) are NOT the control) carries."

## Item 4 — venue mass-law and GATE M2-LINK

**F10. CONFIRMED with AMEND — the generator spec is the one the identity needs, plus three bindings.** 'M ~ p_gal; joint 2×2 (d̂,M̂_z); Bernoulli(S_4D)' realizes latent (z,M) ~ k_g·p_gal·S_4D/S̃_4D,g. Text to fold: "(a) WHOLE-EVENT rejection (on thin-reject, restart from the host draw) — this realizes hosts ∝ w_g·S̃_4D,g automatically with NO per-host contraction in the draw path (S̃_4D,g is needed only in the Σ̃^4D companion); (b) the venue p_gal = the estimator's resolved kernel (F2), untruncated, S(M≤0)=0; (c) FREE cross-check, derived: the completion-class z-marginal is UNCHANGED from the 1D scorer's law — ∫φS_4D dM = S̄_φ(z) by the tower identity — so the extended RHS₂ scorer's accepted-z histogram must match the 1D scorer's; register as a zero-cost consistency gate."

**F11. AMEND — GATE M2-LINK given an executable form** (the prereg's "zero events with |ln L_cat_with_bh| implying survival evaluated at an unlinked mass" is not a decidable predicate and mis-describes the monsters — they are a datum-mass mismatch in mz, not a survival evaluation; the coded numerator contains no survival). Registered form:
> "M2-LINK PASS iff (i) the harness writes the latent provenance triple (g\*, z_true, M_true) per event, and the RHS-F₂ replay confirms the estimator consumed exactly the drawn (d̂, M̂_z) (bit-level); (ii) the standardized latent-vs-datum residual vector ((d̂−d_L(z_true), M̂_z−M_true(1+z_true)) under the donor absolute 2×2) satisfies max Mahalanobis² ≤ the χ²₂ quantile at 1−10⁻³/N_events over the fleet; (iii) monster-class structurally absent: zero accepted events with ln L_cat_with_bh − ln L_cat_no_bh < −50 (the banked monsters sit at −37 to −224 decades while linked draws need a >10σ mass residual to reach −50 nats)."

**F12. AMEND + claim-error flag — GATE ACC's reference.** The claim §2.3's "the z-law is unchanged" is **REFUTED for class G**: hosts reweight S̃_φ,g → S̃_4D,g and the per-host z tilt changes S̄_φ(z) → S̄_{p_gal,g}(z) (it is unchanged only for class Ḡ, by F10(c)). Fold: "GATE ACC replays the EXTENDED class-G law (hosts ∝ w_g·S̃_4D,g via whole-event rejection; exact per-draw F-0); the binomial bands use p̄_s from that replay — the 1D fleet rate 0.5821 is NOT a reference and n_kept may legitimately move."

**F13. AMEND — the S₂ = 0.390 "venue-drift control" has no predicted value; demote to diagnostic.** By F12 the extended venue changes the class-G (g,z) law by construction, so the fresh coded arm's S₂ is EXPECTED to move off 0.390399 ± 0.010344; likewise the riding no-BH 1D LHS has a different (underived) target on the extended venue — the 1D identity with C\* does not transfer (the 1D twin factor S̄_φ is not the extended generator's z-tilt). Fold: "both are REPORT-ONLY drift diagnostics localizing the mass-law's effect; no PASS/FAIL, no comparison to 0.04233/0.03741 as targets."

## Item 5 — ε₂, costing, gate executability, F-0/PA-CA-10 lessons

**F14. AMEND — the band is statistics-limited and the coded-arm discrimination is marginal; register a power gate.** Review-computed: ε₂ = 0.005·r_Malm = 0.001914 **never binds at 12 seeds** (3.35·SEM_LHS₂ alone = 0.00226 > ε₂); band = 3.35σ_comb = 0.00260 (SE target met) / 0.00311 (fallback). A 1D-like 13.1% relative coded displacement is |D_C₂| ≈ 0.0033 on the T-scale — **1.1–1.3× the band** (1D was 4×). Fold: "POWER GATE: at the σ freeze, if |D_C₂| ≤ 2·band, the B-C discrimination clause is declared UNDERPOWERED — no TWIN2 verdict is banked on it; the run returns to the author with the frozen numbers. Registration decision adopted: 24 seeds (+~3 CPU-h, SEM_LHS → ~4.8e-4) — the cheapest power doubling available; ε₂ retained as stated (its operational role is the SE targets, and it binds only beyond ~48 seeds)."

**F15. AMEND — SE-cap precedence.** Target ε₂/5 = 3.83e-4 needs N ≈ (sd/SE)² ≈ 33–98k with-BH-scored draws (1D realized sd = 0.121 at 45k) at ~2× per-draw cost — likely above the 40 CPU-h cap; the fallback ε₂/3 = 6.38e-4 (N≈24k) is the realistic landing. Fold: "if the capped N cannot reach SE ≤ ε₂/3, A21 STOP and return to the author with a fresh costing line — the band is never widened silently."

**F16. CONFIRMED + two §5 additions.** PA-CA-10's h_bounds pin is carried (scorer already pins (0.50, 0.86), `ca_rhs_scorer.py:339`); the PA-CA-11 refuse guard is implemented (`:1010-1016`) and the fold must state it extends to every NEW instrument (fleet harness, companion pass, 2D scorer). Add to §5: "(vi) sky-block independent draw (donor sky×(d_L,M_z) cross-correlations not realized): second-order; 1D bound sd 0.023; the 2D paired sharp-pin residual is reported from the fresh fleet CSVs at the σ freeze" (the claim §2.5 required this numeric statement; the prereg dropped it); "(vii) the L6-DER3 V2 measure-prefactor convention is COMMON-MODE by construction here (the venue draws the coded kernel; constant-Jacobian transport M↔x at fixed z) and cancels in T_w₂; it remains OPEN for the production physics-change proposal."

**F17. CONFIRMED — every decisive stage-0 [AGENT] number re-derived independently.** S₂ vector and fleet 0.390399 ± 0.010344 (12/12 digits match); LHS₂ pair 0.025475 ± 0.000675 / 0.025790 ± 0.000683; C₂\* candidates 0.0652525/0.0660604 from raw JSON scalars (C\* chain re-derived: β_G_φ·ρ/β̄_Ḡ_φ = 0.1704717); bit-identity max rel dev 0.0 on both 2D columns, all 1397 paired rows, 12/12 seeds; dead rows 85 = 6.1%, all A₂=0 with B₂>0 (zero A₂+B₂=0 rows — register the convention "A₂=0 ⟹ w₂=0 ⟹ summand 1" as covering both); k̂ pooled −2.224/+4.763 reproduced AND corroborated by an independent Hill estimator (ξ ≈ 4.3 top-20%, α ≈ 0.23 < 1 — no finite mean; the top single draw carries 100.0000% of the pooled R₂ sum; per-seed φ≡1 sums 791 → 1.09e95 ✓); the four monsters reproduced (6.22e39/2.17e97/1.77e16/8.98e16 with healthy no-BH legs 5.2e-4–3.9e-2); selection scalars verified seed-invariant across all 24 JSONs; the 1D validation chain reproduced (0.042330/0.037411/+0.004919). The unbounded-analog REFUSAL and the fresh-fleet necessity are decisively supported.

---

## VERDICT: **REGISTRATION-READY — conditional on folding amendments F2, F4, F7, F8, F9, F10, F11, F12, F13, F14, F15, F16 verbatim before commit** (plus the F3 line-cite correction :5402-5404 → :5481-5483). No finding breaks the identity's structure; the three open items the prereg delegated to this review (ρ₂, M2-LINK form, B2-R/D_C₂ derivations) are supplied above in registered form. If any of F4/F7/F11/F12/F13/F14 is not folded, the registration is BLOCKED: F4 (an unfrozen constant inside the verdict statistic), F7 (a stamp gap that can silently void the tower identity), F11/F12 (non-executable/mis-referenced gates — the amendment-13/17 class), F13 (a pseudo-control inviting a false gate), F14 (a reachable-verdict gap at plausible displacement sizes).

Files: subject `results/campaign51_20260728/realistic_20260729/PREREGISTRATION_P3_2D_20260825.md`; re-derivation script `/tmp/claude-1000/-home-jasper-Repositories-darksiren-emri/f76e9d1f-e875-48cc-888f-70b6e70d2905/scratchpad/rederive_p3_2d.py`; probes run against `results/campaign51_20260728/realistic_20260729/p3_b0_work/` and `p3_2d_probe.py`.