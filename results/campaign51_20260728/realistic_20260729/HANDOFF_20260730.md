# Handoff — 2026-07-30 (session paused mid-investigation)

Continues `../RUNBOOK_NEXT_SESSION_5.md`, whose §1–§2 items are now DONE.
Nothing in this directory is committed yet (see §6).

## 1. Campaign #53 is complete

All 11 posteriors landed; zero failed tasks across 14 job groups (41/41 h-points
each). The cluster queue is empty.

- 10 realistic runs scored against pre-registered P1–P6 → `REALISTIC_READOUT.md`,
  `score_realistic.py`, `realistic_scores.csv`.
- **σ→0 gate (P5) PASSES in BOTH channels** — byte-identical to the idealized
  baselines (1D md5 `1e81ba22`, 2D md5 `733c8d32`).
- **0.67 closure**: MAP 0.670 on 1343 events (0 excluded). Parabola fit on the
  three central bins → σ_h = 4.42e-4, peak 0.670053, **+0.12σ** from truth
  (baselines: −0.24σ, −0.36σ). Caveat: fitted bins sit 11.3σ out, so this is
  indicative, not a substitute for the `H_VALUES_OVERRIDE` zoom. Its GPU array
  ended in 100/100 TIMEOUT, yielding 1345 vs the baselines' 1590 detections; the
  deficit cannot be split between wall-clock truncation and genuine h=0.67
  detection loss now that the per-task CSVs are merged.

## 2. The 2D bias — state of the investigation

**Observation:** 2D MAP 0.780–0.820 (mean +0.077, 10/10 high, pull +4.04) while
1D is 0.700–0.740 (−0.0095). σ comparable in both channels; no railing.

**Exact structure (verified to 2.8e-16 on 65,108 rows):**
`p_i(h) = w_G(h)·L_cat,i(h) + (1−w_G(h))·L_comp,i(h)`, `w_G = β_G/D(h)`.
`w_G` and `L_comp` are channel-common and bit-identical; only `L_cat` differs.

**Nats budget, h = 0.73 → 0.81** (N=1588; 1 nat/unit-h ≈ 4.5e-4 in h):

| term | value |
|---|---|
| empty-catalogue posterior | +28.45 (of which `N·Δln(1−w_G)` = +31.55) |
| catalogue counter-pull 1D | −38.4 → net −9.3 → peak 0.740 |
| catalogue counter-pull 2D | −18.9 → net +9.5 → peak 0.810 |
| by class: IN-CAT (76 ev) | 1D −18.64 / 2D −15.67 |
| by class: DARK (1512 ev) | 1D −19.10 / **2D −3.27** |

⇒ 83–100% of the deficit is dark events.

**⚠ The 1D channel is NOT a safe headline.** Its unbiasedness is a near-cancellation
of ±130-nat opposing terms (in-cat +138 vs dark −117 nats/unit-h, net +21), and a
separately-measured **+0.017 systematic** sits inside that cancellation (§3).

### Measurements worth not repeating (r1, h=0.73)

- 64.7% of dark events have `L_cat_with_bh == 0` exactly (vs 32.5% in 1D).
- 488 of the 1095 events with a nonzero 1D catalogue term have an identically-zero
  2D term at every h — 487 of them dark.
- Survivors are suppressed: median `L_cat_2D/L_cat_1D` = 7.8e-3.
- Σ ln(L_cat_2D/L_cat_1D), h=0.73 → 0.81:
  **DARK (534 ev) −3165.7 → −3670.5, Δ = −504.8 nats** (strongly h-dependent);
  **IN-CAT (73 ev) −99.4 → −99.1, Δ = +0.27 nats** (h-INDEPENDENT).

## 3. Exonerated — do NOT re-open

- catalogue Jacobian; Fisher frame; p_det estimator choice; p_det inside/outside;
  h-prior sensitivity; `volume_trunc`; the z leg (channel-common); the ln-M draw
  (mean |Δln M| ≤ 0.0009 dex); realization plumbing (σ=0 byte-identical).
- candidate-window **membership** — exact removal of realization-added 2D
  candidates moves MAP 0.81 → 0.82, wrong sign.
- mass-kernel **family** — bounded at +0.002. (`host_mass_kernel="auto"` does
  resolve to `gaussian`, not `trunc_lognormal`, under `absolute_marginal`; the
  scatter guard covers only the z dimension. Real, but perturbative.)
- **Option-A calibration drift** `β_G/Σ_glob` (−25.6%/−26.8%) = the exact h⁻³
  volume Jacobian, (0.73/0.81)³−1 = −26.80%. NOT a defect. Residual after removal
  is **1D-only, +1.667%, worth +0.017 in h** (1D 0.73206 → 0.71528); the 2D leg
  passes at |δ| < 0.4% over ±18% in h — which *removes catalogue-selection
  calibration from the 2D suspect list*.
- **HA — completion term not mass-marginalised.** The dimensional mismatch is
  CONFIRMED (1D: both legs 3D; 2D: 4D catalogue leg vs 3D completion leg), but the
  full code-consistent correction moves r1 0.8133 → 0.8492 and r2 0.7820 → 0.8527,
  mean **+0.053 where −0.077 was required**. Real defect, wrong sign, does not own
  the bias. Worth fixing on correctness grounds, but it will make the MAP worse.
- **HC — mixture-floor / zero-handling.** REFUTED structurally: `physics-floor`
  never fires (65,108/65,108 cells nonzero, 0 excluded events in all 16 combined
  posteriors). The `n_events_empty = 2` asymmetry is a miscount of two non-integer
  JSON keys. Exactly 0.000 in h.

## 4. ~~NEXT: the one unrefuted candidate~~ — SUPERSEDED 2026-07-30

> **HB was subsequently REFUTED** (self-refuted by its own investigator): the
> truncation's h-tilt is −0.317 nats over 0.73→0.81 = **0.063%** of the 504.8-nat
> target, ~50× too small at its ceiling, and **sign-inverted** (removing the window
> moves the MAP *up* by ~+0.0015). Two framing assumptions in this section were also
> wrong: the "why is 1D spared?" screen has no discriminating power for mass
> hypotheses (1D never sees the mass window — `handler.py:592` vs `:605`), and the
> "#51 is a non-control" suggestion is false (153,473 impostors pass the window in
> the unscattered case, making #51 a *stronger* exposure).
>
> **Current state of the investigation → `CLAIM_2D_BIAS_20260730.md`.**
> **Next-session plan → `../RUNBOOK_NEXT_SESSION_6.md`.**
> The rest of this section is retained as the historical record of the HB rationale.

**HB — the hard mass window as support truncation** (the standing "(d2)" thread in
its mass form). Its investigation agent was still running when the session paused;
no verdict exists.

The case for it: the window (`handler.py:594-603`, `:519`) is a HARD cut where the
mass information should enter as a soft kernel weight. The GW mass is near-exact
(median σ_Mz/M_z ≈ 1e-4) while the catalogue mass error is huge (median
σ_lnM = 1.28 ≈ 0.56 dex — the propagated stellar-mass term, NOT the 0.24 dex
intrinsic), so the upper leg is vacuous (M − σ_M < 0) and only a hard LOWER floor
bites: pooled 193 low-side rejections vs 1 high-side. The window bounds depend on
z_min/z_max, which depend on h ⇒ **the truncation is h-dependent by construction**,
a candidate source of the −504.8-nat dark tilt.

Two questions any resumed attack must answer (these have killed every prior
candidate): why is 1D spared, and why did campaign #51 not catch it? On the second,
note the likely answer: at σ_scale=0 essentially no impostor passes the mass window
either, so #51's dark-event 2D catalogue term is ~zero as well — making #51 a
**non-control**, not counter-evidence.

Decisive test proposed (not yet run): A/B on the *candidate-search* mass window for
one realization — widen `sigma_multiplier`, or drop `mass_filter_mask` from the
search while keeping the mass kernel in the likelihood — and read the 2D MAP shift.
That converts "47% of 2D true hosts are lost, one-sidedly" into a signed, sized
contribution.

**Resume note:** the workflow script is at
`~/.claude/projects/-home-jasper-Repositories-MasterThesisCode/9e5f1e9d-3971-4ed8-a067-4aa8532b0fa3/workflows/scripts/dark-completion-attack-wf_f5e50977-072.js`
and the four completed agent results are in that run's `journal.jsonl`. **Workflow
cache-resume is same-session only** — a new session must relaunch, so edit the
script to drop HA and HC (both settled above) and run HB + synthesis only.

## 5. P6 — scored, no re-run needed

Measured retrospectively and validated EXACTLY at σ=0 (same 10 event indices, not
just the same rate).

| seed | in-cat hosts | σ=0 baseline | scattered 1D | scattered 2D |
|---|---|---|---|---|
| 61000 | 76 | 13.2% | 25.0% | 42.6% |
| 62000 | 88 | 22.7% | 35.7% | 50.9% |
| pooled | — | — | **30.3%** | **46.8%** |

P6's photometric leg (10–30%) confirmed for 1D, exceeded for 2D; the ~0%
falsification clause is not triggered. **The spectroscopic leg is unscorable** —
all 164 in-catalogue hosts are photometric, zero spec, which falsifies the
derivation's ~3.4 spec-hosts/seed expectation and undercuts the premise behind the
[RATIFY-R7] deferral.

Two latent defects found:
1. `host_galaxy_index` is a positional label of the *pruned* frame, and the prune
   runs on observed columns (20,834,171 parent rows vs 19,874,547 realized; ~958k
   galaxies, −4.6%, vanish from the searchable catalogue under scatter). The index
   spaces differ ⇒ **a naively-written counter reports garbage.**
2. `docs/derivations/realistic_host_observation_model.md:645` claims "host-miss
   rate logged (P6)". **False in the code** — nothing in `bayesian_inference/`
   ever reads `host_galaxy_index`.

An instrumentation patch exists (NOT applied) at
`/tmp/.../scratchpad/p6/P6_instrumentation.diff` — verbatim predicate extraction so
search and counter cannot diverge, plus a byte-identity regression gate. **/tmp is
volatile; regenerate or relocate it before relying on it.**

## 6. Housekeeping / provenance

- **Nothing here is committed.** 60 MB of pulled posteriors + the scoring script,
  readout, and this handoff.
- **The local `reduced_galaxy_catalogue.csv` is NOT the realization parent**
  (local sha256 `623527929d…` vs sidecar `parent_csv_sha256 7af3f4f4a2…`). They
  differ in exactly one column, `z_error`: the cluster copy carries the #40b
  counted-once PV width. Use the cluster parent for width-sensitive work.
- **Posterior-directory trap:** seed 61000's canonical dir is `posteriors_fixed`
  (plain `posteriors/` is the stale PRE-`ec09ed0` backup), while seed 62000's
  canonical dir IS `posteriors/`. Reading "posteriors/" uniformly mixes eras.
- **Precision hygiene:** never use the 4-dp `w_G` log line
  (`bayesian_statistics.py:2335`) for residual-level work — its 4.8e-4 noise floor
  is comparable to the entire 2D residual structure. Use `D(h)` (`:1145`, 7 s.f.)
  minus `β_Ḡ(h)` (`:1297`) from the per-h logs.
- `w_G(h)` and `β_Ḡ(h)` are bit-identical between idealized and realized runs
  (pure quadrature, no catalogue input). The `ideal_61000.csv` diagnostics extract
  carries a WRONG `w_G` (0.0686 vs 0.1625 at h=0.6) — any ideal↔real comparison
  through it is void. The realized extract matches the logs to 4.5e-5.
- Workspace expires **2026-09-23**.

## 7. Author decisions still open

- **[RATIFY-R7] extra GPU truth seeds.** Measured: realization-level scatter
  sd ≈ 0.006, truth-seed difference 0.023, per-run σ_h ≈ 0.020. More truth seeds
  buy a stable headline; more realizations per seed do not. (The paired seed
  difference is ~1σ of a single run's width — consistent with noise, NOT evidence
  of a per-seed bias.)
- `REALISTIC_READOUT.md` §6 still says "the 1D channel is the defensible one" —
  **that recommendation does not survive §2 and needs rewriting.**
- Whether to fix the confirmed HA dimensional mismatch on correctness grounds,
  knowing it worsens the MAP.
- Whether to fill the 0.67 closure row into `IDEALIZED_BASELINE_READOUT.md` from
  the parabola estimate, or run the zoom first.
