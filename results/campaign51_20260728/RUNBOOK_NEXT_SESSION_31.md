# Runbook — next session (written 2026-08-24, supersedes RUNBOOK_NEXT_SESSION_30; autonomous orchestration session, all delegated rulings tagged [ORCH-*] in rows #174–#177 for the author's trace)

**Read first:** ledger rows **#174 → #177**; `PREREGISTRATION_B0_IDENTITY_20260823.md`
(PA-1…PA-16 + A21-B0-A/B/C + VERDICT); the three banked reviews
(`A20_REVIEW_B0_DESIGN_20260823.md`, `A20_REVIEW_B0_IMPL_20260823.md`,
`A20_REVIEW_B0_VERDICT_20260824.md`); `PROPOSAL_SIGMA_PHI_DIVISOR_20260822.md` (verification
results appended — plan COMPLETE); session board artifact
https://claude.ai/code/artifact/ed640faf-33d7-42da-bcf2-4e2c09e59347.

## 0. Where the campaign stands (one paragraph, typed)

The b0 identity test — the correctness centerpiece — ran end-to-end (three pre-execution
adversarial rounds, two premise refutations caught before compute; 12/12 seed-pairs, all gates
PASS) and adjudicated **UNDISCRIMINATING**: the registered mean-of-odds statistic is
heavy-tailed by construction in this venue (k̂ up to 2.7; one legitimate w≈2e-5 event owns the
fleet means) and the registration's own B-R control caught the resulting band vacuity. The
machinery is fully validated (scorer constructive identity to 1.4e-14; the Σ^φ slot confirmed
venue-internally as exactly 1/r_φ(h)). The twin is NEITHER confirmed NOR refuted — but the
direction is 11/11 clean seeds in the twin's favor (REPORTED-ONLY, tail-biased). Separately the
Σ^φ divisor's measure-first plan is COMPLETE (production r_φ(0.73) = 0.885984 on the canonical
pool; the code's 0.9119 stamp exposed STALE [A11]); production adoption awaits the author.
TWIN-FUSED-MATERIAL stands as ratified leverage (row #173).

## 1. OPEN AUTHOR DECISIONS

1. **[RULE] Ratify the UNDISCRIMINATING verdict as adjudicated** (row #177; A21-B0-A/B/C).
2. **[RULE] Σ^φ divisor production adoption** (§7 item 1) — measure-first now satisfied:
   production r_φ measured + counterfactual flag implemented + degree test + the E-B0(a)
   venue-internal confirmation. If granted: one-slot change + stale-comment fix + gate ledger.
3. **[DO, stage 0] The finite-moment identity statistic redesign** — the decisive open design
   problem: an estimand for catalogue-class calibration whose average a single event cannot
   own (the review's Finding 5 lists what the current data can and cannot separate; median and
   reciprocal forms already refuted PA-6(c)/review). The twin's correctness verdict waits on it.
4. **[RULE] The ⟨S̄_φ⟩ point-prediction miss (×2.34) / M_Ḡ common-mode question** — re-derive or
   bound (§7(ii) blind spot; affects all arms multiplicatively).
5. Book addendum approved (row #175): **Ch 10½ builds now** — the b0 verdict is banked
   (UNDISCRIMINATING is the honest ending §5 wanted); launch the one-page fan-out per design
   amendment 1.
6. Carried: landscape/T1; MFG-a verbatim check; comparand-CSV checksum pinning; workspace
   `emri` expires 2026-09-23.

## 2. Standing rules & ops (session-earned)

- **A21-B0-C (binding for this family):** bands |Ī| ≤ max(3·SEM, ε_I) are undefined at
  k̂_max > 1; amended UNDETERMINED trigger registered in the verdict review.
- **Ops:** watchers must be PID-file-based persistent Monitors (two pattern/exit-status watcher
  defects + a silent OOM cost 4 h — PA-15); `kernel_smeared_survival` and any full-pool pass
  must be row-chunked; drivers require repo-root CWD; cluster agents must be given explicit
  DO-NOT-RESUBMIT-cancelled-tasks instructions (this session's agent re-ran 6 cancelled tasks
  and overwrote local copies — winner map disclosed in row #177); parking incidents 6–7 logged.
- **PA-16 precedent:** mid-fleet venue switches are fine iff pair-consistent per seed and
  manifest-pinned.

## 3. Housekeeping

- Prune candidates unchanged (runbook 30 §3) + `p3_b0_work/*_work` local dirs for
  cluster-superseded seeds 900105–110 (the stale orphan siblings flagged in the retrieval
  manifest; regeneration deterministic).
- Bias-state artifact still needs the rows #166–#177 refresh.
- Chronicler: file this session (rich harness-lesson content).

## 4. Resume recipe (one line)

Rule §1 items 1–2 → the finite-moment redesign (item 3, the new centerpiece) → the twin's
correctness verdict follows from it; Ch 10½ can build in parallel.
