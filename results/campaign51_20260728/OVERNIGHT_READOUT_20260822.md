# Overnight readout — 2026-08-22 (row #160 autonomy grant)

*Comprehension-first (A7). Everything below is committed; rulings are queued, nothing banked
past its A20 review. Rows #161–#162; branch `p3/catalogue-survival-counterfactual`.*

## 1. The goal, and the honest answer

You asked for the scientific lead toward removing the remaining bias. Tonight produced one
**closed** channel, one **new measured mechanism with a bounded effect**, and a sharper map of
what remains:

- The **matched-channel violation is closed at fleet level** (O7): the fused cell removes it —
  paired shift **+0.1249 ± 0.0012 (105σ)**, residual **+0.0076 ± 0.0184** (0.41σ). Score leg
  only; the bias leg under fused is an open follow-up.
- The **catalogue-leg twin is real but sub-material**: carrying S̄_φ in the per-host catalogue
  numerator (the same pairing logic as the completion fix) moves the headline by
  **Δ̄ = +0.0155 ± 0.0037** (12/12 seeds positive, 4.2σ) — **REPORT-BOUND** under the frozen
  0.02 anchor, recovering ~14% of the −0.108.
- The **impostor drag persists at 80.6%** of its coded value under the twin cell, and the
  **rail is untouched** (11/12 seeds still railed). The remaining bias is impostor/photo-z
  venue physics, exactly as the F4/F5 closures indicated — the "true H₀ constraint" resolves,
  on current evidence, to: bias removed where the estimator owned it, plus a measured,
  attributed bound on what this venue cannot constrain.

## 2. The new mechanism, in one breath

`β_G_φ = ∫ f̄·S̄_φ·w_pop dz` — the catalogue leg's own mixture normalizer — carries the
survival factor; the per-host numerator (Gray Eq. A.10 as coded) carries none. Same broken
pairing as the off-cell completion defect, other leg (L6-DER3 §4 said so; the generator's
latent acceptance makes the S̄-carrying form generator-matched). Measured via a branch-only
counterfactual cell with a replica gate against the banked baseline.

## 3. What the K-flat kill test taught (the scientific crux for your ruling)

The constant-S̄_φ arm moved the posterior MORE (+0.039) than the real table (+0.016): the twin
effect = **level** (catalogue-leg suppression rebalancing the mixture; conditioned on the
constant's choice) **⊕ slope** (−0.024; per-event heterogeneity, OPPOSING — S̄_φ relatively
up-weights the low-z impostors). So the registered unnormalized insertion changes the mixture
balance alongside the shape — **the α-pairing sub-convention (unnormalized vs shape-only) is
the actual physics choice**, and the shape-only arm is the registered follow-up that isolates
the slope.

## 4. Tonight's own failures — all caught by the machinery, all on the record

1. **GATE R-P3 fired** (driver's h-grid silently narrowed candidate selection via the h-prior)
   → diagnosed with a canonical-producer discriminator (banked baseline certified at 1.3e-14)
   → fixed → re-run green. New A17 rule: an evaluation grid can be a selection input.
2. **Two unsatisfiable gate registrations** (scalar-path evidence; worker-log observability) —
   both amended PRE-scoring (AMENDMENTS 1, 3). New A17 rule: verify evidence channels
   observable before registering them.
3. **The A20 review caught a reference-convention mismatch in my primary** (trapezoid arm vs
   legacy banked mean_h): the first-scored **+0.0193 is withdrawn**; the number of record is
   **+0.0155 ± 0.0037** (re-referenced to the −0.1083 headline convention; band unchanged).
   New A17 rule: re-derive the baseline statistic through the arm's own scoring path.
4. **O7 MAJOR-5 (owned):** one registered run executed while my branch work mutated the tree —
   numerically uncontaminated (verified twice over), stamp false → **A22 candidate** for you.

## 5. Executed rulings

- **D2 [PHYSICS] done** (`266d7290`): `PRODUCTION_FLAGS` completion cell off→fused for future
  runs-of-record; past runs stand (row #119 bridge); banked arms regenerate pin-independently.
- **D4 done**: G7 row 16 re-graded MEASURED calibration-affecting.
- **D1 done**: O7 closed the fleet fused null by transfer (A20-amended, queued for
  ratification).

## 6. Your morning queue (tags per the binding default) — runbook 29 §1

1. [RULE] Ratify O7 + [DO?] the fused bias-leg reference (its named gap).
2. [RULE] A22 (run-provenance discipline).
3. [RULE] The [P3-IMP] disposition: does +0.0155 warrant the catalogue-leg fork, and under
   which α-pairing sub-convention (the shape-only arm is the registered next measurement).
4. [RULE] Branch fate (`p3/catalogue-survival-counterfactual`, not merged).
5. Carried: landscape/T1 (gated on 3), workspace expiry 2026-09-23.

## 7. Provenance

Main: `ea9f4a52…` (O7 verdict) · `266d7290` [PHYSICS] D2 · row #161 commit · row #162 commit ·
this readout. Branch: `cc017548` (cell) · `743e8d39` (instruments) · `6191c1ea` (LEV) ·
`b3883657` (R-P3 fix) · `4abe249c`/`8183ae33` (AMD-3/band freeze) · verdict + amendments 4–7 +
review. Compute: ~9 CPU-h / 50 granted, all local. Reviews: A20_REVIEW_O7_20260822.md,
A20_REVIEW_P3_TWIN_20260822.md (both zero FATAL, both BANK-WITH-AMENDMENTS, both re-derived).
