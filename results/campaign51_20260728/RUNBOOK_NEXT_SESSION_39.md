# Runbook 39 — after tree 2's night grant (supersedes runbook 38)

**Read first.** Row #268's night grant ("permission granted... you continue autonomously until tomorrow") ran the local half of tree 2 through three sequential [HIER] instrument defects and the B4.3 1D-rail derivation, then filed the tree-2 synthesis docket (row #276) as an INFORMATION-ONLY document — **it lapses with the morning docket, by its own text.** This session opens by presenting that docket to the author, not by launching anything. Docket of record: `tree2_20260830/TREE2_SYNTHESIS_DOCKET_20260830.md`. Author-facing artifact (same content, Tree-2 section appended): `claude.ai/code/artifact/eeb5c7c3-54a5-414c-b05e-c8dfa842bfce`.

## 0. State at hand-off (2026-08-30, night)

**The grant lapsed.** Row #255's [STANDING] grant + row #268's time-bounded extension ("until tomorrow") cover rows #256–#276; nothing below this line runs without a fresh author word. The docket asks for 7 [RULE]s, none actioned.

**Four author items pending, in order their inputs complete.** (i) **PA-HIER-33 ratification** — the Es_null_det null-offset fix (proposed, row #274) now carries T1.4's fresh-data falsifier (row #275): old unweighted null REFUTED ~34.5σ, c-weighted REFUTED ~19.5σ, Bartlett-scale null NOT excluded (2.66σ/2.17σ), Richardson secant null at truth (Z +0.470) — ripe now, nothing further to run. (ii) **The mass-aware 1D production flip** — queued; the A18 arm verdict against [0.64, 0.72] doesn't exist yet; travels with tonight's mirror result (+0.1158 ± 0.0136, above band) and a ripe sub-question (extend the H-grid past the 0.86 matched-class ceiling rail before A18 submits). (iii) **F-ii (mass-law-keyed window)**, ripe now with T5.1's per-venue laws: log window k = Φ⁻¹(1−ε/2) exact-by-construction on the scattered joint_r1 venue; linear k=1.5 needs k≈11.6 there, not ε-keyable; adopt log k=3 / keep linear / arms-first (A1 already = arms-first — confirm order). (iv) **A4** (`mz_sel`/`eff` ratification) — queued behind the still-unsubmitted tree-1 wave-3 blind readout (cluster down); nothing to rule tonight.

**Runner-9 (B8.2 S3 ladder), state at write time (22:27):** stage file `tree2_20260830/b8_s3_runner9_stage.txt` reads `LADDER`; log `b8_s3_runner9_20260830.log` shows the N=106 point of the 3-point ladder, cell S, seed-block 900300, `--workers 8`, START 22:00:51 — confirmed still RUNNING by `ps` (main process + 8 forkserver workers). Work root `b8_cal_harness_work_ladder/` holds only `draw_weight_cache/` and `seed900300_S/` (harness log + per-h `selection_tables_*.json`, no universe checkpoint yet) — read the checkpoint before N=400/1588, per the S2 record's own instruction; do not touch this work root while it runs. Read via `--score-only` (prints bands, never a verdict) once a checkpoint exists.

**Cluster outage:** bwUniCluster Lustre `/pfs/data6` OST 5 has been inactive since 2026-08-29 (runbook 38's incident); still down at docket-filing time. A recovery watcher runs in-session (background poll, 900 s interval, checks module-Python + repo-file readability over `ssh -o BatchMode=yes bwunicluster`; prints only on state change; exits "LUSTRE RECOVERED" when clear) — do not re-launch a second one. SSH auth (`ControlMaster`/`ControlPersist`, OTP-gated, 8 h) can expire independently of the Lustre incident; after any re-auth, confirm with `ssh bwunicluster true` before trusting the watcher's next "up" print.

**Commits of record tonight** (`git log --oneline 868cd231..HEAD`, oldest→newest): `ecd33336` A13 force-add the 41-file diagnostics slice · `6c6f2a63` [PHYSICS] θ-consistent no-BH divisor Σ^φ(θ) + sky_cone_k + candidate-dump hook · `093c7338` tree-2 wave-1 docs (charter, B4.3 derivation, PA-HIER-32, A14/A5, ledger rows) · `5e30de9a` T2.2 candidate-dump readout DEPTH-SKEW-CONFIRMED · `7b1bd9dc` T1.2 re-cert readout (b-axis restored, s-axis still B0-A′) · `5e1e66aa` T2.3 gate presentation, mass-aware 1D leg · `62f7d61e` [PHYSICS] mass-aware 1D catalogue leg flag (Z≡1 by identity) · `2d2d6f94` T2.3 arm (a) readout (Δmean_h +0.116±0.014, MASS-AWARE-MATERIAL) · `9682d754` T1.3-zwin gate revision note 3 · `7e1ed96f` [PHYSICS] θ-consistent z-window `theta_zwindow`+z_window_k · `6e599e5b` test: theta_zwindow handler-level filters (22) · `647e86d9` row #268 author ruling docs · `e35f9d4e` A11 settled by derivation + T5.1 window proposal/re-check · `8b11c75b` T1.3 P1 arm readout (raw null restored, unweighted FAILS, c-weighted PASSES; Es_null_det validity derivation) · `5c13b82b` PA-HIER-33 PROPOSED (row #274) · `8b0bfe59` driver: Richardson half-step nodes + score_lns_R (print-only) · `d9bb7160` harness: B8.2 draw-weight cache (452s→8.6s warm) · `ece1bd1b` harness: B8.2 S1 generator + S2 driver/scorer · `f91d8a37` T1.4 Richardson readout (PA-HIER-32(d) excluded ~34σ, Bartlett not excluded, row #275) · `2ddcb5b9` tree-2 synthesis docket filed (row #276) — HEAD.

## 1. Entry points

- Docket of record: `tree2_20260830/TREE2_SYNTHESIS_DOCKET_20260830.md` (§1 verdict table, §2 the [HIER] three-defect story, §3 the 1D-rail story, §4 four author items + secondary items, §5 cluster queue in submit order, §6 compute ledger F4, §7 next-tree candidates, §8 ten governance incidents).
- Fan-out-1 verifier (superseded scope, still authoritative for the prior tree's A1–A17/P1–P10): `fanout1_20260829/END_VERIFIER_REPORT_PART1_20260830.md`.
- Prior runbook: `RUNBOOK_NEXT_SESSION_38.md` — its cluster-incident paragraph is the same OST-5 outage, still open.
- Registrations behind tonight's items: `PREREGISTRATION_HIER_HTHETA_20260826.md` (PA-HIER-32/33), `PHYSICS_CHANGE_THETA_DIVISOR_20260830.md`, `PHYSICS_CHANGE_THETA_ZWINDOW_20260830.md`, `PHYSICS_CHANGE_MASS_AWARE_1D_LEG_20260830.md`, `PROPOSAL_MASS_LAW_KEYED_WINDOW_20260830.md`, `A11_COMPLETED_WEIGHT_FORK_DERIVATION_20260830.md`, `B4_3_MIXTURE_WEIGHT_DERIVATION_20260830.md`, `TREE2_CHARTER_20260830.md`.
- Harness records for S3: `B8_2_S1_RECORD.md`/`_VERIFIER_REPORT.md`, `B8_2_S2_RECORD.md`/`_VERIFIER_REPORT.md` (checkpointed universe loop, `--score-only` aggregator, the draw-weight cache).

## 2. The tree (state at docket filing; compressed from docket §1 + the author items)

| node | verdict + cap | next step |
|---|---|---|
| T1.1 θ-consistent divisor | BUILT+verified, byte-identical default; `[PHYSICS]` `6c6f2a63` | closed → T1.2 |
| T1.2 S0-A re-cert | b-axis CERTIFIED (Z_b −0.68); s-axis B0-A′ STOP stands | closed → T1.3-zwin |
| T1.3-zwin z-window | BUILT+verified; `[PHYSICS]` `7e1ed96f` | closed → P1 |
| T1.3 P1 arm | literal B0-A′ persists (unweighted Z −3.32); raw null restored (Z +0.31); c-weighted PASSES (Z −1.79) | → Es_null_det validity → T1.4 |
| Es_null_det validity + PA-HIER-33 | PROPOSED, not adopted; Bartlett null +0.0013±0.0008 | **[RULE] (i), ripe** |
| T1.4 Richardson falsifier | EXECUTED; old null REFUTED ~34.5σ, c-weighted REFUTED ~19.5σ, Bartlett not excluded (2.66σ/2.17σ) | feeds (i); S0-B unlaunched until ruled |
| T2.1 B4.3 derivation | mixture-weight h-slope REFUTED as mechanism; depth-skew IS mechanism; Z(h)=1.0999 defect | fed T2.2 + mass-aware remedy |
| T2.2 candidate-dump | DEPTH-SKEW-CONFIRMED, gate-clean (Φ_low 0.7299±0.0139) | closed |
| T2.3 mass-aware 1D leg | BUILT, byte-identical default; `[PHYSICS]` `62f7d61e`; Z=1 by identity | → arm (a) |
| T2.3 arm (a) | MASS-AWARE-MATERIAL, ABOVE band (+0.1158±0.0136, 4/4 seeds); dark class 0; matched rails at 0.86 ceiling | **[RULE] (ii), queued on A18** |
| A11 completed-weight fork | SETTLED BY DERIVATION: neither branch is the estimator; mooted | REPORTED-ONLY, word pending (v) |
| T5.1 mass-law window | DESIGN+REGISTRATION filed; REFUTED r2 → corrected → re-check PASS 6/6 | **[RULE] (iii), ripe** |
| B8.2 S1/S2/S2b | BUILT+VERIFIED; cache 52.6× warm | closed |
| B8.2 S3 ladder | IN FLIGHT (runner-9), N=106, stage LADDER | read before N=400/1588; S4 needs top-tier review before S5 |
| A4 (`mz_sel`/`eff`) | queued behind unsubmitted wave-3 | **[RULE] (iv), queued** |

**Author items for the morning docket** (tags per approval-scope convention): (i) [RULE] ratify PA-HIER-33 (Richardson/Bartlett-null form) + accept the P1/T1.4 re-read — ripe. (ii) [RULE, queued] mass-aware 1D production flip; ripe sub-question: extend the grid past the 0.86 ceiling before A18 submits, YES/NO. (iii) [RULE] F-ii: log k=3 / linear / arms-first (A1 = arms-first, confirm order); ratify T5.1 re-attribution; does the grant cover launching joint_r1 alongside the k-scan — ripe. (iv) [RULE, queued] A4 after wave-3 lands |Δ|≤0.008 — nothing to rule tonight. (v) bundle: A11 one-word ratification; PA-HIER-32(d)-vs-T1.2 scope-note tension (superseded if (i) ratifies); [INFO]-only items (T5.1 pointer-note scope, κ(h) logging, T6/Stage-P/S0-R holds) — ripe, secondary.

## 3. Resume recipe

(i) **If the session lives:** it continues — present the docket, take rulings on (i)/(iii)/(v) and (ii)'s sub-question, then proceed per §5. (ii) **Fresh session:** read the docket + this runbook → check runner-9/S3 output (`b8_cal_harness_work_ladder/`, `--score-only`, bands not verdict) → check the Lustre recovery-watcher state (don't start a second one; re-arm only if it exited) → work the cluster queue (docket §5) in order: **item 0, the local prerequisite, first** — T2.2b (arm (b), ~4–5 CPU-h local, the 17.1 sequencing gate blocking A18) and the B8.2 S3 ladder readout, both runnable now regardless of cluster state. Everything after item 0 (wave-3, A18, S0-B, the T5 k-scan) waits on a fresh `/cluster` preflight `VERDICT: READY` once OST 5 clears. The registered end-of-tree-2 verifier pass (register like `fanout1_20260829/REGISTRATION_END_VERIFIER_PASS_20260829.md`) runs **after** S3 finishes and the cluster arms land — not before.

## 4. Standing rules carried (do not re-learn)

Verifier output is evidence, not authority · subagents never run the registered measurement they built · never end a turn to wait on an untracked process · per-poll SSH, Monitor for watchers · every submission stamps its authorization · exoneration grep is for the MECHANISM, not the tag · banked ✓VER rows can be internally inconsistent — check a row's own counts before escalating · row #223: production changes inside the tree are covered too, every gate still goes to the end verifier · workflow tier-lint mis-parses parentheses inside prompt strings, route through `S()`/`T()` · no backticks inside workflow template strings · `pkill` patterns can self-match the caller's own command line · `--jobs>1` is dead in `hier_s0_driver.py` (`multiprocessing.Pool` workers are daemonic — a driver cannot nest `evaluate()`'s inner pool inside an outer seed-level pool); always launch `--jobs 1` · SSH `ControlPersist` is 8 h and OTP-gated.

**New gotchas from tree 2:** `np.savez` silently APPENDS `.npz` to a path that doesn't already end in `.npz` (`<key>.npz.tmp` → written as `<key>.npz.tmp.npz`, breaking the atomic `tmp_path.replace`) — choose a tmp name already ending `.npz` (`<key>.tmp.npz`) (`B8_2_S2_RECORD.md:396-400`). The B8.2 harness's draw-weight cache (`--work-root/draw_weight_cache/`, keyed by a SHA-256 hash of the pool's z/M array bytes) pays its ~455 s cold cost at most ONCE per work-root (8.6 s warm, 52.6×) — a *new* work-root (a fresh ladder point) recomputes it once; don't read the first universe's wall time as the per-universe rate. `--jobs 1`'s daemonic limit also bounds any pool nested around `b8_cal_harness.py`'s own `--workers N` pool — fine flat at N=8, check before nesting an outer seed-level pool. **The Es_null_det convention lesson:** a null offset derived for one estimator configuration does not transfer to another — the registered correction subtracted a single-isolated-host finite-step bias (+0.0463) from a statistic whose actual likelihood sums hundreds of candidates and is 22× flatter in ln s (true null +0.0013±0.0008), manufacturing a spurious ~3σ fail from a near-null raw score. Pin every null to the arm's own likelihood structure and re-derive (not reuse) it per configuration — now PA-HIER-32's own scope note as well as the PA-HIER-33 finding.

## 5. Rulings of record carried into this session (closed — read before presenting the docket)

- **Row #255:** author ruling ("all ratified from the docket") — A1–A17 granted as orchestrator-itemized (A1=(c) k-scan first, A2 PA-HIER-31 forms, A3 T1.1 as tree-2 node 1, A5–A10/A12–A16 as recorded, **A17 = new [STANDING] grant**, scope = rows #222/#223, lapses at the end-of-tree-2 verifier pass or a narrowing author message); P1–P10 stand. **NOT covered:** A4 (needs wave-3 numbers), A11 (needs one word, not "ratified").
- **Rows #256–#267:** tree 2 opened (charter, #256); A5/A14 housekeeping (#257–#258); T1.1 presented/built/verified (#259–#260); T2.1 derivation, T2.2 built+verified, T2.2 readout DEPTH-SKEW-CONFIRMED+BI closure (#261–#265); PA-HIER-32 appended (#263); T1.2 re-cert readout — b-axis CERTIFIED, s-axis STOP, plus the disclosed PA-HIER-32(d)-vs-T1.2 scope-note tension (#266); T2.3 arm (a) readout — MASS-AWARE-MATERIAL above band (#267).
- **Row #268:** author ruling ("permission granted... continue autonomously until tomorrow") — A18 = as recommended (production arm before any flip); A4 = as recommended (ratify iff |Δ|≤0.008); A11 = decide by derivation, as recommended in spirit; **[STANDING] extension, time-bounded** through the night of 2026-08-30→31, scope unchanged, ends at the morning docket + verifier pass.
- **Rows #269–#276:** A11 settled by derivation (#269); T5.1 window proposal filed, REFUTED round 2 on a factual claim, corrected, re-checked PASS 6/6 (#270–#272, plus a self-raised, orchestrator-adjudicated scope item flagged for the end-of-tree-2 verifier, not settled as precedent); T1.3-zwin P1 readout (#273); PA-HIER-33 proposed (#274); T1.4 Richardson falsifier executed (#275); tree-2 synthesis docket filed, information only (#276).
- **No ruling exists yet** on the docket's 4 primary + 3 secondary [RULE] items — this runbook's job is to put the docket and §2's tables in front of the author before §3's resume recipe executes.
