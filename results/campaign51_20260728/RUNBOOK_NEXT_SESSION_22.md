# Runbook — next session (written 2026-08-19, supersedes RUNBOOK_NEXT_SESSION_21)

**Read first:** ledger rows #127–#129, `results/prod2d_closure_20260818/CAMPAIGN_REPORT_20260819.md`,
then this file. The prod2d closure day-2 arc (T0 budget, regression, counterfactual, cancel +
landscape gating, history rewrite) is fully banked — do NOT redo any of it.

## 0. State at write time

- **Ratified (row #129):** budget **B-UNOWNED** (r = +0.074/+0.087 vs 2σ_total ≈ 0.023);
  regression **R-MIXED** (M-B direction-refuted; catalogue-supported/impostor-borne per-event
  structure); counterfactual **C-MIXED** = catalogue-leg mass-overlap ownership refuted at
  materiality (ΔV1′ = +0.0010/+0.0032; k=2 sensitivity +0.009/+0.016). Landscape/T1 fused
  cells **gated behind final residual resolution** (author [RULE], closure prereg CLUSTER
  FILL-IN 2).
- **Leading candidates for the residual:** (1) completion-leg mass factor g_i/g_frac
  geometry (M-C's home + F1/F2 analog + V-prod venue-bias flavor); (2) alpha_G_phi-path
  (catalogue vs completion normalization) asymmetry. Plus whatever the exploratory
  researcher and the step-back gate surface (incl. model-misspecification class: φ(M) or
  selection population prior vs injected population — bias with all code "correct").
- **In flight at write time (check for their reports FIRST on resume):** two top-tier
  fresh-context agents — (a) the STEP-BACK GATE (first-principles re-derivation of the 2D
  likelihood; verdict GATE-OPEN/GATE-HOLD), (b) the MEASUREMENT AUDIT (rows #119–#128
  recomputation; flags rulings to reconsider — its s_Edd sign-convention check is
  load-bearing for the budget). If a reset killed them, RELAUNCH from the prompts recorded
  in the session (or reconstruct from row #129 items 2/4 — mandate quotes are there).
- Cluster: repo at rewritten main (tag prod2d-closure-base → 26bcd9a4), no jobs running,
  workspace expires 2026-09-23 (0 extensions). Origin fully synced; pushes fast.

## 1. The authorized structure (author directive 2026-08-19, row #129 — follow in order)

1. **GATE first.** No candidate implementation until the step-back agent returns GATE-OPEN.
   If GATE-HOLD: the derivation arc it demands becomes the front; present its findings +
   proposed derivation plan to the author before anything else.
2. **Audit findings** go to the author with the gate verdict (any CRITICAL finding on
   rows #119–#128 re-opens that ruling as a fresh [RULE]).
3. **On GATE-OPEN:** launch ONE exploratory candidate researcher (fresh context; looks for
   additional residual-owner candidates beyond the two leading ones, esp. from the gate
   agent's derivational ranking). Then ONE battery prereg covering 3–4 candidates:
   per-candidate registered instruments as **default-off CLI flags on main** (the
   `--catalogue_mass_overlap` pattern — NOT branches; the one-cluster-repo rule stands),
   per-candidate bands, ONE verifier pre-check, ONE physics-gate presentation to the author.
4. **One fleet, all candidates in parallel cells** (evaluate.sbatch pattern; ~3 min/task;
   V0 probes + N-0/N-1/N-2-style gates per instrument — reuse
   `results/prod2d_closure_20260818/launch_counterfactual.sh` +
   `readout_counterfactual.py` as templates). Rank candidates for readout order; stop early
   if one owns the residual (moves Δ_v by ≥ the residual within uncertainties, toward
   truth) — then the fix fork returns to the author WITH the mechanism derivation.
5. **After the residual is owned and resolved:** un-gate the landscape/T1 round (13 fused
   cells; resubmit with --time=48:00:00 and the RUN_DIR export gotcha; blind T2 predictions
   in MECHANISM_SIGMA_M_SIGMA_Z_DERIVATION.md §5 still stand for the current-estimator
   version).

## 2. Standing constraints (unchanged unless the audit voids one)

Prereg-first + verifier pre-check for every measurement incl. free reads; physics-change
hard gate for instrument code (author presentation BEFORE writing); P7-4 venue scoping
(production-native magnitudes only in budgets); P7-8 one-realization disclosure; append-only
ledgers/preregs; tiering table + ≤3 top-tier per workflow; author WIP in book/design/* +
gen_ch00.py uncommitted — do not touch. Binding value (author verbatim, row #129): the goal
is a scientifically motivated, derivable mathematical description — never bias-removal by
any means.

## 3. Chronicle candidates (run /chronicler at a natural end)

The verifier catching the measure-incoherent V1 ablation pre-execution; the batch-kernel
silent-no-op class (N-2 engagement gate pattern); flags-not-branches for parallel
counterfactual candidates; the 18-worker contention sizing-transfer failure; the
history-rewrite protocol (map + re-tag + ledger note); the elimination-arc day (regression
localizes → counterfactual exonerates → structure-around-the-shift lesson).

## 4. Resume recipe (one line)

Check gate + audit agent reports → present both to author → on GATE-OPEN: exploratory
researcher → battery prereg (flags on main) → verifier → physics gate → one parallel fleet →
readout → fork with mechanism → then un-gate the landscape.

---

## SUPERSESSION NOTE (2026-08-19, evening — read before §1)

The gate returned **GATE-HOLD** and its exit is already executed through the first fix:
rows #130–#131 + `GATE_REVIEW_20260819.md` + `MEASUREMENT_AUDIT_20260819.md` +
`docs/derivations/bscale_completion_normalization.md` → **[PHYSICS] commit (B_scale removed,
derived form default, legacy behind --completion_b_scale legacy)**. Corrected residual of
record: +0.056/+0.069 (stale s_Edd fixed by audit). Expected post-fix production 2D:
≈ −0.05 (base tilt re-exposed — by design, correctness over bias-removal).

§1's ordering is superseded by the ratified tilt-ledger program:
1. Author the **row-#111 1D correct-form transfer proposal** (recon rows #109–#116 for the
   venue-validated terms: density-form event term, measure/Jacobian, LOO weight, α-pairing;
   map onto production; derivation session + ~15 CPU-h validation run).
2. Re-ranked battery (one prereg, flags on main, one fleet): s_Edd re-measurement
   (exact-quadrature, seed61000, 41-pt), f_k-vs-f̄ completeness consistency, D3/F10 J_α;
   g_i only after those (rank 4, per gate review).
3. A post-fix production re-evaluate (derived form, both venues, 41-h) to establish the new
   baseline posterior of record BEFORE the battery reads against it.
4. Landscape/T1 stays gated until the tilt ledger closes.
