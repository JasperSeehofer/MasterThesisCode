# Runbook — next session (2026-08-26 close, supersedes runbook 33; rows #198 → #211; everything committed AND PUSHED)

**Read first:** rows **#204 → #211** (the [P3-2D] execution arc: fleet 48/48 + companion v2 +
C₂\* frozen → gates → LHS₂/RHS₂ → **CONTROL-FAIL** (row #207) → the three-round forensic
(C₂\* exonerated blind; ×r₂ diagnostic refuted as numerology; completion-mass axis exonerated
in BOTH constructions, X_alt = 0.9997) → **PARKED at UNATTRIBUTED-bounded, row #211**).
Mission board: https://claude.ai/code/artifact/1b605b24-078a-48b9-bf92-1e573965f9c3

## 0. State at close

- **[P3-WBHZERO] CLOSED** (rows #198–#202; symmetric window = production physics, `cf4f8a2a`).
- **[P3-2D] PARKED at UNATTRIBUTED-bounded** (row #211). The fresh-eyes entry point is
  `realistic_20260729/STUCK_P3_2D_SYMPTOM_CARD_20260826.md` (independence-clean — hand a
  searcher the CARD ONLY). Exoneration record: rows #207–#210 + PA-2D-9/10 (do NOT re-open:
  C₂\*, the completion-mass axis, the replay machinery). All identity numbers frozen
  pairing-independent in PA-2D-9. **First action on thread resume: the GRANTED-but-UNRUN
  class-G S̄_φ de-double-weight fix + fleet re-run (~2–4 CPU-h)** — measured 13.5–16%,
  necessary regardless of the big residual. Remaining candidate axes (rung-3/4 of the card):
  the class-G contraction-vs-implemented-law question; the acceptance-measure/normalization
  step of the identity derivation.
- **[P3-MKER] UN-HOLDED** (row #211; opened row #206, R0 banked — R&V15 0.55/0.24 dex
  quote-verified; two reportable absences). Next: the two registered zero-compute reads
  (fleet-wide kernel-pull vs window-pull census; the convolved-σ_eff one-formula check), then
  the stage-1/2 ladder ("kernel first, window second", `CLAIM_P3_MKER_20260826.md` §3).
- **[HIER] UN-HOLDED** (rows #192/#195): the (h,θ)-grid prereg → review chain → cluster array.
- Cluster: no jobs; fleet + RHS₂ artifacts retrieved and committed; workspace `emri` expires
  2026-09-23 (extend or archive within ~4 weeks).

## 1. OPEN AUTHOR DECISIONS

None pending. Carried non-blocking: [P3-HGRID] claim card; joint_r1 attribution; MFG-a
verbatim check; F0-SEL follow-up; AMEND-2 stale log-substring gates; bias-state artifact
refresh (rows #166–#211); the WBHZERO proposal §6 caveats (h-dependence; redshift-filter
sibling convention) as candidate threads.

## 2. Standing rules & session-earned ops (delta over runbook 33)

- **Agent parking: 6 incidents across the session** despite escalating opener language; the
  ONE reliable cure is the direct resume message ("kill background, run foreground with
  explicit timeout, deliver the report"). Treat the opener as damage reduction, the resume as
  the mechanism. The cluster-submission agent's "SLURM is async BY DESIGN, submit-and-return"
  phrasing worked — reuse for cluster tasks.
- **The blind-derivation pattern paid twice**: (i) C₂\* exonerated by a deriver firewalled
  from the investigation state; (ii) my sharp-looking ×r₂ single-factor diagnostic was
  REFUTED quantitatively by the un-blind reviewer (one constant cannot fit both arms) — a
  reusable lesson: a reconciling constant close to a registered guard ratio is a HYPOTHESIS,
  not an attribution, until it predicts both statistics within errors.
- **Counterfactual constructions need their confounds stated at registration** (PA-2D-10:
  round 1's independent-host mass-scale confound made a REFUTED verdict
  operationalization-conditional; the confound-free round 2 was decisive).
- Verifier-before-launch continues to pay: the RHS₂ κ̂₂ mis-form and the seed-stride
  collision were both caught pre-cluster (PA-2D-8).
- Row #185 queue-waits stayed trivial (28 s / instant / ~1 min) — cluster-first remains free.

## 3. Resume recipe (one line)

[P3-MKER] zero-compute reads → its stage-1 information forecast + stage-2 prereg → [HIER]
(h,θ)-grid prereg (review chain → cluster) → [P3-2D] resume when fresh (S̄_φ fix first, then
the symptom card to a Stage-L searcher) → paper-facing consolidation (the WBHZERO one-day arc
+ the [P3-2D] forensic discipline are both thesis-chapter material).
