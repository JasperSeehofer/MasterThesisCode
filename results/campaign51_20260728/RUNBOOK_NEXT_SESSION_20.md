# Runbook — next session (written 2026-08-18, supersedes RUNBOOK_NEXT_SESSION_19)

**Read first:** `results/pp_coverage_csym_20260818/CAMPAIGN_REPORT_20260818.md` (the A7
readout, §6 decision table = the PENDING author bundle) + ledger row #125.

## 0. State (end of the 2026-08-18 autonomous-cycle session)

- **The [C-SYM]/[P3] grounding campaign (G-1 + G-2) is EXECUTED and read out; ratification
  is PENDING (row #125 item 3 — six decisions in the report's §6).** Executed under the
  author's autonomy directive (verbatim in row #125 and both preregs); every branch
  presented, not adjudicated.
- **Headlines (registered):** catalogue-leg fusion immaterial in production's regime
  (≤ +0.0002; V-flat bit-inert) ⇒ ~170 CPU-h production counterfactual proposed NOT
  warranted · symmetric form confirmed as the correct form (asymmetry owns the V-deep
  displacement; residual = photo-z venue physics, calibrated-in-bias at spec-z) · BOTH
  insertion legs' costs are photo-z-driven (completion collapses ~70×; catalogue retains a
  MATERIAL +0.0038 floor at σ_z = 0.002) — the paper's σ_z → 0 wording must be per-leg.
- **Products:** `cat1d`/`symmetric` selection cells in the [A3] harness (+13 tests, freeze
  `4dd822ad`); two preregs with 6-part verifier record (14 amendments, verbatim);
  arm-validity preflight pattern (caught 2 voids + resolved an N-A false alarm pre-run);
  full-R cluster-vs-local BIT-EXACTNESS of the harness (an instrument-determinism result
  worth citing); 24 cells + scorer outputs; report; ledger row #125; MFG-a literature row.
- **Anti-void lesson bank (for future preregs):** byte-identity only defined equal-R
  (child seeds drawn per truth×realization); grid headroom vs displaced-MAP tails; channel
  scoping of coverage bands; inert-null cells need inverted engagement semantics (A-B-2).

## 1. Next fronts (the record's order)

1. **Author ratification bundle** — report §6 decisions 1–6 (G-1/G-2 bundles, the [P3]
   presentation pick (a)/(b) now WITH measurements, the per-leg σ_z wording, paper-work
   unblocking). Nothing proceeds on the paper until these return.
2. **Paper integration (already granted row #121 item 4, sequenced on the picks):** method
   convention subsubsection, systematics entry, discussion comparison/caveats, conclusions
   sentence; TO-MAKE figures (FULL-B/D/F ladder table+figure, M-4 histogram, + candidate:
   the G-2 σ_z ladder); `discussion.tex:235` rewrite.
3. **Seeds (not opened):** flattened-detection venue-physics (raised-d50, VERDICT-3);
   2D-channel noise-coupling (+0.01 class); estimator-side-only S̄ instrument knob;
   σ_z = 0.002 coverage-profile distortion; catalogue-leg σ_z-independent floor component.
4. Book ch16 candidate: prodcal arc (runbook-19 item 4) — now optionally extended with this
   cycle's anti-void chain (three catches, one false alarm resolved, a bit-exactness bank).

## 2. Standing constraints

Append-only ledger/prereg discipline; A8-v2 + verifier pre-checks (worked again: Parts I–VI
caught 7 BLOCKING defects pre-run incl. two void-arm classes); top-tier agent cap; branch
calls presented not adjudicated; venue-scoping binds both ways; the autonomy grant was
SESSION-SCOPED (row #125) — do not carry it forward without a fresh author directive.
Workspace expires 2026-09-23 (0 extensions). Author WIP in book/design/* + gen_ch00.py
remains uncommitted — do not touch.

## 3. Resume recipe

1. `git log --oneline -5` — expect the row-#125 chain at HEAD.
2. Read the report §6; collect the author's six rulings (each its own tag).
3. On ratification: open the paper-integration work (front 2) per the picks; G-1/G-2 numbers
   are final in `readout_g1_output.json` / `readout_g2_output.json` + the AMENDMENT-B
   VERDICT block.
