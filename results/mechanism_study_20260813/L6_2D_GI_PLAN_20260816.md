# L6 — the 2D mass-channel (g_i) investigation: recon + registered switch protocol

**Date:** 2026-08-16 · **Authorized:** ledger row #111 item 3 · **Status: PLAN + RECON,
PRESENTED.** The derivation and switch measurements execute next (orchestrator task); this note
pins the target so no re-recon is needed.

## 1. The target (recon, verbatim refs)

The 2D factor g = `completion_mass_factor_g` (`bayesian_statistics.py:2012-2142` — PRODUCTION
code, shared by venue and production):

    g_i(z; h) = ∫ dx_M N(x_M; mu_cond(z), sigma_cond) · phi(x_M·M_z_obs/(1+z)) · M_z_obs/(1+z)
    mu_cond(z) = 1 + proj·(d_L_frac(z,h) − 1),  proj = rho·sigma_Mz/sigma_dL (2×2 block)

**Exactly two h-channels** (both inherited from the 1D branch's arrays; g adds no h-call of its
own): (A) mu_cond via d_L_frac = dist(z,h)/d_obs (`venue_transfer.py:~1602-1606`); (B) node
placement z_nodes via the h-indexed window tables (`:1522-1600`), feeding scale = M_z_obs/(1+z)
against the φ mass-function slope. Generator side: M_z_obs = M_row(1+z_true)(1+σ_Mz(ρe1+√(1−ρ²)e2))
— h-independent, ρ-correlated with the d noise; the estimator models ρ correctly (same 2×2
block) but conditions at trial z, not z_true.

Committed facts: the 2D−1D excess is ≈ +129…+136 nats/h across coded/AM2P/AJREN/A-FULL (variant-
independent!), bias +0.0076 ± 0.0012 with 1D zeroed (A-FULL), coverage not restored in 2D;
REN-neutral, displacement-law-neutral; the L4-T2 1/σ_Mz proxy was ~200× too small (its method,
not the mechanism, per Part 2's composition lesson).

## 2. The registered protocol (stage-4/5 method, c2 path)

1. **Mirror c2**: extend the bit-exact mirror to the 2D channel (c2 = ∫ integ·g); validate
   against stored per-seed `ln_post_2d` (MN0X + AFULL) bit-for-bit.
2. **Exact switches**, each an A/B on the same realizations: (S-A) freeze mu_cond's h — pass
   d_L_frac at h_true into g only (channel A killed); (S-B) freeze g's z_nodes/scale at the
   h_true window (channel B killed); (S-AB) both. The variant-independence of the +129…+136
   excess predicts the owning channel is one g inherits regardless of the 1D form.
3. **Convolution-frame derivation** (orchestrator, before the switches run): the responsibility-
   weighted d ln g/dh terms — channel A ≈ −(proj/h)·⟨∂ln g/∂mu⟩ (h-odd, first-order, survives
   every 1D repair by construction); channel B ≈ −(dz*/dh)/(1+z*)·⟨1 + d ln φ/d ln M⟩. Signs and
   magnitudes to be derived, then measured — never the reverse.
4. Same discipline: xhigh verifier before author presentation; repairs (if any) go to a
   registered arm; the production transfer is DIRECT this time (g is production code).

Cost: mirror + switches = local CPU-min on 15 seeds; no cluster time until a repair arm exists.
