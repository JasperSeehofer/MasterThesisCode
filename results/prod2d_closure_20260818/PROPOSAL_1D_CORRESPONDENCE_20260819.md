# Proposal — the production 1D correspondence measurement (re-presentation of the lapsed row-#111 item)

**Date:** 2026-08-19 · **Status:** PROPOSAL — decision items at §5; nothing runs before the
author rules. Fulfils row #130 gate-exit item (ii) / row #131 [DO] "re-present the lapsed
row-#111 1D correct-form production transfer". Recon basis: rows #104–#116 + 
`results/mechanism_study_20260813/PRODUCTION_TRANSFER_RECON_20260816.md` (fresh sonnet recon
2026-08-19; ledger row refs therein).

## 1. What was authorized, and what actually lapsed

Row #111 item 2 authorized a production `/physics-change` proposal transferring the
venue-validated correct form ("α-pairing, density-form event term, LOO weight; no Jacobian,
no renorm"; A-FULL evidence base: venue 1D bias +0.0373 → +0.0010 ± 0.0011, coverage
restored). Row #112 ("A approved") forked to the 2D-first plan; the 1D item was deferred
and never returned. **Crucially, the preparation work it did produce —
`PRODUCTION_TRANSFER_RECON_20260816.md` — concluded the transfer would be WRONG AS
PREMISED**, verbatim: *"A `/physics-change` proposal claiming 'the venue account repairs
production' would be wrong as premised. The honest path runs through a production-side
correspondence measurement first."* That correspondence measurement (its Option B) is the
thing that lapsed.

## 2. Term-by-term status (why a literal transfer is empty)

| Venue FULL-F term | Production status |
|---|---|
| α-pairing (numerator/denominator same measure) | **Already present** (post-B_scale removal, the Path-A combine is now also derivation-complete on the completion side) |
| Selected-population prior w_pop·S̄_φ | **Structurally present** (`_w_pop_eff` + volume_deconv incl. per-pixel f_k) |
| Density-form GW event term (vs ratio-pdf) | **Absent** — but measured nearly-inert in the venue (FULL-A +2529 vs +2644 paired); production magnitude unknown |
| LOO impostor weight 1/imp_k | **Architecturally inapplicable** (corrects the venue's synthetic ball construction; production sums real GLADE hosts). Whether a real-catalogue density analogue is needed is an open derivation question — exactly what Option B measures |
| Jacobian / kernel renorm | N/A (venue-only ablation switches; never in production) |

## 3. The executable item: Option B — a pinned synthetic mirror of production's own p_i

**Question it answers** (the gate review's open flag): production 1D sits railed at
~0.601 post-fix — is that pure information starvation (photo-z ≫ GW precision, the F5/F4
story), or does a form-defect component contribute (calgate-v2 DS-6, row #98, could NOT
reproduce the production rail in the multi-candidate venue — an unexplained discrepancy
that has never been closed)? The tilt ledger cannot be closed while the base 1D tilt's
composition is unmeasured — it is the largest single entry (deep enough to rail every
scenario) and it is shared by the 2D channel.

**Design sketch (prereg to follow if approved):** build the correspondence harness as a
pinned mirror of production's `p_i` assembly (derived-form combine, volume_deconv kernel,
real-catalogue candidate structure at reduced scale) inside a synthetic universe with known
truth, calibrated in the rows #109–#116 discipline: paired arms toggling (i) the
density-form event term, (ii) a real-catalogue impostor-density analogue (the open
derivation), (iii) photo-z information content (σ_z ladder) — measuring how much of the
mirror's 1D tilt each owns, with the DS-6 discrepancy as a registered reproduction target.
Deliverable: the production 1D tilt DECOMPOSED into starvation vs form components, each
with a derivational status — the base-tilt entry of the tilt ledger, closed the same way
B_scale was.

**Cost anchor** (rows #106–#111 realized): ~25–50 CPU-h class at N=25 seeds for the 1D
arms (the 2D analog ran 406 CPU-h — NOT proposed here); plus one derivation/design
session. Cheap relative to its position on the critical path.

## 4. Sequencing within the ratified tilt-ledger program

1. Post-fix baseline lands (in flight, jobs 6372475/6372476) → new production posteriors of
   record (~0.677/0.679 expected).
2. **This proposal (Option B)** — the base-tilt decomposition.
3. The re-ranked battery fleet (s_Edd exact-quadrature re-measure; f_k-vs-f̄ consistency;
   J_α) — flags-on-main, one prereg, one fleet; independent of item 2, can run in parallel
   once preregistered.
4. g_i geometry only after 2–3 (gate-review rank 4).
5. Landscape/T1 un-gates when the ledger closes.

## 5. Decision items

- **[RULE] Retire the literal row-#111 "transfer the terms" item** as superseded (wrong as
  premised per the project's own 2026-08-16 recon; the two transferable terms are already
  present, the decisive one is inapplicable).
- **[DO] Approve Option B** (correspondence measurement): prereg + verifier pre-check +
  harness build (validation-code only, no production change; /physics-change would be
  triggered only IF a form defect is found and its fix proposed).
- **[DO] Approve the battery prereg authoring** (item 3 above) so it can run while Option B
  is designed.
