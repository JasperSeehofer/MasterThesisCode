<!-- A20 clean-context adversarial review of CONFIRMATION RUN O6 (Opus, artifacts + registration
text only, banked verbatim 2026-08-21 ~23:05). Orchestrator re-derivation performed BEFORE this
record was written: S(F6)/r_prod/delta read independently from the primary artifacts (exact match);
the reviewer's 1.94e-6 = 7-sf-CSV-storage diagnosis verified arithmetically (beta_Gbar storage rel
errors -5.47e-8 @ 0.725 vs -3.53e-8 @ 0.735 over dh=0.01 -> 1.94e-6); the F6 power-collapse
numbers (span 1.534 vs 7.593 nats, v2 gate fails, full mean_h 0.6179 r_low) re-read from the
records. -->

## A20 CLEAN-CONTEXT ADVERSARIAL REVIEW — CONFIRMATION RUN O6

**Verdict recommendation: BANK-WITH-AMENDMENTS.** I could not refute the number, and I re-derived it from scratch. What I can refute is part of what the band's meaning-sentence says the number means.

---

### Independent recomputation (mandatory item 5) — done, from artifacts, no registered measurement re-run

| quantity | my value | banked value | agree |
|---|---|---|---|
| S(F6) — from `o6_work/f6_work/seed910101/simulations/diagnostics/event_likelihoods.csv` via the committed `selfgen_control.csg_channel_matrices` + `score_at_h_gen(…, 0.73, H_GRID_41)` | **−0.02669249255841575** | −0.02669249255841575 | exact |
| r_prod(910101) — re-derived from scratch (my own driver in scratchpad calling `build_csg_selection_objects` → `draw_csg_realization` → `o4.event_geometries`/`build_aligned_tables` → `o6.compute_reference`) | **−0.02669443370359812** | −0.02669443370359812 | exact |
| r_A(910101) | −0.026676909990144694 | −0.026676909990144694 | exact |
| **S(F6) − r_prod** | **+1.9411451823714287e-06** | +1.9411451823714287e-06 | exact; \|·\| ≤ 1e-4 ✓ |
| D6 matched score vs banked `csg_pilot_20260821/.../csgf_seed910101` | −0.15406021492343094 both | — | exact |

Deeper than the registration went: comparing my replica's per-event `B_num` against F6's diagnostics column, **max \|rel\| = 3.5e-15 at both h nodes** (median exactly 0), and the per-event score difference is a *uniform offset*: mean 1.9411e-06, **sd 1.02e-13**. The entire 1.94e-6 is the 7-significant-figure CSV storage of `D_tilde_phi`/`alpha_G_phi` (`5.965099e+07`, `9.529759e+08`; β̄ rel error −5.47e-8 at 0.725 vs −3.53e-8 at 0.735 → 1.94e-8/0.01 = 1.94e-6). The in-tree fused numerator and the replica agree at machine precision.

Measured axis leverage for this seed: S(F6) − S(D6) = **+0.127368** = 1274 band half-widths. The registered ≈1250× statement is correct — this is not another O4-style zero-leverage axis.

---

### Checklist findings

**1. A21 registration–execution identity — PASS (MEASURED).**
`o6_fused_seed_test.py:223-265` calls `csg.run_csg_arm_seed(work_root, ARM, SEED, out_dir, n_events=200, selection_in_completion_numerator=…)` with `None` (D6) / `"fused"` (F6), fresh `d6_work`/`f6_work` roots plus `_refuse_if_idempotent_skip` — verbatim the registered arm table, no extra kwargs. The only in-tree change in `50476453` is the registered plumbing passthrough (`selfgen_control.py:1374, 1397-1403, 1445-1461`); `None` takes the kwarg-omitting branch, so byte-identical. `git status --porcelain darksiren_emri/` is clean; `HEAD == 50476453` == the `git_commit` both arm records carry. **No mid-run "corrected premise" — the O4 failure did not recur.** Env clean: zero `[DIAGNOSTIC OVERRIDES ACTIVE]` lines in either log (`_HOST_QUAD_N` = 50 confirmed).

**2. Band integrity — PASS (MEASURED).** `o6_reference_derivation_output.json` mtime **21:57:26**, its recorded `git_commit` is the *pre-O6* `08f97d84`; registration+reference committed **22:00:29**; D6 work tree created **22:01:59**, F6 finished **23:01:47**. The reference and the numeric bands (`[−0.026794, −0.026594]`) were fixed and committed before any `evaluate()`. Tuning-to-answer is excluded by the ordering, and my from-scratch re-derivation excludes value tampering.

**3. Replica circularity — MAJOR (typed NARRATIVE-HYPOTHESIS, partially refuted).** The identity is real but its power is *narrow*. What O6 genuinely proves: the in-tree `fused` dispatch is the same computation as the harness the O4/A20 restored-arm fleet result was computed in — i.e. **the harness→production transfer of the mechanism holds**, and the off→fused switch through the production dispatch moves this seed by exactly the harness-predicted amount. That is not nothing, and the no-op failure modes are genuinely excluded (V6/L6/D6). What it does **not** prove, and what the band's meaning-sentence overstates:
- the S̄_φ **table itself** is untested. `o4.build_aligned_tables` calls the same `precompute_phi_marginal_survival` the estimator calls (`bayesian_statistics.py:3812`), so an error in that table is common-mode to replica and cell and invisible to every O6 gate. Registered falsifier (ii) is an audit, not an independent implementation — evidence, not authority.
- I *did* independently confirm falsifier (iii): `_phi_survival_table` is the same object at `:3812` → `:3819` (`precompute_phi_selection_integrals`) → `self._phi_survival_table` at `:4133` → `:4997` (numerator `np.interp`). One table, both legs. That claim is earned.
- **nothing about whether the fused convention is physically right**, and nothing about the fleet.

**4. Gate vacuity/power — PASS with one weak gate (MEASURED).** D6 is a real 1852 s regeneration reproducing the banked column bit-exactly (`max_rel_err = 0.0`, 9200 rows) — a fail-able determinism *and* inertness check, and the wall-time floor closes the O4 cached-artifact hole. V6: `differ_fraction = 1.0` — fail-able, kills silent fall-through. L6: both directions checked (D6 log carries `selection_in_completion_numerator='off' … Not a production posterior` at line 27; F6 carries `selection fusion ACTIVE` at line 4 and *not* the off line) — fail-able. **T6 is the weak one**: `rel_err = 0.0` against 7-sf-stored h-only values; it can only fail on a gross normalizer change, and the registration's "must not move the normalizer" is essentially guaranteed by `_use_phi_selection` being set by `normalization_mode`, not by the cell flag. Genuine but low-power; it is not dressed as more than it is.

**5. MINOR — the registered noise-floor derivation is wrong by ~2×, in the direction that matters.** The δ=1e-4 justification claims the identity noise floor is "< 1e-6". The *measured* floor is 1.94e-6, and it is 100% of the observed delta: it comes from a source the derivation never named (7-sf CSV storage of the normalizer columns, which the committed scoring path reads). Band unaffected (50× margin), but had δ been set at the claimed floor, a bit-perfect run would have fired REPLICA-BROKEN on a storage artifact. This belongs in the record and in the A17 checklist.

**6. Secondary claims hygiene — MAJOR (typed PROVISIONAL).** Ledger row #157 item 2, the author's approved [DO], reads: *"ONE C-SG-F seed end-to-end under `fused` … expected matched score ≈ 0."* The registered O6 primary is **not** that test — the null was demoted to REPORTED-ONLY (correctly: per-seed σ̂ ≈ 0.075 against an edge of 0.037 gives no power) and replaced by an implementation-identity test. The registration discloses this in-line and explicitly refuses the fleet claim, which is honest. But the **approved question remains unanswered**, and the verdict word "MECHANISM-CONFIRMED" plus the clause "the off-cell S̄_φ omission account is complete at production numerics" will be read as answering it. No fleet claim is smuggled into the JSON (`secondary_reported_only` is correctly labelled, subtracts 0, names the edge). The smuggling risk is in the *label*, not the artifact.

**7. MINOR — for the deferred fork, note the power collapse.** F6's matched posterior is far less informative than D6's: `span_nats` 1.53 vs 7.59, and F6 fails both un-amended v2 GATE V thresholds (`v2_span_pass: false`, `v2_sigma_pass: false`), passing only under AMENDMENT 1. Any future per-seed null test on the fused cell inherits this. Separately: F6's **full** channel is still `mean_h = 0.6179`, `r_low = true` — the fused cell does not touch the H₀ rail, and row #157's carried caveat must ride with any citation of this verdict.

**8. MINOR — a consistency sentence is literally false.** The reference registration says the shift +0.12737 is "inside the A20 review's independently-derived per-seed shift range (+0.120…+0.127)". My value is +0.127368, i.e. **0.0004 outside** the quoted range (it is within 1 sd of the fleet mean +0.124925 ± 0.004625, so the substance is fine). Cosmetic, but it is a consistency claim in a pre-data record.

**FATAL findings: none.**

---

### Verdict recommendation

**BANK-WITH-AMENDMENTS.** The gates are fail-able and passed, the arms match the registration exactly, the reference was frozen and committed pre-data and reproduces from scratch, and the decisive number reproduces exactly under my own recomputation — with the residual fully diagnosed as CSV storage rounding rather than any convention difference. Bank the number; narrow the sentence.

### Exact amendment text (append to the O6 VERDICT section)

> **A20 amendment 1 (scope of MECHANISM-CONFIRMED).** The banked reading is: *the in-tree `fused` completion numerator, executed end-to-end through `BayesianStatistics.evaluate()`, is numerically identical to the pre-registered harness replica of its own convention* — per-event `B_num` agreement max \|rel\| = 3.5e-15 (A20 re-derivation), the whole +1.94e-6 primary residual being the 7-significant-figure CSV storage of `D_tilde_phi`/`alpha_G_phi` that the committed scoring path reads. Its scientific content is the **harness→production transfer**: the O4/A20 restored-arm result was computed in a standalone harness, and O6 establishes that that harness's fused numerator *is* the production dispatch's, so the off→fused shift measured here (+0.127368 for seed 910101, 1274 band half-widths) is the mechanism operating in the real code path. The clause "the off-cell S̄_φ omission account is **complete** at production numerics" is **withdrawn** and replaced by "…is complete **as an implementation account of the off→fused difference, for this seed, at production numerics**".
>
> **A20 amendment 2 (the approved question is not yet answered).** Row #157 item 2's approved text expected "matched score ≈ 0". O6 does not adjudicate that: the null was demoted to REPORTED-ONLY pre-data (correctly — per-seed σ̂ ≈ 0.075 vs the 0.037 edge affords no power). \|S(F6)\| = 0.02669 (0.43σ from zero on its own SEM 0.0614) is **consistent with, and not evidence for**, the null. The fleet-level "fused nulls the matched channel" claim remains **OPEN** and returns to the author as a fresh [RULE]/[DO] — a multi-seed fused arm is the measurement that would close it. Note for its costing: F6's matched posterior is markedly less informative (GATE V span 1.53 nats vs the off cell's 7.59; both un-amended v2 thresholds fail), so per-seed power is lower than the off-cell campaign's.
>
> **A20 amendment 3 (residual blind spot, disclosed).** No O6 gate tests `precompute_phi_marginal_survival` itself: the replica and the cell under test both consume that same production leaf function, so an error in the S̄_φ table is common-mode and invisible here. Registered falsifier (ii) is an author audit, not an independent implementation. Falsifier (iii) is independently confirmed by this review (`bayesian_statistics.py:3812 → :3819 → :4133 → :4997`, one table object, both legs).
>
> **A20 amendment 4 (A17 checklist fold-in).** The δ=1e-4 derivation's stated identity noise floor ("< 1e-6") omitted the dominant term — the 7-sf diagnostics-CSV storage of the normalizer columns, measured at 1.94e-6. Any future identity band scored off `event_likelihoods.csv` must derive its floor from the CSV's stored precision, not from the estimator's internal precision. Also corrected: the reference registration's "inside the A20 review's per-seed shift range (+0.120…+0.127)" — the value +0.127368 is marginally outside that stated range, though within 1 sd of the fleet mean +0.124925 ± 0.004625.
>
> **Carried caveat.** F6's full channel remains `mean_h = 0.618`, `r_low = true`: MECHANISM-CONFIRMED is a statement about the completion-numerator cell, not about the production posterior, and does not cure the H₀ rail (row #157 item 3's caveat stands; runs-of-record remain on the `off` cell).
