# Workspace Archival Triage — `emri` — 2026-08-27

## 0. Deadline (verified)

- Workspace: `emri` → `/pfs/work9/workspace/scratch/st_ac147838-emri`
- **Expires 2026-09-23 18:19:41** (created 2026-07-25). Verified via `ws_list`: `remaining time: 27 days 7 hours`, `available extensions: 0`.
- **Immovability verified directly, not inferred.** Ran `ws_extend emri 60` (the standard self-service extend command) — it attempted the extension and returned:
  ```
  Info: extending workspace.
  Error: no more extensions.
  ```
  Exit code 255. No workspace time was consumed by this probe (it failed before any change). `ws_extend -h`/`man ws_extend` expose no override flag, no admin-request flag, and no documented fallback reachable from the CLI. bwHPC's standard policy for exhausted self-service extensions is a manual request to the bwHPC helpdesk (a support ticket, not a CLI action) — this is a **process outside our tooling**, not something this triage can execute or verify further. Bottom line for planning purposes: **treat 2026-09-23 as immovable** unless the author files a helpdesk ticket.
- Runway from today (2026-08-27): **27 days**.

## 1. Size overview

| Scope | Size |
|---|---|
| Total workspace (`du -h --max-depth=1`) | **644 GB** |
| `work/` (scratch/intermediate cache, 142 subdirs, see §3) | 104 GB |
| Everything else (the ~130 `run_*`/`injection_*`/campaign dirs) | ~540 GB |
| Local `results/` tree (already retrieved) | 77 GB |
| Local free space (`df -h /`) | 607 GB avail (32% used of 931 GB) |

**607 GB local free vs. 644 GB total on the cluster — the full workspace will NOT fit locally in one shot alongside existing data.** The plan below sequences by priority so the irreplaceable/must-archive material lands first, and treats `work/` (bulk intermediate cache, see §3) as skippable.

## 2. Cross-reference method (caveat on depth)

- `cluster/datasets.yaml` (updated 2026-07-03) and `DATA_INVENTORY.md` (last dated row 2026-07-26) are both **stale relative to the workspace's actual contents** — the workspace has ~30 run directories created after 2026-07-26 (the P3-2D / O4 / O6-O8 / b0-identity / bat / cf / massab / csg-pilot fleets referenced only in `~/.claude/.../MEMORY.md` session notes, never in the two canonical files). This is itself the single biggest finding of this triage: **the canonical dataset registry has not been updated in a month, while the workspace kept growing** — see §5.
- Per-directory local-mirror status was **spot-checked on the largest/most-referenced directories**, not exhaustively diffed file-by-file for all ~130 dirs (that would need a full `rsync --dry-run -avz` per directory, which is itself hours of SSH-bound work). Sizes/file-counts were compared for the directories named below; anything not explicitly spot-checked is marked "not verified" in the table and should be spot-checked with `rsync -n -avz` before being treated as safe to skip.

## 3. Size-ranked inventory with classification

Legend: **MUST-ARCHIVE** = load-bearing and not (fully) local; **REPRODUCIBLE** = cheap-ish to regenerate from a recorded seed/commit if lost; **ALREADY-LOCAL** = verified present locally at matching size; **UNKNOWN** = absent from both `datasets.yaml` and `DATA_INVENTORY.md`.

| Dir (size) | In datasets.yaml / DATA_INVENTORY.md? | Local mirror? | Class | Notes |
|---|---|---|---|---|
| `work/` (104G, 142 subdirs `b0_seed9001xx`, `bden_seed9001xx`, …) | No | Not verified (spot-checked 1 subdir: `b0_seed900101` = 184M of per-h `selection_tables_h_*.json`) | **REPRODUCIBLE (low priority)** | Looks like intermediate per-seed selection-table cache feeding the P3/b0-identity analyses whose *final* outputs already live in `results/prod2d_closure_20260818/` (14G local, has the actual banked JSON/MD artifacts: `o8_bias_leg_reference_output.json`, `csg_pilot_bands_output.json`, `GATE_REVIEW_20260819.md`, etc.). Losing `work/` likely means re-running CPU-only evaluate steps against already-archived CRBs — not a GPU re-simulation. Verify before treating as fully disposable: confirm every `results/prod2d_closure_20260818/*_output.json` / `*.md` that cites a `work/<subdir>` path has its cited numbers already frozen in the committed file. |
| `run_20260628_seed600` (56G) | **Yes** — `datasets.yaml: crbs.seed600` "post-massfix (verify commit vs commission-base)"; `DATA_INVENTORY.md` "seed600 Ω_m era mismatch" provenance row, "evidence-locker 'seed-600 production run'" | Partial — `results/run_20260628_seed600_figures/` local is only 8.4M / 26 files (figures only, not raw CRBs) | **MUST-ARCHIVE** | This is the raw GPU-simulated CRB (`cramer_rao_bounds.csv` + injections) underlying every seed600 A/B arm below. **Sole copy of the actual GPU output** — regenerating requires re-running the GPU simulation at the pre-G11 Ω_m=0.25 era config, which DATA_INVENTORY.md flags as no longer the default cosmology; costly and error-prone to reproduce byte-identically. Highest-priority single item. |
| `run_20260726_seed600_ab_vdeconv` (35G) | **Yes** — the `volume_deconv` arm of the seed600 must-not-change gate (`DATA_INVENTORY.md` 2026-07-26 row, `SEED600_GATE_REGISTRATION.md`) | Not found locally (only the 72K semantics-note `results/seed600_ab_20260710/`, unrelated content) | **MUST-ARCHIVE** | Downstream CPU `evaluate` posteriors from seed600 CRB — reproducible in principle from `run_20260628_seed600` + recorded `git_commit`/seed (confirmed present in `run_metadata_17.json`: commit `66b9922f`, seed 617, `strategy: physics-floor`), but doing so for all 3 arms is a real CPU-hour cost and this is a *registered gate result* cited in `DATA_INVENTORY.md`/`MULTISEED_READOUT_20260726.md` as evidence — treat as load-bearing, not just cache. |
| `run_20260726_seed600_ab_genmarg_zres` (35G) | **Yes** — same gate, the canonical `generator_marginal`+`pdet_z_resolved` production-config arm | Not found locally | **MUST-ARCHIVE** | Same reasoning; this is the arm declared **canonical production configuration** (`ce6338e`). Arguably the single most scientifically important of the three seed600 arms. |
| `run_20260726_seed600_ab_absmarg` (35G) | **Yes** — the `absolute_marginal` (V1, since superseded) comparison arm | Not found locally | **MUST-ARCHIVE** (lower urgency than the two above — superseded config, but still the registered gate comparison arm) | |
| `csg_pilot_20260821` (33G) | No | Not verified | **UNKNOWN** | Post-dates DATA_INVENTORY.md; matches `results/prod2d_closure_20260818/csg_pilot_bands_output.json` (local, small JSON) — that JSON may already contain the *distilled* result, making the 33G raw pilot data reproducible-but-uninventoried. Flag for author: confirm whether `csg_pilot_bands_output.json` is a complete summary or whether the raw 33G is needed for the C-SG campaign's audit trail (row #145-#157 in memory notes describe this as a load-bearing campaign). |
| `run_20260729_seed61000` (30G) | No | `results/campaign51_20260728/{realistic_20260729/seed61000, run_seed61000}` exist locally but not size-diffed | **UNKNOWN / not verified** | Named after "campaign51" (2026-07-28), post-dates both registry files. Needs a real `rsync -n` diff, not just existence check. |
| `run_20260401_seed200` (26G) | **Yes** — `datasets.yaml`/`DATA_INVENTORY.md` document `phase45-seed200-20260501` as derived from this and describe it as **"superseded"** by later campaigns; explicitly marked historical | Partial: `results/figures_seed200/`, `results/combined_posterior_seed200.json` local (small, derived) | **REPRODUCIBLE / low priority** | Oldest campaign in the workspace (Apr 2026), explicitly superseded per DATA_INVENTORY.md. Low urgency — the derived posterior is already local; the raw CRB is nice-to-have, not load-bearing. |
| `run_20260729_seed62000` (18G) | No | `results/campaign51_20260728/{realistic_20260729/seed62000, run_seed62000}` exist, not size-diffed | **UNKNOWN / not verified** | Same campaign51 family as seed61000. |
| `realizations_20260729` (14G) | No | Not found locally | **UNKNOWN** | Has `run_metadata_realization.json` (seed-tagged) — likely reproducible with recorded seed, but uninventoried; the P3-2D/O4 fleet work in Aug 2026 memory notes references realization sidecars extensively (`cluster/SKILL.md` gotcha #10 specifically warns these are fragile/path-sensitive) — worth checking before assuming easy reproduction. |
| `run_20260726_seed{1000,2000,3000,90000}_prodstack` + `_densecore` (7.1G+9G+12G+8.9G+12G+... ≈ 60G total) | **Yes** — the 2026-07-26 five-seed production-stack campaign, **author-ratified PASS**, canonical production result (`MULTISEED_READOUT_20260726.md`) | `results/lcat_h_dependence_20260725/MULTISEED_READOUT_20260726.md` local (readout doc only, not raw CRBs/posteriors) | **MUST-ARCHIVE** | This is the campaign whose readout **lifted the campaign NO-GO** and set the canonical config. The readout doc is local but the underlying per-seed CRBs/posteriors are not verified local — this is exactly the "load-bearing scientific result, sole copy at risk" case the task asked to flag explicitly. |
| `run_20260707_seed{2000,3000,90000}` (8.9G+12G+72M) | **Yes** — source CRBs for the prodstack campaign above | Not verified | **MUST-ARCHIVE** | Upstream GPU CRBs feeding the prodstack posteriors; same criticality argument. |
| `run_20260703_seed1000` / `run_20260703_seed900` (6.8G / 7.2M) | **Yes** — extensively discussed in DATA_INVENTORY.md (RAILED campaign, seed900 "DEFECTIVE" injection-pool finding) | `results/campaign_phase2_runs/` local is essentially empty (2 small files) | **MUST-ARCHIVE (historical/diagnostic value)** | seed1000 is the CRB behind the entire Phase-2 rail investigation (issues #29/#30) — heavily cited diagnostic history. Not reproducible cheaply (GPU sim) and forensically important even though superseded for production. |
| `run_20260725_seed1000_zcut{02,03,05}` (7.0G+7.0G+6.2G) | **Yes** — the z_cut truncation scan, "option (b) DEAD" finding | Not verified | **REPRODUCIBLE (CPU-only re-eval of seed1000 CRB)** | These are `--max_redshift` re-evaluations of the same seed1000 CRB — cheap to regenerate *if* seed1000 CRB itself survives (see row above, which is why seed1000 raw CRB is the must-archive item, not these derived re-evals). |
| `run_20260719_seed1000_exp40` (7.0G) | **Yes** — EXP-40 fallback-exoneration re-eval | Not verified | REPRODUCIBLE (same reasoning) | |
| `o4_shards_20260821` (11G) | No | Not verified | **UNKNOWN** | Post-dates registry; "O4" appears in memory notes as a resolved mechanism-study thread. |
| `p3_b0_identity_fleet_20260823` (9.5G) | No | Not verified | **UNKNOWN** | Matches memory entry "2026-08-23/24 b0 identity: UNDISCRIMINATING" — a **ratified verdict** (b0 identity test adjudicated, "twin 11/11 directional REPORTED-ONLY"). Load-bearing for a closed scientific question; flag for archival even though uninventoried. |
| `run_production_h0p73_20260506` (9.4G) | **Yes** — Phase 48/49 F1 production sweep, explicitly marked "**Not paper-grade**" and superseded, but its pre-F1 archive subfolder is referenced as the source of comparison | Not verified | REPRODUCIBLE / low priority (explicitly superseded, "No figure refresh from these posteriors") | |
| `p3_2d_rhs2_20260826` (4.8G), `p3_2d_fleet_20260825` (555M) | No | Not verified — but git log shows local `.../ca_rhs_work/` dirs (untracked, per git status) that look like a local mirror in progress | **UNKNOWN, likely IN-PROGRESS LOCAL SYNC** | Git status at session start shows `results/campaign51_20260728/realistic_20260729/ca_rhs_work/*_twin_work/`, `*_coded_work/` freshly created and untracked — this looks like an active retrieval already underway for the P3-2D RHS work (rows #198-#211, "session close at the [P3-2D] park"). **Do not duplicate effort here — check with the author/session state before re-pulling; likely already being handled.** |
| ~16 `run_2026080{4,5}_*` and `run_2026081{7,9}_*`/`run_20260820_*` "iiib"/"joint_r1" pairs (≈3.3-3.4G each, ≈53G total) | No (all post-date DATA_INVENTORY.md) | **Mixed — spot-checked 5 of ~16 pairs**: `frozeng` FULLY local (6.5G local vs 6.7G cluster), `fusioncf_{fused,off}` FULLY local (13G local vs 13.4G cluster), `postfix` mostly MISSING (36M local vs 6.7G cluster — figures/summary only), `d1_{a1,a2}` mostly MISSING (55M local vs 13.4G cluster), `n2sel1d` PARTIAL (4.6G local vs 6.7G cluster, ~69%) | **SPLIT: `postfix`, `d1_a1`, `d1_a2` = MUST-ARCHIVE; `n2sel1d` = MUST-ARCHIVE (partial, top up); `frozeng`, `fusioncf_fused`, `fusioncf_off` = ALREADY-LOCAL** | The `cf_v0/v1/v2k05/v2k2` (Aug 19) and `bat_eoff/jker/v0` (Aug 19-20) pairs (~8 pairs, ~27G) were **not spot-checked** — same naming family as the ones above, so likely a mix; treat as UNKNOWN/not-verified and check before the deadline. |
| `run_closure_h0p*` family (May 2026, ~7 dirs, ~7.6G total) | **Yes** — closure-verification runs, explicitly documented in DATA_INVENTORY.md dataset registry | Not verified, but derivative posteriors are described as already extracted into `scripts/bias_investigation/outputs/` (repo-tracked, not `results/`) | **REPRODUCIBLE / low priority** | Rescaled from `phase45-seed200-20260501` (itself superseded); low scientific urgency. |
| `run_20260727_massab_*` (8 dirs, ~1.1G each, ~8.8G total) | No | Not verified | **UNKNOWN** | Mass-ablation cell study, post-dates registry; small enough to archive cheaply regardless of classification. |
| Small dirs (<1G): `run_20260330_*`, `run_20260328_*`, all `injection_*` pools, `run_20260619_seed*_phase50` (mostly empty/broken symlinks per datasets.yaml retirement notes), `run_20260702_seed600_voldeconv_coarse`, `run_20260517_dev_pilot`, `run_mcfix_validate`, `run_prod2d_20260818`, `run_equatorial_revert_20260428`, `RETIRED_injection_*`, `scripts/` | **Mixed** — most explicitly RETIRED in `datasets.yaml`/`DATA_INVENTORY.md` | Not verified individually | **REPRODUCIBLE / RETIRED — lowest priority** | Total volume is trivial (a few hundred MB); archive last, in bulk, cheaply — no need to triage line-by-line given the size. |

**Total size of directories not yet confirmed present locally at matching size ≈ 400-450 GB** (the MUST-ARCHIVE + UNKNOWN rows above). This is the practical "must move" volume; it does not fit in the 607 GB free space alongside everything else already local, so sequencing matters (§4).

## 4. Estimated transfer time & recommended sequence

No bandwidth benchmark was run in this triage (would require an actual multi-GB transfer, out of scope for a read-only pass). Using a conservative bwHPC-to-home-DTN planning figure of ~50-150 MB/s over `rsync -avz` for compressed scientific CSV/JSON (compression on text-heavy CSV can help; on already-dense JSON less so):

- 450 GB / 100 MB/s ≈ **~1.3 hours of pure transfer time**, likely 2-4 hours in practice with SSH/rsync overhead, retries, and the workspace being read concurrently by other jobs.
- This easily fits inside the 27-day runway; the deadline is not a bandwidth problem, it is a **triage-attention** problem — nobody had inventoried the 130 directories until this pass.

**Recommended sequence** (read-only recommendations; author or a follow-up write-enabled session executes):

1. **Today/this week — the four seed600 items (161 GB total: 56G raw CRB + 3×35G A/B arms).** These are the highest-confidence MUST-ARCHIVE items: registered gate results, GPU-origin, cited by name in `DATA_INVENTORY.md`. `rsync -avz --partial bwunicluster:$WS/run_20260628_seed600/ ./results/_archive/run_20260628_seed600/` and similarly for the three `_ab_` arms. This alone consumes ~161G of the 607G local headroom — leaves 446G for everything else, comfortably.
2. **This week — the prodstack/densecore + seed{1000,2000,3000,90000,900} raw CRBs (~65 GB).** The ratified production campaign; same urgency tier as seed600.
3. **Next — the ~53 GB of `iiib`/`joint_r1` pairs not yet confirmed local** (`postfix`, `d1_a1`, `d1_a2`, `n2sel1d` top-up, and the 8 unverified `cf_v*`/`bat_*` pairs, pending a spot-check). Budget ~80 GB to be safe if all turn out missing.
4. **Then the UNKNOWN cluster (`csg_pilot_20260821` 33G, `run_20260729_seed6{1,2}000` 48G, `o4_shards_20260821` 11G, `p3_b0_identity_fleet_20260823` 9.5G, `realizations_20260729` 14G, `p3_2d_*` 5.3G, `massab_*` 8.8G ≈ 130 GB total)** — first **check whether the P3-2D `ca_rhs_work/` sync already in progress (visible in git status as untracked local dirs) covers some of this**, to avoid duplicate transfer; then pull the rest.
5. **Last, in one bulk sweep, everything explicitly RETIRED or superseded** (the small run_2026033*/0328*, retired injection pools, `run_20260619_*`, `run_equatorial_revert_*`, closure-h0p* family) — trivial volume, no urgency, but cheap enough to grab anyway rather than leave a gap in the archival record.
6. **Deprioritize/possibly skip `work/` (104 GB)** unless a spot-check confirms its per-h `selection_tables_*.json` cache is NOT fully redundant with the already-archived `results/prod2d_closure_20260818/*_output.json` distillations. This is the one place where skipping saves real space (104 GB) if the check comes back clean.

**Destination:** local `results/_archive/<original-dirname>/` (or wherever the author prefers a persistent, non-`results/`-polluting location — e.g. a dedicated `archive/` top-level dir, since `results/` is presumably meant for curated/committed outputs, not a raw dump of 130 cluster directories). This triage does not create that directory or move anything — copy-only actions are for the author or an explicitly write-enabled follow-up run.

## 5. Explicitly flagged: load-bearing, single-copy risk

- **`run_20260628_seed600` (56G)** — sole copy of a GPU-simulated CRB tied to a specific (now non-default) Ω_m era; cannot be regenerated byte-identical without checking out old code and burning fresh GPU-hours. Everything else "seed600" downstream depends on this surviving.
- **`run_20260726_seed600_ab_genmarg_zres` (35G)** — the arm holding the **canonical production configuration** posteriors (`generator_marginal` + `pdet_z_resolved`, declared canonical at commit `ce6338e`). If this workspace expires before it's archived, the concrete numeric evidence behind that canonical declaration is gone (only the *readout doc* would survive, not the raw per-event posteriors it summarizes).
- **The 2026-07-26 five-seed prodstack campaign raw CRBs/posteriors (~65G across `run_20260707_seed{2000,3000,90000}` + `run_20260726_seed*_prodstack`/`_densecore`)** — this is the campaign whose ratified readout **lifted the standing campaign NO-GO**; `MULTISEED_READOUT_20260726.md` is local but the primary data it's a summary of is not confirmed local.
- **`p3_b0_identity_fleet_20260823` (9.5G)** — backs a closed, ratified verdict ("b0 identity: UNDISCRIMINATING") entirely absent from both canonical registry files; the only place this campaign is documented at all is a session-memory note, which is not durable in the way a git-tracked file is.
- **General pattern, not a single item:** the entire post-2026-07-28 fleet (`csg_pilot`, `o4_shards`, `p3_*`, `bat_*`, `cf_v*`, `massab_*`, `seed6{1,2,3,4,5}000`, `realizations_20260729`) — roughly 30 directories, ~250 GB — exists in the workspace and in scattered session-memory notes but **in neither `cluster/datasets.yaml` nor `DATA_INVENTORY.md`**. Recommend the author (or a mechanical follow-up task) backfill both registry files from the memory-note session history before or alongside the archival copy, so the *next* triage doesn't have to reconstruct provenance from git commit messages and MEMORY.md prose.

## 6. Free space

- **Cluster:** `ws_list` reports only remaining time and expiry, not a quota/usage number (bwHPC workspaces are typically allocation-based, not quota-metered per-user in the `ws_list` output); no separate quota command was found beyond `du`. Total measured usage: 644 GB.
- **Local:** `df -h /` → 931G filesystem, 277G used, **607G available (32% used)**. Comfortably sufficient for the ~450G "must move" volume if `work/` (104G) is deprioritized and the transfer is sequenced per §4 rather than attempted as one atomic `rsync` of the whole workspace.
